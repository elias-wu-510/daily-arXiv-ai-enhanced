<div id=toc></div>

# Table of Contents

- [cs.CV](#cs.CV) [Total: 120]


<div id='cs.CV'></div>

# cs.CV [[Back]](#toc)

### [1] [Hunyuan3D-Buffalo 1.0: A Unified Multimodal Model for Scalable 3D Generation, Understanding, and Editing](https://arxiv.org/abs/2608.02711)
*Junliang Ye,Kenkun Liu,Guocun Wang,Yang Li,Yansong Qu,Chunshi Wang,Jingwei Xu,Yunhan Yang,Zibo Zhao,Jiachen Xu,Jiaao Yu,Lifu Wang,Zhihao Liang,Zhuo Chen,Chunchao Guo*

Main category: cs.CV

TL;DR: Hunyuan3D-Buffalo 1.0 is a unified multimodal framework supporting 3D understanding, text-to-3D generation, instruction-guided 3D editing, and text-grounded part generation within a single architecture, trained on a large-scale 87M 3D multimodal corpus.


<details>
  <summary>Details</summary>
Motivation: Unified 3D modeling is constrained by scarce multimodal data, particularly the lack of large-scale and geometrically consistent editing data, unlike the progress seen in unified 2D image generation models.

Method: The framework combines Hunyuan3D-VLM (for semantic, structural, and spatial understanding) with Hunyuan3D DiT (for high-fidelity 3D synthesis). It is trained on an 87M-scale corpus comprising 25M understanding samples, 50M text-to-3D pairs, and 12M editing pairs generated by Nano3D-v2. Editing and part generation condition the diffusion process on source object representation to preserve structure and unedited regions.

Result: Achieves state-of-the-art or leading performance on text-to-3D generation and 3D editing benchmarks, with strong understanding and part-generation capabilities. Analysis shows both generation and understanding improve editing, confirming the effectiveness of unified 3D multimodal training.

Conclusion: Unified 3D multimodal training is effective, and scaling multimodal data can overcome the data scarcity limitation in 3D modeling.

Abstract: Recent advances in image generation have demonstrated the potential of unified multimodal models that integrate understanding, generation, and editing. However, unified 3D modeling remains constrained by scarce multimodal data, particularly the lack of large-scale and geometrically consistent editing data. To address this limitation, we propose Hunyuan3D-Buffalo 1.0, a unified framework supporting 3D understanding, text-to-3D generation, instruction-guided 3D editing, and text-grounded part generation within a single architecture. To enable scalable training, we construct an 87M-scale 3D multimodal corpus, comprising 25M understanding samples, 50M text-to-3D pairs, and 12M editing pairs generated using Nano3D-v2. Architecturally, the framework combines Hunyuan3D-VLM for semantic, structural, and spatial understanding with Hunyuan3D DiT for high-fidelity 3D synthesis. The VLM provides multimodal semantic conditions for generation, while editing and part generation additionally condition the diffusion process on the source object representation to preserve its overall structure and unedited regions. Extensive experiments show that Hunyuan3D-Buffalo 1.0 achieves state-of-the-art or leading performance on text-to-3D generation and 3D editing benchmarks, while exhibiting strong understanding and part-generation capabilities. Our analysis further shows that both generation and understanding improve editing, demonstrating the effectiveness of unified 3D multimodal training. Project Page: https://tencent-hunyuan.github.io/Hunyuan3D-Buffalo1.0/

</details>


### [2] [Quo Vadis, World Modeling?](https://arxiv.org/abs/2608.02713)
*Yu Yang,Xuemeng Yang,Licheng Wen,Lingdong Kong,Xiaobin Hu,Dongyue Lu,Wei Chow,Xiyan Huang,Yuxiang Feng,Yue Liao,Jianbiao Mei,Daocheng Fu,Rong Wu,Pinlong Cai,Ran Yi,Ying Tai,Jiangning Zhang,Botian Shi,Yong Liu,Shuicheng Yan*

Main category: cs.CV

TL;DR: This paper proposes shifting world modeling from physical state prediction to agent-centric interactive world proxies that provide versatile feedback (outcomes, memories, skills, verification signals) for continually improving agents. It organizes proxies into six functional forms and describes three progressive levels of agent empowerment.


<details>
  <summary>Details</summary>
Motivation: Real-environment interaction for agent improvement is costly, slow, unsafe, and hard to parallelize. Classical world models only predict physical states, which is too narrow for agents that need actionable feedback beyond raw state transitions.

Method: The paper conceptualizes Agent-Centric Interactive World Proxies, shifting from physical state transitions to agent-usable information transitions. It maps the design space into six proxy forms (dynamics, spatial, execution, memory/experience, skill, reward/verification) and defines three progressive levels of agent empowerment (inference-time guidance, training-time optimization, and agent-proxy co-evolution).

Result: The paper provides a systematic framework and roadmap for building world proxies that enable agents to plan better, learn faster, and evolve continually, recasting world modeling into an agent-centric paradigm.

Conclusion: World modeling should be reformulated around agent-usable information transitions rather than physical state prediction, establishing a roadmap for interactive world proxies that support continual agent improvement across inference, training, and co-evolution stages.

Abstract: Continually improving agents require dynamic interaction feedback beyond static supervision, yet direct real-environment interaction is costly, slow, unsafe, and hard to parallelize. World modeling offers a natural intermediate proxy that allows agents to query lower-cost, more controllable feedback before committing to real actions. Classical world models instantiate this proxy primarily through future physical-state prediction, a formulation useful yet narrow for agents that require actionable feedback beyond raw state transitions. In this work, we conceptualize Agent-Centric Interactive World Proxies, shifting the fundamental paradigm from physical state transitions to agent-usable information transitions, such as execution outcomes, retrieved experiences or skills, and verification signals, broadening the scope of world modeling to provide versatile feedback for continually improving agents. To systematically map this design space, we organize world proxies into six functional forms based on their feedback modalities: dynamics, spatial, execution, memory/experience, skill, and reward/verification proxies, which together characterize the primary ways world modeling serves agent improvement. We further analyze how these proxies empower agents across three progressive levels: L.1 Inference-Time Guidance, where proxy outputs enrich in-context information for superior decisions; L.2 Training-Time Optimization, where proxy outputs yield rewards, critiques, or synthetic rollouts for policy learning; and L.3 Agent-Proxy Co-Evolution, where real-environment evidence continuously updates both the proxy and the agent for co-evolution. Ultimately, this work recasts world modeling into an agent-centric paradigm, establishing a roadmap for building world proxies that empower agents to plan better, learn faster, and evolve continually.

</details>


### [3] [Oh Deer, How Should I Handle This? Seasonal Priors for Selective Wildlife Annotation and Classification](https://arxiv.org/abs/2608.02762)
*Hugo Markoff,Christoph Praschl,Anton Hjalte Jørgensen,Christian Emil Mogensen,Mathias Bech Skadhauge,Sara Beery,Michael Ørsted,David C. Schedl*

Main category: cs.CV

TL;DR: This paper studies adult-male identification in red deer from aerial imagery, showing that seasonal antler cycles link annotation quality, downstream classification, and selective prediction. Using 7,295 multi-modal crop sets, they demonstrate that matched RGB+thermal review recovers more labels than single modalities, and a biologically grounded seasonal calendar can guide annotation protocols and modality weighting.


<details>
  <summary>Details</summary>
Motivation: Fine-grained wildlife classification in aerial imagery suffers from unreliable labels due to few-pixel animals, seasonal visual cue variation, and ambiguous modality-specific evidence. A biologically grounded framework is needed to predict where annotation and prediction will be unreliable.

Method: Using 7,295 RGB-only, thermal-only, and matched RGB+thermal crop sets labeled by three annotators, the authors link seasonal structure to annotation quality, downstream classification, and selective prediction. They compare single vs. multi-modality review, examine seasonal confidence patterns, apply soft seasonal priors, and implement uncertainty-band abstention for selective prediction.

Result: Matched RGB+thermal review resolves more samples than either single modality, recovering majority-male labels missed by RGB or thermal alone in both human- and model-based classification. Months with high annotator abstention correlate with lower classifier confidence. Soft seasonal priors mainly benefit the season-limited thermal view. Uncertainty-band abstention improves covered accuracy up to 98.9%, though with reduced coverage and deferral falling disproportionately on males.

Conclusion: A biologically grounded seasonal calendar effectively predicts where annotation and prediction are unreliable, and can guide both annotation protocol design and modality weighting for wildlife classification tasks.

Abstract: Fine-grained wildlife classification in aerial imagery is limited not only by model performance, but also by unreliable labels: animals occupy few pixels, key visual cues vary seasonally, and modality-specific evidence can be ambiguous. We study adult-male identification in red deer, where the antler cycle defines predictable windows of reliable evidence for both annotation and prediction. Using 7,295 RGB-only, thermal-only, and matched RGB+thermal crop sets labeled by three annotators, we show that seasonal structure links (I) annotation quality, (II) downstream classification, and (III) selective prediction. Matched RGB+thermal review resolves more samples than either single modality, recovering majority-male labels otherwise missed by RGB or thermal alone, in human based as well as model based classification. Months with high annotator abstention also show lower classifier confidence, and soft seasonal priors mainly benefit the season-limited thermal view. Uncertainty-band abstention further improves covered accuracy up to 98.9%, though at reduced coverage and with deferral that falls disproportionately on males. Overall, a biologically grounded seasonal calendar predicts where annotation and prediction are unreliable, and can guide both annotation protocol design and modality weighting.

</details>


### [4] [Confident but Unreliable: A Behavioral Safety Audit of Vision-Language Models on Brain MRI](https://arxiv.org/abs/2608.02790)
*Amir Sabbaghziarani,Mohammadsajad Abavisani,Sergey Plis*

Main category: cs.CV

TL;DR: A behavioral audit of six medical vision-language models on 4,102 brain MRI images reveals that while answer coverage is near-complete, verbalized-confidence calibration is poor (ECE 0.27-0.40), with 33-46% of answers being high-confidence errors. Medical adaptation improves tumor detection accuracy but not confidence reliability, arguing that VLM evaluation should report confidence calibration, confident errors, hallucination, and abstention alongside accuracy.


<details>
  <summary>Details</summary>
Motivation: Vision-language models are increasingly used in medical imaging, but their stated confidence is rarely evaluated separately from correctness. In high-stakes medical settings, models can appear competent while lacking reliable self-knowledge, making this a critical but understudied failure mode.

Method: The authors present an automatically graded behavioral audit of six instruction-tuned VLMs (five general-purpose, one medical specialist) on 4,102 brain MRI images (4,032 slices from 250 subjects plus 70 non-brain controls). Labels are derived from public metadata and released expert segmentation masks, avoiding new human annotation. They evaluate answer coverage, verbalized-confidence calibration (ECE), mean confidence on errors, high-confidence error rates, hallucination, and abstention.

Result: Answer coverage is near-complete, but calibration is poor: ECE ranges 0.27-0.40, mean confidence on incorrect answers ranges 0.82-0.97, and 33-46% of answered items are high-confidence errors. The most accurate model is also the most confident on its errors. Medical adaptation improves tumor-presence detection but not confidence reliability. Hallucination and abstention vary independently from multiple-choice accuracy.

Conclusion: Medical-image VLM evaluation should report verbalized-confidence reliability, confident error, hallucination, and abstention metrics alongside accuracy, as accuracy alone is insufficient to assess trustworthiness in clinical settings.

Abstract: Vision-language models (VLMs), including medical specialists, are increasingly proposed for medical imaging, yet their stated confidence is rarely evaluated separately from correctness. We use brain MRI as a controlled, high-stakes testbed for a broader failure mode in frontier multimodal systems: models can appear competent while lacking reliable self-knowledge. We present an automatically graded behavioral audit and pilot study of six instruction-tuned VLMs (five general-purpose and one medical specialist) on 4,102 images (4,032 axial/coronal/sagittal MRI slices from 250 subjects plus 70 non-brain/noise controls), with labels derived from public metadata and released expert segmentation masks rather than new human annotation.
  Across models, answer coverage is near-complete, but verbalized-confidence calibration is poor: ECE ranges from 0.27 to 0.40, mean confidence on incorrect answers ranges from 0.82 to 0.97, and 33-46% of answered items are high-confidence errors. The most accurate model is also the most confident on its errors, while a base/specialist family contrast suggests that medical adaptation improves tumor-presence detection without improving confidence reliability. Open-ended diagnostics further show that hallucination and abstention vary separately from multiple-choice accuracy. These findings argue that medical-image VLM evaluation should report verbalized-confidence reliability, confident error, hallucination, and abstention alongside accuracy.

</details>


### [5] [Better, Stronger, Faster, and Broader: Structured All-Mask Prediction for MLLM-Based Segmentation](https://arxiv.org/abs/2608.02791)
*Jiazhen Liu,Mingkuan Feng,Long Chen*

Main category: cs.CV

TL;DR: STAMPlus resolves the MLLM segmentation trilemma (performance, dialogue, speed) via Structured All-Mask Prediction: non-autoregressive multi-target mask prediction decoupled from autoregressive dialogue, achieving SOTA segmentation while preserving multimodal ability and cutting 12-category latency from 13.50s to 5.16s.


<details>
  <summary>Details</summary>
Motivation: MLLM-based segmentation faces a core trilemma: embedding-prediction methods disrupt language modeling, while next-token generation is inefficient for dense masks. Binary masks also cannot retain multiple semantic/instance identities without repeated predictions.

Method: Proposes All-Mask Prediction, decoupling autoregressive dialogue from non-autoregressive mask prediction. STAMP (binary) emits a <SEG> trigger, fuses image-aligned mask tokens with patch features, and uses hybrid attention for one-pass foreground/background classification. STAMPlus extends this with Structured All-Mask Prediction: generates a target list with IDs and optional boxes, binds IDs to a shared multi-class mask space, and jointly predicts all targets in one pass.

Result: STAMPlus achieves SOTA segmentation across referring/reasoning, open-vocabulary semantic, instance-aware, and remote-sensing small-target settings. It preserves general multimodal instruction following and reduces 12-category latency from 13.50s (repeated STAMP) to 5.16s.

Conclusion: STAMPlus resolves the segmentation trilemma beyond single-target prediction, showing accurate target cues improve segmentation and learned spatial grounding benefits look-twice reasoning.

Abstract: MLLM-based segmentation faces a core segmentation trilemma: high segmentation performance, preserved dialogue ability, and fast inference. Embedding-prediction methods may disrupt language modeling through pixel-level objectives, whereas next-token generation is inefficient for dense masks. We propose All-Mask Prediction, decoupling autoregressive dialogue from non-autoregressive mask prediction. Its binary instantiation, STAMP (Simultaneous Textual All-Mask Prediction), emits an in-vocabulary <SEG> trigger, fuses image-aligned mask tokens with corresponding patch features, and uses hybrid attention to classify all tokens as foreground or background in one pass. It thereby combines strong referring and reasoning segmentation with preserved multimodal ability and efficient inference. However, binary masks cannot retain multiple semantic or instance identities without repeated target-specific predictions. We therefore propose Structured All-Mask Prediction and develop STAMPlus. It generates a target list with explicit IDs and optional boxes, binds these IDs to a shared multi-class mask space, and jointly predicts all targets in one non-autoregressive pass. A single unified checkpoint retains STAMP's referring and reasoning capabilities while extending to open-vocabulary semantic, instance-aware, and remote-sensing small-target segmentation, where high-resolution mask-token scaling preserves finer spatial evidence. Across these settings, STAMPlus achieves state-of-the-art segmentation performance, preserves general multimodal instruction following, and reduces 12-category latency from 13.50s for repeated STAMP inference to 5.16s. Further analyses show that accurate target cues improve segmentation and learned spatial grounding benefits look-twice reasoning. Overall, STAMPlus resolves the trilemma beyond single-target prediction.

</details>


### [6] [PixelUp: Zero-Shot Semantic Feature Upsampling for Fine-Grained Vision Tasks](https://arxiv.org/abs/2608.02792)
*Deepank Singh,Anurag Nihal,Vedhus Hoskere*

Main category: cs.CV

TL;DR: PixelUp is a zero-shot, VFM-agnostic feature upsampler that uses a coarse-to-fine windowed cross-attention architecture guided by multi-scale semantic features to recover fine-grained pixel details from Vision Foundation Model patch tokens, achieving state-of-the-art results on dense prediction tasks.


<details>
  <summary>Details</summary>
Motivation: Self-supervised Vision Foundation Models produce patch-token features that are too coarse for dense prediction tasks. Existing upsampling methods have limitations: learnable upsamplers are encoder-specific and need retraining, while image-guided methods using shallow pixel encoders introduce textural artifacts and lack semantic guidance.

Method: PixelUp introduces a zero-shot VFM-agnostic upsampler with a coarse-to-fine chain of windowed cross-attention architecture, guided by multi-scale semantic features to achieve semantic awareness during feature upsampling.

Result: PixelUp outperforms both VFM-specific and VFM-agnostic upsamplers, achieving +1.2 mIoU on semantic segmentation and +0.25 δ1 on NYUv2 depth estimation across VFMs. It also improves training-free open-vocabulary and unsupervised semantic segmentation by +1.3 mIoU and +0.5 mIoU respectively.

Conclusion: PixelUp demonstrates that a zero-shot, VFM-agnostic upsampler with multi-scale semantic guidance can achieve state-of-the-art performance across multiple dense prediction downstream tasks, eliminating the need for per-encoder retraining while avoiding textural artifacts from image-guided methods.

Abstract: Self-supervised Vision Foundation Models (VFMs) have become essential backbones for downstream tasks due to their strong and transferable visual representations. However, their patch-token-level features are often too coarse for dense prediction tasks such as semantic segmentation and depth estimation when accurate fine-grained predictions are required. Feature upsampling methods have been developed to recover pixel-level detail but still face limitations. Learnable upsamplers are often designed for a specific encoders and must be retrained for different encoders. Image-guided methods that use shallow pixel encoders often introduce textural artifacts and lack the semantic guidance needed for accurate downstream predictions. We introduce PixelUp, a zero-shot VFM-agnostic upsampler achieving semantic awareness through a coarse-to-fine chain of windowed cross-attention architecture guided by multi-scale semantic features. We demonstrate that PixelUp outperforms both VFM-specific and VFM-agnostic upsamplers, achieving state-of-the-art performance on dense prediction tasks with an average improvement of +1.2 mIoU on semantic segmentation and +0.25 $δ_1$, on NYUv2 depth estimation across VFMs. PixelUp further improves training-free open-vocabulary and unsupervised semantic segmentation by an average of +1.3 mIoU and +0.5 mIoU, respectively. Code available at https://pixelup-project.vercel.app/

</details>


### [7] [SAGE: Semantic Explainability of Attention-Based Survival Models in Computational Pathology](https://arxiv.org/abs/2608.02803)
*Abdallah Lamane,Abdul Rahman Diab,Ren-Chin Wu,William Lotter*

Main category: cs.CV

TL;DR: SAGE is a post-hoc framework that extracts global, language-grounded explanations from frozen ABMIL survival models in computational pathology by scoring patches against a 25-concept histological dictionary via a pathology vision-language model, aggregating scores with model attention, and quantifying concept-risk associations across patient cohorts. Validated on seven TCGA cancer cohorts with three foundation models, it recovered established prognostic features and revealed cancer-specific biology.


<details>
  <summary>Details</summary>
Motivation: ABMIL attention maps only provide local explanations—they show where a model focuses but not which histological features drive predictions or how the model behaves across an entire patient cohort. There is a need for global, interpretable explanations of what ABMIL survival models learn.

Method: SAGE uses a frozen pathology vision-language model to score image patches against a dictionary of 25 histological concepts, aggregates these concept scores weighted by the ABMIL model's learned attention, and quantifies the relationship between each concept and prediction risk across a patient cohort.

Result: Across seven TCGA cancer cohorts and three foundation models, SAGE recovered established prognostic features (e.g., necrosis as adverse) and revealed cancer-specific biology, including a favorable angiogenic signature in renal cell carcinoma consistent with molecular subtypes. Ablations confirmed associations depend on learned attention, not concept prevalence alone, and the concept dictionary captures much of the prognostic information in foundation model features.

Conclusion: SAGE provides a scalable, model-agnostic framework for cohort-level interpretation of ABMIL survival models, enabling pathologists to understand model behavior and supporting potential biomarker identification.

Abstract: Attention-based multiple instance learning (ABMIL) is the predominant approach for slide-level prediction in computational pathology, yet its attention maps provide only local explanations: they indicate where a model focuses but not which histological features drive its predictions or how the model behaves across a patient cohort. We present Semantic Attention Global Explanations (SAGE), a post-hoc framework that extracts global, language-grounded explanations from a frozen ABMIL model. Using a pathology vision-language model, SAGE scores image patches against a dictionary of 25 histological concepts, aggregates these scores according to the model's learned attention, and quantifies how each concept relates to prediction risk across a cohort. Applied to survival prediction using seven TCGA cancer cohorts and three foundation models, SAGE recovered established prognostic features, such as the adverse association of necrosis, while revealing cancer-specific biology, including a favorable angiogenic signature in renal cell carcinoma consistent with known molecular subtypes. Ablation studies demonstrated that these associations depend on the model's learned attention rather than concept prevalence alone, and that the concept dictionary captures much of the prognostic information encoded by the foundation model features. Through semantically-grounded explanations, SAGE provides a scalable, model-agnostic framework for understanding what ABMIL survival models learn, enabling pathologists to interpret model behavior at the cohort level and offering the potential for biomarker identification.

</details>


### [8] [A Unified 2D Framework for DeepLesion Detection, Segmentation and Short Report Generation](https://arxiv.org/abs/2608.02805)
*Ruida Cheng,Tejas S. Mathai,Benjamin Hou,Qingqing Zhu,Zhiyong Lu,Matthew McAuliffe,Ronald M. Summers*

Main category: cs.CV

TL;DR: The authors developed a unified 2D lesion analysis framework on the DeepLesion dataset that integrates LLM-based reasoning, lesion detection, segmentation, and radiology report generation, achieving strong performance across all tasks including a 28.5% Dice score improvement over nnUNet for lesion segmentation.


<details>
  <summary>Details</summary>
Motivation: Building on prior work that used short-form findings with the ULS23 DeepLesion dataset, the authors aimed to extend their approach to the original DeepLesion dataset and address the challenging issue of lesion segmentation while integrating spatial and anatomical context into report generation.

Method: A unified 2D lesion analysis framework that integrates LLM-based reasoning, lesion bounding box detection, segmentation, and radiology report generation, applied to the original DeepLesion dataset.

Result: Detection achieved mAP50 of 70.1% and mAP50-95 of 46.4%; segmentation achieved Dice of 62.6% (28.5% improvement over nnUNet); report generation achieved BLEU_1 of 64.3%, BLEU_4 of 49.6%, METEOR of 34.7%, and ROUGE_L of 60.1%.

Conclusion: The framework successfully addresses the segmentation challenge in the original DeepLesion dataset with significant improvement over baseline, and effectively integrates spatial and anatomical context for report generation. Implementation and models are publicly released on GitHub.

Abstract: In previous work, we integrated large language models (LLMs) into the lesion segmentation model based on the ULS23 DeepLesion dataset, using short-form findings from the reports. In this study, we developed a unified 2D lesion analysis framework that integrates LLM-based reasoning, lesion bounding box detection, segmentation, and radiology report generation from the original DeepLesion dataset. In the testing phase, we achieved relatively high lesion bounding box detection accuracy with mAP50 of 70.1%, mAP50-95 of 46.4%; Lesion segmentation performance with a Dice score of 62.6%; short report generation accuracy with BLEU_1 score of 64.3%, BLEU_4 score of 49.6%, METEOR of 34.7%, and ROUGE_L of 60.1%. In this work, we address the challenging issue of segmentation in the original DeepLesion dataset and achieve a 28.5% Dice score improvement over the nnUNet lesion segmentation model. We also integrated spatial and anatomical context into the DeepLesion short report generation. We released the implementation, dataset, and models on Github. https://github.com/ruida/2D_DeepLesion_Foundation

</details>


### [9] [In-Context Collapse in Vision-Language Models and How to Mitigate it?](https://arxiv.org/abs/2608.02830)
*Mohammad Rostami*

Main category: cs.CV

TL;DR: This paper identifies and characterizes "in-context collapse" in vision-language models (VLMs): a sharp, sometimes catastrophic accuracy drop that occurs as many-shot in-context learning demonstrations accumulate, despite assumptions that more demonstrations should help. The collapse is localized to the vision-language integration pathway via causal lesion-and-rescue experiments, and can be corrected by CircA, a lightweight, one-time trained intervention that transfers collapse-resistance across task families.


<details>
  <summary>Details</summary>
Motivation: Many-shot in-context learning (ICL) is widely assumed to monotonically improve with more demonstrations, but the authors observe this assumption fails for some VLMs—accuracy sharply drops, sometimes below chance, as demonstrations accumulate. Understanding why this happens and how to fix it is critical for reliable deployment of VLMs.

Method: The authors evaluate many-shot ICL across an open VLM panel (0.5B–11B) and Claude Sonnet 4.5 on synthetic classification, natural-image classification, and VQA benchmarks. They use parameter-matched lesion-and-rescue experiments (adapters on connector/early-mid layers vs. late readout) to causally localize the collapse, and propose CircA—a one-time integration vaccine trained on a single synthetic task that transfers collapse-resistance to unseen task families.

Result: In-context collapse is graded and reproducible across models. Robustness to accumulating demonstrations and the ability to learn novel rules in context are dissociable capabilities, yielding three reproducible regimes. The collapse is causally localized to the vision-language integration pathway: an adapter on the connector/early-mid layers restores accuracy from 0.39 to 0.91 at 16 shots, while an equal-capacity late-readout adapter does not. CircA transfers collapse-resistance (chance→0.71/0.60 on CIFAR/Fashion). Interestingly, late readout layers are best for weight-based consolidation (higher accuracy, less forgetting, fewer parameters), but not for in-context integration.

Conclusion: In-context collapse is an integration failure at the vision-language interface, not a general learning failure. It is correctable by a lightweight, transferable intervention (CircA), and the optimal layers for in-context integration differ fundamentally from those for weight-based consolidation.

Abstract: Many-shot in-context learning (ICL) lets vision-language models (VLMs) adapt from image--label demonstrations without weight updates, and is widely assumed to improve as more demonstrations are supplied. We show the opposite: as demonstrations accumulate, a subset of VLMs undergo an \emph{in-context collapse}, a sharp, sometimes catastrophic accuracy drop spanning synthetic classification, natural-image classification, and VQA benchmarks, in some models falling below chance while outputs remain well-formed. Across an open VLM panel ($0.5$B--$11$B) and a frontier model (Claude Sonnet 4.5), the collapse is graded. Two capabilities turn out to be dissociable: robustness to accumulating demonstrations and the ability to learn a novel rule in context, their combinations yield three reproducible regimes. A parameter-matched lesion-and-rescue causally localizes the collapse to the vision-language integration pathway: an adapter on the connector and early/mid layers restores genuine learning (remap accuracy $0.39!\rightarrow!0.91$ at 16 shots), while an equal-capacity adapter on the late readout does not. We propose \textsc{CircA}, whose core is a one-time integration vaccine: trained once on one synthetic task, it transfers collapse-resistance to unseen task families (chance$\rightarrow$$0.71$/$0.60$ on CIFAR/Fashion). The layers best for in-context integration are not the layers best for weight-based consolidation, the late readout achieves higher accuracy and less forgetting at fewer parameters. The collapse is an integration failure at the vision--language interface, correctable by a lightweight, transferable intervention.

</details>


### [10] [CURV: Enhancing Chart Understanding Through Curriculum Visual Grounded Reasoning](https://arxiv.org/abs/2608.02833)
*Xuehang Guo,Pingyue Zhang,Ruiyi Zhang,Zhenhailong Wang,Hanrui Lyu,Heng Ji,Tong Sun,Qingyun Wang,Manling Li*

Main category: cs.CV

TL;DR: CURV is a curriculum learning framework that develops intrinsic visual grounded reasoning in MLLMs for chart question answering (CQA) by reformulating it as multi-step visual grounded reasoning with dynamic spatial attention, supported by a scalable three-level curriculum dataset (CCQA), achieving up to 20.50% improvements over baselines.


<details>
  <summary>Details</summary>
Motivation: Current multimodal large language models (MLLMs) struggle with chart question answering because they lack intrinsic visual grounded reasoning capabilities, leading to inaccurate perception and reasoning that is disconnected from visual evidence, even though extrinsic chain-of-thought prompting and visual cues help.

Method: CURV reformulates CQA as multi-step visual grounded reasoning, where each step coordinates logical reasoning with dynamic visual grounding through spatial attention concentration. A curriculum learning approach is used, supported by CCQA, a three-level curriculum dataset with scalable synthetic generation covering diverse chart types and reasoning patterns, progressing from basic single-operation reasoning to complex multi-chart compositional tasks.

Result: CURV achieves up to 20.50% improvements over baselines, generalizes to real-world benchmarks (up to 12.30%) and out-of-domain multimodal reasoning tasks (up to 10.20%).

Conclusion: The effectiveness of internalizing visual reasoning with dynamic grounding for enhanced chart understanding capabilities is validated, demonstrating that curriculum learning can help MLLMs develop intrinsic visual grounded reasoning abilities.

Abstract: Chart question answering (CQA) requires multimodal large language models (MLLMs) to integrate visual comprehension with logical reasoning, yet current models struggle with accurate visual grounding and coherent reasoning chains. While extrinsic chain-of-thought prompting and visual cues significantly improve performance, current MLLMs lack intrinsic visual grounded reasoning capabilities, leading to inaccurate perception and reasoning disconnected from visual evidence. To address these limitations, we propose CURV, a curriculum learning framework that develops intrinsic visual reasoning capabilities by reformulating CQA as multi-step visual grounded reasoning, where each step coordinates logical reasoning with dynamic visual grounding through spatial attention concentration. To assist model learning, we further introduce CCQA, a three-level curriculum dataset with scalable synthetic generation across diverse chart types and reasoning patterns. Our curriculum systematically progresses from basic single-operation reasoning to complex multi-chart compositional tasks. Experiments demonstrate that CURV achieves up to $\uparrow20.50\%$ improvements over baselines and is generalizable to real-world benchmarks (up to $\uparrow12.30\%$) and out-of-domain multimodal reasoning tasks (up to $\uparrow10.20\%$), validating the effectiveness of internalizing visual reasoning with dynamic grounding for enhanced chart understanding capabilities. Code is available at: https://xhguo7.github.io/CURV/.

</details>


### [11] [A Human-in-the-Loop Deep Learning Framework for Color Reconstruction of Lenticular Films](https://arxiv.org/abs/2608.02835)
*Saptarshi Neil Sinha,Tiago Kleist,Giorgio Trumpy*

Main category: cs.CV

TL;DR: A human-in-the-loop deep learning framework for color reconstruction in historical lenticular films (e.g., Kodacolor), featuring an editable vector-based lenticule boundary representation, decoupled detection/refinement architecture, and luminance-preserving chrominance merging to produce exhibitable color reconstructions where automated methods fail.


<details>
  <summary>Details</summary>
Motivation: Historical lenticular films encode color in a distinctive spatial format that requires specialized reconstruction. Existing automated approaches (doLCE, deep-doLCE) fail on challenging cases such as curved lenticules, low-contrast, or badly captured regions, preventing proper color recovery for archival/exhibition purposes.

Method: The framework introduces (1) an editable vector-based representation of lenticule boundaries that experts can interactively refine, (2) a decoupled architecture enabling targeted corrections and iterative fine-tuning that embeds expert knowledge into the detection model, and (3) a post-processing stage that merges reconstructed chrominance with the original film scan's luminance to preserve texture details from the silver emulsion.

Result: On a challenging lenticular film sequence where previous automated approaches fail, the HITL approach successfully produces high-quality, exhibitable color reconstructions with preserved texture, demonstrating robustness where automated methods are unsuitable.

Conclusion: This is the first work combining expert guidance, editable intermediate representations, and texture-preserving post-processing for lenticular film color reconstruction, advancing the state of the art in historical film restoration.

Abstract: Historical lenticular films, such as those created with the Kodacolor process, encode color information in a distinctive spatial format. This structure requires specialized techniques for accurate color reconstruction. While recent signal processing approaches like doLCE and deep learning methods like deep-doLCE have advanced automated color recovery, they often fail with cases such as curved lenticules, low-contrast, or badly captured regions. We propose a human-in-the-loop (HITL) deep learning framework which is designed for color reconstruction in lenticular films. Our approach introduces an editable, vector-based representation of lenticule boundaries, allowing experts to interactively refine boundary positions before color extraction and demosaicing. This decoupled architecture enables targeted corrections and iterative fine-tuning, embedding expert knowledge into the detection model and improving robustness across challenging frames. To preserve image details using information solely present in the original silver emulsion, we merge the reconstructed chrominance with the original film scan's luminance. We evaluate our pipeline on a challenging lenticular film sequence where previous automated approaches fail and the reconstructed colors are not suitable for exhibition. In contrast, our HITL approach successfully produces high-quality, exhibitable color reconstructions with preserved texture. This work is the first to combine expert guidance, editable intermediate representations, and texture-preserving post-processing for lenticular film color reconstruction, advancing the state of the art in this field.

</details>


### [12] [Localize, Don't Beautify: Client-Side Control of Image-Editing APIs for Cosmetic Surgery Previews](https://arxiv.org/abs/2608.02841)
*Sukhrobbek Ilyosbekov*

Main category: cs.CV

TL;DR: This study investigates whether client-side control alone can confine AI image edits (e.g., cosmetic surgery previews) to intended regions without model internals. Testing 6 commercial editors and 1 inpainting model, a masked composite approach improved localization by median 0.446 over prompt-only outputs across 196 scored edits, regardless of editor, without requiring provider access.


<details>
  <summary>Details</summary>
Motivation: Commercial image editors used for cosmetic previews often over-edit beyond the requested region (e.g., a nose edit changes skin or lighting). Existing methods for edit confinement require model internals not exposed by public APIs, so the authors ask whether client-side control alone can achieve region-localized edits.

Method: A pilot benchmark tested six commercial editing configurations plus one mask-based inpainting model on facelift-style and rhinoplasty edits. Three levels of client-side control were compared: (1) prompt alone, (2) masked composite (cutting edited region out and pasting back using landmark-derived masks), and (3) model-requested inpainting inside masks. Editing was scored using ArcFace cosine for identity preservation and CIELAB pixel-change ratios for localization within requested versus protected regions.

Result: Of 210 attempted edits, 196 were scored. The masked composite improved localization over prompt-only by a median of 0.446 (95% CI: 0.421-0.457) on 12 frontal faces while preserving edit strength in the requested region. Editors varied in edit strength versus identity retention trade-offs. The inpainting model did not outperform the simple composite, and no editor moved outputs closer to postoperative references in identity-embedding space.

Conclusion: Client-side masking and compositing can enforce region-localized edits across all tested editors without access to model internals, at low provider cost. The study is about control rather than clinical accuracy—no surgeons evaluated outputs, and each condition was generated once—but within that scope, surgical previews can be kept within intended regions using only client-side techniques.

Abstract: Ask a commercial image editor to preview a cosmetic procedure and it will often change more of the face than the request names: a nose edit can also smooth skin or alter lighting. Existing methods for confining an edit to one region require access to the model's internals, which a public editing API does not expose. We ask how much control is possible from the client side alone. In a pilot benchmark, six commercial editing configurations and one mask-based inpainting model perform facelift-style jaw-neck and rhinoplasty edits at three levels of client-side control: the prompt alone; cutting the edited region out of the response and pasting it back onto the original photograph through a landmark-derived mask (a masked composite); and asking the model itself to inpaint inside the mask where supported. Of 210 attempted edits, 196 could be scored. ArcFace cosine measures identity preservation; a CIELAB pixel-change ratio measures how much change lands inside the requested region rather than a protected facial zone. On the 12 frontal faces the regional metric could score, the masked composite improved localization over the paired prompt-only output by a median of 0.446 (95% face-clustered bootstrap interval 0.421-0.457) while changing the requested region about as much. Editors differed in edit strength versus identity retention, and the one inpainting model we tested did not beat the simple composite. Against each face's input-to-postoperative baseline, no editor moved its outputs closer to the postoperative photograph in identity-embedding terms. This is a study of control, not clinical accuracy: no surgeons rated the outputs, and each condition was generated once. Within that scope, keeping a surgical preview inside its intended region needs no access to the model; a mask and composite on the client enforce it across every editor tested, at low provider cost.

</details>


### [13] [Test Time Adaptation Methods for Point Cloud Registration in Laparoscopic Surgery](https://arxiv.org/abs/2608.02883)
*Nina Bodelot,Soufiane Belharbi,Eric Granger*

Main category: cs.CV

TL;DR: This paper analyzes and adapts test-time adaptation (TTA) methods for 3D point cloud registration in laparoscopic surgery, where intraoperative point clouds are noisy, sparse, and occluded compared to clean synthetic training data. The authors modify four representative TTA approaches from three families (model, normalization, and input adaptation) to handle asymmetric domain shifts between preoperative and intraoperative clouds, replacing classification-based objectives with registration-suitable ones. Evaluation on P2P and P2ILReg datasets shows all methods improve registration on P2P, input adaptation is most promising for clinical use due to low inference latency and consistent error reductions.


<details>
  <summary>Details</summary>
Motivation: Supervised registration networks are trained on synthetic organ pairs due to the lack of ground-truth transformations for real laparoscopic data. However, real intraoperative reconstructions suffer from domain shift (noise, sparsity, occlusion), degrading correspondence estimation at test time. Existing TTA methods cannot be directly applied because they rely on logits, entropy, class prototypes, or cache memories that are unavailable for registration, and registration involves an asymmetric shift affecting mainly the intraoperative cloud.

Method: The authors analyze and modify TTA methods from three families for 3D registration: (1) model adaptation via auxiliary-task updates, (2) input adaptation via backpropagation-free token purging and feature alignment, and (3) normalization adaptation via layer-normalization calibration. They adapt these methods to handle asymmetric shifts between preoperative and intraoperative clouds and replace classification-based entropy objectives with registration-appropriate losses. Evaluation uses a correspondence-based model trained on clean synthetic data, tested on corrupted synthetic (8 corruptions at 5 severity levels) and real target data.

Result: All TTA methods improve registration performance on P2P, but normalization adaptation degrades performance on P2ILReg. Considering computational overhead, input adaptation emerges as the most promising for laparoscopic surgery, providing low inference latency and consistent error reductions across both datasets.

Conclusion: Input adaptation is the most practical TTA strategy for point cloud registration in laparoscopic surgery, offering a good balance of effectiveness, speed, and consistency. The public code enables further exploration and benchmarking of TTA for registration tasks.

Abstract: 3D point cloud registration in laparoscopic surgery estimates the transformation between an intraoperative organ reconstructed from video and its preoperative mesh. Because ground-truth transformations are unavailable for real data, supervised networks are trained on synthetic organ pairs. At test time, real reconstructions differ from synthetic data and are noisy, sparse, and occluded, which degrades correspondence estimation. Test-time adaptation (TTA) can reduce this domain shift, but existing methods mainly rely on logits, entropy, class prototypes, or cache memories unavailable in registration. Registration also involves paired inputs with an asymmetric shift that primarily affects the intraoperative cloud. We analyse and modify state-of-the-art TTA methods from three families to 3D registration: model, normalization, and input adaptation. We analyze four representative approaches based on auxiliary-task model updates, backpropagation-free token purging, feature alignment, and layer-normalization calibration. We modify them to handle asymmetric shifts between preoperative and intraoperative point clouds and replace classification-based entropy objectives. Using a correspondence-based model trained on clean synthetic source data, we evaluate adaptation to corrupted synthetic and real target data on P2P and P2ILReg. For synthetic targets, we apply eight corruptions, including uniform noise and global density reduction, at five severity levels. All methods improve registration on P2P, whereas normalization adaptation degrades performance on P2ILReg. Considering the computational overhead of backpropagation-based adaptation, input adaptation is the most promising option for laparoscopic surgery, providing low inference latency and consistent error reductions across datasets. Code: https://github.com/ninaa-git/survey_pc_registration_tta

</details>


### [14] [Modeling Scientific Experiment Scenes: Dataset and Model](https://arxiv.org/abs/2608.02892)
*Minghao Zou,Qingtian Zeng,Shangkun Liu,Cong Liu,Paul L. Rosin,Guanghui Yue,Jun Liu,Wei Zhou*

Main category: cs.CV

TL;DR: This paper introduces PhysScene, the first Scene Graph Generation (SGG) dataset for physical experiment scenes, and proposes the Cross-Modal Dual-Path Generator (CM-DPG) to address challenges of long-tail predicate distributions and visual-textual semantic gaps in SGG.


<details>
  <summary>Details</summary>
Motivation: Existing SGG benchmarks focus on daily life images and overlook scientific experiment scenes, which are increasingly important for automated experimental analysis and smart education. These scenes have specialized instruments, task-specific semantics, and dense fine-grained physical relations not captured by current benchmarks.

Method: The paper proposes CM-DPG (Cross-Modal Dual-Path Generator), which enhances object-level semantic representations through joint visual-textual encoding, improves relational reasoning using complementary visual and geometric cues, and incorporates relation-aware pre-training, caption-derived pseudo-supervision, and adaptive weighting to support balanced learning across head and tail predicates.

Result: Extensive experiments on PhysScene and VG150 show that CM-DPG achieves competitive performance across multiple evaluation settings. Ablation studies validate the contribution of each component. The dataset and code are publicly available.

Conclusion: PhysScene bridges the gap in SGG for scientific experiment scenes, and CM-DPG effectively addresses long-tail predicate distributions and visual-textual semantic gaps, providing a strong baseline for open-vocabulary SGG in this domain.

Abstract: Scene Graph Generation (SGG) is fundamental to structured visual understanding, yet existing benchmarks focus mainly on daily life images and overlook scientific experiment scenes with specialized instruments, task-specific experimental semantics, and dense, fine-grained physical relations. These scenes are increasingly important for automated experimental analysis and smart education. To bridge this gap, we introduce PhysScene, the first SGG dataset for physical experiment scenes, providing densely annotated scene graphs and benchmarks under multiple supervision and protocol settings. PhysScene further exposes two key algorithmic challenges for SGG: pronounced long-tail relational predicate distributions and a substantial visual-textual semantic gap. To address these challenges, we propose the Cross-Modal Dual-Path Generator (CM-DPG), a model for robust open-vocabulary SGG. The model enhances object-level semantic representations through joint visual-textual encoding and improves relational reasoning using complementary visual and geometric cues. We also incorporate relation-aware pre-training, caption-derived pseudo-supervision, and adaptive weighting to support balanced learning across head and tail predicates. Extensive experiments on PhysScene and VG150 show that CM-DPG achieves competitive performance across multiple evaluation settings, with ablation studies validating the contribution of each component. The dataset and code are publicly available at https://github.com/ZMH-SDUST/CM-DPG.

</details>


### [15] [RealWeather: Realistic and Scene-Faithful Weather Translation with Driving World Models](https://arxiv.org/abs/2608.02953)
*Yuwei Ning,Liangzhi Wang,Yi Xiao,Zhenhua Wu,Yun Pang,Mingkun Chan,Jichang Li,Guanbin Li*

Main category: cs.CV

TL;DR: RealWeather is a driving world model for realistic and scene-faithful weather translation that learns authentic weather dynamics from real-world videos, using Progressive Realism Bootstrapping and Scene-Fidelity RL Optimization.


<details>
  <summary>Details</summary>
Motivation: Collecting paired videos of the same scenes under different weather conditions at scale is impractical, and existing methods relying on synthetic data, 3D weather editing, or geometry-conditioned generation often compromise weather realism or scene fidelity.

Method: RealWeather employs (1) Progressive Realism Bootstrapping, an iterative data-refinement strategy assisted by a Pseudo-Clear Generation pipeline that progressively replaces pseudo-style conditioning inputs with the model's own increasingly realistic videos, and (2) Scene-Fidelity RL Optimization, a reward-driven policy optimization that explicitly penalizes alterations to safety-critical driving elements.

Result: RealWeather significantly outperforms existing methods in visual realism and structural preservation, while enabling robust long-tail weather scenario generation and strong zero-shot out-of-distribution generalization.

Conclusion: The progressive bootstrapping strategy bridges the pseudo-to-real domain gap, allowing the model to adapt seamlessly to real-world input distributions and naturally support bidirectional clear-adverse weather translation while strictly enforcing structural integrity.

Abstract: Realistic weather translation is valuable for developing and evaluating autonomous driving systems, yet collecting paired videos of the same scenes under different weather conditions at scale is impractical. Existing methods therefore rely on synthetic data, 3D weather editing, or geometry-conditioned generation, often compromising weather realism or scene fidelity. We propose RealWeather, a driving world model for both realistic and scene-faithful weather translation. Our key idea is to learn authentic weather dynamics directly from real-world videos. Specifically, RealWeather employs Progressive Realism Bootstrapping, an iterative data-refinement strategy. Assisted by an auxiliary Pseudo-Clear Generation pipeline, training initially starts with pseudo-style conditioning videos. As training proceeds, these inputs are progressively replaced with increasingly realistic videos generated by the model itself. This strategy bridges the pseudo-to-real domain gap, allowing the model to adapt seamlessly to real-world input distributions and naturally support bidirectional clear adverse translation. Furthermore, to strictly enforce structural integrity and suppress hallucinations, we introduce Scene-Fidelity RL Optimization, a reward-driven policy optimization strategy that explicitly penalizes alterations to safety-critical driving elements. Extensive experiments demonstrate that RealWeather significantly outperforms existing methods in visual realism and structural preservation, while enabling robust long-tail weather scenario generation and strong zero-shot out-of-distribution generalization.

</details>


### [16] [Material-Segmented Per-Pixel Emissivity Correction for Thermographic Anomaly Detection in Cultural Heritage Digital Twins](https://arxiv.org/abs/2608.02964)
*Jonathan Klingspon,Scott McAvoy,Maurizio Seracini,Falko Kuester*

Main category: cs.CV

TL;DR: A training-free pipeline uses SAM 3.1 open-vocabulary segmentation on RGB imagery to derive per-pixel LWIR emissivity for quantitative thermography, improving temperature retrieval accuracy by reducing mean absolute error from 1.97K to 0.91K on synthetic benchmarks, though corrections are small on typical weathered heritage surfaces and concentrated on genuine low-emissivity exceptions.


<details>
  <summary>Details</summary>
Motivation: Quantitative longwave thermography of heritage surfaces suffers from the global-constant emissivity assumption in inverse-Planck temperature retrieval. On heterogeneous surfaces, emissivity varies within a single field of view, producing apparent-temperature artifacts that can mimic or mask subsurface anomalies, limiting the reliability of heritage thermography.

Method: The pipeline applies SAM 3.1 open-vocabulary segmentation to a colocated RGB channel, maps segments to a material-keyed LWIR emissivity table compiled from primary measurement literature, and propagates the field into a per-pixel inverse-Planck solve on raw radiometric data. Since no public dataset with raw radiometry, temperature reference, and colocated RGB exists, evaluation uses a physics-based synthetic benchmark plus four real datasets.

Result: On the synthetic benchmark, the correction reduces mean absolute error from 1.97 K to 0.91 K at 20 K contrast and beats the best fitted global constant on every layout when the emissivity table is accurate. However, on heritage-realistic emissivity distributions it does not outperform the global constant. Analysis reveals weathered outdoor heritage emissivities cluster near the conventional default, so corrections are small on typical surfaces. The dominant failure mode is open-vocabulary segmentation matching appearance rather than material, and a contraindicated regime where emissivity-defined anomalies are suppressed is characterized.

Conclusion: The pipeline provides a quantified operating-regime map and measurement-backed findings that temper the heritage application claim: the correction is beneficial primarily for genuine low-emissivity exceptions rather than typical weathered heritage surfaces, and practitioners should be aware of the segmentation failure mode and the contraindicated regime for emissivity-defined anomalies.

Abstract: Quantitative longwave thermography of heritage surfaces is limited by the global-constant emissivity assumption in inverse-Planck temperature retrieval; on heterogeneous surfaces emissivity varies within one field of view, producing apparent-temperature artifacts that mimic and mask subsurface anomalies. We present a training-free pipeline that derives per-pixel emissivity by applying SAM 3.1 open-vocabulary segmentation to a colocated, co-calibrated RGB channel, mapping segments to a material-keyed LWIR emissivity table compiled from primary measurement literature, and propagating the field into a per-pixel inverse-Planck solve on raw radiometric data. Lacking any public dataset with raw radiometry, a temperature reference, and a colocated RGB camera, we evaluate on a physics-based synthetic benchmark and four real datasets. On the benchmark, under a palette spanning the low-emissivity exceptions, the correction cuts mean absolute error from 1.97 K to 0.91 K at 20 K contrast and, with an accurate table, beats the best fitted global constant on every layout; on a heritage-realistic emissivity distribution it does not. We contribute a quantified operating-regime map, and a measurement-backed finding that tempers the heritage claim: weathered outdoor heritage emissivities cluster near the conventional default, so the correction is small on typical surfaces and concentrated on genuine low-emissivity exceptions. We characterize the dominant failure mode, in which open-vocabulary segmentation matches appearance rather than material, and the contraindicated regime in which emissivity-defined anomalies are suppressed.

</details>


### [17] [Qwen-3D: A Generalist 3D Vision-Language Model for Spatial Understanding](https://arxiv.org/abs/2608.02980)
*Lucy Lin,Ayush Jain,Yifan Liu,Katerina Fragkiadaki*

Main category: cs.CV

TL;DR: Qwen-3D is a geometry-aware Large Multimodal Model that compresses visual information from long videos using 3D geometric cues (depth and camera pose), augments visual tokens with 3D Rotary Positional Embeddings for direct attention in 3D scene space, and uses a query-based segmentation decoder to bridge language and dense geometric prediction.


<details>
  <summary>Details</summary>
Motivation: Existing 3D LMMs lag behind specialist 3D perception systems on grounding and segmentation tasks. The key limitation is geometry-aware decoding: current methods communicate 3D predictions through language tokens, proposal selection, or lightweight grounding queries, creating a bottleneck between language reasoning and dense geometric prediction. Additionally, scaling LMMs to long videos is challenging due to frame-centric tokenization and limited context windows.

Method: Qwen-3D compresses visual information within the Qwen backbone using multi-view geometric cues. It augments visual tokens with 3D Rotary Positional Embeddings (RoPE), allowing attention to operate directly in 3D scene space rather than across independent image frames. It also incorporates a query-based segmentation decoder that grounds language directly in the underlying 3D scene representation, unifying referential grounding, instance segmentation, and visual question answering across both images and videos.

Result: Qwen-3D surpasses existing 3D LMMs and outperforms several large proprietary 2D models across diverse benchmarks. It maintains strong performance on standard 2D vision-language benchmarks by jointly training on 2D and 3D data.

Conclusion: Geometry-aware decoding with a query-based segmentation decoder that grounds language directly in 3D scene space resolves the bottleneck between language reasoning and dense geometric prediction, enabling efficient long-horizon visual reasoning over static scenes while preserving strong 2D performance.

Abstract: Large Multimodal Models (LMMs) have achieved remarkable success on images and short videos, yet scaling them to long videos remains challenging due to frame-centric tokenization and limited context windows. 3D geometry provides a natural compression mechanism for visual streams: depth and camera pose enable observations from multiple views and time steps to be fused into a persistent, world-aligned representation. While recent 3D LMMs leverage geometry-aware representations to improve spatial reasoning, they continue to lag behind specialist 3D perception systems on grounding and segmentation tasks. We argue that a key limitation is geometry-aware decoding: existing methods communicate 3D predictions through language tokens, proposal selection, or lightweight grounding queries, creating a bottleneck between language reasoning and dense geometric prediction. Building on these insights, we introduce Qwen-3D, a geometry-aware LMM that compresses visual information within the Qwen backbone using multi-view geometric cues, enabling efficient long-horizon visual reasoning over static scenes. Qwen-3D augments visual tokens with 3D Rotary Positional Embeddings, allowing attention to operate directly in 3D scene space rather than across independent image frames and thereby facilitating scalable cross-view and temporal reasoning. To bridge language and geometry, Qwen-3D incorporates a query-based segmentation decoder that grounds language directly in the underlying 3D scene representation, unifying referential grounding, instance segmentation, and visual question answering across both images and videos. Across a diverse set of benchmarks, Qwen-3D surpasses existing 3D LMMs and outperforms several large proprietary 2D models. Notably, Qwen-3D achieves these improvements while maintaining strong performance on standard 2D vision-language benchmarks by jointly training on 2D and 3D data.

</details>


### [18] [V-FIND: Revealing the Intrinsic Forgery Knowledge Encoded in Video Forgery Detectors](https://arxiv.org/abs/2608.03008)
*Shichao Kan,Chengpeng Hong,Jingtong Dou,Chuancheng Shi,Yuhan Liu,Linrui Xu,Yixiong Liang,Yigang Cen,Yanpeng Sun,Fei Shen,Tat-Seng Chua*

Main category: cs.CV

TL;DR: The paper proposes V-FIND, a framework that discovers sparse forgery-discriminative neurons within video forgery detectors, enabling strong detection performance with a frozen backbone and a lightweight linear classifier, rather than full-model retraining.


<details>
  <summary>Details</summary>
Motivation: Existing video forgery detection relies on resource-intensive full-model retraining, while the latent forgery-discriminative knowledge inside detectors remains unexplored. The authors ask whether detection can be achieved by uncovering and activating sparse forensic knowledge within the detector.

Method: V-FIND first localizes critical layers exhibiting pronounced discrepancies between real and forged videos, then identifies latent anchor neurons that consistently carry forgery-discriminative signals, organizing them into a compact forensic subspace. The backbone is frozen and only a lightweight linear classifier is trained.

Result: The method delivers strong detection performance across multiple external benchmarks for generated videos, and neuron intervention experiments provide direct evidence for the functional specificity of the discovered neurons.

Conclusion: Video forgery detectors contain sparse, extractable, and reusable forgery-discriminative knowledge, offering a new perspective on understanding and exploiting their intrinsic forensic capability.

Abstract: As generated videos become increasingly realistic, reliable video forgery detection is increasingly important. Existing studies typically optimize and use video forgery detectors as black boxes, while the latent forgery-discriminative knowledge inside them remains largely unexplored. Instead of continuing to rely on resource-intensive full-model retraining to steadily improve detection performance, we ask whether video forgery detection can also be achieved by uncovering and activating sparse forensic knowledge within the detector. We find that forgery-discriminative knowledge is not uniformly distributed across the full representation space, but is concentrated in a sparse set of functionally specialized neurons. Based on this insight, we propose a video forgery-intrinsic neuron discovery (V-FIND) framework. V-FIND first localizes critical layers that exhibit pronounced discrepancies between real and forged videos, and then identifies latent anchor neurons that consistently carry forgery-discriminative signals, organizing them into a compact forensic subspace. With the original backbone frozen and only a lightweight linear classifier trained, this subspace still delivers strong detection performance across multiple external benchmarks for generated videos. Further neuron intervention experiments provide direct evidence for the functional specificity of the discovered neurons. Overall, these results suggest that video forgery detectors contain sparse, extractable, and reusable forgery-discriminative knowledge, offering a new perspective on understanding and exploiting their intrinsic forensic capability.

</details>


### [19] [Clinically-Grounded Hierarchical Classification for Consistent Chest X-ray Interpretation](https://arxiv.org/abs/2608.03016)
*Jong Hak Moon,Minjun Kim,Minjun Kim*

Main category: cs.CV

TL;DR: CHASE is a unified single-stage framework for chest X-ray classification that mirrors radiologists' coarse-to-fine reasoning through a three-level taxonomy (9 anatomical regions, 17 sub-regions, 28 findings), jointly optimizing multi-level supervision, cross-level probability alignment, and hierarchy-violation penalties within a shared Vision Transformer backbone to achieve anatomically consistent predictions.


<details>
  <summary>Details</summary>
Motivation: Chest X-ray interpretation is inherently hierarchical, but existing automated systems treat it as flat classification, failing to capture inter-level dependencies or enforce coherence between coarse (anatomical) and fine (pathological) predictions.

Method: CHASE uses a shared Vision Transformer backbone with three components: (1) multi-level supervision across the three-level taxonomy, (2) cross-level probability alignment to enforce consistency, and (3) a hierarchy-violation penalty. This jointly constrains fine-grained findings to be anatomically supported by coarser-level context rather than predicted in isolation.

Result: Experiments show CHASE outperforms flat and hierarchical baselines across all three levels, achieves superior probabilistic hierarchy consistency, and produces level-wise attention maps confirming anatomically grounded predictions.

Conclusion: Structured enforcement of cross-level dependencies in a single-stage framework yields better and more anatomically consistent chest X-ray classification, aligning automated predictions with radiologists' reasoning.

Abstract: Accurate chest X-ray interpretation is inherently hierarchical. Clinical decisions depend not only on what abnormality is present but where it is situated, requiring reasoning from broad anatomical systems down to specific pathological findings. Yet existing automated systems largely treat this as a flat classification problem, failing to capture inter-level dependencies or enforce coherence between coarse and fine predictions. We propose CHASE (Classification with Hierarchical Analysis and Structured Enforcement), a unified single-stage framework that mirrors radiologists' coarse-to-fine reasoning through a clinically driven three-level taxonomy of 9 anatomical regions, 17 sub-regions, and 28 pathological findings. CHASE jointly optimizes multi-level supervision, cross-level probability alignment, and a hierarchy-violation penalty within a shared Vision Transformer backbone. This ensures that fine-grained findings are anatomically supported by their coarser-level context rather than predicted in isolation. Experiments demonstrate that CHASE outperforms flat and hierarchical baselines across all levels while achieving superior probabilistic hierarchy consistency, with level-wise attention maps confirming anatomically grounded predictions. Code is available at: https://github.com/yejix-ai/CHASE.

</details>


### [20] [Standalone DINOv3 for Training-Free Open-Vocabulary Semantic Segmentation in Remote Sensing](https://arxiv.org/abs/2608.03023)
*Changhao Zhao,Haoxiang Li,Yuke Li,Hai Liu,LingLin Zeng*

Main category: cs.CV

TL;DR: DinoSplat-OV is a training-free framework that adapts DINOv3 (with its DINO.txt image-text contrastive capability) to remote sensing open-vocabulary semantic segmentation, using Text-aware Laplacian Propagation and Gaussian Splatting Upsampling to handle dense, multi-scale, large imagery.


<details>
  <summary>Details</summary>
Motivation: Remote sensing semantic segmentation requires costly pixel-level annotations, motivating training-free open-vocabulary approaches. The release of DINOv3 with DINO.txt enables open-vocabulary capability in a standalone DINO backbone, but this potential has not yet been exploited for remote sensing segmentation.

Method: The framework uses a Text-aware Laplacian Propagation module to de-noise patch-level predictions by combining textual semantic affinities with local visual similarity, and a Gaussian Splatting Upsampling module that reconstructs pixel-level features via RGB-guided anisotropic aggregation and test-time optimization. A global-anchor sliding-window strategy supports large-scale imagery processing.

Result: Experiments on UDD5, DOTA, and LoveDA datasets demonstrate competitive or superior performance over existing training-free methods for remote sensing open-vocabulary segmentation.

Conclusion: DinoSplat-OV effectively fills the gap of DINO-series models in training-free open-vocabulary remote sensing segmentation and provides a viable new path for future advances in this direction.

Abstract: Remote sensing semantic segmentation is hindered by costly pixel-level annotations, motivating training-free open-vocabulary methods. Recently, the recent release of DINOv3 brings DINO.txt, which equips the standalone DINO backbone with image-text contrastive learning and thus opens up the possibility of open-vocabulary segmentation. We propose DinoSplat-OV, a training-free framework that adapts DINOv3 to remote sensing without fine-tuning or additional pretraining. Targeting the dense distribution, multi-scale nature, and large size of remote sensing imagery, we design two core modules. Its Text-aware Laplacian Propagation module de-noises patch-level predictions by combining textual semantic affinities with local visual similarity, improving regional consistency while preserving boundaries. Its Gaussian Splatting Upsampling module reconstructs pixel-level features through RGB-guided anisotropic aggregation and test-time optimization. A global-anchor sliding-window strategy further supports large-scale imagery. Experiments on UDD5, DOTA, and LoveDA demonstrate competitive or superior performance over existing training-free methods, effectively filling the gap of DINO-series models in training-free open-vocabulary segmentation and providing a viable new path for further advances in this direction.

</details>


### [21] [CAPE-T2V: Captioner-Anchored Prompt Enhancement toward Two-Sided Conditioning Alignment in Text-to-Video Generation](https://arxiv.org/abs/2608.03046)
*Yizhuo Jia,Jingyun Hua,Yuanxing Zhang*

Main category: cs.CV

TL;DR: CAPE-T2V addresses the PE-Caption gap in text-to-video diffusion transformers—where inference-time prompt enhancer (PE) outputs differ from DiT training captions despite shared schemas—through a two-step approach: (1) fine-tuning the PE with three types of training examples to better map source inputs to target captions, and (2) fine-tuning the DiT on video-derived captions rewritten by this "Anchored PE," ensuring the same PE is used at inference. This achieves higher scores on multiple benchmarks and demonstrates a smaller distributional gap between training captions and inference-time PE outputs.


<details>
  <summary>Details</summary>
Motivation: Despite shared schemas between PE outputs and DiT training captions, residual mismatches persist in detail selection, information organization, descriptive granularity, and phrasing. This PE-Caption gap negatively impacts T2V generation quality, motivating a framework that aligns both the prompt enhancer and the diffusion transformer side of the pipeline.

Method: CAPE-T2V is a two-step framework. First, it constructs three types of PE training examples—pairing captioner-generated targets with concise source captions, detailed source captions, or pseudo user prompts—and fine-tunes the PE to map inputs to paired targets. Second, it fine-tunes the DiT on video-derived captions rewritten by this Anchored PE and uses the same PE to rewrite user prompts at inference time.

Result: CAPE-T2V achieves higher aggregate scores than a baseline using the same caption schema on StoryEval, VBench-2.0, and T2V-CompBench across Wan2.2 and LTX-2.3. It also exhibits a smaller PE-Caption gap, with its DiT fine-tuning captions closer in distribution to inference-time PE outputs as measured by squared maximum mean discrepancy.

Conclusion: The results support CAPE-T2V as an effective approach to mitigating the PE-Caption gap in T2V generation, achieving two-sided conditioning alignment between prompt enhancement and DiT training.

Abstract: Text-to-video (T2V) diffusion transformers (DiTs) are trained with detailed video captions, whereas inference often relies on user prompts rewritten by a prompt enhancer (PE). Prior work has improved generation by optimizing the PE, the DiT, or both; some methods have also sought to narrow the training-inference mismatch through shared schemas. Yet even within a shared schema, inference-time PE outputs and DiT training captions may still differ in detail selection, information organization, descriptive granularity, and phrasing. We refer to this residual mismatch as the PE-Caption gap and introduce CAPE-T2V, a two-step Captioner-Anchored Prompt Enhancement framework toward two-sided conditioning alignment in T2V generation. First, CAPE-T2V constructs three types of PE training examples, pairing captioner-generated targets with concise source captions, detailed source captions, or pseudo user prompts derived from those targets. It then fine-tunes the PE to map each input to its paired target. Second, CAPE-T2V fine-tunes the DiT on video-derived captions rewritten by the Anchored PE; the same PE rewrites user prompts at inference. Relative to a baseline using the same caption schema, CAPE-T2V achieves higher aggregate scores on StoryEval, VBench-2.0, and T2V-CompBench across Wan2.2 and LTX-2.3. Further, CAPE-T2V exhibits a smaller PE-Caption gap than the baseline: its DiT fine-tuning captions are closer in distribution to inference-time PE outputs, as measured by squared maximum mean discrepancy in a fixed embedding space. Overall, these results support CAPE-T2V as an effective approach to mitigating the PE-Caption gap. The project is available at https://github.com/yizzz927/CAPE-T2V.

</details>


### [22] [AIDE: Automated Instruction via Distilled Expertise for Reference-Free Motor Skill Coaching](https://arxiv.org/abs/2608.03047)
*Yoshiki Ito*

Main category: cs.CV

TL;DR: AIDE (Automated Instruction via Distilled Expertise) generates natural-language coaching feedback on motor skills from a learner's pose sequence alone at inference, using expert references only during training. A teacher model learns feedback generation from paired learner-expert poses via a frozen language model, and a student model distills this capability by replacing the explicit expert comparison with an auxiliary module that produces complementary tokens from the learner's pose alone. AIDE outperforms reference-free baselines on most metrics and matches methods that require expert demonstrations at both training and inference.


<details>
  <summary>Details</summary>
Motivation: Expert coaches are scarce and expensive, limiting access to quality motor-skill feedback that could accelerate learning. Existing reference-based methods require expert demonstrations at both training and inference time, which restricts practical deployment in real-world scenarios where expert references are typically unavailable at inference.

Method: AIDE uses a two-stage distillation framework: (1) a teacher model learns to generate feedback from paired learner-expert poses using a frozen language model, producing separate learner tokens and difference tokens encoding the learner-expert discrepancy; (2) a student model inherits the teacher's encoder and weight initialization but replaces explicit expert comparison with an auxiliary module that generates complementary tokens from the learner's pose alone, enabling reference-free inference.

Result: On the ExpertAF dataset, AIDE outperforms reference-free baselines on most metrics and performs comparably to methods requiring expert demonstrations at both training and inference. LLM-based evaluation also supports these findings.

Conclusion: AIDE demonstrates that expert references can be exploited during training and effectively distilled into a model that generates quality feedback from the learner's pose alone at inference, achieving competitive performance without requiring expert demonstrations at deployment time—a significant step toward practical, scalable coaching feedback systems.

Abstract: Generating natural-language coaching feedback on motor skills can accelerate learning, yet expert coaches are scarce and expensive. Existing reference-based methods require expert demonstrations at both training and inference time, limiting practical deployment. We propose AIDE (Automated Instruction via Distilled Expertise), a framework that exploits expert references only during training and generates feedback from a learner's pose sequence alone at inference. A teacher model first learns to generate feedback from paired learner-expert poses via a frozen language model, producing separate learner tokens and difference tokens that encode the learner-expert difference. A student model then inherits the teacher's encoder and weight initialization, replacing the explicit expert comparison with an auxiliary module that produces complementary tokens from the learner's pose alone. On the ExpertAF dataset, AIDE outperforms reference-free baselines on most metrics and performs comparably to methods requiring expert demonstrations at both training and inference, with LLM-based evaluation supporting these findings.

</details>


### [23] [PDD-RRG: Posterior Diagnostic Decision for Study-level Radiology Report Generation](https://arxiv.org/abs/2608.03055)
*Yang Yu,Yiming Ji,Bin Dai,Dong Zhang,Zhiyong Zhou,Shoushan Li,Yakang Dai*

Main category: cs.CV

TL;DR: PDD-RRG introduces a posterior diagnostic decision framework that aggregates reports generated from multiple data subsets using Bayesian posterior probabilities and learned thresholds, improving clinical efficacy of existing RRG models without retraining.


<details>
  <summary>Details</summary>
Motivation: Existing radiology report generation (RRG) methods fail to fully utilize all examination-relevant information, and incorporating multi-view images or historical data can sometimes introduce avoidable diagnostic errors due to conflicting inputs.

Method: A decision-making stage is added after report generation. Multiple data subsets are created and an existing RRG model generates reports from different perspectives. Bayesian posterior probabilities and learned thresholds for each clinical observation are used to derive an aggregated diagnostic conclusion, which then refines the generated report.

Result: Experiments on MIMIC-CXR show that PDD-RRG effectively enhances the clinical efficacy of existing RRG models without requiring any retraining.

Conclusion: Post-hoc diagnostic decision integration can improve RRG clinical performance by reconciling potentially conflicting diagnoses from diverse input perspectives.

Abstract: Automatic radiology report generation (RRG) aims to simulate the workflow of radiologists, assisting them in clinical diagnosis. However, existing methods often fall short in utilizing all information relevant to the examination, as is typically done in clinical practice. Although some works attempt to incorporate multi-view images and historical data, these additional inputs may sometimes lead to avoidable diagnostic errors on the contrary. To address these challenges, we introduce a decision-making stage after report generation for the first time and propose a Posterior Diagnostic Decision framework (PDD-RRG) to integrate potentially conflicting diagnoses. Specifically, we create various subsets of input data and utilize an existing RRG model to generate reports from different perspectives. Then the Bayesian posterior probability and the learned thresholds for each clinical observation are calculated to obtain an aggregated diagnostic conclusion, which is subsequently used to refine the generated report. Experiments on MIMIC-CXR demonstrate that our proposed PDD-RRG can effectively enhance the clinical efficacy of existing RRG models without any retraining.

</details>


### [24] [TASQ: Temporal-Adaptive Bit Sparsification Quantization for Diffusion Models](https://arxiv.org/abs/2608.03057)
*Seokho Han,Dongwei Wang,Jinhee Kim,Yiran Chen,Kang Eun Jeon,Huanrui Yang,Jong Hwan Ko*

Main category: cs.CV

TL;DR: TASQ is a temporal-adaptive quantization method for diffusion models that separates storage precision from per-step computation precision, using a shared max-precision weight buffer with a learned Temporal-Spatial LSB Mask to reduce BitOps at less sensitive denoising steps, achieving 25-50% cycle reduction over static quantization and 6.1-7.5x over naive 8-bit bit-serial execution.


<details>
  <summary>Details</summary>
Motivation: Static quantization assigns one weight precision across all denoising steps, forcing the worst-case (most sensitive step) precision on every step. This causes unnecessary computational cost because many steps can tolerate lower precision. The goal is to decouple storage precision (fixed by worst case) from computation precision (adaptable per step).

Method: TASQ stores one shared maximum-precision weight buffer and learns a Temporal-Spatial LSB Mask that selects lower effective precision per layer and per denoising stage by truncating least-significant bits (LSB). A Temporal-Precision Engine maps this learned schedule to bit-serial execution, where cycles scale with effective precision and switching precision incurs no measured cycle overhead, eliminating per-stage weight copies and runtime search.

Result: On PixArt-Sigma, SANA-1.6B, and SDXL-Turbo, TASQ achieves quality comparable to static quantization while reducing execution cycles by 25-50% over static quantization and by 6.1-7.5x over naive static 8-bit bit-serial execution.

Conclusion: TASQ demonstrates that decoupling storage precision from per-step computation precision in diffusion model quantization can significantly reduce computational cost without quality loss, providing an efficient hardware-aware solution for temporal-adaptive quantization.

Abstract: Static quantization assigns one weight precision to every denoising step. To preserve quality, that precision must accommodate the most quantization-sensitive step, even though many other steps can tolerate fewer bits. The resulting model may satisfy its memory budget, but it repeatedly pays worst-case arithmetic throughout the denoising trajectory. We introduce Temporal-Adaptive Bit Sparsification Quantization (TASQ) to separate these two costs. TASQ stores one shared maximum-precision weight buffer and learns a Temporal-Spatial LSB Mask that selects a lower effective precision for each layer and denoising stage by truncating least-significant bits. Storage therefore remains fixed by the worst case, while BitOPs decrease at less sensitive stages without per-stage weight copies or runtime search. A Temporal-Precision Engine maps the learned schedule to bit-serial execution, where cycles scale with effective precision and switching precision has no measured cycle overhead. On PixArt-Sigma, SANA-1.6B, and SDXL-Turbo, TASQ achieves quality comparable to static quantization with less computation. Together with the Temporal-Precision Engine, it reduces execution cycles by 25 to 50 percent over static quantization and by 6.1 to 7.5x over a naive static 8-bit bit-serial execution. Code is available at https://github.com/seokho-han/tasq.

</details>


### [25] [RIDGE: Re-Noising with Internal Dynamic Guidance for Image Editing](https://arxiv.org/abs/2608.03059)
*Ruiliang Gong,Zhen Wang,Yanghao Wang,Long Chen*

Main category: cs.CV

TL;DR: RIDGE is an inversion-free, training-free flow-based image editing method that re-noises an evolving approximation of the clean target state and applies internal dynamic guidance to improve the trade-off between source preservation, target alignment, and perceptual quality.


<details>
  <summary>Details</summary>
Motivation: Existing inversion-free flow-based editing relies on equal-displacement construction, which keeps displacement between noisy source and target-side states constant across noise levels. This is inconsistent with noising dynamics—displacement should contract as noise increases—leading to overly aggressive updates at high noise levels.

Method: RIDGE maintains the edited state as an evolving approximation to the unavailable clean target state. It re-noises this approximation using the same noise level and noise sample as the clean source state, allowing noisy displacement to decrease naturally with increasing noise. It also applies internal dynamic guidance during early high-noise steps, using a clean target state prediction with a soft dynamic mask derived internally from the model, without external segmentation or detection models.

Result: Experiments on two benchmarks using SD3 Medium and FLUX.1-dev backbones show RIDGE offers a favorable aggregate trade-off among source preservation, target alignment, and perceptual quality.

Conclusion: RIDGE provides an effective inversion-free and training-free editing approach by correcting the displacement inconsistency in equal-displacement construction and leveraging internal model guidance, achieving a better balance in editing quality metrics.

Abstract: Inversion-free flow-based image editing avoids latent inversion, but still requires a target-side state at every editing step. The widely used equal-displacement construction keeps the displacement between the noisy source state and the target-side state unchanged across noise levels. This is inconsistent with noising, under which the displacement between two clean states noised with the same noise level and noise sample should contract as the noise level increases. Thus, it can lead to overly aggressive updates at high noise levels. We introduce RIDGE: Re-Noising with Internal Dynamic Guidance for Image Editing, an inversion-free and training-free method that maintains the edited state as an evolving approximation to the unavailable clean target state. RIDGE re-noises this approximation using the same noise level and noise sample as the clean source state, allowing their noisy displacement to decrease naturally with increasing noise. Since the edited state initially contains limited target semantics, RIDGE further applies internal dynamic guidance during the early high-noise steps. A clean target state prediction guides the provisional edited state through a soft dynamic mask derived internally from the model, focusing guidance on regions that require modification without external segmentation or detection models. Experiments on two benchmarks using two backbones, SD3 Medium and FLUX.1-dev, show that RIDGE offers a favorable aggregate trade-off among source preservation, target alignment, and perceptual quality.

</details>


### [26] [Global Graph-Validated Optimization for VLM-based 3D Indoor Scene Generation](https://arxiv.org/abs/2608.03064)
*Jialu Huang,Yingxuan You,Fei Wang,Zheng Dang*

Main category: cs.CV

TL;DR: This paper introduces a graph-based intermediate representation with hybrid search-and-refinement strategy for open-vocabulary 3D indoor layout generation, addressing limitations of implicit inter-asset modeling and local optimization by separating semantic coherence (GSV) from physical feasibility (GPFS).


<details>
  <summary>Details</summary>
Motivation: Existing methods for text-to-3D indoor layout generation using LLMs/VLMs model inter-asset relations implicitly or rely on local pairwise constraints and local optimization, which are poorly aligned with the global, highly non-convex layout space. This leads to locally plausible yet globally inconsistent or physically infeasible scenes.

Method: The method consists of two key components: (1) Global Semantic Verification (GSV) represents scenes as structured graphs and enforces semantic constraints through rule-based verification to produce globally consistent semantic scaffolds; (2) Global Physical Feasibility Search (GPFS) combines evolutionary search for global exploration with gradient-based refinement for local exploitation, reducing dependence on VLM-proposed initialization.

Result: The method achieves state-of-the-art performance in open-vocabulary 3D indoor layout generation, improving both semantic consistency and physical plausibility over existing approaches.

Conclusion: By decoupling semantic coherence from physical feasibility and combining global search with local refinement, layout generation moves beyond local relational modeling and initialization-sensitive optimization toward globally consistent reasoning and search.

Abstract: We study open-vocabulary 3D indoor layout generation, which synthesizes diverse and physically plausible scenes from unlabeled 3D assets using free-form language instructions. Recent methods leverage large language models (LLMs) and vision-language models (VLMs) to generate structured scenes from text. However, most model inter-asset relations implicitly or rely on local pairwise constraints and local optimization. These formulations are poorly aligned with the global, highly non-convex layout space, often yielding locally plausible yet globally inconsistent or physically infeasible scenes. We address this problem with a graph-based intermediate representation that separates semantic coherence from physical feasibility, together with a hybrid search-and-refinement strategy. First, Global Semantic Verification (GSV) represents scenes as structured graphs and enforces semantic constraints through rule-based verification. This explicit validation removes contradictory configurations and produces a globally consistent semantic scaffold. Second, Global Physical Feasibility Search (GPFS) combines evolutionary search for global exploration with gradient-based refinement for local exploitation. It reduces dependence on VLM-proposed initialization and improves robustness in non-convex and discontinuous feasible spaces. Together, GSV and GPFS move layout generation beyond local relational modeling and initialization-sensitive optimization toward globally consistent reasoning and search. Experiments show that our method achieves state-of-the-art performance in open-vocabulary 3D indoor layout generation, improving both semantic consistency and physical plausibility.

</details>


### [27] [LDU-Bench: Multimodal LLM Evaluation for Lithography Defect Understanding under Layout-Varying Circuit Backgrounds](https://arxiv.org/abs/2608.03078)
*Huanglong Ji,Botong Zhao,Shujing Lv,Yue Lv*

Main category: cs.CV

TL;DR: LDU-Bench is a multi-task multimodal benchmark for lithography defect understanding, decomposing the review workflow into four tasks (defect triage, morphology recognition, coarse localization, and image-conditioned cause analysis) and evaluating MLLMs with task-level metrics, diagnostic readouts, and the Lithography Closure Score (LCS). Experiments reveal that while existing MLLMs can reliably perform defect triage, this capability does not transfer to downstream review stages, with morphology alignment, localization, and evidence-to-cause mapping remaining major bottlenecks.


<details>
  <summary>Details</summary>
Motivation: In lithography review, merely detecting whether a defect exists is insufficient for engineering inspection—models must also understand defect morphology, spatial location, and potential causes with supporting evidence. Existing multimodal LLMs lack a systematic benchmark that evaluates these deeper understanding capabilities.

Method: LDU-Bench decomposes the lithography review workflow into four independent tasks built from real lithography and IC review images: defect triage, morphology recognition, coarse localization, and image-conditioned cause analysis. The benchmark uses task-level metrics, diagnostic readouts, and the Lithography Closure Score (LCS) for systematic evaluation.

Result: Existing MLLMs perform defect triage relatively reliably, but this capability does not stably transfer to downstream review stages. Morphology alignment, effective localization, and evidence-to-cause mapping are identified as major bottlenecks. Diagnostics show this capability break reflects insufficient structured understanding across semantic levels rather than single-metric fluctuation.

Conclusion: LDU-Bench provides a quantifiable and diagnostic unified platform for evaluating the usability, failure points, and capability boundaries of industrial MLLMs in lithography review chains.

Abstract: Multimodal large language models have demonstrated strong defect recognition capability in industrial anomaly detection. However, in lithography review, merely determining whether an image contains a defect is insufficient for engineering inspection; models must also understand defect morphology, spatial location, and the potential causes supported by visible evidence. To this end, this paper proposes LDU-Bench, a multi-task multimodal benchmark for lithography defect understanding. Constructed from real lithography and integrated-circuit review images, LDU-Bench decomposes the review workflow into four independent tasks: defect triage, morphology recognition, coarse localization, and image-conditioned cause analysis. It systematically evaluates models using task-level metrics, diagnostic readouts, and the Lithography Closure Score (LCS). Experimental results show that although existing MLLMs can perform defect triage relatively reliably, this ability does not stably transfer to downstream review stages. Morphology alignment, effective localization, and evidence-to-cause mapping remain the major bottlenecks. Further diagnostics indicate that this capability break is not a fluctuation of a single metric, but reflects insufficient structured understanding across semantic levels. Overall, LDU-Bench provides a quantifiable and diagnostic unified platform for evaluating the usability, failure points, and capability boundaries of industrial MLLMs in lithography review chains.

</details>


### [28] [CorePath: A Breast-Specialized Pathology Foundation Model for Core Needle Biopsy Diagnosis and Risk-Controlled Report Generation](https://arxiv.org/abs/2608.03079)
*Ting Yin,Danning Li,Chen Shu,Xiaoxia Yao,Boyu Fu,Yujing Chang,Tianyu Shi,Mengna Feng,Jie Chen,Jing Fu,Xiuli Xiao,Tianlin Li,Mumin Shao,Jiaxin Bi,Wenchuan Zhang,Xiaoyan Wu,Xiao Han,Zhang Zhang,Yuhao Yi,Hong Bu*

Main category: cs.CV

TL;DR: CorePath is a breast-specialized multimodal pathology foundation model fine-tuned from PRISM on 7,901 paired core needle biopsy whole-slide images and diagnostic reports. It outperforms general pathology foundation models across cancer detection, invasion assessment, and histological subtyping. CorePath-CRG adds conformal subtype-confidence gating with Learn-Then-Test risk control, achieving zero non-breast hallucinations in released reports and enabling selective report release with subtype-level fallback.


<details>
  <summary>Details</summary>
Motivation: Breast core needle biopsy (CNB) is critical for breast cancer diagnosis but remains challenging due to limited tissue sampling, lesion heterogeneity, and subtle morphologic overlap that can obscure subtype distinctions. There is a need for a domain-specialized foundation model that can accurately diagnose and generate reliable reports for CNB.

Method: CorePath is fine-tuned from PRISM using 7,901 paired CNB whole-slide images and diagnostic reports from two centers. The model is evaluated across six CNB cohorts and two public benchmarks without task-specific retraining. CorePath-CRG extends the model with conformal subtype-confidence gating combined with Learn-Then-Test risk control to enable selective report release, subtype-level fallback, and deferral.

Result: CorePath achieved weighted AUCs of 0.9526-0.9735 for five-class CNB histological subtyping across private centers, and outperformed leading pathology foundation models on public benchmarks (0.7780 for BCNB invasive carcinoma subtyping, 0.8178 for BRACS lesion stratification, 0.8252 for BRACS fine-grained classification). It reduced non-breast hallucinations from 30.1% to 2.8% in report generation. CorePath-CRG achieved zero non-breast hallucinations among released outputs with the strongest overall performance in LLM-based Evaluation Scores and quantitative report-generation metrics.

Conclusion: Domain-specialized foundation models with statistical risk control offer a promising approach for accurate breast CNB diagnosis and reliable report generation, significantly improving both diagnostic accuracy and report fidelity compared to general pathology foundation models.

Abstract: Breast core needle biopsy (CNB) is central to breast cancer diagnosis yet remains challenging because limited tissue sampling, lesion heterogeneity, and subtle morphologic overlap can obscure subtype distinctions. We developed CorePath, a breast-specialized multimodal pathology foundation model fine-tuned from PRISM using 7901 paired CNB whole-slide images and diagnostic reports from two centers. Evaluated across six CNB cohorts and two public breast pathology benchmarks without task-specific retraining, CorePath consistently outperformed PRISM across cancer detection, invasion assessment, and histological subtyping. It achieved weighted area under the receiver operating characteristic curves (AUCs) of 0.9526-0.9735 for five-class CNB histological subtyping across private centers. On public benchmarks, CorePath outperformed leading pathology foundation models, achieving the highest weighted AUCs of 0.7780 for BCNB invasive carcinoma subtyping, 0.8178 for BRACS lesion stratification, and 0.8252 for BRACS fine-grained classification. In report generation, CorePath reduced the overall non-breast hallucinations from 30.1% to 2.8%, demonstrating improved domain fidelity after breast-specific adaptation. CorePath-CRG further combined conformal subtype-confidence gating with Learn-Then-Test risk control to enable selective report release, subtype-level fallback, and deferral. CorePath-CRG achieved zero non-breast hallucinations among released outputs and showed the strongest overall performance in pathologist-validated LLM-based Evaluation Scores and quantitative report-generation metrics across most centers. These results demonstrate that domain-specialized foundation models with statistical risk control offer a promising approach for accurate breast CNB diagnosis and reliable report generation.

</details>


### [29] [DiverseDiT++: Quantifying, Analyzing, and Promoting Representation Diversity in Diffusion Transformers](https://arxiv.org/abs/2608.03082)
*Binglei Li,Mengping Yang,Zhiyu Tan,Xiaomeng Yang,Zhizhong Huang,Junping Zhang,Hao Li*

Main category: cs.CV

TL;DR: This paper analyzes representation dynamics in Diffusion Transformers (DiTs), introduces a Weighted Diversity Score (WDS) metric to measure block-wise representation diversity, shows WDS strongly correlates with synthesis quality (r=-0.869 with log FID), and proposes DiverseDiT++ which promotes diverse representation learning via long residual connections and a representation diversity loss, achieving consistent gains on ImageNet.


<details>
  <summary>Details</summary>
Motivation: Despite advances in DiTs enabled by representation alignment with external encoders (e.g., REPA), the underlying mechanisms of internal representation learning in DiTs remain poorly understood. The authors aim to systematically investigate these dynamics to provide principled guidance for model optimization.

Method: 1) Introduces Weighted Diversity Score (WDS) metric to quantify block-wise representation diversity in DiTs. 2) Conducts systematic analysis across diverse settings to reveal that representation diversity across blocks is critical for effective representation learning. 3) Proposes DiverseDiT++ framework with (a) long residual connections to diversify input representations across blocks and (b) a representation diversity loss encouraging blocks to learn distinct features.

Result: WDS shows a strong correlation with synthesis quality (Pearson's r=-0.869 with log(FID)). DiverseDiT++ achieves consistent performance gains and convergence acceleration across different backbones and model sizes on ImageNet 256x256 and 512x512.

Conclusion: Representation diversity across blocks is a critical factor for effective representation learning in DiTs. WDS serves as both a performance indicator and a principled optimization guide, leading to the effective DiverseDiT++ framework that validates these findings empirically.

Abstract: Recent advances in Diffusion Transformers (DiTs) have enabled remarkable progress in visual synthesis, benefiting from their superior scalability. To facilitate DiTs' capability of capturing meaningful internal representations, recent works such as REPA incorporate external pretrained encoders for representation alignment. However, the underlying mechanisms governing representation learning within DiTs remain poorly understood in the community. To this end, this paper first presents a systematic analysis of the representation dynamics of DiTs via quantifying the diversity of block-wise representations. Specifically, we introduce a novel metric, termed the Weighted Diversity Score (WDS), to measure the representational discrepancies across different blocks. Through extensive investigations on the evolution and influence of internal representations under various settings, we reveal that representation diversity across blocks is a critical factor for effective representation learning in DiTs. More importantly, WDS exhibits a strong correlation with synthesis quality across diverse settings, model scales, and training stages (Pearson's $r=-0.869$ with $\log(\text{FID})$), suggesting its potential as an indicator to reflect model performance and a principled guide for model optimization. Based on this key finding, we propose DiverseDiT++, a novel framework that explicitly promotes diverse representation learning. Concretely, our method incorporates long residual connections to diversify input representations across blocks and a representation diversity loss to encourage blocks to learn distinct features. Extensive experiments on ImageNet $256\times256$ and $512\times512$ demonstrate that our DiverseDiT++ yields consistent performance gains and convergence acceleration when applied to different backbones with various sizes,...

</details>


### [30] [GSTEP: Global Spatio-Temporal Density-Driven Visual Token Pruning for Efficient Video Large Language Models](https://arxiv.org/abs/2608.03083)
*Mengjie Zhang,Qihui Zhu,Tao Zhang,Shuangwu Chen,Huihuang Qin,Yu Guo,Shenghao Ye,Zijian Wen,Yunpeng Hou,Dong Jin,Xiaobin Tan,Huasen He,Jian Yang*

Main category: cs.CV

TL;DR: GSTEP is a plug-and-play token pruning framework for VideoLLMs that models video as a continuous spatio-temporal information flow, using global spatio-temporal density-aware sampling to prune redundant visual tokens while preserving performance and achieving significant speedups.


<details>
  <summary>Details</summary>
Motivation: VideoLLMs suffer from expensive inference due to redundant spatio-temporal visual tokens in long videos. Existing token pruning methods use segment-level local pruning, which under-preserves short but semantically dense segments and discards tokens that are locally non-salient but globally critical.

Method: GSTEP models video as a continuous spatio-temporal information flow. It constructs a token-level spatio-temporal density by combining (1) a continuous temporal density from a smoothed centered frame-level change signal and (2) intra-frame spatial density, then performs global token sampling that jointly balances information density and coverage.

Result: On LLaVA-OneVision-7B, GSTEP prunes 75% of visual tokens, preserves up to 100.2% of the original average performance across benchmarks, and achieves a 1.17x end-to-end speedup. Extensive experiments show consistent accuracy-efficiency trade-offs and generalization across multiple VideoLLMs and evaluation settings.

Conclusion: GSTEP demonstrates that global spatio-temporal density-aware pruning, rather than segment-level local pruning, achieves strong accuracy-efficiency trade-offs and generalizes well across model architectures and benchmark settings.

Abstract: Video large language models (VideoLLMs) achieve strong video understanding performance, but their inference remains expensive due to the large number of redundant spatio-temporal visual tokens in long videos. Existing token pruning methods alleviate this cost by reducing redundant tokens, yet most of them rely on segment-level local pruning, where videos are partitioned into isolated segments and tokens are selected independently within each segment. Such designs may under-preserve short but semantically dense segments and discard tokens that appear non-salient locally but remain critical from a global perspective. To address this issue, we propose GSTEP (Global Spatio-Temporal Density Pruning), a plug-and-play pruning framework that models video as a continuous spatio-temporal information flow. GSTEP constructs a token-level spatio-temporal density by combining a continuous temporal density, obtained from a smoothed centered frame-level change signal, with intra-frame spatial density, and then performs global token sampling by jointly balancing information density and coverage. Extensive experiments on multiple VideoLLMs and public benchmarks demonstrate that GSTEP consistently achieves strong accuracy-efficiency trade-offs and generalizes well across model architectures and evaluation settings. On LLaVA-OneVision-7B, GSTEP prunes 75% of visual tokens, preserves up to 100.2% of the original average performance across benchmarks, and achieves a 1.17 end-to-end speedup.

</details>


### [31] [SUV: Future Scene Understanding as Video Generation for End-to-End Driving](https://arxiv.org/abs/2608.03084)
*Yibo Yuan,Jiacheng Fu,Jiangtong Zhu,Yi Li,Jianhua Han,Meng Tian,Zhuohan Liu,Zhiwei Xiong,Hang Xu,Jianwu Fang,Jianru Xue*

Main category: cs.CV

TL;DR: SUV is a unified end-to-end driving framework that casts future scene understanding as video generation using a pretrained video foundation model, jointly predicting future appearance, semantics, relative depth, and instance-level dynamics as video streams with a shared video expert, while an action expert attends to these streams to generate ego trajectories. It achieves state-of-the-art performance on NAVSIM-v2 benchmarks (91.0 EPDMS on navtest, 36.9 on navhard) and competitive RFS of 7.94 on WOD-E2E using only a single front camera.


<details>
  <summary>Details</summary>
Motivation: Existing end-to-end driving methods model future scenes using task-specific heads and output formats with limited scalability. The authors propose that video generation could serve as a shared predictor for coherent future scene understanding in driving.

Method: SUV casts future scene understanding as video generation using a pretrained video foundation model. It models future appearance, semantics, relative depth, and instance-level dynamics as video streams via a shared video expert (no stream-specific heads). A joint video-action attention mechanism allows the action expert to attend to latent representations of all future streams and generate ego trajectories.

Result: SUV predicts all four future streams simultaneously and outperforms recent state-of-the-art methods on both NAVSIM-v2 splits, achieving 91.0 EPDMS on navtest and 36.9 on navhard. On WOD-E2E, it achieves a competitive RFS of 7.94, using only a single front camera with no candidate-trajectory selection.

Conclusion: Controlled ablations show that structured future supervision and direct future-stream access yield higher trajectory planning scores, validating the video generation approach as an effective shared predictor for end-to-end driving.

Abstract: End-to-end driving requires a coherent understanding of future scenes, yet existing methods model these scenes using task-specific heads and output formats, with limited scalability. Can video generation instead provide a shared predictor? We introduce SUV, a unified end-to-end driving framework that casts future Scene Understanding as Video generation using a pretrained video foundation model. SUV models future appearance, semantics, relative depth, and instance-level dynamics as video streams with a shared video expert, without stream-specific visual prediction heads. Through joint video-action attention, the action expert attends to the latent representations of all future streams and generates the ego trajectory. Experiments show that SUV directly predicts all four future streams, while controlled ablations show that structured future supervision and direct future-stream access yield higher trajectory planning scores. With only a single front camera and no candidate-trajectory selection, SUV outperforms a broad set of recent state-of-the-art methods on both NAVSIM-v2 splits, achieving 91.0 EPDMS on navtest and 36.9 on navhard. On the long-tail WOD-E2E benchmark, SUV achieves a competitive RFS of 7.94.

</details>


### [32] [Channel-wise Dynamic Knowledge Distillation via Adaptive Sample Generation for Action Recognition](https://arxiv.org/abs/2608.03100)
*Ping Li,Chenhao Ping,Jie Song,Mingli Song*

Main category: cs.CV

TL;DR: The paper proposes ASCD, an adaptive sample-aware channel-wise dynamic knowledge distillation approach for compressing large action recognition models. It addresses two limitations of existing KD methods: fixed input samples causing suboptimal feature alignment, and uniform distillation strength failing to account for channel-wise importance differences.


<details>
  <summary>Details</summary>
Motivation: Existing knowledge distillation methods for action recognition suffer from (1) reliance on fixed input samples leading to suboptimal feature alignment between frozen teacher and learnable student models, and (2) applying uniform distillation strength across all channels, which fails to capture their varying importance in encoding distinct knowledge such as motion tempo or magnitude across training epochs.

Method: The ASCD approach operates in two stages: (1) An adaptive sample generation module creates updated samples by incorporating semantics from sample gradients, derived by minimizing feature loss weighted by channel centroid frequency differences per layer, while preserving motion details via Gaussian masking on frequency features. (2) A channel-wise dynamic distillation module trains the student on these generated samples, guided by sample gradients and feature frequencies. Samples are updated periodically rather than per epoch for efficiency.

Result: Extensive experiments on three video benchmarks (UCF101, Kinetics-400, Something-Something-v2) and two image datasets (CIFAR-100, ImageNet) demonstrate state-of-the-art performance.

Conclusion: The proposed adaptive sample-aware channel-wise dynamic KD approach outperforms existing methods across video and image benchmarks, providing an effective solution for compressing large action recognition models.

Abstract: Knowledge Distillation (KD) offers a promising yet underexplored path for compressing large action recognition models. However, existing KD methods suffer from two key limitations: 1) reliance on fixed input samples leads to suboptimal feature alignment between the frozen teacher (larger model) and the learnable student (smaller model), and 2) applying a uniform distillation strength for all channels fails to account for their varying importance in capturing distinct knowledge (e.g., motion tempo or magnitude) across training epochs. This motivates us to develop an Adaptive Sample-aware Channel-wise Dynamic (ASCD) KD approach, which operates in two stages. First, we use an adaptive sample generation module to create updated samples by incorporating semantics from sample gradients, which are derived by minimizing a feature loss weighted by channel centroid frequency differences at each layer. Meanwhile, crucial motion-related details are preserved by applying a Gaussian mask to frequency features. Second, we employ a channel-wise dynamic distillation module to train student on these generated samples, guided by sample gradients and feature frequencies. For efficiency, samples are updated periodically rather than per epoch. Extensive experiments on three video benchmarks (UCF101, Kinetics-400, Something-Something-v2) and two image datasets (CIFAR-100, ImageNet) demonstrate the state-of-the-art performance of our method. Code is available at https://github.com/mlvccn/ASCD_KD_Action.

</details>


### [33] [Double Down on Defense: Strengthening Deep Perceptual Hashes against Evasion Attacks without Retraining](https://arxiv.org/abs/2608.03101)
*Bangjie Sun,Nayoung Kim,Mun Choon Chan,Jun Han*

Main category: cs.CV

TL;DR: DualShield is a plug-in defense that improves the robustness of deep perceptual hashes against adversarial perturbations without retraining, using randomized smoothing at matching time and input hardening at publication time to provide certified and empirical robustness.


<details>
  <summary>Details</summary>
Motivation: Deep perceptual hashes used for near-duplicate image matching are vulnerable to adversarial perturbations that can cause near-duplicates to evade detection, threatening trust and safety applications.

Method: DualShield combines two mechanisms: (1) matching-time randomized smoothing that aggregates decisions over perturbed reference-query pairs, and (2) publication-time hardening that adds an optimized imperceptible perturbation to each reference image before publication.

Result: DualShield achieves a certified l2 radius of ~0.3, and substantially reduces attack success rates across eight deep perceptual hashes and three datasets under adaptive white-box, black-box, and image-transformation attacks, while preserving low collision rates.

Conclusion: Deep perceptual hashes can be made robust to adversarial perturbations without costly retraining by improving the matching procedure and hardening reference images before publication.

Abstract: Near-duplicate image matching is crucial for trust and safety, provenance verification, copyright enforcement, and large-scale visual search. Modern platforms increasingly rely on deep perceptual hashes, which map visually similar images to nearby representations despite common image transformations. However, adversarial perturbations can cause near-duplicates to evade matching. We present DualShield, a plug-in defense that improves the robustness of existing deep perceptual hashes without retraining or modifying their underlying models. DualShield combines matching-time randomized smoothing, which aggregates decisions over perturbed reference-query pairs, with publication-time hardening, which adds an optimized imperceptible perturbation to each reference image before publication. Together, these mechanisms provide certified and empirical robustness. DualShield achieves a certified $\ell_2$ radius of approximately 0.3, guaranteeing that query perturbations within this radius cannot evade matching. We further evaluate it against adaptive white-box, black-box, and image-transformation attacks. Across eight deep perceptual hashes and three datasets, DualShield substantially reduces attack success rates while preserving low collision rates. These results show that deep perceptual hashes can be strengthened without costly retraining by improving the matching procedure and hardening reference images before publication.

</details>


### [34] [FaithIR: Rethinking Infrared Image Super-Resolution from Perceptual Sharpness to Task Relevant Fidelity](https://arxiv.org/abs/2608.03106)
*Axi Niu,Zhenguo Wu,Kang Zhang,Qingsen Yan,Jinqiu Sun,Yanning Zhang*

Main category: cs.CV

TL;DR: FaithIR is a faithful infrared image super-resolution framework composed of a patch-level conditioning branch and a pixel-level restoration branch, designed to preserve authentic thermal structures and improve downstream machine perception tasks like object detection and semantic segmentation.


<details>
  <summary>Details</summary>
Motivation: Existing IISR methods generate artificial textures, over-sharpened edges, and spurious high-frequency details that distort authentic thermal structures and semantic information, which is harmful for downstream perception tasks.

Method: FaithIR uses a patch-level conditioning branch to capture global thermal/structural information and a pixel-level restoration branch for dense local reconstruction under structural guidance, operating directly in the pixel domain.

Result: Experiments on FLIR-IISR, M3FD, and FMB show strong reconstruction fidelity, cross-dataset generalization, and superior performance in object detection and semantic segmentation.

Conclusion: Preserving faithful infrared structures is more important for reliable machine perception than merely pursuing perceptual sharpness.

Abstract: Infrared image super-resolution (IISR) is important for downstream tasks such as object detection and semantic segmentation. Existing IISR methods often produce artificial textures, over-sharpened edges, and spurious high-frequency details that distort authentic thermal structures and semantic information. To address this issue, we propose FaithIR, a faithful infrared super-resolution framework for reliable machine perception. FaithIR consists of a patch-level conditioning branch that captures global thermal and structural information and a pixel-level restoration branch that performs dense local reconstruction under structural guidance. The entire restoration process is performed directly in the pixel domain to preserve infrared-specific structures and task-relevant information. Extensive experiments on FLIR-IISR, M3FD, and FMB demonstrate strong reconstruction fidelity, cross-dataset generalization, and superior performance in object detection and semantic segmentation. These results show that demonstrate that preserving faithful infrared structure preservations is more important for reliable machine perception than merely pursuing perceptual sharpness alone.

</details>


### [35] [A Unified Resolution-Conditioned Framework for Orthogonal Line-Scanning Image Fusion](https://arxiv.org/abs/2608.03107)
*Yiming Gong,Kai Wang*

Main category: cs.CV

TL;DR: Presents a unified resolution-conditioned fusion framework (RELA with FiLM conditioning and Adaptive RELA) that recovers near-isotropic resolution from orthogonal line-scanning microscopy across multiple optical configurations using a single model, achieving 34-40 dB PSNR and generalizing to unseen slit widths.


<details>
  <summary>Details</summary>
Motivation: Laser line-scanning microscopy provides fast volumetric imaging but anisotropic lateral resolution. While orthogonal line scans can recover near-isotropic resolution via deep learning, existing methods require a separate model per optical configuration (slit width), which is impractical and fails to generalize.

Method: A unified framework based on Rank Enhanced Linear Attention (RELA). Feature-wise Linear Modulation (FiLM) continuously conditions the network on the resolving-power ratio for cross-configuration adaptation. Adaptive RELA replaces fixed kernels with ratio-conditioned multi-scale depthwise convolutions and uses a learnable attention temperature. Training data is generated via a physics-grounded separable PSF model verified at 48.3 dB against measured optics.

Result: Achieves 34-40 dB PSNR across configurations, versus 24.3 dB for unconditioned multi-slit training and 4-9 dB loss for per-slit specialists outside their training setting. Generalizes smoothly to unseen intermediate configurations. Ablations show FiLM resolves configuration ambiguity, global linear attention captures long-range correspondences, and adaptive temperature adds 2 dB in the near-isotropic regime.

Conclusion: A single resolution-conditioned model can effectively replace per-configuration specialists in line-scanning microscopy fusion, with FiLM conditioning and adaptive attention mechanisms being key to robust cross-configuration performance.

Abstract: Laser line-scanning microscopy enables fast volumetric imaging but produces anisotropic lateral resolution. Orthogonal line scans provide complementary directional information that can recover near-isotropic resolution, yet existing deep-learning methods require a separate model for each optical configuration. We present a unified, resolution-conditioned fusion framework based on Rank Enhanced Linear Attention (RELA). Feature-wise Linear Modulation (FiLM) conditions the network continuously on the resolving-power ratio, enabling one model to adapt across slit widths. We further introduce Adaptive RELA, which replaces fixed-kernel rank enhancement with ratio-conditioned multi-scale depthwise convolutions and uses a learnable attention temperature to adjust selectivity with degradation severity. Training data spanning multiple slit configurations are generated using a physics-grounded separable point-spread-function model verified against measured optical data at 48.3 dB accuracy. The resulting model achieves 34-40 dB PSNR across configurations, whereas unconditioned multi-slit training collapses to 24.3 dB and per-slit specialists lose 4-9 dB outside their training setting. It also generalizes smoothly to unseen intermediate configurations without interpolation artifacts. Ablations show that FiLM resolves configuration ambiguity, global linear attention captures long-range directional correspondences, and adaptive temperature yields an additional 2 dB in the challenging near-isotropic regime, where complementary signals are weak.

</details>


### [36] [Multimodal Plant Root Phenotyping with Integration of 3D Skeleton Extraction and Language Analysis](https://arxiv.org/abs/2608.03109)
*Jiakai Lin,Zijun Li,Guoyu Lu*

Main category: cs.CV

TL;DR: This paper presents a multimodal robotic AI framework for plant root phenotyping that integrates unsupervised 3D skeleton extraction (W-LBC) with language-guided reasoning via fine-tuned GPT, achieving interpretable root analysis across 12 plant species.


<details>
  <summary>Details</summary>
Motivation: Root phenotyping is essential for understanding below-ground structures and improving agricultural sustainability, yet existing approaches lack interpretability and data efficiency in analyzing complex 3D root architectures.

Method: The framework combines (1) an unsupervised skeleton extraction network based on Weighted Laplacian Contraction (W-LBC) to generate structural representations from dense point clouds, (2) quantitative morphological descriptors (root count, length, branching angle, density) computed from the skeleton graph, and (3) an Evidence-First language modeling framework that fine-tunes GPT as a chatbot using automatically generated instruction-response pairs where quantitative evidence precedes natural-language reasoning.

Result: The structure-guided framework achieves robust, interpretable reasoning across 12 plant species with diverse root architectures, demonstrating that GPT can associate numerical structure with semantic meaning to produce biologically consistent explanations.

Conclusion: By integrating unsupervised 3D geometric perception with large-scale language understanding, the approach bridges quantitative analysis and semantic interpretation, establishing a unified paradigm for explainable robotic plant root phenotyping.

Abstract: Plant root phenotyping is fundamental to understanding below-ground structures, optimizing crop management, and improving agricultural sustainability. This paper presents a multimodal robotic AI framework that integrates 3D skeleton extraction with language-guided reasoning for interpretable and data-efficient root analysis. We develop an unsupervised skeleton extraction network based on Weighted Laplacian Contraction (W-LBC) to generate high-fidelity structural representations from dense point clouds captured by robotic 3D sensing platforms. Quantitative morphological descriptors, including root count, length, branching angle, and density, are computed from the reconstructed skeleton graph to capture geometric and topological characteristics. Building on these features, we introduce an Evidence-First language modeling framework that fine-tunes GPT as an interactive analytical chatbot using automatically generated instruction--response pairs. Each training sample provides measurable evidence before natural-language reasoning, enabling the model to ground interpretation in quantitative morphology. Through supervised fine-tuning, GPT associates numerical structure with semantic meaning, producing biologically consistent explanations of growth patterns and adaptive traits. Experiments show that the structure-guided framework achieves robust, interpretable reasoning across 12 plant species with diverse root architectures. By integrating unsupervised 3D geometric perception with large-scale language understanding, our approach bridges quantitative analysis and semantic interpretation, establishing a unified paradigm for explainable robotic plant root phenotyping.

</details>


### [37] [Adaptive Two-Stage Visual Token Pruning for Efficient Inference in Video-Language Models](https://arxiv.org/abs/2608.03112)
*Paribesh Regmi,Qingshuang Chen,Chi Zhang,Heba Aly,Yelin Kim,Hongda Mao*

Main category: cs.CV

TL;DR: A two-stage adaptive token pruning strategy for video-language models that prunes redundant frames first, then performs content-dependent token pruning within retained frames, achieving +7% accuracy on video captioning at 10% token retention with 95% computation reduction, without any training or fine-tuning.


<details>
  <summary>Details</summary>
Motivation: Vision-language models for video suffer from high inference latency due to processing thousands of tokens per frame across multiple frames. Existing token reduction methods are designed for single images, ignoring temporal/inter-frame redundancies, and use fixed uniform pruning ratios that don't account for content-dependent redundancy variation across videos.

Method: A post-hoc two-stage adaptive pruning approach: (1) frame-level pruning to remove redundant frames, (2) token-level pruning within retained frames, where the pruning ratio is adaptively determined per video by analyzing the correlation structure of token embeddings to quantify redundancy.

Result: +7% accuracy improvement on a video captioning benchmark at 10% token retention, with 95% reduction in computation TFLOPs, achieved without any additional training or fine-tuning.

Conclusion: Content-dependent adaptive pruning is significantly more effective than fixed-ratio pruning for video inputs, and temporal redundancy can be exploited post-hoc without model retraining to achieve substantial efficiency gains while even improving accuracy.

Abstract: Vision-language models excel at image and video understanding but suffer from high inference latency due to the need to process thousands of tokens per image, limiting their deployment on resource-constrained edge devices and in real-time surveillance applications. This challenge is further amplified in video processing, where multiple frames must be analyzed simultaneously. Existing token reduction techniques are largely developed for single-image inputs and therefore fail to account for the temporal and inter-frame redundancies present in video sequences. In addition, these methods generally rely on a fixed, uniform pruning ratio applied across all inputs, which is suboptimal because the degree of redundancy can vary significantly between different videos, necessitating content-dependent pruning levels to preserve critical information. To address these limitations, we propose a two-stage adaptive token pruning strategy specifically designed for video processing. In the first stage, we prune out the redundant frames, and in the second stage, token-level pruning is applied within the retained frames. Crucially, the pruning ratio in the second stage is determined adaptively based on the content of each video. This is achieved by analyzing the correlation structure of token embeddings to quantify redundancy, which is used to determine the ratio. Importantly, our method is entirely post-hoc and requires no additional training or fine-tuning, while achieving strong empirical gains; notably, it improves accuracy by +7\% on a video captioning benchmark at 10\% token retention, while reducing computation TFLOPs by 95\%.

</details>


### [38] [Non-Destructive Quantification of Urea Adulteration in Bovine Milk Using Transmittance Multispectral Imaging](https://arxiv.org/abs/2608.03113)
*Sharukshan Niranjan,Iresha Ranaweera,Tharindu Chandrarathne,Kalana Dissanayaka,Roshan Godaliyadda,Vijitha Herath,Parakrama Ekanayake,Janak Vidanarachchi*

Main category: cs.CV

TL;DR: This paper presents a multispectral imaging (MSI)-based method for quantitative urea detection in adulterated bovine milk. Using an in-house 12-band imaging system (365-940 nm) with controlled density balancing, the method achieves high predictive accuracy via multiple linear regression (R²=0.9599) and a feed-forward neural network (R²=0.9773), offering a rapid, low-cost, non-destructive screening alternative to conventional laboratory methods.


<details>
  <summary>Details</summary>
Motivation: Urea adulteration of bovine milk is a significant food safety and quality concern, but existing detection methods (laboratory analytical techniques and spectroscopy) require specialized instrumentation, reagents, sample preparation, or laboratory operation, making them unsuitable for rapid, low-cost routine screening.

Method: An in-house-built multispectral imaging system capturing twelve discrete spectral bands (365-940 nm) was used to acquire transmittance images of milk samples with controlled urea addition and water for density balancing (specific gravity verified at 1.032, 20°C). Quantitative urea estimation was performed using a regression framework: multiple linear regression for initial mapping, followed by a feed-forward neural network to improve predictive performance.

Result: Multiple linear regression achieved a validation R² of 0.9599, while the feed-forward neural network improved performance to a validation R² of 0.9773 for quantitative urea prediction.

Conclusion: Transmittance multispectral imaging is feasible for accurate, non-destructive urea quantification under controlled density-balanced conditions, demonstrating potential as a rapid, cost-effective screening approach for milk-quality assessment.

Abstract: Adulteration of bovine milk using urea remains a major food quality and health concern, motivating the development of rapid and quantitative screening tools. Conventional approaches, including laboratory-based analytical methods and spectroscopic techniques, have been used for urea detection; however, many remain less suitable for rapid, low-cost routine screening due to requirements such as specialized instrumentation, sample preparation, chemical reagents, or laboratory operation. This study introduces a pragmatic, cost-effective, accurate, and laboratory-validated MSI-based method for quantitative urea estimation under controlled density conditions using a multispectral-imaging-based regression framework. An in-house-built multispectral imaging system operating in twelve discrete spectral bands (365--940~nm) was used to acquire multispectral images of milk samples prepared with controlled urea addition and water for density balancing. Fresh milk was obtained on the day of image acquisition, and the specific gravity of the milk was verified to be 1.032 at 20°C using a hydrometer. Multiple linear regression provided an initial mapping with a high validation $R^2$ of 0.9599, while a feed-forward neural network further improved predictive performance with a validation $R^2$ of 0.9773. These results demonstrate the feasibility of transmittance multispectral imaging for accurate, non-destructive urea quantification under controlled density-balanced conditions, supporting its potential as a rapid screening approach for milk-quality assessment.

</details>


### [39] [SeCo-SBIR: Semantically Consistent Prompt Learning for Zero-Shot Sketch-Based Image Retrieval](https://arxiv.org/abs/2608.03120)
*Long Hoang Dang,Tuan Nguyen Huu,Nguyen Minh Hieu,Tu Minh Phuong*

Main category: cs.CV

TL;DR: SeCo-SBIR is a semantically consistent prompt learning framework for zero-shot sketch-based image retrieval that resolves the tension between task-specific adaptation and zero-shot generalization through text-guided multi-modal prompting and perturbation-based consistency constraints.


<details>
  <summary>Details</summary>
Motivation: Adapting CLIP for ZS-SBIR via prompt learning faces a fundamental tension: the model must bridge the sketch-photo domain gap through task-specific adaptation, yet the added flexibility risks overfitting to seen training categories and eroding CLIP's zero-shot generalization ability.

Method: SeCo-SBIR uses two key components: (1) a text-guided multi-modal prompting strategy that routes learnable prompt vectors through CLIP's text encoder and projects intermediate representations into the visual encoder at every layer via learnable coupling functions, injecting transferable semantic knowledge into the visual pathway; and (2) a perturbation-based consistency constraint using an asymmetric InfoNCE objective that aligns the adapted model with a frozen CLIP reference branch (augmented inputs to frozen branch, clean inputs to trainable branch), anchoring learned representations to CLIP's generalizable feature space. It also incorporates lightweight adapters and a multi-objective loss combining triplet, NT-Xent, and classification terms.

Result: SeCo-SBIR achieves state-of-the-art results on all three standard ZS-SBIR benchmarks across categorical, generalized, and across-dataset settings.

Conclusion: The framework successfully resolves the adaptation-generalization tension in ZS-SBIR by injecting transferable semantic knowledge from the text encoder while anchoring learned representations to CLIP's generalizable feature space via consistency constraints.

Abstract: Adapting CLIP for zero-shot sketch-based image retrieval (ZS-SBIR) via prompt learning faces a fundamental tension: the model must bridge the sketch-photo domain gap through task-specific adaptation, yet the added flexibility risks overfitting to seen training categories and eroding CLIP's zero-shot generalization. We present SeCo-SBIR, a semantically consistent prompt learning framework that resolves this tension from both sides. First, a text-guided multi-modal prompting strategy routes learnable prompt vectors through CLIP's text encoder and projects the resulting intermediate representations into the visual encoder at every layer via learnable coupling functions. Because the text encoder has already learned robust, abstract category-level semantics from large-scale language supervision, this mechanism injects transferable semantic knowledge directly into the visual pathway - adapting the model to the sketch-photo domain while inherently favoring generalization to unseen classes. Second, a perturbation-based consistency constraint addresses the residual overfitting risk from the learnable coupling functions by aligning the adapted model with a frozen CLIP reference branch using an asymmetric InfoNCE objective - augmented inputs feed the frozen branch while clean inputs feed the trainable branch - anchoring the learned representations to CLIP's generalizable feature space. Together with lightweight adapters and a multi-objective loss combining triplet, NT-Xent, and classification terms, SeCo-SBIR achieves state-of-the-art results on all three standard ZS-SBIR benchmarks across categorical, generalized, and across-dataset settings.

</details>


### [40] [Rectify Then Diffuse: Disentangling Concepts Before Denoising Trajectory Unfolds](https://arxiv.org/abs/2608.03135)
*Ning Zhu,An Chen,Mengfei Zhao,Juntao Xu,Jingze Liang,Boyuan Gu,Liang-Jian Deng*

Main category: cs.CV

TL;DR: RTD is a training-free framework that treats compositional text-to-image generation as a boundary-condition problem, rectifying the initial latent allocation before denoising via soft-overlap disentanglement and isotropic gradient rectification, achieving state-of-the-art compositional fidelity.


<details>
  <summary>Details</summary>
Motivation: Text-to-image diffusion models struggling with multiple concepts often omit or merge them. The authors trace this to an early coordination bottleneck where prompt-conditioned attention allocates overlapping spatial support to different concepts, causing their attention to remain coupled during denoising. This motivates a boundary-condition approach rather than repeatedly controlling the evolving trajectory.

Method: Two key components: (1) Soft-Overlap Disentanglement (SOD) converts normalized overlap between pilot concept maps into a differentiable, layout-agnostic separation objective; (2) Isotropic Gradient Rectification (IGR) normalizes the SOD gradient and applies a bounded latent displacement with consistent scale across prompts and initializations. The framework rectifies the initial allocation once before standard denoising (training-free).

Result: RTD achieves state-of-the-art compositional fidelity with robust gains. On AE-Bench object pair subset, it improves BLIP-VQA by 45.8% and ImageReward by 19.6% over CO3, while running 2.3x faster.

Conclusion: Compositional failures stem from an early attention allocation bottleneck, and rectifying the initial latent state once (as a boundary condition) is more effective and efficient than repeatedly steering the denoising trajectory. RTD demonstrates this with strong quantitative gains and faster runtime.

Abstract: Text-to-image diffusion models can generate individual concepts well, but they often omit or merge concepts incorrectly with multiple concepts. We trace these failures to an early coordination bottleneck: before denoising begins, prompt-conditioned attention may allocate different concepts to strongly overlapping spatial support, which can keep their attention coupled as denoising proceeds. This observation motivates treating compositional generation as a boundary-condition problem rather than repeatedly controlling the evolving trajectory. To this end, we propose Rectify-then-Diffuse (RTD), a training-free framework that rectifies the initial allocation once before standard denoising. Firstly, we propose Soft-Overlap Disentanglement (SOD), which converts normalized overlap between pilot concept maps into a differentiable and layout-agnostic separation objective. Secondly, we introduce Isotropic Gradient Rectification (IGR), which normalizes the SOD gradient and applies a bounded latent displacement with a consistent scale across prompts and initializations. Extensive experiments show that RTD achieves state-of-the-art compositional fidelity and robust gains. On the AE-Bench object pair subset, RTD improves BLIP-VQA by 45.8% and ImageReward by 19.6% over CO3 while running 2.3$\times$ faster. Code will be released at https://github.com/Z-yiwei/rectify-then-diffuse

</details>


### [41] [Frozen High-Resolution Inference for Cross-City Object Detection: An AI City Challenge 2026 Study](https://arxiv.org/abs/2608.03136)
*Jaeuk Kim*

Main category: cs.CV

TL;DR: In AI City Challenge 2026 Track 6, the authors tested frozen 1120x1120 inference (vs. 704x704 training) of an RF-DETR-Large detector in an air-gapped platform with only aggregate COCO-style AP feedback. The frozen-1120 run improved AP from 0.3272 to 0.3654 without any parameter update, gaining most on small objects. Warm-start fine-tuning reached 0.3470 but was confounded by a higher confidence threshold, making it descriptive rather than a controlled comparison. They release all commands, configs, and evidence boundaries for every claim.


<details>
  <summary>Details</summary>
Motivation: To investigate cross-city object detection generalization under aggregate-only feedback, where only a single AP number over a hidden source-target mixture is available and no target labels exist.

Method: Archived configurations of a single RF-DETR-Large detector were analyzed inside an air-gapped Training-as-a-Service platform. They compared frozen 1120x1120 inference of a checkpoint trained at 704x704 against baseline and fine-tuned variants, using the hidden aggregate AP as the sole evaluation signal, plus ablation-style checks (gray-world normalization, orientation audit).

Result: Frozen-1120 inference achieved the highest aggregate AP (0.3272 -> 0.3654, +0.0382) with no parameter update, largest relative gain on small objects and largest absolute gain on medium objects at 2.53x input pixels. Warm-start fine-tuning reached 0.3470 but used a different confidence threshold (0.05 vs 0.01), so it is a descriptive outcome. Gray-world normalization had no meaningful effect; a rectangular run was found to use unintended portrait orientation.

Conclusion: A frozen large-resolution inference can outperform fine-tuning under aggregate-only cross-city feedback, and in-domain validation is an unreliable model-selection signal. These are exploratory, audited findings about the aggregate mixture and do not establish target-city-specific improvements.

Abstract: Cross-city object detection requires a detector trained in one city to generalize to an unlabeled target city. In AI City Challenge 2026 Track 6, we analyze archived configurations of a single RF-DETR-Large detector inside an air-gapped Training-as-a-Service platform whose server returns only an aggregate COCO-style AP over a hidden mixture of source- and target-city images. Frozen 1120 x 1120 inference of a checkpoint trained at 704 x 704 achieved the highest aggregate AP among the evaluated configurations (0.3272 -> 0.3654, +0.0382) without any parameter update, with the largest relative gain on small objects and the largest absolute gain on medium objects, at 2.53x the input pixels. A warm-start 1120px fine-tuning recipe reached 0.3470 while its in-domain validation AP rose (0.767 -> 0.789), a caution that in-domain validation is an unreliable model-selection signal under aggregate-only cross-city feedback. Because that run's evaluation used a higher confidence threshold than the inference-only runs (0.05 vs. 0.01), we treat its score as a descriptive archived outcome rather than a controlled verdict on fine-tuning. Gray-world normalization did not meaningfully change the frozen-1120 result, and a rectangular run was found by audit to have used an unintended portrait orientation. We release verbatim platform commands, configuration snapshots, and an explicit evidence boundary for every claim. Each configuration was submitted once and the best was selected on the hidden server, so these are exploratory, audited findings about the aggregate mixture; they do not establish target-city-specific improvement.

</details>


### [42] [From Routes to Steps: Separating Semantic Progress from Local Execution in Vision-and-Language Navigation](https://arxiv.org/abs/2608.03143)
*Xiangyun Huang,Xiangchen Wang,Runfeng Lin,Yihao Xu,Kangyu Huang,Jiang Hengchen,Xiwang Dong,Lin Jiarong*

Main category: cs.CV

TL;DR: Route2Step is a framework for Vision-and-Language Navigation (VLN) that decouples semantic progress tracking from action generation via an explicit step-level interface, improving SR from 48.1% to 55.3% and SPL from 43.3% to 48.2% on R2R-CE.


<details>
  <summary>Details</summary>
Motivation: Existing VLM-based navigators supervise progress tracking and action execution jointly through next-action prediction alone, making it impossible to distinguish progress-tracking errors from execution errors. This ambiguity causes agents to continue making decisions from erroneous progress states.

Method: Route2Step has two modules: (1) an Instruction Analysis Module (M_IA) that predicts the semantic progress state (active sub-instruction) from global instruction and visual history, and (2) an Action Generation Module (M_AG) that generates local action chunks conditioned on the predicted state and recent observations. A step-alignment procedure called E-SPA associates sub-instructions with route-level demonstration segments to enable state supervision without manual temporal labels. Direct action supervision is reserved only for rollout groups that repeatedly fail under the correct active sub-instruction.

Result: On R2R-CE, Route2Step improves SR from 48.1% to 55.3% and SPL from 43.3% to 48.2%, using 190K state-level corrective samples while requiring only 11.5K directly action-supervised states. Experiments in real-world indoor and outdoor environments demonstrate practical applicability.

Conclusion: Decoupling progress tracking from action generation with an explicit step-level interface resolves the ambiguity between tracking and execution errors, leading to more effective supervision and improved navigation performance in both simulated and real-world settings.

Abstract: Vision-and-Language Navigation (VLN) requires an agent to follow a route-level instruction by executing its constituent steps from egocentric visual observations. Existing VLM-based navigators typically supervise both capabilities through next-action prediction alone, making progress-tracking errors difficult to distinguish from execution errors. When an agent deviates from the route, a corrective action label may recover the next movement but does not indicate whether the agent selected the wrong sub-instruction or failed to execute the correct one. Consequently, the agent may continue making decisions from an erroneous progress state. To resolve this ambiguity, we propose \textbf{Route2Step}, a framework that decouples semantic progress tracking from action generation through an explicit step-level interface. The Instruction Analysis Module ($\mathcal{M}_{\mathrm{IA}}$) predicts this state from the global instruction and visual history. Conditioned on the predicted state and recent observations, the Action Generation Module ($\mathcal{M}_{\mathrm{AG}}$) generates local action chunks. To supervise the progress state without manual temporal labels, E-SPA, a step-alignment procedure, associates sub-instructions with their corresponding portions of route-level demonstrations. These alignments enable state supervision for incorrect progress estimates, while direct action supervision is reserved for rollout groups that repeatedly fail under the correct active sub-instruction. On R2R-CE, Route2Step improves SR from 48.1\% to 55.3\% and SPL from 43.3\% to 48.2\%, using 190K state-level corrective samples while requiring only 11.5K directly action-supervised states. Experiments in real-world indoor and outdoor environments further demonstrate the practical applicability of Route2Step. The project page is: https://sisyphus-hxy.github.io/Route2Step/.

</details>


### [43] [CROSS: Cascaded Distillation and Dual-Constraint Grounding for Remote Sensing Referring Segmentation](https://arxiv.org/abs/2608.03147)
*Tingzhang Luo,Ruizhong Liu,Yichao Liu,Cheng Fan,Yu Liu,Jianyuan Guo*

Main category: cs.CV

TL;DR: CROSS is a tightly integrated paradigm for Referring Remote Sensing Image Segmentation that addresses two fundamental limitations through Linguistic-Guided Cascaded Distillation (LGCD) and Perspective-Spatial Contrastive Learning (PSCL), achieving state-of-the-art performance with robust localization under spatial perturbations.


<details>
  <summary>Details</summary>
Motivation: Existing RRSIS approaches heavily rely on strong pre-trained VLM and SAM capabilities but leave two fundamental limitations unaddressed: architectural weak-coupling (unidirectional flow causes localization drift and wastes SAM's structural guidance) and object-centric semantic bias (overemphasis on dominant objects while ignoring spatial reasoning essential for RRSIS).

Method: CROSS proposes two key components: (1) Linguistic-Guided Cascaded Distillation (LGCD) that distills SAM's geometric affinities as soft regularizers into VLM intermediate layers to inject dense structural priors, and (2) Perspective-Spatial Contrastive Learning (PSCL) that imposes cross-anchored constraints using mask-filtered deceptive distractors and spatial-linguistic counterfactuals as hard negatives to shatter semantic shortcuts.

Result: Extensive experiments on RRSIS benchmarks show CROSS achieves state-of-the-art performance and maintains precise localization even under severe spatial description perturbations.

Conclusion: CROSS stands as a robust new paradigm for RRSIS, demonstrating that addressing architectural coupling and semantic bias yields more reliable and spatially accurate referring segmentation.

Abstract: Referring Remote Sensing Image Segmentation (RRSIS) has achieved significant progress through the integration of VLMs and the Segment Anything Model (SAM). However, this progress largely relies on strong pre-trained capabilities, while leaving two fundamental limitations insufficiently addressed: (1) Architectural Weak-Coupling, where the unidirectional flow forces reliance on coarse VLM prompts and wastes SAM's pixel-level structural guidance, causing localization drift; and (2) Object-Centric Semantic Bias, where models overemphasize dominant object semantics while remaining insensitive to spatial reasoning crucial for RRSIS. Motivated by these observations, we propose CROSS, a tightly integrated paradigm for RRSIS. First, we introduce Linguistic-Guided Cascaded Distillation (LGCD) to bridge the architectural gap, which distills SAM's geometric affinities as soft regularizers into VLM intermediate layers, injecting dense structural priors to refine localization. Second, Perspective-Spatial Contrastive Learning (PSCL) imposes cross-anchored constraints by mining mask-filtered deceptive distractors and spatial-linguistic counterfactuals as hard negatives, explicitly shattering semantic shortcuts to enforce genuine logical consistency. Extensive experiments on RRSIS benchmarks demonstrate that CROSS achieves state-of-the-art performance and maintains precise localization even under severe spatial description perturbations, standing as a robust new paradigm for RRSIS.

</details>


### [44] [Surface Keypoint Representation for Multi-Object and Articulated Human-Object Interaction Generation](https://arxiv.org/abs/2608.03158)
*Xiaogang Peng,Zeyu Han,Zichong Meng,Yiming Xie,Jihua Zhu,Gang Hua,Huaizu Jiang*

Main category: cs.CV

TL;DR: The paper proposes a framework for human-object interaction (HOI) generation that handles multi-object and articulated object scenarios using surface keypoint trajectories for object motion representation and a spatio-temporal contact distance field for whole-body contact modeling, with three-stage factorization into object motion generation, contact prediction, and motion synthesis.


<details>
  <summary>Details</summary>
Motivation: Existing HOI generation methods are limited to single rigid objects and do not generalize to scenarios with variable numbers of objects or articulated objects with diverse joint mechanisms. A more general object representation and contact modeling approach is needed.

Method: The method uses surface keypoint trajectories as object motion representation (tracking non-collinear surface points per rigid component), introduces a spatio-temporal contact distance field extending distance-based contact modeling to whole-body/multi-object/articulated settings, and factorizes HOI generation into three stages: object motion generation from text/waypoints, contact distance field prediction, and whole-body motion synthesis with contact-guided optimization.

Result: Experiments on ParaHome, HIMO, ARCTIC, and OMOMO demonstrate better or comparable performance to existing methods across single-object, multi-object, and articulated interaction settings.

Conclusion: The proposed surface keypoint trajectory representation and spatio-temporal contact distance field enable effective HOI generation across diverse scenarios including multi-object coordination and various articulation mechanisms without explicit joint-type specification.

Abstract: Daily activities require humans to coordinate whole-body motion with the motion of surrounding objects. Despite recent progress in human-object interaction (HOI) generation, most existing methods assume interactions with a single rigid object and do not extend well to scenarios involving a variable number of objects or articulated objects with diverse joint mechanisms. We propose surface keypoint trajectories as an object motion representation: for each rigid component, whether a standalone object or one part of an articulated assembly, we track a small set of non-collinear surface points over time. This representation handles multi-object coordination and diverse articulation mechanisms directly from point dynamics without requiring explicit joint-type specification. To model when and where each body region contacts each object, we introduce a spatio-temporal contact distance field that extends distance-based contact modeling to whole-body, multi-object, and articulated settings. We factorize HOI generation into three stages: generating object motions from text or waypoints, predicting the contact distance field, and synthesizing whole-body motion with contact-guided optimization. Experiments on ParaHome, HIMO, ARCTIC, and OMOMO demonstrate better or comparable performance to existing methods across single-object, multi-object, and articulated interaction settings.

</details>


### [45] [Frequency-Decorrelated Temporal Ensembles for EEG--fNIRS Imagined-Handwriting Decoding](https://arxiv.org/abs/2608.03176)
*Xiao Fan,Hongbin Guo,Yubo Han,Yi Zhang*

Main category: cs.CV

TL;DR: FRED is a multimodal BCI system for subject-independent imagined-handwriting classification using sparse-montage EEG and fNIRS, achieving 0.7952 overall accuracy (4th on private split) through frequency-diverse multi-scale temporal EEG modeling, cross-band ensembles, transductive pseudo-labeling, and Hungarian-assignment block-constrained decoding, while showing fNIRS contributes negligibly.


<details>
  <summary>Details</summary>
Motivation: Imagined handwriting decoding from scalp EEG is difficult because signals are noisy and stroke sequences vary across individuals; reliable subject-independent recognition remains unsolved, motivating a task-adapted system for the multimodal four-class handwriting-trajectory challenge.

Method: FRED models imagined handwriting as a multi-second motor sequence and trains a compact multi-scale temporal network on three complementary EEG frequency views (three seeds each). It uses cross-band ensemble members with less-correlated errors, transductive pseudo-label training, three EEG-Conformer members, posterior aggregation, a paradigm-aware decoder, and Hungarian assignment under known 12-trial block class quotas.

Result: Clean nine-member ensemble achieves 0.8076/0.7242/0.7492 on public/private/overall partitions. With pseudo-labeling and Conformers: 0.8498/0.7718/0.7952, ranking fourth on private split. Session/block-constrained decoding raises accuracy from 0.7600 to 0.7952. fNIRS-only is at chance (0.2511); adding fNIRS to EEG only improves by +0.0025.

Conclusion: Frequency-diverse temporal EEG modeling and protocol-matched structured inference are the principal performance sources in this sparse-montage EEG-fNIRS setting; fNIRS contributes almost nothing to decoding accuracy.

Abstract: Imagined handwriting offers a temporally rich paradigm for non-invasive neural decoding, yet reliable recognition across unseen participants remains difficult because scalp EEG is noisy and internally generated stroke sequences vary across individuals. The Multimodal Brain-Computer Interface Grand Challenge provides synchronized EEG and fNIRS for four-class subject-independent handwriting-trajectory classification. We propose FRED, a task-adapted system that models imagined handwriting as a multi-second motor sequence and trains a compact multi-scale temporal network on three complementary EEG frequency views. With three seeds per view, cross-band members produce substantially less-correlated errors than same-band replicas, yielding a clean nine-member ensemble accuracy of 0.8076/0.7242/0.7492 on the public/private/overall test partitions without test-set adaptation or output constraints. The submitted pipeline further incorporates transductive pseudo-label training, three EEG-Conformer members, posterior aggregation, and a paradigm-aware decoder. Because every 12-trial randomization block contains three instances of each class, the final predictions are obtained by Hungarian assignment under the known block quota. On one fixed posterior pool, independent, session-constrained, and block-constrained decoding achieve 0.7600, 0.7758, and 0.7952 overall accuracy, respectively. The complete system reaches 0.8498/0.7718/0.7952, ranking fourth on the private split. A modality audit finds fNIRS-only decoding at chance (0.2511 overall), while adding fNIRS to EEG changes accuracy by only +0.0025. These results identify frequency-diverse temporal EEG modeling and protocol-matched structured inference as the principal sources of performance in this sparse-montage EEG--fNIRS setting. The source code is available at https://github.com/XiuFan719/EEG-fNIRS-fuse-method-for-MM-challenge.

</details>


### [46] [EditFlow3D: Automated Local Editing of 3D Assets with Trajectory Preservation](https://arxiv.org/abs/2608.03179)
*Rui Nie,Chuang Wang,Haitao Zhou,Jiahe Song,Buyu Li,Sheng Wang,Qian Yu*

Main category: cs.CV

TL;DR: EditFlow3D is a training-free framework for local 3D editing that uses a VLM-driven workflow to automatically generate visual guidance and refined 3D masks, enabling precise localized edits while preserving non-target regions. It introduces EditFlow-Bench, a new benchmark for broader local editing categories.


<details>
  <summary>Details</summary>
Motivation: Existing 3D editing methods lack a simple yet accurate way to obtain 3D masks and struggle to achieve desired edits while faithfully preserving the structure and appearance of non-target regions.

Method: EditFlow3D uses a VLM-driven workflow to interpret editing intent and automatically construct a visual guidance image plus a refined 3D editing mask. It employs mask-guided differential flow to focus edits on target regions and step-wise trajectory preservation to maintain consistency of non-target regions with the source asset. It operates in the native representation space of a pretrained 3D generative model without requiring training.

Result: Quantitative results, qualitative comparisons, and a user study on both existing Edit3D-Bench and the newly introduced EditFlow-Bench demonstrate that EditFlow3D achieves more accurate target-region editing and better preserves non-target regions than existing 3D editing methods.

Conclusion: EditFlow3D provides a more accurate and faithful approach to local 3D editing, and the introduction of EditFlow-Bench complements existing benchmarks to better evaluate a broader range of structural and appearance edits.

Abstract: Controllable local editing of 3D assets requires precise target localization and appropriate visual guidance. However, existing methods lack a simple yet accurate way to obtain 3D masks and struggle to achieve the desired edit while faithfully preserving the structure and appearance of non-target regions. To address these challenges, we present EditFlow3D, a training-free framework for local 3D editing. Given a source asset and an edit instruction, a VLM-driven workflow interprets the editing intent and automatically constructs a visual guidance image and a refined 3D editing mask, enabling localized editing in the native representation space of a pretrained 3D generative model. Specifically, mask-guided differential flow focuses the edit on the target region, while step-wise trajectory preservation maintains consistency between non-target regions and the source asset without directly replacing intermediate features. Since the existing Edit3D-Bench covers only a limited range of local editing categories, we further introduce EditFlow-Bench as a complementary benchmark encompassing a broader variety of structural and appearance edits, and evaluate EditFlow3D on both benchmarks. Quantitative results, qualitative comparisons, and a user study demonstrate that EditFlow3D achieves more accurate target-region editing and better preserves non-target regions than existing 3D editing methods.

</details>


### [47] [CrossScope: A Role-Asymmetric World Model for Joint Dual-Scope Surgical Video Prediction](https://arxiv.org/abs/2608.03211)
*Wanhao Liu,Jinsong Lin,Rulin Zhou,Chi Kit Ng,Wenbin Pan,Zhiqing Tang,Dongyue Li,Liwei Luo,Yanshen Wu,Panshuo Li,Zhiyong Xiong,Huxin Gao,Tamas Haidegger,Hongliang Ren*

Main category: cs.CV

TL;DR: This paper introduces CrossScope, a role-asymmetric dual-scope surgical world model for predicting future frames in Mother-Child ERCP procedures, where cross-view evidence is selectively routed based on prediction target and spatial requirements rather than symmetric multi-view fusion.


<details>
  <summary>Details</summary>
Motivation: Visual world models typically learn from single observation streams, limiting their ability to model cooperative systems with multiple independently moving observers. In Mother-Child ERCP, two flexible scopes provide complementary but role-dependent views without calibrated stereo relationships, yet conventional multi-view fusion assumes symmetric information exchange which is inappropriate here.

Method: CrossScope is a dual-stream surgical world model with view-specific experts and geometry-guided residual interactions. It learns two complementary communication directions: geometric motion cues from the Mother view guide Child-view prediction, while pose-aligned Child appearance supports Mother-view prediction only when valid spatial correspondence exists. This enables selective, role-aware evidence routing.

Result: A paired dual-scope benchmark (synchronized phantom and real-world ERCP episodes) is established with evaluations on visual fidelity, structural preservation, target localization, and motion consistency. CrossScope consistently outperforms strong surgical video generation baselines, validating the importance of role-aware evidence routing.

Conclusion: Role-asymmetric cross-view communication is crucial for multi-observer visual world modeling in cooperative surgical settings, and CrossScope demonstrates the effectiveness of preserving view-specific representations while enabling target-specific evidence routing.

Abstract: Visual world models typically learn future dynamics from a single observation stream, limiting their ability to model cooperative systems with multiple independently moving observers. We investigate this challenge in Mother--Child endoscopic retrograde cholangiopancreatography (ERCP), where two flexible scopes provide complementary yet role-dependent views without a calibrated stereo relationship. Unlike conventional multi-view fusion that assumes symmetric information exchange, we formulate \textbf{role-asymmetric dual-scope future prediction}, where cross-view evidence is selectively transferred according to the prediction target and its underlying spatial requirements. We propose \textbf{CrossScope}, a dual-stream surgical world model that preserves view-specific experts while enabling target-specific evidence routing through geometry-guided residual interactions. CrossScope learns two complementary communication directions: geometric motion cues from the Mother view guide Child-view future dynamics, while pose-aligned Child appearance supports Mother-view prediction only when valid spatial correspondence is established. This design allows each scope to contribute task-relevant evidence without compromising its view-specific representation. To evaluate this problem, we establish a paired dual-scope benchmark comprising synchronized phantom and real-world ERCP episodes, with evaluations assessing visual fidelity, structural preservation, target localization, and motion consistency. Experiments demonstrate that CrossScope consistently outperforms strong surgical video generation baselines, validating the importance of role-aware evidence routing for multi-observer visual world modeling.

</details>


### [48] [iFAN: Inference-Aware Learning for Plain Mask Transformers](https://arxiv.org/abs/2608.03216)
*Fang Li,Yu He,Haoyang Tong,Lichen Ma,Jingling Fu,Wenxiao Fan,Tongxuan Liu,Luohang Liu,Ke Zhang,Junshi Huang*

Main category: cs.CV

TL;DR: iFAN is a training-only framework for plain mask transformers that addresses the mismatch between training and inference in query-based segmentation. It introduces Adjusted Probability-Mask Ranking (APMR) to align query competition with mask quality and Cross-Layer Self-Distillation (CLSD) to transfer superior intermediate predictions to the final layer, improving segmentation across multiple datasets with negligible inference cost.


<details>
  <summary>Details</summary>
Motivation: Query-based mask transformers use pixel-wise competition among query predictions at inference, but this process is not explicitly optimized during training. Two key mismatches exist: (1) the query with the highest probability-mask score doesn't necessarily produce the most accurate mask, and (2) final-layer decoding may discard better predictions from intermediate layers.

Method: iFAN introduces two components: (1) Adjusted Probability-Mask Ranking (APMR) which aligns query competition with predicted mask quality and suppresses high-confidence but inaccurate competitors, and (2) Cross-Layer Self-Distillation (CLSD) which transfers stronger intermediate-layer predictions to the final layer. Both objectives are training-only, keeping inference identical to standard final-layer decoding.

Result: Experiments on COCO, ADE20K, and Cityscapes show consistent improvements across panoptic, instance, and semantic segmentation tasks, across different architectures, backbone scales, and input resolutions. iFAN improves performance by an average of 1.20 PQ, 1.30 AP, and 0.63 mIoU with negligible additional parameters, FLOPs, and inference latency.

Conclusion: iFAN is a general and effective training framework that closes the train-inference gap in mask transformers without altering the efficient inference pipeline, demonstrating broad applicability across tasks and architectures.

Abstract: Query-based mask transformers assemble segmentation outputs through pixel-wise competition among query predictions of the final layer, yet this inference process is not explicitly optimized during training. We identify two key mismatches: the query with the highest probability-mask score does not necessarily produce the most accurate mask, and final-layer decoding may discard superior predictions from intermediate layers. To address these issues, we propose Inference-Aware Learning (iFAN), a general training framework for plain mask transformers. iFAN introduces Adjusted Probability-Mask Ranking (APMR), which aligns query competition with predicted mask quality and suppresses high-confidence but inaccurate competitors. We further employ Cross-Layer Self-Distillation (CLSD) to transfer stronger intermediate predictions to the final layer. The ranking and distillation objectives are training-only, while inference retains efficient final-layer decoding. Experiments on COCO, ADE20K, and Cityscapes demonstrate consistent improvements across panoptic, instance, and semantic segmentation, as well as across different architectures, backbone scales, and input resolutions. Overall, iFAN improves performance by an average of 1.20 PQ, 1.30 AP, and 0.63 mIoU, with negligible additional parameters, FLOPs and inference latency.

</details>


### [49] [Self-Supervised Representation-Guided Generative Dataset Distillation](https://arxiv.org/abs/2608.03218)
*Mingzhuo Li,Guang Li,Linfeng Ye,Jiafeng Mao,Takahiro Ogawa,Konstantinos N. Plataniotis,Miki Haseyama*

Main category: cs.CV

TL;DR: SRG is a framework that uses self-supervised learning (SSL) representation geometry to guide generative dataset distillation, producing compact synthetic datasets that preserve discriminative structure in pretrained representation spaces.


<details>
  <summary>Details</summary>
Motivation: Existing dataset distillation methods target randomly initialized networks, but modern systems use frozen pretrained encoders with lightweight adapters. Distilled samples need to preserve the discriminative geometry of the pretrained representation space, which current generative objectives fail to consider.

Method: SRG translates SSL geometry into diffusion guidance via three objectives: prototype alignment, inter-class discrimination, and intra-class assignment. It uses a stage-wise guidance strategy during diffusion sampling—early denoising anchored to the latent of the real image whose SSL representation is nearest the assigned prototype, while later denoising is guided by SSL-space objectives.

Result: SRG consistently outperforms evaluated generative baselines across multiple datasets and IPC (images per class) settings. Cross-encoder evaluation shows transfer across pretrained representation spaces.

Conclusion: Representation-guided generation is effective for dataset distillation with pretrained SSL models, as it balances visual realism from generative priors with representativeness and class-discriminative structure in SSL space.

Abstract: Dataset distillation compresses a large training set into a compact synthetic set while retaining its downstream utility. Most existing methods target randomly initialized networks, whereas modern vision systems often adapt frozen pretrained encoders with lightweight modules. Distilled samples should therefore preserve the discriminative geometry of the pretrained representation space, which existing generative objectives do not explicitly consider. We propose self-supervised representation-guided generative dataset distillation (SRG), a framework that translates the SSL geometry into diffusion guidance. Specifically, SRG constructs class-wise prototypes from real-image SSL representations and performs guidance through three SSL-space objectives for prototype alignment, inter-class discrimination, and intra-class assignment. During diffusion sampling, it adopts a stage-wise guidance strategy: early denoising is anchored to the latent of the real image whose SSL representation is nearest to the assigned prototype, whereas later denoising is guided by the SSL-space objectives. This division preserves the visual realism provided by the generative prior while progressively steering samples toward representative and class-discriminative regions of the SSL representation space. SRG consistently outperforms the evaluated generative baselines across multiple datasets and IPC settings. A cross-encoder evaluation further indicates transfer across pretrained representation spaces. These results demonstrate the effectiveness of representation-guided generation for dataset distillation with pretrained SSL models.

</details>


### [50] [Open-Linguistic Concept Unified Learning for Cross-Site Interpretable Dermatology Image Diagnosis](https://arxiv.org/abs/2608.03225)
*Chengyu Wu,Junpeng Tan,Wanxiang Luo,Yaqi Wang,Yandong Wen,Yefeng Zheng*

Main category: cs.CV

TL;DR: UniCon is an open-linguistic unified concept learning framework for multimodal interpretable vision-language diagnosis that enables cross-site generalization of concept-based models without dataset-specific retraining.


<details>
  <summary>Details</summary>
Motivation: Concept-based models in computer-aided diagnosis are limited by rigid dataset-specific adaptation that hinders cross-site generalization across diverse modalities (e.g., dermoscopic vs. clinical photos) due to heterogeneous concept taxonomies, costly label engineering, and inflexible intervention mechanisms.

Method: UniCon introduces three contributions: (1) a unified concept prototype codebook creating a shared semantic representation space across heterogeneous concept systems without retraining; (2) open-linguistic multi-faceted semantic specifications to overcome sparse textual labels and improve boundary sensitivity; (3) a reliability-gated bottleneck aggregation-based intervention interface enabling transferable clinician corrections.

Result: Extensive experiments show UniCon achieves top-tier diagnostic accuracy while successfully bridging disparate clinical taxonomies and enabling unprecedented cross-site intervention capabilities.

Conclusion: UniCon addresses the scalability bottleneck of concept-based CAD deployment across sites and modalities by providing a robust, transferable, and intervention-capable interpretable framework.

Abstract: Human-interpretable computer-aided diagnosis is crucial for clinical decision making. Concept-based models excel by providing transparent reasoning and enabling post-hoc, clinician-in-the-loop interventions. However, their rigid dataset-specific adaptation inherently restricts cross-site generalization. Applying them across diverse modalities, such as dermoscopic and clinical photographs, is challenging due to heterogeneous concept taxonomies varying in availability, granularity, and semantics across cohorts. Consequently, adapting Foundation Vision-Language Models (FVLMs) demands costly label engineering and repeated post-training. Existing intervention mechanisms remain rigidly tied to predefined concepts, lacking adaptability and hindering scalable dermatology CAD deployment. To address these bottlenecks, we propose UniCon, an open-linguistic unified concept learning framework for multimodal interpretable vision-language diagnosis. UniCon resolves these challenges through three contributions: (1) A shared semantic representation space via a unified concept prototype codebook, seamlessly coordinating heterogeneous concept systems across modalities without dataset-specific retraining. (2) Open-linguistic based multi-faceted semantic specifications to overcome sparse textual label limitations, improving boundary sensitivity in uncertain clinical contexts. (3) A robust, cross-site adjustable intervention interface powered by reliability-gated bottleneck aggregation, enabling consistent reasoning and transferable clinician corrections. Extensive experiments demonstrate that beyond securing top-tier diagnostic accuracy, UniCon successfully bridges disparate clinical taxonomies, unlocking unprecedented cross-site intervention capabilities. Code is available at https://github.com/wuchengyu123/UniCon.

</details>


### [51] [CIGTSurv: Clinical Information Guided Tri-modal Survival Prediction with Local Prototype Association and Global Feature Alignment](https://arxiv.org/abs/2608.03247)
*Jing Dai,Qibin Zhang,Weiwei Zhou,Mingde Xu,Jingsong Liu,Jingdong Zhang,Hongming Xu*

Main category: cs.CV

TL;DR: CIGTSurv is a clinical information guided tri-modal framework for survival prediction that transforms clinical tabular data into high-dimensional embeddings using pretrained foundation models, then uses a dual-level interaction mechanism (local prototype association via cross-attention and global feature alignment via MMD loss) to model cross-modal interactions between pathology images, genomic data, and clinical information, achieving SOTA performance on five TCGA cancer cohorts.


<details>
  <summary>Details</summary>
Motivation: Clinical information is underutilized in multimodal survival prediction despite its importance in reflecting patient health, because it is discrete, sparse, and low-dimensional. Additionally, heterogeneity across modalities makes modeling cross-modal interactions challenging.

Method: 1) A holistic text template with pretrained foundation models converts clinical tabular data into high-dimensional tokenized embeddings. 2) Using clinical information as an anchor, a dual-level interaction mechanism is introduced: a Local Prototype Association (LPA) module based on cross-attention for token-level correspondences, and a Global Feature Alignment (GFA) loss based on Maximum Mean Discrepancy (MMD) for implicit cross-modal distribution consistency.

Result: Extensive experiments on five TCGA cancer cohorts demonstrate that CIGTSurv achieves state-of-the-art (SOTA) survival prediction performance.

Conclusion: CIGTSurv effectively leverages clinical information through tokenized embeddings and dual-level cross-modal interaction, advancing multimodal survival prediction with publicly available source code.

Abstract: Multimodal learning has significantly advanced survival prediction by integrating pathology images with genomic data. However, clinical information, despite its critical role in reflecting a patient' s overall health, remains underutilized due to its discrete, sparse, and low-dimensional nature. Furthermore, the inherent heterogeneity across these modalities pose significant challenges in modeling cross-modal interactions. In this paper, we propose CIGTSurv, a Clinical Information Guided Tri-modal framework for Survival prediction. Specifically, we first design a holistic text template and use pretrained foundation models to transform clinical tabular data into high-dimensional tokenized embeddings. Using clinical information as an anchor, we then introduce a dual-level interaction mechanism: 1) a local prototype association (LPA) module based on cross-attention to explicitly learn token-level correspondences between different modalities, and 2) a global feature alignment (GFA) loss based on Maximum Mean Discrepancy (MMD) to implicitly enhance cross-modal distribution consistency. Extensive experiments on five TCGA cancer cohorts demonstrate that CIGTSurv achieves state-of-the-art (SOTA) survival prediction performance. Our source code is publicly available at https://github.com/Daijing-ai/CIGT-Surv.git.

</details>


### [52] [Clarity Contrast and Similarity Selection for Multi-Focus Image Fusion](https://arxiv.org/abs/2608.03252)
*Yicheng Zhang,Haoyou Deng,Zhiqiang Li,Wenti Yin,Nong Sang,Changxin Gao*

Main category: cs.CV

TL;DR: This paper proposes CSNet, a novel multi-focus image fusion network with explicit interaction between source images via a Clarity Contrast Attention Module (CCAM) that mutually enhances sharp features and suppresses blurry ones, plus a Similarity Selection Strategy to refine ambiguous boundary regions affected by Defocus Spread Effect, achieving state-of-the-art performance.


<details>
  <summary>Details</summary>
Motivation: Most existing deep learning-based MFIF methods lack explicit interaction between source images, which limits their performance and interpretability. Additionally, the Defocus Spread Effect degrades boundary pixels in all source images, making boundary regions difficult to handle.

Method: The method consists of two key components: (1) Clarity Contrast Attention Module (CCAM) which contrasts clarity differences between source images to mutually enhance sharp features while suppressing blurry ones, identifying focused regions and boundaries; (2) Similarity Selection Strategy which reconstructs an initial clear image and selects optimal pixels by comparing similarity to refine ambiguous boundary areas affected by Defocus Spread Effect.

Result: Extensive experiments demonstrate that CSNet achieves state-of-the-art performance both quantitatively and qualitatively in multi-focus image fusion.

Conclusion: Through the interactive approach between source images, CSNet effectively preserves focused regions and recovers natural boundaries, producing high-quality all-in-focus fused images. Code is publicly available.

Abstract: Multi-focus image fusion (MFIF) aims to generate an all-in-focus image from multiple images of the same scene focused at different regions. Most existing deep learning-based methods lack explicit interaction between the source images, which limits their performance and interpretability. This paper presents a novel Clarity Contrast and Similarity Selection Network (CSNet), to bridge direct information exchange for MFIF. Specifically, by contrasting the clarity differences between source images within our proposed Clarity Contrast Attention Module (CCAM), we mutually enhance sharp features while suppressing blurry ones. This allows us to identify the exactly focused regions in each source and locate the focused-defocused boundaries. Moreover, the Defocus Spread Effect (DSE) degrades pixels in all source images around the boundaries. To further refine these ambiguous areas, we introduce a Similarity Selection Strategy, which reconstructs an initial clear image from source images and selects optimal pixels by comparing the similarity among them. Through this interactive approach, CSNet effectively preserves focused regions as well as recovering natural boundaries to fuse an all-in-focus output. Extensive experiments demonstrate that our method achieves state-of-the-art performance both quantitatively and qualitatively. Our code is available on Github: https://github.com/ZYC-HUST/CSNet.

</details>


### [53] [NanoMorph-3D: An End-to-End Physics-Driven Unrolling Framework for Nanomaterial Reconstruction](https://arxiv.org/abs/2608.03257)
*Beiyuan Zhang,Hesong Li,Ziqi Wu,Ruiwen Shao,Ying Fu*

Main category: cs.CV

TL;DR: NanoMorph-3D is a unified end-to-end deep learning framework for 3D nanomaterial reconstruction from electron tomography, addressing the missing wedge problem and noise via a physics-driven unrolled network, hierarchical attention with physics-normalization, dual-domain sinusoidal attention, and unsupervised domain adaptation.


<details>
  <summary>Details</summary>
Motivation: Standard electron tomography suffers from the missing wedge problem causing geometric distortions, and existing learning-based methods either use physics-blind post-processing or shallow architectures with local receptive fields that cannot capture complex 3D topologies of nanomaterials.

Method: The method includes: (1) a comprehensive Nanomorphological Taxonomy-based synthetic dataset modeling nonlinear electron attenuation; (2) a Physics-Driven Unrolled Network mapping proximal gradient descent into a learnable architecture; (3) hierarchical attention with Physics-Normalization for long-range 3D dependencies and scale invariance; (4) a Dual-Domain strategy with Sinusoidal Attention to model physical projection trajectories and enforce sinogram consistency; (5) an unsupervised dual-stream mechanism for simulation-to-reality gap bridging.

Result: Experiments demonstrate that NanoMorph-3D reconstructs diverse nanomaterial topologies with superior fidelity and speed compared to existing approaches.

Conclusion: NanoMorph-3D provides a physics-grounded, learning-based solution that mitigates missing wedge artifacts and noise interference while achieving accurate and fast 3D nanomaterial reconstruction, though the simulation-to-reality gap remains a key challenge addressed via unsupervised adaptation.

Abstract: Precise 3D characterization of nanomaterials is essential for unlocking structure-property relationships. However, standard electron tomography is fundamentally limited by the missing wedge problem. Consequently, conventional algorithms suffer from severe geometric distortions, a challenge further complicated by pervasive noise interference. Current learning-based methods either rely on physics-blind post-processing or employ end-to-end architectures constrained by local receptive fields, failing to capture complex 3D topologies. We propose NanoMorph-3D, a unified end-to-end framework grounded in a comprehensive Nanomorphological Taxonomy. Powered by a large-scale synthetic dataset explicitly modeling non-linear electron attenuation, we design a Physics-Driven Unrolled Network mapping proximal gradient descent into a learnable architecture. To capture complex internal topologies, we formulate a hierarchical attention mechanism with Physics-Normalization for long-range 3D dependencies and scale invariance. Crucially, our Dual-Domain strategy leverages Sinusoidal Attention to explicitly model physical projection trajectories, enforcing strict sinogram consistency to mitigate missing wedge artifacts. Finally, an unsupervised dual-stream mechanism bridges the simulation-to-reality gap. Experiments demonstrate NanoMorph-3D reconstructs diverse topologies with superior fidelity and speed.

</details>


### [54] [Efficient Video Dataset Distillation via Cluster-Guided Prototype Blending](https://arxiv.org/abs/2608.03269)
*Chongle Ren,Guang Li,Wenbo Huang,Naoki Saito,Takahiro Ogawa,Miki Haseyama*

Main category: cs.CV

TL;DR: ProtoBlend is a construction-based video dataset distillation framework that replaces iterative gradient optimization with a select-allocate-blend approach, achieving competitive accuracy-efficiency trade-offs on action recognition benchmarks.


<details>
  <summary>Details</summary>
Motivation: Existing video dataset distillation methods rely on iterative gradient-based optimization of condensed videos, which is computationally expensive due to the temporal dimension. The paper investigates whether effective distilled videos can be constructed without gradient-based optimization, addressing challenges of temporal segment selection, intra-class diversity coverage, and information density per sample.

Method: ProtoBlend uses a three-stage framework: (1) teacher-guided temporal clip selection keeps high-confidence segments from source videos; (2) cluster-guided prototype allocation partitions selected clips in teacher feature space, assigning one distilled slot per intra-class cluster; (3) each prototype is blended with an in-cluster anchor, with teacher predictions combined using the same coefficient for mixture-source supervision.

Result: Experiments on four trimmed action-recognition benchmarks show ProtoBlend achieves competitive accuracy-efficiency trade-offs without iterative optimization of distilled videos.

Conclusion: The paper demonstrates that effective video distillation can be achieved through non-gradient construction methods, offering a more efficient alternative to iterative optimization approaches while maintaining competitive performance.

Abstract: Video dataset distillation aims to compress a large video dataset into a compact surrogate set that preserves its training utility. Most existing approaches synthesize condensed videos through iterative optimization, whose cost is amplified by the temporal dimension. Rather than further reducing the number of optimized variables, we investigate whether effective distilled videos can be constructed without gradient-based optimization of the stored videos. Such a construction-based approach must address three challenges: selecting informative temporal segments, covering diverse intra-class variations under a limited videos-per-class budget, and increasing the information carried by each stored sample. To this end, we propose ProtoBlend, an efficient select-allocate-blend framework. First, teacher-guided temporal clip selection retains a high-confidence segment from each source video. Second, cluster-guided prototype allocation partitions the selected clips in the teacher feature space and assigns one distilled slot to each intra-class cluster. Third, each prototype is blended with an in-cluster anchor, while their teacher predictions are combined using the same coefficient to provide mixture-source supervision. Experiments on four trimmed action-recognition benchmarks demonstrate that ProtoBlend achieves a competitive accuracy-efficiency trade-off without iterative optimization of the distilled videos.

</details>


### [55] [GUI-Lens: Coarse-to-Fine Cropping for GUI Grounding with General-Purpose VLMs](https://arxiv.org/abs/2608.03270)
*Zichuan Fu,Shirong Wang,Wenlin Zhang,Guojing Li,Yimin Deng,Jingtong Gao,Junjia Qi,Hanyu Yan,Yefeng Zheng,Xiaopeng Li,Wanyu Wang,Xian Wu,Xiangyu Zhao*

Main category: cs.CV

TL;DR: GUI-Lens is a coarse-to-fine GUI grounding framework that enables a general-purpose VLM to locate UI elements through iterative, progressively zoomed-in views guided by OCR text and detected UI components, achieving up to 24.9 percentage points improvement and state-of-the-art results with GPT-5.5.


<details>
  <summary>Details</summary>
Motivation: Existing GUI grounding methods rely on direct click prediction from VLMs, which allows visual ambiguity or inaccurate initial estimates to propagate to the final result, especially on high-resolution, densely populated interfaces.

Method: GUI-Lens extracts OCR text and UI component detections as coordinate references. The VLM iteratively selects the region and scale of the next view, which is cropped and enlarged for finer details, progressing through successively focused views. Intermediate crops and clicks are validated against the instruction, and the final local position is mapped back to original screen coordinates.

Result: On four GUI grounding benchmarks with three general-purpose VLM backends, GUI-Lens improves overall grounding accuracy by up to 24.9 percentage points and achieves state-of-the-art performance with GPT-5.5.

Conclusion: The coarse-to-fine active observation approach effectively mitigates ambiguity in GUI grounding by replacing direct click prediction with iterative visual refinement, leading to significant accuracy gains across benchmarks and model backends.

Abstract: GUI grounding maps natural-language instructions to click locations and is essential for reliable GUI agents. The task remains difficult on high-resolution, densely populated interfaces because a vision-language model (VLM) may recognize a requested control without locating it precisely enough for interaction. Most existing methods provide various forms of localization assistance, but still rely on a direct click prediction, allowing visual ambiguity or an inaccurate initial estimate to propagate to the final result. In this paper, we introduce GUI-Lens, a coarse-to-fine grounding framework that allows a general-purpose VLM to determine the target through active visual observations. Specifically, GUI-Lens extracts OCR text and detected UI components from the screenshot and presents their positions as coordinate references. Using the instruction, the current view, and these references, the VLM selects the region and scale of the next view, which is cropped and enlarged to provide finer visual details. This process continues over successively focused views until the target is determined. Proposed crops and clicks are checked against the instruction throughout the process, and the final local position is mapped back to the original screen coordinates. Experiments on four GUI grounding benchmarks and three general-purpose VLM backends show that GUI-Lens improves overall grounding accuracy by up to 24.9 percentage points and achieves state-of-the-art performance with GPT-5.5.

</details>


### [56] [3DGSI-Assessor: A Large-Scale Dataset and An LMM-based Method for 3D Gaussian Splatting Image Quality Assessment](https://arxiv.org/abs/2608.03279)
*Yuke Xing,Jiarui Wang,William Gordon,Zhu Li,Guangtao Zhai,Yiling Xu*

Main category: cs.CV

TL;DR: The paper introduces 3DGS-IEval-15K+, a large-scale multi-dimensional IQA dataset for compressed 3D Gaussian Splatting, and proposes 3DGSI-Assessor, an LMM-based all-in-one framework that predicts overall, geometry, and color quality scores in a single forward pass, achieving state-of-the-art results.


<details>
  <summary>Details</summary>
Motivation: 3DGS compression introduces representation-specific distortions (floating artifacts, surface scattering) that conventional IQA metrics fail to capture. Additionally, geometric and color attributes can be distorted independently yet existing metrics only report a single overall score, making diagnosis of dimension-specific distortions impossible.

Method: They build a large-scale dataset (15,200 images, 10 scenes, 6 compressors, 20 viewpoints) with 45,600 MOS annotations across overall/geometry/color dimensions. They then propose 3DGSI-Assessor, an LMM-based framework integrating global semantic and dimension-specific local features to predict all three quality dimensions simultaneously.

Result: 3DGSI-Assessor achieves state-of-the-art performance on 3DGS-IEval-15K+ and demonstrates competitive generalization on other novel view synthesis benchmarks.

Conclusion: The work addresses the gap in 3DGS-specific quality assessment by providing a large-scale multi-dimensional benchmark and an all-in-one assessment framework, with dataset and code to be released publicly.

Abstract: 3D Gaussian Splatting (3DGS) has become a dominant representation for real-time novel view synthesis (NVS), yet its storage footprint makes compression indispensable for practical deployment. 3DGS training and compression introduce representation-specific distortions such as floating artifacts and surface scattering, which conventional image quality assessment (IQA) metrics fail to capture. Moreover, the independent compression of geometric and color attributes may lead to decoupled dimension-specific distortions that must be diagnosed separately, yet existing metrics report only a single overall score. To address these gaps, we present 3DGS-IEval-15K+, a large-scale, multi-dimensional IQA dataset for compressed 3DGS, comprising 15,200 images from 10 diverse scenes, produced by 6 representative 3DGS algorithms at systematically designed compression levels and rendered from 20 strategically selected viewpoints spanning both training views and challenging novel views, annotated with 45,600 mean opinion scores (MOSs) across overall, geometry, and color quality. Based on 3DGS-IEval-15K+, we propose 3DGSI-Assessor, an all-in-one 3DGS IQA framework that integrates global semantic and dimension-specific local features within a large multimodal model (LMM), predicting all three dimensions in a single forward pass. 3DGSI-Assessor achieves state-of-the-art performance on 3DGS-IEval-15K+, and exhibits competitive generalization on other NVS benchmarks. Dataset and code will be released at https://github.com/YukeXing/3DGSI-Assessor.

</details>


### [57] [Test-Time Scaling for Safe Text-Guided Image Generation via Intermediate Clean Estimates](https://arxiv.org/abs/2608.03284)
*Jinya Sakurai,Shueicheng Yan,Xun Xu*

Main category: cs.CV

TL;DR: This paper proposes a test-time safety mechanism for text-to-image diffusion models that detects prohibited content by analyzing intermediate clean image estimates with a sparse margin objective, and intervenes by optimizing a structured low-rank residual in text-conditioning space via truncated backpropagation, achieving weight-preserving, scalable safety control.


<details>
  <summary>Details</summary>
Motivation: Training-based unlearning methods are computationally expensive and cause catastrophic interference, while existing test-time defenses are prompt-centric and overlook visual signals for detecting prohibited content.

Method: Leverages intermediate clean images during generation with a sparse margin objective for violation detection. Upon detection, intervenes by optimizing a structured low-rank residual in the text-conditioning space via truncated backpropagation, enabling weight-preserving detection and test-time scaling.

Result: Experiments on Stable Diffusion v1.4 and v3.5 across nudity removal, IP protection, and style erasure show superior performance in suppression, fidelity, and preservation compared to weight-preserving baselines, with near-unchanged inference latency for non-violating cases.

Conclusion: The proposed approach provides a scalable, flexible, weight-preserving solution for safe generative deployment, balancing safety suppression with general capability preservation.

Abstract: Ensuring safety and policy compliance in text-to-image diffusion models remains a critical challenge, as benign or adversarial prompts can often elicit prohibited content, e.g. nudity and protected intellectual property. While training-based unlearning methods are effective, they are computationally expensive and prone to catastrophic interference with general capabilities. Conversely, existing test-time defenses are primarily prompt-centric, relying on modifying textual descriptions only, and overlook the visual signals for detection. In this paper, we propose to leverage the intermediate clean image estimated during the generation process and employ a sparse margin objective to detect prohibited concepts. When a violation is detected, we immediately intervene by optimizing a structured low-rank residual in the text-conditioning space via truncated backpropagation. This design allows weight-preserving detection, keeps non-violating inference latency nearly unchanged as the maximum budget increases, and offers flexibility in safety performance via test-time scaling. Extensive experiments on Stable Diffusion v1.4 and v3.5 across nudity removal, IP protection, and style erasure demonstrate superior performance across suppression, fidelity and preservation compared to prior weight-preserving baselines, providing a scalable and flexible solution for safe generative deployment.

</details>


### [58] [Recurrent Contrastive Learning for Imbalanced Medical Image Classification](https://arxiv.org/abs/2608.03304)
*Zhiyuan Zhu,Xinling Meng,Junxuan Yu,Jiongquan Chen,Qiongying Ni,Tuhang Shao,Yuhao Huang,Luping Zhou,Ruiyang Huang,Yuxue Wang,Rongliang Zhang,Xue Wang,Tianhong Tang,Likun Wang,Junbo Chen,Yong Jiang,Yongping Lu,Xin Yang*

Main category: cs.CV

TL;DR: RCL (Recurrent Contrastive Learning) addresses class imbalance in medical image classification by progressively expanding the latent support region of tail classes using recurrent historical feature reuse, a Temporal Memory Queue, and Temporal Anchors built on a DINOv3+LoRA backbone. It achieves consistent improvements over strong baselines on three imbalanced medical datasets.


<details>
  <summary>Details</summary>
Motivation: Class imbalance in medical images stems from disparities in disease incidence. Existing methods (class resampling, loss reweighting) only improve learning within the observed feature distribution and fail to explicitly enlarge the latent support region of tail classes. This leaves tail-class representations overly compact and vulnerable to head-class encroachment, producing biased decision boundaries.

Method: RCL progressively expands the support region of tail classes by recurrently reusing historical feature states across training phases. It uses DINOv3 with LoRA adapters as the backbone for robust embeddings, a Temporal Memory Queue (TMQ) to preserve corpus-level features across phases and provide diversified global contrastive references, and Temporal Anchors (TARs) built on TMQ to form an anchor field around tail classes that enlarges their support region and suppresses head-class encroachment.

Result: Extensive experiments on three imbalanced medical datasets show that RCL achieves consistent improvements over strong baselines. Code is publicly available.

Conclusion: RCL effectively enlarges tail-class support regions, suppresses head-class encroachment, and improves inter-class separation, demonstrating that recurrently leveraging historical feature states is a promising direction for addressing class imbalance in medical image classification.

Abstract: Medical image classification often suffers from class imbalance due to the inherent disparities in disease incidence. Existing approaches, such as class resampling and loss reweighting, mainly improve learning within the observed feature distribution, but do not explicitly enlarge the latent support region of tail classes. As a result, tail-class representations remain overly compact and are easily encroached upon by head classes, leading to biased decision boundaries. In this work, we propose Recurrent Contrastive Learning (RCL) for imbalanced medical image classification. RCL progressively expands the support region of tail classes by recurrently reusing historical feature states across training phases. Specifically, we adopt DINOv3 with LoRA adapters as the backbone to provide robust feature embeddings. We then devise a Temporal Memory Queue (TMQ) to preserve corpus-level features across training phases and provide diversified global references for contrastive learning. Based on TMQ, we construct Temporal Anchors (TARs) to form an anchor field around tail classes. This field enlarges the support region of tail classes, suppresses head-class encroachment, and improves inter-class separation. Extensive experiments on three imbalanced medical datasets demonstrate that RCL achieves consistent improvements over strong baselines. The code is available at https://github.com/dndins/RCL.

</details>


### [59] [LocAnyMed: Vision-Language Grounding for Multimodal Medical Images](https://arxiv.org/abs/2608.03322)
*Zihan Wang,Tong Liu,Zhiwei Wang,Tao Huang,Wentao Jiang,Sihan Ma,Shanshan Ye,Xiaohui Yang,Jing Zhang*

Main category: cs.CV

TL;DR: This paper introduces LocAnyMed-200K, a multimodal medical visual grounding dataset with ~200K image-query-answer examples across CT, optical imaging, ultrasound, and X-ray, unifying heterogeneous localization resources into a free-form instruction format. Full-parameter fine-tuning of LocateAnything-3B on this dataset improves F1@IoU 0.50 from 10.64 to 85.59. The authors also derive LocAnyMed-CoT-20K, a rationale-augmented subset with structured reasoning, enabling both accurate localization and interpretable clinical evidence.


<details>
  <summary>Details</summary>
Motivation: General-purpose visual grounding models are trained predominantly on natural images and fail to effectively localize in medical images. Existing medical localization resources are fragmented across modalities, datasets, and task formulations, creating a gap in interpretable medical AI that requires unified, large-scale domain-specific supervision.

Method: The authors construct LocAnyMed-200K by harmonizing heterogeneous detection and localization resources into a unified free-form instruction format supporting one/multiple bounding boxes, point coordinates, and no-target outputs for negative queries. They fine-tune LocateAnything-3B on this dataset and derive a rationale-augmented subset (LocAnyMed-CoT-20K) with structured reasoning connecting anatomical context, visual observations, and spatial conclusions.

Result: Full-parameter fine-tuning of LocateAnything-3B on LocAnyMed-200K improves F1@IoU 0.50 from 10.64 to 85.59 on a held-out evaluation split. The CoT-augmented subset further improves cross-source generalization when used in fine-tuning.

Conclusion: Large-scale domain-specific supervision can effectively equip general grounding models with medical localization capabilities. The LocAnyMed resources provide a unified foundation for studying both localization accuracy and rationale quality across heterogeneous medical imaging modalities, with code publicly released.

Abstract: Medical visual grounding connects free-form clinical queries to spatial evidence in medical images and is an important component of interpretable medical artificial intelligence. However, general-purpose grounding models are predominantly trained on natural images, while existing medical localization resources remain fragmented across imaging modalities, datasets, and task formulations. To address this gap, we construct LocAnyMed-200K, a multimodal medical visual grounding dataset containing approximately 200K image-query-answer examples across computed tomography, optical medical imaging, ultrasound, and X-ray. We harmonize heterogeneous detection and localization resources into a unified free-form instruction format that supports one or multiple bounding boxes, point coordinates, and no-target outputs for negative queries. Full-parameter fine-tuning of LocateAnything-3B on LocAnyMed-200K improves F1@IoU 0.50 from 10.64 to 85.59 on a held-out evaluation split, demonstrating that large-scale domain-specific supervision can equip a general grounding model with effective medical localization capabilities. Beyond spatial coordinates, a clinically interpretable grounding system should also communicate the evidence supporting its prediction. We therefore derive LocAnyMed-CoT-20K, a rationale-augmented subset that connects anatomical context, visual observations, and spatial conclusions through structured reasoning and further improves cross-source generalization through fine-tuning. Together, these resources provide a unified foundation for studying both localization accuracy and rationale quality across heterogeneous medical imaging modalities. The code is publicly available at https://github.com/MiliLab/LocAnyMed.

</details>


### [60] [PolyLayout: Multi-room Manhattan Layout Estimation](https://arxiv.org/abs/2608.03323)
*Gustav Hanning,Shaohui Liu,Rémi Pautrat,Marc Pollefeys,Kalle Åström,Viktor Larsson*

Main category: cs.CV

TL;DR: PolyLayout is a multi-room layout estimation method that parameterizes room layouts as Manhattan 3D polygons and jointly optimizes them across multiple rooms using a neural network scorer on top of pre-trained visual features, with explicit model-based camera projection and polygon updates.


<details>
  <summary>Details</summary>
Motivation: Existing room layout estimation methods are limited by poor generalization to new datasets, restrictive geometric assumptions, and failure to exploit shared building structure across rooms (e.g., dominant directions, ground plane, ceiling height).

Method: PolyLayout parameterizes room layouts as Manhattan 3D polygons and jointly optimizes them across multiple rooms. A neural network predicts the optimization objective on top of pre-trained visual features, trained end-to-end with supervision only on output layouts. Camera projection and polygon updates remain explicit and model-based, separating learned scoring from geometry to improve generalization. The optimization adaptively refines polygon topology via iterative wall split and merge operations while utilizing structural cues across rooms.

Result: The paper introduces two new multi-view multi-room layout benchmarks by providing layout annotations to existing datasets, and experiments show PolyLayout outperforms prior approaches in both accuracy and robustness.

Conclusion: PolyLayout demonstrates that separating learned scoring from model-based geometry enables joint multi-room layout estimation with better generalization, accuracy, and robustness, supported by new benchmarks for multi-view multi-room layout estimation.

Abstract: Estimating room layouts from multi-view imagery is a core task for indoor scene understanding. Existing methods are typically limited either by poor generalization to new datasets or restrictive geometric assumptions of the room shape or camera configuration. Most also estimate rooms independently, failing to exploit shared building structure such as dominant directions, ground plane or ceiling height.
  We propose PolyLayout, a multi-room layout estimation method that parameterizes room layouts as Manhattan 3D polygons and optimizes them jointly across multiple rooms. The optimization objective is predicted by a neural network on top of robust pre-trained visual features and trained end-to-end with supervision only on output room layouts. At the same time, camera projection and polygon updates remain explicit and model-based. This separation between learned scoring and geometry improves generalization to new datasets and camera parameters. During optimization, PolyLayout adaptively refines the polygon topology through iterative wall split and merge operations while jointly utilizing structural cues across rooms. We introduce two new multi-view multi-room layout benchmarks by providing layout annotations to existing datasets, and experiments show that PolyLayout outperforms prior approaches, both in terms of accuracy and robustness.
  Project page: https://ghanning.github.io/PolyLayout

</details>


### [61] [SPADE: An Input-Adaptive Sparse Attention Engine for Fast Video Diffusion Models Inference](https://arxiv.org/abs/2608.03335)
*Shanghao Liu,Renze Chen,Size Zheng,Yuanqiang Liu,Yun,Liang,Hailong Yang*

Main category: cs.CV

TL;DR: SPADE is a training-free sparse-attention engine for video diffusion transformers that achieves 2.26x-3.40x attention acceleration and 1.49x-1.80x end-to-end inference speedup while preserving generation quality across Hunyuan-Video and Wan 2.1/2.2.


<details>
  <summary>Details</summary>
Motivation: Video diffusion transformers (vDiTs) suffer from quadratic self-attention cost at video-token scales, making inference prohibitive. The key challenge is achieving input-adaptive sparsity—selecting critical Q/K/V tokens with negligible overhead for end-to-end gains.

Method: SPADE consists of three components: (i) vDiT-SSR, a specification defining 3D blocking candidates and formalizing dynamic masks via Summarizer/Estimator expressions; (ii) runtime scheme generation using SICS and a head-wise policy; and (iii) an executor with low-overhead index search, flash block-sparse attention, and kernel grouping.

Result: Across Hunyuan-Video and Wan 2.1/2.2 for text-to-video and image-to-video generation, SPADE accelerates attention by 2.26x-3.40x and end-to-end inference by 1.49x-1.80x while preserving quality and increasing sparsity.

Conclusion: SPADE demonstrates that training-free, input-adaptive sparse attention can significantly improve vDiT inference efficiency without quality degradation, and the code is open-sourced.

Abstract: Video diffusion transformers (vDiTs) generate high quality but pay quadratic self-attention cost, making inference prohibitive at video-token scales. The challenge is input-adaptive sparsity: selecting critical Q/K/V tokens with negligible overhead and executing them for end-to-end gains. We present SPADE, a training-free sparse-attention engine of three parts: (i) vDiT-SSR, a specification defining 3D blocking candidates and formalizing dynamic masks via Summarizer/Estimator expressions; (ii) runtime scheme generation using SICS and a head-wise policy; and (iii) an executor with low-overhead index search, flash block-sparse attention, and kernel grouping. Across Hunyuan-Video and Wan 2.1/2.2 for text-to-video and image-to-video generation, SPADE raises sparsity and speed while preserving quality, accelerating attention by 2.26x-3.40x and end-to-end inference by 1.49x-1.80x. Our code is open-sourced at https://github.com/6somehow/DAC-SPADE.

</details>


### [62] [When Oracle Conditioning Misleads Deployment: Conditioning-Availability Bias in Echocardiographic Segmentation](https://arxiv.org/abs/2608.03342)
*Dang P. M. Cao,Hieu D. Pham,Hieu Pham*

Main category: cs.CV

TL;DR: This paper studies shortcut learning and auxiliary-variable shift in phase-conditioned echocardiographic segmentation, introducing a complementary gap pair to measure performance degradation when moving from oracle-conditioned training to deployment with estimated phase signals. Experiments on CAMUS and EchoNet-Dynamic reveal severe failures and latent sensitivity to phase perturbations, with deployment-aware checkpoint selection and phase perturbation mitigating both gaps.


<details>
  <summary>Details</summary>
Motivation: Conditional segmentation models are often trained with clean auxiliary signals that may not be available or equally reliable at deployment. This protocol-level mismatch represents a form of shortcut learning and auxiliary-variable shift that has not been systematically studied in echocardiographic segmentation.

Method: The paper introduces a complementary gap pair: (1) a deployable oracle-estimated pathway gap measuring loss when using estimated phase versus oracle phase, and (2) an oracle-random pathway gap probing sensitivity to incorrect/random phase. Experiments include deployment-aware checkpoint selection and phase perturbation on CAMUS and EchoNet-Dynamic datasets, with subgroup analyses and a downstream EF audit.

Result: On CAMUS, one strong-cyclic oracle-selected run fails severely with estimated phase, while sensitivity to incorrect phase persists across three runs. On EchoNet-Dynamic, the current estimator remains usable but random-phase testing reveals strong latent sensitivity. Deployment-aware checkpoint selection and phase perturbation reduce both gaps with minimal mean Dice change. The EF audit shows recovering segmentation does not necessarily recover EF error or signed bias.

Conclusion: The complementary gap pair provides a principled test of whether oracle-conditioned performance survives the inference pathway actually available at deployment, revealing that current phase-conditioned models are vulnerable to auxiliary-variable shift and that segmentation recovery does not guarantee downstream clinical metric recovery.

Abstract: Conditional segmentation models may be trained and evaluated with auxiliary signals cleaner than those available at deployment. We study this protocol-level manifestation of shortcut learning and auxiliary-variable shift in phase-conditioned echocardiographic segmentation. The complementary gap pair measures loss on the deployable oracle-estimated pathway and probes sensitivity on the oracle-random pathway. On held-out CAMUS data, one strong-cyclic, oracle-selected run fails severely with estimated phase, while sensitivity to incorrect phase persists across three runs. On EchoNet-Dynamic, the current estimator remains usable, but random-phase testing reveals strong latent sensitivity. Deployment-aware checkpoint selection and phase perturbation reduce both gaps with little change in mean Dice. Exploratory subgroup analyses quantify variation across measured strata, and a downstream ejection fraction (EF) audit shows that recovering segmentation does not necessarily recover EF error or signed bias. Together, the gaps test whether oracle-conditioned performance survives the inference pathway actually available at deployment.

</details>


### [63] [Can Text-to-Image Models Draw from the Right Frame of Reference?](https://arxiv.org/abs/2608.03357)
*Zheyuan Gu,Ruihang Li,Yong Huang,Yiqian Zhang,XIangzhao Hao,Jiaxin Niu,Jiahao Hu,Zhenyu Zhang*

Main category: cs.CV

TL;DR: FoR-T2I is a new benchmark with 1,200 prompt pairs that isolates whether text-to-image models can follow spatial instructions under a specified frame of reference (object-intrinsic) versus the default camera view. Across 22 T2I models, accuracy drops 41.8% on frame-of-reference prompts, and a VLM-gated rewriting strategy improves FoR accuracy from 25.0% to 29.2%.


<details>
  <summary>Details</summary>
Motivation: Existing T2I benchmarks reveal layout failures but rarely isolate whether models can follow a specified frame of reference when it differs from the camera view, leaving a critical gap in evaluating spatial instruction following.

Method: The authors construct FoR-T2I with 1,200 prompt pairs from controlled spatial layouts, where each pair contains a camera-view (Cam) prompt and a frame-of-reference (FoR) prompt describing the same target placement via an oriented anchor object. They evaluate 22 T2I models, analyze gaps by relation type and camera view, compare training-free prompting and feedback-based mitigation strategies, and propose a VLM-gated rewriting approach that selects rewritten prompts using visual feedback.

Result: Mean final accuracy is 41.8% lower on FoR prompts than matched Cam prompts; the best model achieves only 44.3% FoR accuracy. The proposed VLM-gated rewriting approach improves average FoR accuracy from 25.0% to 29.2% under the same generation budget.

Conclusion: Current T2I models struggle significantly more when spatial layouts are described through an object's orientation rather than directly in image coordinates, and there remains substantial room for improvement in frame-of-reference understanding.

Abstract: Spatial instruction following has become a crucial requirement for text-to-image (T2I) generation. A common challenge arises when directional expressions are interpreted under different frames of reference. For example, ``the left of'' may refer to the viewer's image coordinates or to the intrinsic orientation of an object, leading to different expected layouts. Existing T2I benchmarks reveal important layout failures, yet they rarely isolate whether models can follow a specified frame of reference when it differs from camera view. To mitigate this gap, we introduce FoR-T2I, a benchmark for evaluating this distinction with 1,200 prompt pairs built from controlled spatial layouts. In each pair, the camera-view (Cam) prompt states the target relation in camera view, while the frame-of-reference (FoR) prompt describes the same target placement through an oriented anchor object. Across 22 closed-source and open-source T2I models, mean final accuracy is 41.8\% lower on FoR prompts than on matched Cam prompts; even the best-performing model achieves only 44.3\% FoR accuracy. This suggests that current models struggle more when the same layout is described through an object's orientation rather than directly in image coordinates. We further analyze this gap by relation type and camera view, compare several training-free prompting and feedback-based mitigation strategies, and propose a VLM-gated rewriting approach that selects rewritten prompts using visual feedback, improving average FoR accuracy from 25.0\% to 29.2\% under the same generation budget.

</details>


### [64] [DRPFNet: Dual-domain Residual Progressive Fusion Network for RGB-Thermal Object Detection](https://arxiv.org/abs/2608.03370)
*Zian Wang,Changchun Li*

Main category: cs.CV

TL;DR: DRPFNet is a Dual-domain Residual Progressive Fusion Network for RGB-thermal object detection that solves cross-scale fusion, noise suppression, and information degradation issues through a three-level hierarchical system (structure, feature, enhancement), achieving competitive performance on two public RGB-T datasets.


<details>
  <summary>Details</summary>
Motivation: Existing RGB-T fusion methods independently fuse RGB and thermal features at each scale using attention/transformer mechanisms, leading to three key limitations: (1) lack of cross-level knowledge inheritance due to independent per-scale fusion, (2) continuous noise suppression failure from missing bidirectional optimization, and (3) information degradation from the absence of frequency-spatial collaboration.

Method: DRPFNet constructs a unified information flow optimization system at three synergistic levels: (1) Structural level: cross-scale propagation via bottom-up knowledge accumulation and bidirectional enhancement for smooth information flow; (2) Feature level: collaborative extraction of RGB high-frequency edges and thermal low-frequency structures through frequency band separation and edge guidance; (3) Enhancement level: edge-guided dual-domain refinement to improve foreground-background discrimination for precise object localization.

Result: Extensive experiments on two public RGB-T datasets demonstrate that DRPFNet achieves competitive detection performance with competitive efficiency, validating the effectiveness of the hierarchical collaborative strategy.

Conclusion: The proposed three-level hierarchical fusion strategy effectively addresses the limitations of prior per-scale independent fusion methods, achieving robust RGB-T object detection through synergistic optimization of structure, feature representation, and enhancement.

Abstract: RGB-thermal (RGB-T) object detection aims to fuse complementary information from visible and thermal modalities to achieve robust detection under varying illumination and weather conditions. Current methods typically employ attention mechanisms or transformers to perform cross-modal fusion independently at each feature scale, directly combining RGB and thermal features in the spatial domain. However, they still face significant limitations: cross-level knowledge inheritance caused by independent fusion at each scale,suppressing noise continuously due to the lack of bidirectional optimization, and information degradation induced by the absence of frequency-spatial collaboration. To address these issues, we propose DRPFNet, a Dual-domain Residual Progressive Fusion Network that constructs a unified information flow optimization system from three synergistic levels:structure, feature, and enhancement. At the structural level, we establish cross-scale propagation through bottom-up knowledge accumulation and bidirectional enhancement,ensuring smooth information flow. At the feature level, we collaboratively extract RGB high-frequency edges and thermal low-frequency structures via frequency band separation and edge guidance, guaranteeing representation quality. At the enhancement level, we enhance foreground-background discrimination through edge-guided dual-domain refinement,achieving precise object localization.Extensive experiments on two public RGB-T datasets demonstrate that our method achieves competitive performance with competitive efficiency, validating the effectiveness of this hierarchical collaborative strategy.

</details>


### [65] [Residual Flow Matching with Dynamic Cross-Interaction for 3D Multi-Person Motion Prediction](https://arxiv.org/abs/2608.03379)
*Wei Wei,Yinyuan Zhao,Ruixuan Yu*

Main category: cs.CV

TL;DR: A Prior-Guided Residual Flow Matching framework for 3D multi-person motion prediction that uses a deterministic coarse prior to anchor kinematics, a dynamic cross-interaction mechanism for reliable inter-person context, and a decoupled joint-motion architecture for fine-grained coherence, achieving state-of-the-art accuracy.


<details>
  <summary>Details</summary>
Motivation: Existing Flow Matching for multi-person motion prediction suffers from structural inconsistency and unreliable cross-agent interactions when predicting skeletal sequences directly from pure noise, particularly during early noise-dominated integration steps.

Method: 1) A Deterministic Coarse Prior (DCP) formulates generation as a conditional flow over motion residuals, anchoring kinematics. 2) A Dynamic Cross-Interaction (DCI) mechanism synchronizes inter-agent message-passing with integration progress for reliable social context. 3) A decoupled joint-motion architecture with bidirectional fusion preserves kinematic coherence.

Result: State-of-the-art prediction accuracy across multiple 3D multi-person motion datasets.

Conclusion: Prior-guided residual flow matching with dynamic interactions effectively improves both prediction accuracy and motion fidelity for multi-person scenarios.

Abstract: 3D multi-person motion prediction requires modeling both individual kinematics and inter-person interactions. While Flow Matching is effective for multi-hypothesis generation to improve prediction accuracy, directly predicting skeletal sequences from pure noise often compromises structural consistency and introduces unreliable cross-agent interactions during early noise-dominated integration steps. To address this, we propose a Prior-Guided Residual Flow Matching framework. First, a Deterministic Coarse Prior (DCP) establishes a kinematic anchor, formulating the generative process as a conditional flow over motion residuals to simplify the generative objective and preserve structural stability. Second, a Dynamic Cross-Interaction (DCI) mechanism temporally synchronizes inter-agent message-passing with the integration progress, ensuring the extraction of reliable social contexts and improving multi-person motion fidelity. Finally, a decoupled joint-motion architecture with bidirectional fusion effectively preserves fine-grained kinematic coherence. Extensive experiments demonstrate that our approach achieves state-of-the-art prediction accuracy across multiple datasets. Code is available at https://github.com/Wei-Wei-a/Residual-Flow-Matching-with-Dynamic-Cross-Interaction-for-3D-Multi-Person-Motion-Prediction.

</details>


### [66] [FreqAdapt: Frequency-Adaptive Processing for RAW Object Detection](https://arxiv.org/abs/2608.03385)
*Hanxi Li,Huiling Li*

Main category: cs.CV

TL;DR: FreqAdapt is a lightweight frequency-domain module that adaptively enhances RAW images for object detection by mapping ISP operations to the Fourier domain, achieving state-of-the-art performance with good interpretability and seamless integration into existing detection frameworks.


<details>
  <summary>Details</summary>
Motivation: RAW sensor images contain richer information and better noise characteristics than sRGB images for object detection, especially in adverse conditions, but existing methods predominantly rely on sRGB inputs processed by ISP for visualization rather than detection purposes.

Method: FreqAdapt maps ISP operations to the Fourier frequency domain, performs domain separation based on physical properties of ISP operations, and uses an adaptive frequency domain encoder that jointly analyzes amplitude spectrum, phase spectrum, and RAW image features to predict ISP parameters with a learnable fusion mechanism for adaptive feature enhancement.

Result: Extensive experiments on multiple datasets with diverse lighting and weather conditions show FreqAdapt achieves state-of-the-art performance while maintaining lightweight efficiency and physical interpretability.

Conclusion: FreqAdapt provides a novel lightweight frequency-domain solution for RAW-domain visual perception, seamlessly integrable into existing object detection frameworks, offering both performance gains and interpretability.

Abstract: Existing object detection methods predominantly utilize sRGB inputs, which are compressed from RAW sensor data using Image Signal Processors (ISP) originally designed for visualization purposes. Compared to RGB images, RAW images possess favorable noise characteristics and richer information representation, which are crucial for object detection, particularly under challenging conditions such as adverse weather or low-light environments. In this paper, we propose FreqAdapt, a lightweight module for adaptive RAW data enhancement in the frequency domain. Unlike traditional spatial domain processing methods, FreqAdapt innovatively maps ISP operations to the Fourier frequency domain and performs domain separation based on the physical properties of ISP operations, ensuring each operation is performed in its most suitable domain. Meanwhile, through an adaptive frequency domain encoder that jointly analyzes amplitude spectrum, phase spectrum, and RAW image features, we provide global context for ISP parameter prediction and employ a learnable fusion mechanism to achieve adaptive feature enhancement. Extensive experiments on multiple datasets with diverse lighting and weather conditions demonstrate that FreqAdapt achieves state-of-the-art performance while maintaining lightweight efficiency and good physical interpretability. Furthermore, our module can be seamlessly incorporated into existing object detection frameworks, providing a novel solution for visual perception tasks in the RAW domain.

</details>


### [67] [SRAP: SVD-Refined Adversarial Perturbations for Imperceptible Face-Swap Defense](https://arxiv.org/abs/2608.03395)
*Sungwon Cho,Kwanghyun Ko,Myungjoo Kang*

Main category: cs.CV

TL;DR: SRAP is a deepfake defense method that improves PGD-based adversarial perturbations by combining per-channel truncated SVD refinement with identity-importance masking, producing more visually imperceptible perturbations while maintaining identity-disruption effectiveness against face-swapping models.


<details>
  <summary>Details</summary>
Motivation: PGD-generated adversarial perturbations used to protect facial images from face-swapping are visually degraded due to (1) perturbations spread across identity-insensitive regions and (2) visually salient high-frequency components. These spatial and spectral inefficiencies need to be addressed to improve perturbation quality.

Method: The paper analyzes PGD perturbations via identity-sensitivity estimation and singular-value decomposition (SVD), finding that later singular components carry disproportionate high-frequency energy while leading components preserve most perturbation energy and defense utility. Based on this, SRAP applies per-channel truncated SVD refinement (suppressing high-rank, high-frequency residuals) combined with an identity-importance mask at every optimization step to restrict perturbations to identity-relevant locations.

Result: Experiments on CelebA-HQ and VGGFace2-HQ show SRAP substantially improves protected-image fidelity across all reported metrics while maintaining competitive identity-disruption performance, achieving a favorable trade-off between face-swap defense effectiveness and visual imperceptibility.

Conclusion: SRAP demonstrates that addressing both spatial (identity-importance masking) and spectral (SVD truncation) inefficiencies in adversarial perturbations enables strong deepfake defense with significantly better visual quality, offering a practical proactive protection approach for facial images.

Abstract: Deepfake technologies pose increasing threats to facial privacy and identity security, motivating proactive defenses that protect facial images before misuse. Although adversarial perturbations generated by projected gradient descent (PGD) can disrupt the identity representations used by face-swapping models, their visual quality is degraded by two characteristics: perturbations are distributed broadly over the image, including identity-insensitive regions, and they contain visually salient high-frequency components. We analyze these spatial and spectral inefficiencies through identity-sensitivity estimation and the singular-value decomposition (SVD) of PGD perturbations. Our analysis shows that later singular components contain a disproportionate amount of high-frequency energy, while the leading components preserve most of the perturbation energy and defense utility. Based on these observations, we propose SRAP, which combines per-channel truncated SVD refinement with an identity-importance mask at every optimization step. The SVD refinement suppresses high-rank, high-frequency residuals, while the mask restricts perturbations to locations that strongly influence identity representations. Experiments on CelebA-HQ and VGGFace2-HQ demonstrate that SRAP substantially improves protected-image fidelity across all reported metrics while maintaining competitive identity-disruption performance, yielding a favorable trade-off between face-swap defense and visual imperceptibility.

</details>


### [68] [Distilled Roads: Generalisable Road Network Extraction Across Sensors, Resolutions, and Region](https://arxiv.org/abs/2608.03407)
*Sanayya,Rakshith Sathish,Ashwathi Nambiar*

Main category: cs.CV

TL;DR: A framework that reframes global road extraction as a continual adaptation problem, combining cross-resolution knowledge distillation, multi-sensor training, and topology-aware supervision to achieve a single model that generalizes across 0.3-1.0m satellite imagery, outperforming SOTA by up to 22 F1 points and 15 APLS points with 3x faster inference.


<details>
  <summary>Details</summary>
Motivation: Existing road segmentation models generalize poorly to unseen environments due to narrow resolution-region training, requiring costly retraining which risks catastrophic forgetting.

Method: A continual adaptation framework combining (1) cross-resolution knowledge distillation across a resolution-decreasing curriculum, (2) multi-sensor training, and (3) topology-aware supervision, rather than architectural innovations.

Result: Outperforms state-of-the-art on City-Scale and Global-Scale benchmarks by up to 22 F1 points and 15 APLS points, while being the most efficient with 3x faster inference.

Conclusion: Robustness across diverse sub-meter satellite imagery can be achieved through targeted training strategies (data curricula, distillation, topology-aware losses) rather than increasingly complex architectures.

Abstract: Road network segmentation from satellite imagery remains challenging due to large geographic variation in road appearance, occlusions, and domain shifts introduced by differing resolutions and sensors. Existing models, typically trained under narrow resolution--region combinations, generalise poorly to unseen environments such as rural settings, regions with distinct road materials, or imagery from new satellite platforms, often producing broken or disconnected predictions. Adapting these models to new domains usually requires retraining or fine-tuning, which is costly and risks catastrophic forgetting.
  In this work, we reframe global road extraction as a continual adaptation problem rather than an architectural one. Our framework combines cross-resolution knowledge distillation across a resolution-decreasing curriculum, multi-sensor training, and topology-aware supervision, yielding a single model that generalises across $0.3-1.0$ m imagery from multiple satellite platforms across continents. On publicly available benchmarks, including City-Scale and Global-Scale, our model outperforms state-of-the-art results by up to $22$ F1 points and $15$ APLS points, while remaining the most efficient, with $3\times$ faster inference. Our results suggest that improved robustness across diverse sub-meter satellite imagery can be achieved through targeted training strategies, such as data curricula, distillation, and topology-aware losses, rather than increasingly complex architectures.

</details>


### [69] [Earth Embeddings](https://arxiv.org/abs/2608.03410)
*Adam J. Stewart,Heng Fang,Isaac A. Corley,Xiao Xiang Zhu*

Main category: cs.CV

TL;DR: This chapter reviews Earth embeddings—compact vector representations of locations, image patches, or pixels that package model feature outputs as reusable data—covering their types, trade-offs, applications, and practical guidance, along with open problems.


<details>
  <summary>Details</summary>
Motivation: Earth observation is shifting from foundation models users must run themselves toward embedding products that provide reusable feature outputs without requiring users to download and process raw satellite imagery.

Method: The chapter categorizes Earth embeddings (location encoders, patch products, pixel products), compares them across coverage, resolution, dimensionality, storage cost, licenses, and reproducibility, and reviews applications in land cover mapping, crop mapping, ecology, hazard modeling, socioeconomic prediction, and semantic search, with two case studies.

Result: Evidence is presented on when embeddings outperform conventional features and when pooling, fusion, or spatial transfer limit performance, along with practical workflows for similarity search and land cover mapping.

Conclusion: The chapter provides guidance for choosing, evaluating, storing, compressing, and publishing embeddings, and highlights open problems in oceanic/atmospheric coverage, uncertainty, and benchmarking.

Abstract: Earth observation is moving from foundation models that users must run themselves toward embedding products that package model feature outputs as reusable data without needing to download and process the imagery used to generate them. Earth embeddings are vectors that summarize locations, image patches, or pixels, letting users analyze compact features instead of repeatedly training or running large models on raw satellite imagery. This chapter explains the main types of Earth embeddings, from implicit location encoders to explicit patch and pixel products, and compares their coverage, resolution, dimensionality, storage cost, licenses, and reproducibility. We review their use in land cover and crop mapping, ecological and hazard modeling, socioeconomic prediction, and semantic search, with evidence on when embeddings improve on conventional features and when pooling, fusion, or spatial transfer limit performance. Two case studies show practical workflows for similarity search and land cover mapping. We close with guidance for choosing, evaluating, storing, compressing, and publishing embeddings, and with open problems in oceanic and atmospheric coverage, uncertainty, and benchmarking.

</details>


### [70] [HyperbolicDiffusion: Sharp & Scalable Tiled Generation on the Hyperbolic Plane](https://arxiv.org/abs/2608.03422)
*Hugo Caselles-Dupré*

Main category: cs.CV

TL;DR: HyperbolicDiffusion is a training-free method for generating images directly on the hyperbolic plane (H2), using a planar tiled diffusion approach with overlapping windows, a compact dynamic program for window placement, and a geometry-based repair stage to handle curvature-induced artifacts.


<details>
  <summary>Details</summary>
Motivation: The hyperbolic plane has no rectangular canvas like Euclidean space, and its area grows exponentially with radius, making it challenging to adapt standard planar diffusion models to generate content on H2.

Method: The authors introduce HyperbolicBloom-Cover, a dynamic program that optimally places overlapping diffusion windows on H2 with theoretical guarantees. Permanent surface IDs form a shared latent canvas where a standard diffusion model denoises local windows and fuses predictions back onto H2. A second geometry-derived stage re-noises and repairs regions where curvature causes residual disagreement and blur at multi-window junctions.

Result: The generated hyperbolic fields are sharp, reprojectable, and consistent across viewpoints, producing prompt-driven generative imagery comparable to Escher's Circle Limit series.

Conclusion: HyperbolicDiffusion successfully extends training-free diffusion generation to the hyperbolic plane, overcoming geometric challenges with an efficient window coverage scheme and a curvature-aware repair mechanism.

Abstract: Planar tiled diffusion denoises overlapping windows of one rectangular canvas. The hyperbolic plane has no such canvas, and its area grows exponentially with radius. We introduce HyperbolicDiffusion, a training-free method for generating finite visual fields directly on the hyperbolic plane H2. Our Hyperbolic Blooming Cover reduces window placement to a compact dynamic program that runs in seconds while providing strong theoretical guarantees. Permanent surface IDs form a shared latent canvas: a standard diffusion model denoises local windows, whose predictions are fused back onto H2. Because curvature causes residual disagreement and blur at multi-window junctions, a geometry-derived second stage re-noises and repairs precisely those regions. The resulting fields are sharp, reprojectable, and consistent across viewpoints, providing a prompt-driven generative counterpart to Escher's Circle Limit series.

</details>


### [71] [SGFormer: Structure-Guided Transformer for Robust Local Feature Matching](https://arxiv.org/abs/2608.03423)
*Runyu Zhu*

Main category: cs.CV

TL;DR: SGFormer is a structure-guided Transformer for local feature matching that addresses "attention divergence" in detector-free matching methods like LoFTR, where high-confidence matches fall outside overlapping regions under large viewpoint changes. It introduces a Triple-Structure-Attention (TSA) module that uses shallow local features to guide the transformer to focus on salient structures in overlapping areas, improving matching accuracy and reliability.


<details>
  <summary>Details</summary>
Motivation: Existing detector-free matching methods (e.g., LoFTR) use unconstrained attention mechanisms that globally distribute attention, causing "attention divergence"—where similar features in irrelevant regions receive equal weighting, leading to matches outside valid overlapping regions, especially under large viewpoint variations. This limits matching reliability in challenging photogrammetric environments.

Method: SGFormer uses a semi-dense coarse-to-fine pipeline with a Triple-Structure-Attention (TSA) module integrated into the backbone. TSA leverages shallow local features from early network layers to enhance representation around salient structures, guiding subsequent transformer stages to focus attention on salient structure regions globally, reinforcing visually consistent areas while suppressing non-overlapping regions.

Result: Extensive experiments demonstrate that SGFormer significantly mitigates attention divergence and improves matching accuracy compared to existing approaches.

Conclusion: Structure-guided attention is an effective strategy for improving feature matching reliability in challenging photogrammetric scenarios, particularly those with large viewpoint changes, by focusing transformer attention on salient structures within overlapping regions.

Abstract: Local feature matching is a fundamental component of photogrammetry, enabling accurate image correspondence critical for tasks such as 3D reconstruction, stereo mapping, and visual localization. While recent detector-free matching methods, like LoFTR, have advanced the field, the global features obtained by leveraging the global-range modeling capacity of the unconstrained attention mechanism compromise the model's attention to the salient structures in certain scenarios. This limitation leads to a phenomenon we define as attention divergence, wherein a portion of high-confidence matches are distributed outside the valid matching region (overlapping region), especially in scenes with large viewpoint variations. This occurs because similar features in irrelevant regions may receive equal weighting and consideration within the standard Transformer, limiting matching reliability in challenging photogrammetric environments. To address this issue in feature matching, we propose SGFormer (Structure-Guided Transformer), a novel structure-aware matching network that adaptively updates attention on features near salient structure in overlapping regions. SGFormer employs a semi-dense coarse-to-fine pipeline and incorporates the proposed Triple-Structure-Attention (TSA) module into the backbone net for extracting distinctive features. The TSA module utilizes shallow local features from early network layers to enhance the representation around salient structure, guiding subsequent transformer stages to intensify the model's focus on regions with salient structure across the global scope. SGFormer, thereby reinforcing attention to visually consistent areas while mitigating the influence of non-overlapping regions. Extensive experiments show that SGFormer significantly mitigates attention divergence and improves matching accuracy.

</details>


### [72] [OliveGemma: A 3 Billion Visual Language Model for Recognising the Mediterranean & European Diet](https://arxiv.org/abs/2608.03428)
*Dimitrios I. Zaridis,Traianos Tsiokris,Vasileios C. Pezoulas,Daphni Plati,Eugenia Mylona,Eleni Georga,Nikos Tsiknakis,Antonis Sakellarios,Dimitrios I. Fotiadis*

Main category: cs.CV

TL;DR: OliveGemma, a vision language model built on PaliGemma-2-3B and fine-tuned with LoRA on 17,340 images from three European food datasets, achieves 92.96% top-1 accuracy on Mediterranean/European dish recognition, outperforming CNN baselines and larger zero-shot frontier models.


<details>
  <summary>Details</summary>
Motivation: Image-based dietary assessment needs scalable alternatives to self-reported food diaries, but fine-grained food recognition is challenging due to high intra-class variability and visually similar dishes.

Method: OliveGemma is built on the open-weight PaliGemma-2-3B architecture and fine-tuned with LoRA (PEFT) on a unified corpus of 17,340 images from three European research project datasets (MedGR, ODIN, VIPPSTAR), reconciled into 216 composed dish categories with 102,642 instruction-style QA items covering dish recognition, ingredients, class boundaries, visual evidence, and overall food understanding.

Result: OliveGemma achieves 92.96% ± 0.91% top-1 accuracy under 3-fold cross-validation, beating DenseNet-121 by 7.31%, and surpassing zero-shot frontier models (Gemini Flash 3/3.5, GPT-5.4 Mini, Claude Haiku 4.6) by 8%, 46%, and 64% respectively. It achieves 90.79% ± 1.3% Exact-Set on likely ingredients and is second best on Top-3/Top-5 accuracy among all baselines.

Conclusion: PEFT adaptation of a small VLM can surpass substantially larger proprietary models on specialized food recognition tasks, demonstrating the viability of efficient fine-tuning for domain-specific vision-language applications.

Abstract: Image based dietary assessment offers a scalable alternative to self reported food diaries, yet fine-grained food recognition remains challenging due to high intra-class variability and visually similar dishes. This study presents OliveGemma, a vision language model for recognising and reasoning about Mediterranean and European cuisine. Built on the open-weight PaliGemma-2-3B architecture, OliveGemma is fine-tuned with LoRA on a unified corpus of 17,340 images from three European research project datasets (MedGR, ODIN, and VIPPSTAR), reconciled into a vocabulary of 216 composed dish categories and paired with 102,642 instruction style question-answer items covering dish recognition, likely and visible ingredients, class boundary discrimination, visual evidence and overall visual food understanding. Under a 3-fold cross-validation scheme, OliveGemma achieves a top-1 accuracy of 92.96% +/- 0.91%, exceeding the strongest CNN baseline (DenseNet-121) by 7.31% and outperforming zero-shot frontier models with exact instructions and bounded classes including Gemini Flash 3 and 3.5, GPT-5.4 Mini, and Claude Haiku 4.6 by 8%, 46%, and 64% respectively. Furthermore, OliveGemma demonstrates competitive performance on Top-3 and Top-5 accuracy, being second best across CNNs and frontier models, surpassed only by DenseNet-121. In addition, OliveGemma achieves 90.79% +/- 1.3% Exact-Set on the likely ingredients of the food categories. These results demonstrate that PEFT adaptation of a small VLM can surpass substantially larger proprietary models on specialised food recognition. The model is publicly available at https://huggingface.co/JamesZar/OliveGemma-3B and the experiments and results can be found at https://github.com/tsiokris/OliveGemma.

</details>


### [73] [SLAMFormer-$\infty$: Infinite SLAM Transformer for Unbounded Frontend and Backend Processing](https://arxiv.org/abs/2608.03429)
*Zhijian Fang,Weicheng Zheng,Yijun Yuan,Weibang Wang,Zhuoguang Chen,Chang Sun,Junhao Huang,Kenan Li,Minghui Qin,Hang Zhao*

Main category: cs.CV

TL;DR: SLAMFormer-∞ is the first geometric transformer for SLAM that supports long-range frontend and backend processing without explicit distance bounds, using memory conditions instead of a first-frame-anchored formulation. It generalizes to trajectories exceeding 17km with superior performance in trajectory estimation and scene reconstruction.


<details>
  <summary>Details</summary>
Motivation: Existing geometric transformers for SLAM rely on first-frame-anchored formulations that limit their ability to handle long-range sequences, as explicit distance bounds constrain the scope of processing.

Method: SLAMFormer-∞ uses memory conditions to define flexible coordinate systems and scales for input frames, enabling more expressive structural conditioning. The frontend preserves efficient local computation while the backend jointly optimizes long-range trajectories and scene geometry in a globally consistent manner.

Result: SLAMFormer-∞ achieves superior or highly competitive performance in both trajectory estimation and scene reconstruction across large-scale datasets, and successfully operates on sequences exceeding 17km.

Conclusion: The memory-conditioned design removes explicit distance bounds, enabling scalable long-range SLAM without sacrificing local efficiency or global consistency.

Abstract: We introduce the Infinite SLAM Transformer (SLAMFormer-$\infty$), the first geometric transformer capable of supporting both long-range frontend and backend processing without an explicit distance bound. Instead of relying on a first-frame-anchored formulation, SLAMFormer-$\infty$ employs memory conditions to define flexible coordinate systems and scales for input frames, enabling more expressive structural conditioning. Built upon this formulation, the frontend preserves efficient local computation, while the backend jointly optimizes long-range trajectories and scene geometry in a globally consistent manner. Experimental results demonstrate that SLAMFormer-$\infty$ achieves superior or highly competitive performance in both trajectory estimation and scene reconstruction across large-scale datasets. Notably, SLAMFormer-$\infty$ generalizes to extremely long trajectories, successfully operating on sequences exceeding $17\mathrm{km}$.

</details>


### [74] [Dual-domain U-Nets with embedded back projection operators for motion-resolved 4D CBCT reconstruction](https://arxiv.org/abs/2608.03430)
*Ivo Herzig,Pascal Paysan,Daniel Barco,Marc André Stadelmann,Frank-Peter Schilling,Igor Peterlik,Michal Walczak,Lijin Aryananda,Woo Sang Ahn,Rudolf Marcel Füchslin,Lukas Lichtensteiger*

Main category: cs.CV

TL;DR: The paper proposes a deep learning method for 4D cone beam CT (CBCT) reconstruction from conventional free-breathing scans without needing a respiratory signal or projection binning. A CNN extends U-Net with back-projection skip connections to predict a static volume at maximum inhalation plus ten displacement vector fields spanning a breathing cycle, achieving expert-preferred tumor visibility and comparable image quality to traditional SART-TV while adding 4D motion modeling capability.


<details>
  <summary>Details</summary>
Motivation: 4D CBCT is important for image-guided radiation therapy of thoracic cancers, but its clinical use is limited by long scan times that cause high patient dose and motion/sparse-sampling artifacts. Existing approaches require respiratory signals or explicit projection binning, limiting practicality.

Method: A non-patient-specific CNN extends U-Net architecture: the encoder processes filtered projection stacks, the decoder operates in the volume domain, and skip connections are replaced with non-trainable back-projection functions at multiple resolutions to transfer features between domains. The network predicts a static volume at maximum inhalation plus ten DVFs spanning a breathing cycle. Training uses simulated CBCT scans.

Result: On 11 simulated patients and 13 clinical free-breathing scans, the method matched SART-TV image quality (mean RMSE: -1.19 HU, PSNR: +0.09 dB, SSIM: -0.009) while enabling 4D reconstruction. Clinical experts preferred the method for tumor visibility (59% vs. 36% no preference) and esophagus visibility (47% vs. 42%). The method also showed sharper dynamic structures and fewer motion streak artifacts than traditional reconstruction.

Conclusion: The proposed CNN can predict static volumes and full 4D respiratory motion models from a single free-breathing scan without respiratory surrogates or projection binning, reducing motion artifacts while adding motion-modeling capability for 4D CBCT in image-guided radiation therapy.

Abstract: Four-dimensional cone beam CT (4D CBCT) is important for image-guided radiation therapy of thoracic cancers, but its use is limited by long scan times, causing high patient dose and motion/sparse-sampling artifacts.
  We propose a deep learning method for motion-resolved 4D CBCT reconstruction from conventional free-breathing scans, without a respiratory signal or explicit projection binning. Our CNN takes free-breathing 3D CBCT projections as input and predicts a static volume at maximum inhalation plus ten displacement vector fields (DVFs) spanning a breathing cycle.
  The network extends U-Net: the encoder acts on filtered projection stacks, the decoder acts in the volume domain, and skip connections are replaced with non-trainable back-projection functions at multiple resolutions to transfer features between domains. The model is trained on simulated CBCT scans and evaluated on 11 unseen simulated patients and 13 clinical free-breathing scans. Two additional models (60 s and 6 s scans) were evaluated by clinical experts on three and two scans, comparing single phases of our 4D reconstruction to reference 3D SART-TV images for tumor and esophagus visibility.
  Experts preferred our method for tumor visibility (59% vs. 36% no preference, 5% reference) and esophagus visibility (47% vs. 42%, 11%). On simulated data, image quality matched SART-TV (mean RMSE: -1.19 HU, PSNR: +0.09 dB, SSIM: -0.009) while enabling 4D reconstruction. On clinical scans, our method showed sharper dynamic structures (e.g., diaphragm) and fewer motion streak artifacts than traditional reconstruction.
  This non-patient-specific CNN predicts static volumes and full 4D respiratory motion models from a single free-breathing scan, without a respiratory surrogate or projection binning, reducing motion artifacts while adding motion-modeling capability.

</details>


### [75] [Hi-Token: Hierarchical Coordinate Tokenization for Generative Visual Grounding](https://arxiv.org/abs/2608.03471)
*Xiuyuan Zhu,Ke Lu,Kun Dong,Siwen Jiao,Hao Wu,Zijin Du,Shun Mao,Dongming Zhang,Jian Xue*

Main category: cs.CV

TL;DR: The paper proposes Hi-Token, an axis-specific structured coordinate representation for bounding boxes in generative VLMs, and Hi-GAR, a geometry-based reward for GRPO training, to improve visual grounding accuracy. Experiments across three VLM backbones and RefCOCO benchmarks show consistent gains.


<details>
  <summary>Details</summary>
Motivation: Current generative VLMs treat bounding-box coordinates as independent output symbols, ignoring numerical order and axis semantics. This implicit representation is identified as a significant source of error in visual grounding tasks.

Method: Hi-Token encodes each coordinate with axis-specific tokens for hundreds, tens, and ones digits, adding coarse-to-fine structure and increasing token reuse while keeping the existing VLM architecture unchanged. Hi-GAR complements this with a geometry-based reward for Group Relative Policy Optimization (GRPO), using box overlap and coordinate accuracy at multiple scales during training only.

Result: Controlled comparisons show Hi-Token improves localization across the evaluated IoU range, and Hi-GAR further reduces low-overlap predictions. Consistent gains are achieved across three VLM backbones and the RefCOCO family, with Hi-R1 outperforming strong specialist baselines on most metrics.

Conclusion: Structured coordinate generation provides an effective approach to generative visual grounding, as evidenced by analyses of token frequency, digit boundaries, object scale, and IoU distributions explaining the improvements.

Abstract: Generative Vision-Language Models (VLMs) commonly treat bounding-box coordinates as independent output symbols, leaving numerical order and axis semantics implicit. We identify this representation as an important source of error in visual grounding. Hi-Token encodes each coordinate with axis-specific tokens for the hundreds, tens, and ones digits, which adds coarse-to-fine structure and increases token reuse while retaining the existing VLM architecture. Hi-GAR complements this representation with a geometry-based reward for Group Relative Policy Optimization (GRPO), using box overlap and coordinate accuracy at multiple scales. Controlled comparisons under matched training conditions show that Hi-Token improves localization throughout the evaluated IoU range. Hi-GAR further reduces low-overlap predictions and is used only during training. Experiments on three VLM backbones and the RefCOCO family show consistent gains across models and benchmarks. Hi-R1 achieves higher values than strong specialist baselines on most reported metrics. Analyses of token frequency, digit boundaries, object scale, and IoU distributions explain the effects of coordinate representation and reward training. The results show that structured coordinate generation provides an effective approach to generative visual grounding.

</details>


### [76] [MT-Web2Code: Benchmarking Coding Agents on Multi-Turn Regional Reconstruction and Localized Modification](https://arxiv.org/abs/2608.03474)
*Qiming Li,Shujie Hu,Haohan Liu,Xiaocheng Feng,Songxiang Liu,Guanglu Wan*

Main category: cs.CV

TL;DR: MT-Web2Code is the first multimodal coding benchmark for multi-turn web UI generation, covering macro-level regional reconstruction and micro-level localized modification across 102 tasks in 16 domains, using a Reverse-Corruption Trajectory Engine for scalable data construction and a dual-axis evaluation protocol.


<details>
  <summary>Details</summary>
Motivation: Existing LVLM benchmarks for web UI generation focus only on single-turn full-page generation from scratch, ignoring the real-world iterative workflow where developers repeatedly fix and modify existing codebases. This gap motivates a benchmark that captures multi-turn, localized editing scenarios.

Method: The benchmark contains 102 tasks spanning 16 vertical domains with multi-turn macro-level regional reconstruction and micro-level localized modification. A scalable Reverse-Corruption Trajectory Engine iteratively injects structural and stylistic defects into golden pages to construct deterministic repair trajectories without costly turn-level annotation. A dual-axis evaluation protocol measures target-region fidelity and preservation of unaffected content, using a 5-dimensional VLM-based rubric for regional reconstruction and deterministic pixel-grounded alignment for localized modification.

Result: Experiments on 13 frontier coding agents reveal they struggle to faithfully reconstruct target regions while preserving unaffected content, lack fine-grained visual-code alignment for localized edits, and suffer from error snowballing over multiple turns.

Conclusion: Beyond benchmarking, the deterministic evaluation metrics provide fine-grained feedback signals that may facilitate future research on training iterative UI coding agents. Code and data will be released.

Abstract: Recent advances in Large Vision-Language Models (LVLMs) have demonstrated impressive capabilities in web UI generation. However, existing benchmarks predominantly focus on single-turn full-page generation from scratch, overlooking the iterative workflow of real-world frontend engineering, where developers repeatedly reconstruct missing regions and modify localized elements within existing codebases. To bridge this gap, we introduce MT-Web2Code, the first multimodal coding benchmark for multi-turn Macro-Level Regional Reconstruction and Micro-Level Localized Modification, which contains 102 tasks spanning 16 vertical domains. To construct deterministic repair trajectories without costly turn-level human annotation, we develop a scalable Reverse-Corruption Trajectory Engine that iteratively injects structural and stylistic defects into golden pages. We further propose a dual-axis evaluation protocol that measures target-region fidelity and the preservation of unaffected content, where regional reconstruction is assessed by a 5-dimensional VLM-based rubric and localized modification by deterministic pixel-grounded alignment. Experiments on 13 frontier coding agents reveal that current agents struggle to faithfully reconstruct target regions while preserving unaffected content, lack fine-grained visual-code alignment for localized edits, and suffer from error snowballing over multiple turns. Beyond benchmarking, our deterministic evaluation metrics provide fine-grained feedback signals that may facilitate future research on training iterative UI coding agents. Our evaluation code and data will soon be released.

</details>


### [77] [From Multi-Resolution Cells to Gigapixel Whole Slide Images Foundation Model for Computational Pathology](https://arxiv.org/abs/2608.03508)
*Basit Alawode,Moshira Ali Abdalla,Dwarikanath Mahapatra,Muhammad Muzammal Naseer,Sajid Javed*

Main category: cs.CV

TL;DR: MRPT is a Multi-Resolution Pyramid Transformer for computational pathology that hierarchically aggregates multi-resolution information from cellular to tissue to WSI levels using Consecutive Cross-Resolution Attention (CCRA), pre-trained on 624M patches, 2.4M regions, and 36K WSIs, outperforming existing foundation models and MLLMs across 34 datasets in classification, phenotyping, and VQA tasks.


<details>
  <summary>Details</summary>
Motivation: Existing ViTs in computational pathology are pre-trained on single-resolution WSIs, limiting their generalization across arbitrary resolutions. Since gigapixel WSIs contain diagnostic patterns at multiple scales (cellular morphology, tissue architecture, global context) — mirroring how pathologists examine slides — a multi-resolution approach is needed for robust and generalizable representations.

Method: MRPT hierarchically aggregates multi-resolution information from cellular to tissue and WSI levels. It uses a biologically meaningful Consecutive Cross-Resolution Attention (CCRA) mechanism to capture scale-independent interactions and enforces multi-resolution semantic consistency by aligning embeddings across resolutions. The model is pre-trained in a multi-resolution self-supervised manner on 624M patches, 2.4M regions, and 36K WSIs.

Result: On 34 diverse datasets, MRPT surpasses recent foundation models and Multimodal Large Language Models (MLLMs) in cancer subtype classification, tissue phenotyping, and Visual Question Answering (VQA) for WSI understanding.

Conclusion: MRPT demonstrates that multi-resolution self-supervised pre-training with explicit cross-resolution attention and semantic consistency alignment yields rich coarse-to-fine histopathology features that generalize better than single-resolution pre-trained models across diverse computational pathology tasks.

Abstract: Vision Transformers (ViTs) and their hierarchical variants have achieved strong performance in Computational Pathology (CPath). However, most are pre-trained on single-resolution Whole Slide Images (WSIs), limiting their generalization across arbitrary resolutions. Gigapixel WSIs inherently contain diagnostic patterns at multiple scales, including cellular morphologies, tissue architectures, and global context, mirroring how expert pathologists examine WSIs. We introduce Multi-Resolution Pyramid Transformer (MRPT), a model that hierarchically aggregates multi-resolution information from cellular to tissue and WSI levels. MRPT employs a biologically meaningful Consecutive Cross-Resolution Attention (CCRA) mechanism to capture scale-independent interactions and enforces multi-resolution semantic consistency by aligning embeddings across resolutions, yielding robust and generalizable WSI representations. Pre-trained in a multi-resolution self-supervised manner on 624M patches, 2.4M regions, and 36K WSIs, MRPT learns rich coarse-to-fine histopathology features. Extensive experiments on 34 diverse datasets show that MRPT surpasses recent foundation models and Multimodal Large Language Models (MLLMs) in cancer subtype classification, tissue phenotyping, and Visual Question Answering (VQA) for WSI understanding.

</details>


### [78] [How Many Labels Are Enough? ALDA: Active Learning Deployment Advisor for Medical Image Classification](https://arxiv.org/abs/2608.03511)
*Julia Machnio,Mads Nielsen,Mostafa Mehdipour Ghazi*

Main category: cs.CV

TL;DR: ALDA is a deployment-oriented framework that helps select the best active learning strategy for medical imaging by fitting parametric learning-curve models to candidate strategies during a pilot phase, predicting annotation costs to reach clinical targets, and using a risk-aware rule that prefers robust strategies with narrow deployment windows.


<details>
  <summary>Details</summary>
Motivation: Active learning promises to reduce annotation costs in medical imaging, but choosing the wrong sampling strategy before knowing the full annotation budget can actually increase costs. Practitioners need a principled way to select among AL strategies under clinical performance constraints.

Method: ALDA fits a parametric learning-curve model to each candidate AL strategy during a short pilot phase, estimates whether each strategy can reach a required clinical performance target, and predicts the number of expert annotations needed. It introduces a deployment window quantifying cost sensitivity to uncertainty in the clinical threshold, and selects among near-optimal strategies the one with the narrowest deployment window (most robust to threshold changes).

Result: Experiments on four medical imaging classification domains show ALDA predicts the deployment-optimal method from a pilot of only 15-30% of the intended budget and reduces annotation costs by up to 82% compared to a poor strategy choice.

Conclusion: Rather than introducing a new sampling heuristic, ALDA provides a practical decision layer that answers the deployment-critical question of how many labels are enough, making AL adoption safer and more cost-effective in clinical settings.

Abstract: Active learning (AL) promises to reduce the cost of medical imaging projects by lowering the number of clinical labels required. However, practical deployment requires committing to a sampling strategy before the full annotation budget is spent, and choosing the wrong strategy can increase rather than decrease costs. We propose Active-Learning Deployment Advisor (ALDA), a deployment-oriented framework for AL method selection under clinical performance constraints. Given a short pilot phase, ALDA fits a parametric learning-curve model to each candidate strategy, estimates whether that strategy is expected to reach a required clinical performance target, and predicts the number of expert annotations needed to do so. In addition to absolute annotation cost, ALDA introduces a deployment window that quantifies the sensitivity of this cost estimate to uncertainty in the clinical threshold. The final recommendation follows a risk-aware rule: among strategies with near-optimal predicted cost, ALDA prefers the strategy with the narrowest deployment window, the most robust to threshold revisions. Experiments on four medical imaging classification domains show that ALDA predicts the deployment-optimal method from a pilot of 15-30% of the intended budget and reduces annotation costs by up to 82% compared with a poor strategy choice. Rather than introducing a new sampling heuristic, ALDA provides a practical decision layer that answers a deployment-critical question: how many labels are enough?

</details>


### [79] [Detecting Pose Estimation Failures via Keypoint Self-Consistency](https://arxiv.org/abs/2608.03516)
*Robin Chan*

Main category: cs.CV

TL;DR: This paper proposes a simple yet effective method for detecting pose estimation failures by analyzing geometric self-consistency between 2D keypoint predictions, using hand-crafted features such as pairwise distances, reprojection consistency, and render/mask consistency, which outperforms confidence-based approaches.


<details>
  <summary>Details</summary>
Motivation: Keypoint-based pose estimation methods often predict keypoints independently, neglecting the geometric property that rotations preserve object shapes. This leads to inaccurate pose estimates that limit reliability in downstream tasks, and there is a need to detect these failures.

Method: The proposed approach extracts hand-crafted geometric features that capture the self-consistency of keypoint predictions, including pairwise distances, reprojection consistency, and render/mask consistency. These features are fed into a logistic regression classifier to detect pose estimation failures.

Result: A logistic regression classifier trained on the proposed geometric features reliably detects pose estimation failures and outperforms confidence-based approaches such as conformal keypoint predictions that rely solely on keypoint uncertainty.

Conclusion: Simple geometric self-consistency features between 2D keypoints are effective for detecting pose estimation failures, offering a lightweight and reliable alternative to uncertainty-based methods.

Abstract: One common approach to pose estimation involves predicting object keypoints in an image, followed by using Perspective-n-Point algorithms to compute the object's rotation and translation relative to the camera. While rotations preserve object shapes, this property is often neglected in keypoint-based pose estimation methods, where keypoints are typically predicted independently from each other. As imprecise keypoint predictions negatively affects pose estimation accuracy, it also limits its reliability in downstream tasks. In this work, we explore whether such inaccurate pose estimates can be identified by simply examining spatial locations between 2D keypoints. We propose a set of hand-crafted geometric features that capture the self-consistency of keypoint predictions, including pairwise distances, reprojection consistency, as well as render and mask consistency. Despite its simplicity, a logistic regression classifier trained on these features reliably detects pose estimation failures, outperforming confidence-based approaches like conformal keypoint predictions that rely solely on keypoint uncertainty.

</details>


### [80] [GVCCTurbo: Rate-Compute Quality Scheduling for Codebook Driven Generative Compression](https://arxiv.org/abs/2608.03517)
*Ziyue Zeng,Dingjie Peng,Xun Su,Hiroshi Watanabe*

Main category: cs.CV

TL;DR: GVCCTurbo proposes a BPP-driven scheduler for generative compression that decouples expensive prior refreshes from codebook corrections, allowing bitrate targets to control compute allocation and achieving ~44% decoding-time reduction at a small quality cost in the ultra-low-bitrate regime.


<details>
  <summary>Details</summary>
Motivation: Current generative codecs couple each finite-rate correction with a fresh prior evaluation, so reducing sampler length also removes correction slots carrying target-dependent information—making bitrate an inflexible consequence rather than a controllable schedule input.

Method: GVCCTurbo introduces a BPP-driven scheduler that calibrates an atom-count operating point and skip-gap ratio once per protocol, then maps a target codebook-payload bitrate to a trajectory length and refresh period. It supports both GVCC-style rectified-flow video and DDCM-style diffusion image compression via the same endpoint-prediction and finite-rate steering interface.

Result: In a controlled 720p Wan-GVCC study, prior evaluations drop from 20 to 9, yielding ~44% measured decoding-time reduction across the schedule family with a small LPIPS cost on high-motion content. The BPP-aware interior point uses 2.9% fewer bits while achieving consistently higher PSNR at comparable LPIPS. Native 1080p curves position the full zero-shot codec in the ultra-low-bitrate regime.

Conclusion: BPP-to-compute scheduling offers a controllable extension of sampler-length tuning without requiring the allocated point to dominate every boundary point, supporting zero-training deployment and compatibility with future distilled priors.

Abstract: Codebook-driven generative compression uses a pretrained image or video generator as a zero-shot visual prior and transmits compact codebook indices to guide reconstruction at ultra-low bitrate. Current codecs tie each finite-rate correction to a fresh prior evaluation, so shortening the sampler also removes correction slots that carry target-dependent information. We propose GVCCTurbo, a BPP-driven scheduler that separates expensive prior refreshes from codebook corrections: after calibrating an atom-count operating point and skip-gap ratio once per protocol, it maps a target codebook-payload bitrate to a trajectory length and refresh period, making BPP a schedule input instead of a fixed consequence of sampler length. The same endpoint-prediction and finite-rate steering interface covers GVCC-style rectified-flow video and DDCM-style diffusion image compression, preserving zero-training deployment and compatibility with future distilled priors. Native 1080p curves position the complete zero-shot codec in the ultra-low-bitrate regime. In a controlled 720p Wan-GVCC study, the scheduler cuts prior evaluations from 20 to 9 for a $\sim\!44\%$ measured decoding-time reduction shared across the whole schedule family, at a small shared LPIPS cost on high-motion content; within that family, uniform refresh thinning (pure-skip) is a boundary point, and the BPP-aware interior point trades $2.9\%$ fewer codebook-payload bits for consistently higher PSNR at comparable LPIPS. These results support BPP-to-compute scheduling as a controllable extension of sampler-length tuning, without requiring the allocated point to dominate every boundary point.

</details>


### [81] [MinerU.Chem: A High-Precision System for Optical Chemical Structure and Reaction Recognition](https://arxiv.org/abs/2608.03525)
*Haote Yang,Jiang Wu,Jingchao Wang,Xingjian Wei,Lixin Ma,Linye Li,Chen Zhu,Xiaolong Wu,Yuheng Lu,Ziran Zhu,Junyuan Gao,Lingli Ge,Yuan Xu,Huijie Ao,QianQian Wu,Dechen Lin,Huaiyu Gu,Lu Chen,Shengxin Lu,ShaSha Wang,Yuanyuan Cao,Zhejia Yu,Ruijie Zhang,Zimai Tian,Jiaxing Sun,Yinfan Wang,Jiahe Song,Chuang Wang,Yubin Wang,Rui Nie,Hao Zheng,Bowen Jiang,Hongbin Lai,Yifan He,Chengjin Liu,Tingting Zhang,Liqun Wei,Lijun Wu,Bin Wang,Yuqiang Li,Guangyu Wang,Wei Li,Bowen Zhou,Dahua Lin,Conghui He*

Main category: cs.CV

TL;DR: MinerU.Chem is a document parsing system for organic chemistry literature that converts molecular structures, reaction schemes, and experimental conditions from PDFs/figures into machine-readable data (Molecule Summary Lists and Reaction Summary Lists), using CARBON as its core molecular structure representation and achieving 93.02% SMILES exact-match accuracy, outperforming GPT-5.6-Sol by 18.15 percentage points.


<details>
  <summary>Details</summary>
Motivation: General-purpose document parsing systems cannot reliably convert chemistry-specific content (molecular structures, reaction diagrams, complex tables) into machine-readable data, which limits data production for chemistry knowledge bases and AI-for-Chemistry tasks such as reaction prediction, retrosynthesis, and drug design.

Method: MinerU.Chem is built on MinerU's general document parsing pipeline with five added chemistry-specific modules: (1) chemistry relevance filtering, (2) molecular structure detection, (3) molecule identifier extraction, (4) molecular structure recognition using CARBON as core representation (preserving visual layout and chemical semantics, supporting MolFile and SMILES export), and (5) reaction scheme parsing. These modules convert chemistry-related image regions into Molecule Summary Lists and Reaction Summary Lists.

Result: On the SMILES-evaluable subset of MolRecBench-Wild (N=2,392), MinerU.Chem achieves 93.02% SMILES exact-match accuracy for molecular structure recognition, outperforming the best comparison system (GPT-5.6-Sol at 74.87%) by 18.15 percentage points.

Conclusion: MinerU.Chem provides an effective chemistry-specific document parsing solution integrated into the MinerU online platform, significantly improving molecular structure recognition accuracy compared to general-purpose systems and enabling downstream AI-for-Chemistry applications.

Abstract: In organic chemistry papers and patents, molecular structures, reaction schemes, and experimental conditions are often presented as molecular structure depictions, reaction diagrams, and complex tables or figures. Such information is difficult for general-purpose document parsing systems to directly convert into machine-readable data. This limits data production for organic chemistry knowledge base construction and for AI for Chemistry tasks such as reaction prediction, retrosynthesis, condition recommendation, molecular property prediction, and drug molecule design. This report introduces MinerU.Chem, a document parsing system for organic chemistry literature integrated into the MinerU online platform. Built on top of MinerU's general document parsing pipeline, MinerU.Chem adds five chemistry-specific modules: chemistry relevance filtering, molecular structure detection, molecule identifier extraction, molecular structure recognition, and reaction scheme parsing. Together, these modules convert organic-chemistry-related image regions in documents into a Molecule Summary List and a Reaction Summary List. For molecular structure recognition, MinerU.Chem uses CARBON (Complex Atomic Representation and Bonding Object Notation) as its core representation. CARBON enables recognition results to preserve both the visual layout of the original image and complex chemical semantics, while supporting the export of standard downstream formats such as MolFile and SMILES. On the SMILES-evaluable subset of MolRecBench-Wild (N=2,392), MinerU.Chem's molecular structure recognition module achieves a SMILES exact-match accuracy of 93.02%, outperforming the best evaluated comparison system, GPT-5.6-Sol (74.87%), by 18.15 percentage points. The system has been integrated into the MinerU online platform and is available at https://mineru.net/OpenSourceTools/Extractor .

</details>


### [82] [IRIS: Visual-Semantic Binding for Forgery-Resistant Watermarking of Diffusion Images](https://arxiv.org/abs/2608.03539)
*Xiaoyan Feng,Zheng Gao,Tong Guan,Rui Bao,Bokang Zeng,Xiaoyu Li,Jiaojiao Jiang*

Main category: cs.CV

TL;DR: IRIS is a training-free in-generation diffusion watermarking scheme that embeds an intrinsic ring identifier derived from the generated image's own semantics, binding the watermark to visual content so it cannot be transplanted onto foreign images while remaining robust to common distortions and regeneration.


<details>
  <summary>Details</summary>
Motivation: Existing in-generation diffusion watermarks either embed patterns independent of the image content, allowing attackers to transplant marks onto non-generated images (forgery), or bind to proxy images rather than the image they actually mark. There is a need for watermarks that genuinely bind to visual semantics of the target image.

Method: IRIS reads a content code from the non-watermarked generated image, derives a one-time ring from this code and a secret key, then returns to the final low-noise steps of the same sampling trajectory to blend the ring in after the semantics are settled. It uses a shared canonicalization between embedding and detection to achieve sensitivity balance—robust to common distortions and mild regeneration but flipping under semantic change. Detection recomputes the ring from the query image and key alone.

Result: On three prompt datasets, IRIS detects reliably while staying close to its same-seed non-watermarked counterpart in fidelity, surpassing in-generation marks. It withstands both forgery (transplanted fixed-pattern marks) and regeneration attacks that strip post-hoc marks, outperforming compared watermarking schemes.

Conclusion: IRIS achieves semantic-bound watermarking with high fidelity, robustness to common processing, and resilience against both transplantation forgeries and regeneration attacks, addressing a key weakness of existing in-generation and post-hoc watermarking approaches.

Abstract: Most in-generation diffusion watermarks embed patterns independent of the image that carries them, and attackers transplant the marks onto images the generator did not produce, resulting in forgery. Binding the mark to visual semantics prevents such transplantation, yet existing bindings anchor to a proxy image rather than the image they mark. Realizing visual-semantic binding inside generation faces two challenges. The mark derives from the image itself yet enters the sampling trajectory before that image exists, and may itself shift the semantics it binds. The binding also meets opposite sensitivity demands, breaking under semantic change while holding through common processing. We present IRIS, a training-free watermarking scheme that embeds an Intrinsic Ring Identifier from Semantics. IRIS reads a content code from the non-watermarked generated image, derives a one-time ring from the code and a secret key, returns to the final low-noise steps of the same trajectory and blends the ring in, after the semantics it binds are settled. To meet the opposite sensitivity demands, the code is read through a canonicalization shared between embedding and detection, holding through common distortions and mild regeneration while flipping under semantic change. Detection recomputes the ring from the query image and the key alone, and the mark therefore fails on a foreign or spliced image, with acceptance tracking semantic displacement. On three prompt datasets IRIS detects reliably and stays close to its same-seed non-watermarked counterpart, a fidelity prior in-generation marks do not reach. While forgeries transfer fixed-pattern marks and regeneration strips post-hoc marks, IRIS alone among the compared marks withstands both.

</details>


### [83] [S$^3$-Diff: Structural Semantic Synergy Diffusion Model for High Fidelity Super Resolution of Pathological Images](https://arxiv.org/abs/2608.03540)
*Jiaming Liang,QiHui Han,Guangye Ou,Jiawen Liu,Haolin Chen,Xi Zhong,Jiazhou Chen,Xiaoqi Sheng,Hongmin Cai*

Main category: cs.CV

TL;DR: S3-Diff is a diffusion-based framework for high-fidelity super-resolution of pathological images, using a Specimen-aware Structural Anchoring module with SAM for tissue morphology preservation and Structure-guided Semantic Fidelity Tuning with DINOv3 to guide denoising, outperforming SOTA methods in reconstruction quality and downstream survival analysis.


<details>
  <summary>Details</summary>
Motivation: Low-resolution pathology images are common in clinical workflows due to device, storage, and transmission limitations, yet current super-resolution techniques over-smooth diagnostically relevant morphology and cause semantic drift that harms downstream clinical interpretation.

Method: S3-Diff employs two key components: (1) Specimen-aware Structural Anchoring (SSA) combines SAM-extracted prognosis-aware tissue support with LR-HR gradient discrepancies to generate specimen-specific structural anchors preserving pathological morphology; (2) Structure-guided Semantic Fidelity Tuning (SSFT) adapts DINOv3 representations with SSA-derived structural supervision, combining adapted semantic energy with LR-derived edge and grayscale cues to guide denoising and suppress stochastic artifacts while maintaining structural consistency.

Result: Extensive experiments show S3-Diff consistently outperforms state-of-the-art methods in both reconstruction quality and downstream survival analysis performance.

Conclusion: The structural-semantic synergy approach effectively addresses the over-smoothing and semantic drift problems in pathology image super-resolution, and the source code will be made public for the research community.

Abstract: Digital pathology relies on high-resolution whole slide images for accurate diagnosis, yet limitations in imaging devices, storage, and transmission often make lower-resolution pathology images more common in clinical workflows. Current super-resolution techniques often tend to smooth diagnostically relevant morphology, leading to over-smoothed textures and semantic drift that compromise downstream clinical interpretation. To this end, we develop the Structural Semantic Synergy Diffusion Model (S3-Diff), a diffusion framework for high-fidelity super-resolution of pathological images. The core of S3-Diff is Specimen-aware Structural Anchoring (SSA), which combines prognosis-aware tissue support extracted by a fixed SAM with LR-HR gradient discrepancies to generate a specimen-specific structural anchor to preserve pathological morphology. Concurrently, we introduce Structure-guided Semantic Fidelity Tuning (SSFT) to adapt DINOv3 representations using SSA-derived structural supervision. SSFT combines the adapted semantic energy with LR-derived edge and grayscale cues. The resulting control guides denoising to suppress stochastic artifacts and maintain structural consistency. Extensive experimental results demonstrate that S3-Diff consistently outperforms state-of-the-art methods in both reconstruction quality and downstream survival analysis performance. The source code will be made public.

</details>


### [84] [Test-Time Augmentation for Tabular-to-Image Classifiers under Distribution Shifts](https://arxiv.org/abs/2608.03557)
*Malena Loza,Felipe Grijalva,Eva Milara,Luis Bote-Curiel,Francisco J. Lara-Abelenda,David Chushig-Muzo*

Main category: cs.CV

TL;DR: This paper evaluates Test-Time Augmentation (TTA) techniques on tabular-to-image encoded representations under Out-Of-Distribution (OOD) conditions. Using six encoding methods, twenty-five TTA techniques, and two TableShift benchmark datasets, the authors find that composite and photometric TTA strategies improve OOD robustness, while frequency-domain transformations degrade performance due to disruption of feature-to-intensity mappings.


<details>
  <summary>Details</summary>
Motivation: Tabular-to-image conversion methods leverage deep learning for tabular data, but their robustness under distribution shifts is largely unexplored. The authors aim to fill this gap by investigating whether TTA, a well-established robustness technique in image classification, can improve OOD generalization for these image representations of tabular data.

Method: Six tabular-to-image encoders (TINTO, IGTD, DeepInsight, BIE, DistanceMatrix, Fotomics) were combined with twenty-five TTA techniques categorized into six types (Geometric, Photometric, Structural, Frequency/Encoding, Mixup, Composite). Models were evaluated on HELOC and Voting datasets from the TableShift benchmark, which contain paired in-distribution and OOD test subsets.

Result: TTA improved OOD classification performance overall. Composite and photometric augmentation strategies offered the best robustness-to-variance trade-off. Frequency/encoding transformations that altered the encoder's feature-to-intensity mapping consistently hurt performance.

Conclusion: TTA is a promising, low-cost approach for improving robustness and generalization of classifiers trained on tabular-derived image representations under distribution shifts, with strategy selection (composite/photometric over frequency-based) being critical for success.

Abstract: Tabular-to-image methods that convert tabular data into visual representations have emerged as a novel paradigm for leveraging the high performance of deep learning models. Despite their advantages, the robustness of these methods under distribution shifts remains under explored. Test-Time Augmentation (TTA) is an effective approach in image classification to improve model generalization and robustness, where predictions over multiple transformed views of each input are aggregated. This work evaluates the impact of TTA techniques on predictive performance under Out-Of-Distribution (OOD) for representations generated by tabular-to-image methods. Six tabular-to-image encoding methods were considered: TINTO, IGTD, DeepInsight, BIE, DistanceMatrix, Fotomics. Twenty-five TTA techniques were used, organized into six types: Geometric, Photometric, Structural, Frequency/Encoding, Mixup, and Composite. We employed two datasets from the TableShift benchmark (HELOC and Voting) that provide in-distribution and OOD test subsets designed to evaluate the effect of distribution shifts on tabular data. The results indicate that TTA improves OOD performance, with composite and photometric strategies providing the best trade-off between robustness and variance. In contrast, frequency-domain transformations that alter the encoder's feature-to-intensity mapping consistently degrade performance. These findings highlight TTA as a promising approach for improving the robustness and generalization of classifiers trained on image representations derived from tabular data, particularly under distribution shifts.

</details>


### [85] [Compass: Degradation-Simulated Reciprocal Learning with Lightweight Needle RWKV for Multimodal Crack Segmentation under Missing Modalities](https://arxiv.org/abs/2608.03559)
*Hui Liu,Chen Jia,Fan Shi,Xu Cheng,Mianzhao Wang,Shengyong Chen*

Main category: cs.CV

TL;DR: Compass is a lightweight network (2.58M parameters) for robust crack segmentation under arbitrary missing modalities, achieving SOTA performance via Degradation Simulation Distillation, a Needle backbone, and Evidential Topology-Preserving Fusion. It maintains high F1 (0.8216) and mIoU (0.8434) even with 90% depth modality missing on CrackDepth.


<details>
  <summary>Details</summary>
Motivation: Existing multimodal crack segmentation methods fail to handle missing modalities, which causes semantic degradation in pixel-level performance. There is a need for a lightweight approach that maintains robustness under incomplete modality conditions without high computational cost.

Method: Compass consists of three components: (1) Degradation Simulation Distillation (DSD) which mimics severe missing conditions and performs reciprocal distillation to decouple complete perception from degradation adaptation, including Feature-Aware Prototype Transmitter (FAPT) for modality-agnostic prototype-guided feature completion; (2) Needle Block as a lightweight backbone that injects crack-direction cues into WKV modulation with connectivity-aware gating and anisotropic context probing; (3) Evidential Topology-Preserving Fusion (ETPF) using Dempster-Shafer evidential combination with uncertainty-gated decoding to preserve crack topology while suppressing unreliable features.

Result: Experiments on three datasets demonstrate state-of-the-art performance across diverse missing modality scenarios. With 90% depth modality missing on CrackDepth, Compass achieves F1 of 0.8216 and mIoU of 0.8434 using only 2.58M parameters.

Conclusion: Compass effectively addresses semantic degradation from missing modalities in multimodal crack segmentation while maintaining a lightweight architecture and low computational cost. Code is publicly available.

Abstract: In multimodal crack segmentation for industrial facilities, the key challenge is preventing missing modalities from degrading pixel-level performance while maintaining low computational cost. Existing methods struggle to address semantic degradation caused by missing modalities. We propose Compass, a lightweight network for robust crack segmentation under arbitrary missing modalities. Compass comprises Degradation Simulation Distillation (DSD), Needle Block, and Evidential Topology-Preserving Fusion (ETPF). DSD constructs a degradation simulation stream that mimics more severe missing conditions and performs reciprocal distillation with the original stream, decoupling complete perception from degradation adaptation. Within DSD, Feature-Aware Prototype Transmitter (FAPT) performs modality agnostic prototype-guided feature completion to maintain semantic integrity under incomplete modality conditions. As a lightweight backbone, Needle injects crack-direction cues into WKV modulation and combines connectivity-aware gating with anisotropic context probing for structure-aware modeling. ETPF fuses multimodal features via Dempster-Shafer evidential combination with uncertainty-gated decoding, preserving crack topology while suppressing unreliable features. Experiments on three datasets demonstrate state-of-the-art (SOTA) performance under diverse missing modality scenarios. Even with 90\% depth modality missing on CrackDepth, Compass achieves F1 of 0.8216 and mIoU of 0.8434 with only 2.58M parameters. The code is available at https://github.com/Karl1109/Compass.

</details>


### [86] [Beyond Simply Environment Scaling: Designing Effective Environment Distributions for Multimodal Agent Learning](https://arxiv.org/abs/2608.03571)
*Kejian Zhu,Zhuoran Jin,Dongqi Huang,Hongbang Yuan,Yupu Hao,Kang Liu,Jun Zhao*

Main category: cs.CV

TL;DR: The paper investigates multimodal agent training and finds that simply scaling up the number of training environments does not always improve performance. It proposes two methods—Ability-aware Environment Selection (AES) for diversity and Hierarchical Difficulty Curriculum (HDC) for structured difficulty—to construct more effective training environment distributions, with experiments showing both improve multimodal agent training.


<details>
  <summary>Details</summary>
Motivation: Recent work builds large-scale multimodal environment pools for agent training, but simply increasing the number of environments does not always benefit performance. The authors identify limitations in current multimodal environment distributions and study how to build more effective training distributions.

Method: The authors analyze limitations in multimodal environment distributions through experiments and propose two methods: (1) Ability-aware Environment Selection (AES) for selecting diverse environment sets, and (2) Hierarchical Difficulty Curriculum (HDC) that organizes curriculum learning via two difficulty levels—harness weakening and state-scale progression.

Result: Experiments demonstrate that both AES and HDC effectively improve multimodal agent training compared to simply increasing the number of environments.

Conclusion: Effective training environment distributions require both diversity and a structured difficulty progression, rather than merely scaling up the number of environments. The proposed AES and HDC methods provide complementary improvements in these two dimensions.

Abstract: Recent works train agents by constructing large-scale multimodal environment pools. However, we find that simply increasing the number of multimodal environments does not always benefit. We further analyze the limitations in current multimodal environment distributions through a series of experiments. Based on these findings, we study how to build more effective training environment distributions from two dimensions: **diversity** and **difficulty structure**. For diversity, we propose **Ability-aware Environment Selection (AES)** to obtain diverse environment sets. For difficulty structure, we propose **Hierarchical Difficulty Curriculum (HDC)**, which organizes curriculum learning through two difficulty levels: harness weakening and state-scale progression. Experiments show that AES and HDC effectively improve multimodal agent training.

</details>


### [87] [SlimVLM: Sensitivity-aware Dynamic Structured Pruning with Adaptive Visual Token Selection for Efficient Vision-Language Models](https://arxiv.org/abs/2608.03580)
*Yaozhi Wen,Jialong Guo,Zhenliang Ni,Han Shu,Xinghao Chen*

Main category: cs.CV

TL;DR: SlimVLM is a structured pruning framework for compressing Vision-Language Models (VLMs) by adaptively removing redundant visual tokens based on text-to-visual attention scores, and applying sensitivity-aware dynamic pruning ratios per module based on reconstruction error, achieving state-of-the-art performance on multimodal benchmarks.


<details>
  <summary>Details</summary>
Motivation: VLMs have large parameter sizes causing significant computational overhead, limiting deployment on resource-constrained devices. While pruning works well for LLMs, directly applying it to VLMs causes performance drops because redundant visual tokens interfere with importance estimation.

Method: SlimVLM introduces two key components: (1) an adaptive visual token selection strategy that uses average text-to-visual attention scores to assess visual token importance and removes redundant ones during pruning based on a threshold, and (2) a sensitivity-aware dynamic pruning mechanism that determines appropriate pruning ratios per module by calculating linear reconstruction error between pruned and unpruned module outputs.

Result: SlimVLM outperforms existing methods across multiple multimodal benchmarks, achieving state-of-the-art performance.

Conclusion: By addressing the visual token interference problem and adapting pruning ratios to each module's sparsity tolerance, SlimVLM effectively compresses VLMs while preserving task performance, enabling deployment on resource-constrained devices.

Abstract: While Vision-Language Models (VLMs) have demonstrated remarkable performance in processing and understanding both text and images, their large parameter sizes lead to significant computational overhead, limiting their deployment on resource-constrained devices. While pruning has been effective for compressing Large Language Models (LLMs), directly applying it to VLMs leads to significant performance drops, largely due to redundant visual tokens interfering with importance estimation. To this end, we propose SlimVLM, a structured pruning framework designed to compress VLMs while preserving their task performance. We introduce an adaptive visual token selection strategy for VLMs that leverages average text-to-visual attention scores to assess the importance of visual tokens, removing redundant ones during pruning based on a set threshold, thereby optimizing the importance calculation. Recognizing the varying tolerance to sparsity across different modules, we also propose a Sensitivity-aware dynamic pruning mechanism that determines the appropriate pruning ratio for each module by calculating the linear reconstruction error between the outputs of the pruned and unpruned modules, ensuring overall performance stability. Experimental results show that SlimVLM outperforms existing methods across multiple multimodal benchmarks, achieving state-of-the-art performance.

</details>


### [88] [Geospatial-Prior Guidance for 3D Semantic Scene Completion](https://arxiv.org/abs/2608.03618)
*Meng Wang,Shougao Zhang,Wenzhe He,Ruihui Li,Nan Hu,Zhuo Tang,Kenli Li*

Main category: cs.CV

TL;DR: GeoScene is a geospatially guided framework that jointly uses satellite imagery and structured OpenStreetMap cues as soft priors for 3D semantic scene completion, learning complementary voxel-wise reliability weights to control feature refinement in observed and unobserved regions.


<details>
  <summary>Details</summary>
Motivation: Inferring complete 3D geometry and semantics from onboard images is challenging because occlusions and restricted fields of view leave large scene regions underconstrained, and satellite imagery alone offers limited structural guidance due to appearance unreliability.

Method: GeoScene jointly uses satellite imagery and structured OpenStreetMap cues as soft priors, learning complementary voxel-wise reliability weights for onboard observations and geospatial guidance to control feature refinement in observed and unobserved regions.

Result: Experiments on SemanticKITTI and SSCBench-KITTI-360 show GeoScene consistently improves both geometric and semantic completion under the geospatial-prior-assisted setting, with the most pronounced benefits for large-scale static and geospatially structured classes.

Conclusion: GeoScene validates that geospatial priors can effectively complement onboard observations for 3D semantic scene completion, preserving local visual evidence while exploiting large-scale road and building structure beyond onboard visibility.

Abstract: Inferring complete 3D geometry and semantics from onboard images remains challenging because occlusions and restricted fields of view leave large scene regions underconstrained. Although satellite imagery provides wide-area context, appearance cues alone offer limited structural guidance and may be unreliable because of spatial or temporal discrepancies. We present GeoScene, a geospatially guided framework that jointly uses satellite imagery and structured OpenStreetMap cues as soft priors for 3D semantic scene completion. GeoScene learns complementary voxel-wise reliability weights for onboard observations and geospatial guidance, and uses them to control feature refinement in observed and unobserved regions. This design preserves local visual evidence while exploiting large-scale road and building structure beyond onboard visibility. Experiments on SemanticKITTI and SSCBench-KITTI-360 demonstrate that GeoScene consistently improves both geometric and semantic completion under the geospatial-prior-assisted setting, with the most pronounced benefits for large-scale static and geospatially structured classes.

</details>


### [89] [SEER: A Self-Grounded Evidence Interface for Controlled Spatial Relation Classification](https://arxiv.org/abs/2608.03631)
*Feixiang Liu,Likun Wang,Qiang Qiu,Hui Xu,Huawei Shen,Xueqi Cheng*

Main category: cs.CV

TL;DR: SEER is a training-free inference-time evidence interface for frozen VLMs that improves spatial relation reasoning by making query-specific evidence explicit through pair localization with hidden candidate relations, query-specific views with explicit subject/object roles, and complementary full-image plus sparse box geometry evidence.


<details>
  <summary>Details</summary>
Motivation: VLMs can recognize both entities in a spatial relation question yet still answer from the wrong instance or an ambiguous global view, so making query-specific evidence explicit may mitigate this failure mode.

Method: SEER hides candidate relations during pair localization, constructs a query-specific view with explicit subject/object roles, and retains the full image and sparse box geometry as complementary evidence. An optional refinement swaps entity roles and changes the forward decision only when exactly one visual state obeys the corresponding inverse relation.

Result: On an image-disjoint GQA-Train900 test, SEER pools to +3.94 [2.17,5.72] over Full; gains remain positive under counterbalancing and on unique entity names. On 2,434 filtered EmbSpatial pair-relation questions, gains range from +4.35 to +11.79 across three models.

Conclusion: Query-specific evidence construction is the principal intervention driving improvements, with reciprocal consistency serving as a smaller protocol-specific refinement.

Abstract: Spatial relation questions require a model to identify the queried subject and object before comparing their layout. Yet a VLM can recognize both entities and still answer from the wrong instance or an ambiguous global view. We ask whether making query-specific evidence explicit can mitigate this failure and propose SEER (Self-grounded Evidence for Entity-Relation Reasoning), a training-free inference-time evidence interface for frozen VLMs. SEER hides candidate relations during pair localization, constructs a query-specific view with explicit subject/object roles, and retains the full image and sparse box geometry as complementary evidence. For relation-choice protocols with exact inverse support, an optional refinement swaps the entity roles and changes the forward decision only when exactly one visual state obeys the corresponding inverse relation. On an image-disjoint GQA-Train900 test frozen before model scoring, SEER pools to +3.94 [2.17,5.72] over Full; the gain remains positive under label-independent grounding-order counterbalancing and on the 535 rows whose entity names are unique. The unchanged protocol yields +4.35 to +11.79 on all 2,434 filtered EmbSpatial pair-relation questions across three models. Matched controls separate local refocus from role-explicit conditioning. These results establish query-specific evidence construction as the principal intervention, with reciprocal consistency as a smaller protocol-specific refinement.

</details>


### [90] [Learning Biomechanically Plausible Human Motion from Sparse Radar Point Clouds](https://arxiv.org/abs/2608.03637)
*Jonas Leo Mueller,Markus Gambietz,Alexander Weiss,Daniel Krauss,Bjoern M. Eskofier*

Main category: cs.CV

TL;DR: This paper introduces a radar-based human pose estimation framework that integrates a full-body biomechanical skeletal model into a differentiable, end-to-end trainable pipeline. Instead of predicting unconstrained keypoint coordinates, the system predicts joint angles and body segment proportions, which are converted to 3D positions via differentiable forward kinematics. A contact classification loss enforces physical plausibility. The method achieves competitive pose accuracy with interpretable biomechanical outputs on a rehabilitation exercise dataset.


<details>
  <summary>Details</summary>
Motivation: Existing radar-based pose estimation methods focus on improving learning algorithms but represent the body as unconstrained keypoint coordinates, lacking anatomical fidelity. This limits the interpretability and biomechanical validity of the reconstructed poses, which is critical for clinical motion analysis applications.

Method: The framework combines: (1) subject-specific body segment proportions predicted from radar point cloud features to scale a biomechanical skeleton, (2) a motion prediction network mapping temporal radar sequences to generalized coordinates (joint angles), (3) differentiable forward kinematics converting predicted joint angles into 3D positions, and (4) a contact classification loss for physically plausible foot-ground interaction. The pose network is supervised through forward kinematics with subject-specific geometry fitted beforehand.

Result: Using leave-one-subject-out cross-validation on 11 healthy participants performing rehabilitation exercises, the framework achieves 6.456 +/- 1.759 cm MPJPE, 8.083 +/- 0.884 degrees MPJAE, 0.935 +/- 0.009 contact classification F1, and 3.4 +/- 1.3% scaling error.

Conclusion: This proof-of-concept demonstrates the feasibility of recovering interpretable biomechanical descriptors from a single low-cost radar sensor in a controlled laboratory setting, establishing a prerequisite for future clinical motion analysis applications.

Abstract: Radar-based human pose estimation has focused on improving learning algorithms while representing the body as unconstrained keypoint coordinates. We address the underexplored dimension of anatomical fidelity by integrating a full-body skeletal model into a differentiable, end-to-end trainable radar-based pose estimation framework, in which the pose network is supervised through forward kinematics while subject-specific geometry is fitted beforehand. Subject-specific body segment proportions are predicted from radar point cloud features to scale a biomechanical skeleton. A motion prediction network maps temporal radar sequences to generalized coordinates, and differentiable forward kinematics converts predicted joint angles into 3D positions. A contact classification loss encourages physically plausible foot-ground interaction. Under leave-one-subject-out cross-validation on 11 healthy participants performing rehabilitation exercises, the framework achieves 6.456 +/- 1.759 cm mean per-joint position error (MPJPE), 8.083 +/- 0.884 degrees mean per-joint angle error (MPJAE), 0.935 +/- 0.009 contact classification F1, and 3.4 +/- 1.3 % scaling error. This proof-of-concept study demonstrates the feasibility of recovering interpretable biomechanical descriptors from a single low-cost radar sensor in a controlled laboratory setting, a prerequisite for future clinical motion analysis.

</details>


### [91] [When Do Fewer Visual Tokens Accelerate Multimodal Inference? A Break-Even Study Across Decision Locations and Hardware](https://arxiv.org/abs/2608.03649)
*Hao Dou,Ruiwen Tian*

Main category: cs.CV

TL;DR: This paper challenges the assumption that reducing visual tokens guarantees lower end-to-end latency for vision-language models. Through a reproducible evaluation protocol that accounts for decision overhead, shared work, and avoidable operators, the authors show that autoregressive probe policies remain slower than full inference despite state reuse. A lightweight post-vision predictor achieves significant latency reductions on both RTX 3090 and A100, while a pre-vision image-size rule also shows improvements (though less statistically robust). Pre-vision routing has a structural advantage: it can skip preprocessing and vision encoding entirely, compensating for a nearly eightfold smaller token reduction compared to post-vision pruning.


<details>
  <summary>Details</summary>
Motivation: The paper is motivated by the common assumption that fewer visual tokens lead to faster inference. The authors aim to rigorously evaluate whether token reduction policies actually improve end-to-end latency, accounting for the overhead of decision-making and the shared computational work that cannot be avoided.

Method: The authors propose a reproducible evaluation protocol that decomposes latency at the stage level, reconciling decision overhead, shared work, and avoidable operators with measured end-to-end latency. They test two autoregressive probes with state reuse, a lightweight post-vision predictor, and a pre-vision image-size rule across 30 examples on RTX 3090 and A100 GPUs, with statistical corrections including the Holm method.

Result: Autoregressive probes remain slower than Full inference. The lightweight post-vision predictor yields statistically significant latency reductions on both GPUs (surviving Holm correction). The pre-vision image-size rule also shows latency improvements but does not remain significant after correction. On A100, pre-vision routing's ability to skip vision encoding outweighs the post-vision policy's nearly eightfold larger token reduction.

Conclusion: Token reduction does not automatically translate to lower end-to-end latency. Where the policy operates in the pipeline matters greatly: pre-vision routing offers structural latency advantages by avoiding preprocessing and vision encoding entirely, even with smaller token reductions. Reported quality metrics are conditional on Full-answered examples and do not represent benchmark accuracy.

Abstract: Fewer visual tokens do not guarantee lower end-to-end latency. We evaluate break-even with a reproducible protocol that accounts for decision overhead, shared work, and the operators each policy can avoid. A stage-level decomposition reconciles these components with measured end-to-end latency. In a 30-example pilot, the two tested autoregressive probes remain slower than Full despite state reuse. A lightweight post-vision predictor yields paired confidence intervals below zero on RTX 3090 and A100 and remains significant after a conservative all-pairs Holm correction. A pre-vision image-size rule also yields intervals below zero on both GPUs, although neither comparison remains significant after the same correction. Pre-vision routing has a structural opportunity unavailable to post-vision pruning: it can avoid preprocessing and vision encoding. On A100, this opportunity outweighs a nearly eightfold larger downstream token reduction by the post-vision policy. Reported quality is conditional on examples answered correctly by Full and is not benchmark accuracy.

</details>


### [92] [Morphology-Aware Implicit Super-Resolution Network for Pathological Images](https://arxiv.org/abs/2608.03664)
*Jiaming Liang,QiHui Han,Haolin Chen,Chengxin Ye,Jiawen Liu,Jiazhou Chen,Xiaoqi Sheng,Hongmin Cai*

Main category: cs.CV

TL;DR: Morph-ISR is a morphology-aware implicit super-resolution framework for digital pathology that reformulates SR as continuous coordinate-based reconstruction, using an Implicit Position-aware Kernel Generator and a Morphological Fidelity Prior to preserve fine-grained cellular morphology. It achieves the best LPIPS and ST-LPIPS scores (reduced by up to 38.37% and 39.55% over second-best) on TCGA and SurGen datasets while maintaining strong PSNR/SSIM.


<details>
  <summary>Details</summary>
Motivation: Clinical deployment of digital pathology is limited by hardware costs of high-resolution whole-slide imaging. Super-resolution offers a cost-effective alternative, but existing SR methods fail to preserve fine-grained cellular morphology, causing texture oversmoothing and blurred structural boundaries under complex tissue variability.

Method: Morph-ISR reformulates super-resolution as a continuous coordinate-based reconstruction problem and introduces: (1) an Implicit Position-aware Kernel Generator (IPKG) to adaptively model spatially varying tissue morphology, and (2) a Morphological Fidelity Prior (MFP) that leverages semantic guidance from a pre-trained cell segmentation network to enforce boundary-preserving and region-aware reconstruction.

Result: On TCGA and SurGen datasets, Morph-ISR achieves the best LPIPS and ST-LPIPS among evaluated methods, reducing them by up to 38.37% and 39.55% respectively over the second-best methods, while maintaining strong PSNR and SSIM, with compact parameterization and high throughput for edge deployment.

Conclusion: The results demonstrate that Morph-ISR preserves diagnostically relevant cellular boundaries and nuclear textures more effectively than existing SR methods, and its compact design supports efficient edge deployment for clinical use. Code and trained models will be released upon publication.

Abstract: Accurate diagnosis in Digital Pathology (DP) relies on high-resolution whole-slide images, yet clinical deployment is often limited by hardware costs. Super-Resolution (SR) offers a promising alternative by computationally enhancing low-resolution acquisitions. However, existing SR methods frequently struggle to preserve fine-grained cellular morphology, leading to texture oversmoothing and blurred structural boundaries under complex tissue variability. To address this issue, we propose Morph-ISR, a morphology-aware implicit super-resolution framework for DP that restores diagnostically relevant details with sub-pixel precision. Morph-ISR reformulates SR as a continuous coordinate-based reconstruction problem and integrates an Implicit Position-aware Kernel Generator (IPKG) to adaptively model spatially varying tissue morphology. To further enhance structural fidelity, a Morphological Fidelity Prior (MFP) is introduced, leveraging semantic guidance from a pre-trained cell segmentation network to enforce boundary-preserving and region-aware reconstruction, thereby improving the representation of critical cellular boundaries and nuclear textures. Experiments on TCGA and SurGen datasets show that Morph-ISR achieves the best LPIPS and ST-LPIPS among the evaluated methods, reducing them by up to 38.37% and 39.55%, respectively, over the second-best methods while maintaining strong PSNR and SSIM. These results demonstrate superior preservation of diagnostically relevant cellular boundaries and nuclear textures, while compact parameterization and high throughput support efficient edge deployment. Code and trained models will be released upon publication.

</details>


### [93] [XiDepth: a Lightweight and Efficient Network for Self-supervised Monocular Depth Estimation](https://arxiv.org/abs/2608.03666)
*Elena Izzo,Riccardo Toniolo,Lamberto Ballan*

Main category: cs.CV

TL;DR: XiDepth is a lightweight self-supervised monocular depth estimation architecture based on the XiNet operator block, achieving state-of-the-art performance on KITTI with only 0.8M parameters while reducing FLOPs by 40% and energy consumption by 35% compared to leading methods on embedded devices like Raspberry Pi 4.


<details>
  <summary>Details</summary>
Motivation: Existing self-supervised monocular depth estimation models that achieve good performance typically rely on depth-wise convolutions and attention mechanisms, which incur high energy costs and face compatibility issues in embedded environments. The goal is to achieve resource-efficient neural networks without compromising performance for deployment on computationally constrained devices.

Method: The proposed method, XiDepth, is a lightweight architecture built on the XiNet operator block, designed to enhance feature extraction while maintaining low computational complexity and energy demand, making it suitable for embedded deployment.

Result: On the KITTI dataset, XiDepth achieves state-of-the-art performance with only 0.8M parameters. On a Raspberry Pi 4, it reduces FLOPs by 40% and energy consumption by 35% compared to leading methods, confirming suitability for real-world embedded applications.

Conclusion: XiDepth demonstrates that effective depth estimation can be achieved with a lightweight architecture that is both computationally efficient and energy-friendly, making it viable for deployment on constrained embedded devices without sacrificing accuracy.

Abstract: Self-supervised monocular depth estimation has emerged as an appealing solution to design lightweight and effective models for deployment on computationally constrained devices due to its reduced reliance on expensive depth sensors. By eliminating the need for ground-truth annotations and leveraging the simplicity of monocular camera setups, this approach facilitates cost-effective data collection and broad applicability across fields such as computer vision and robotics. A critical challenge is achieving resource-efficient neural networks without compromising the overall performance. State-of-the-art models generally adopt depth-wise convolutions and attention mechanisms; however, these functions often incur high energy costs and face compatibility issues in embedded environments. To address this, we propose XiDepth, a lightweight architecture based on the XiNet operator block, designed to enhance feature extraction while maintaining low computational complexity and energy demand. On the KITTI dataset, XiDepth achieves state-of-the-art performance with only 0.8M parameters. Tests on a Raspberry Pi 4 further confirm its suitability for real-world embedded applications, reducing FLOPs by 40% and energy consumption by 35% compared to leading methods.

</details>


### [94] [Keep the Needle, Prune the Haystack: Defect-Preserving Token Pruning for Efficient Zero-Shot Anomaly Detection](https://arxiv.org/abs/2608.03681)
*Yanning Hou,Jingyuan Zhang,Xiaoyun Wang,Qixiang Ma,Sihang Zhou,Ke Xu*

Main category: cs.CV

TL;DR: KeepAD is a defect-preserving token pruning framework for zero-shot visual anomaly detection that reduces token retention below 20% while limiting AUROC degradation to within 2.7 percentage points, achieving up to 7.9x speedup over CLIP-based baselines across six industrial and seven medical benchmarks.


<details>
  <summary>Details</summary>
Motivation: Existing zero-shot anomaly detection methods perform dense computation over all images and spatial tokens, which is inefficient since normal samples dominate real-world scenarios and anomalies occupy only small regions. While token pruning offers a solution, it introduces an asymmetric risk: pruning normal tokens only causes redundant computation, but pruning anomalous tokens can eliminate the only evidence for detection. This risk is severe in early layers where pruning benefits are greatest but anomaly semantics are unreliable.

Method: KeepAD formulates token selection as high-recall, anomaly-aware routing. In shallow layers, it combines coverage-preserving selection over local 2x2 patch neighborhoods with deterministic anomaly rescue. In deeper layers, frozen normal and abnormal prototypes guide pruning under an image-adaptive token budget, aggressively removing low-risk normal tokens while preserving local anomaly evidence. Dense-to-sparse self-distillation supervises early token routing without additional inference overhead.

Result: On six industrial and seven medical zero-shot anomaly detection benchmarks, KeepAD reduces token retention to below 20% while limiting average image-level and pixel-level AUROC degradation to within 2.7 percentage points. At the most aggressive operating point, it achieves a 7.9x speedup over the strongest CLIP-based baseline.

Conclusion: KeepAD demonstrates that efficient token pruning can be made safe for anomaly detection by explicitly designing for high recall of anomaly evidence, balancing computational efficiency with detection accuracy across diverse industrial and medical benchmarks.

Abstract: Zero-shot visual anomaly detection has achieved remarkable progress, with recent vision-only approaches further improving performance while simplifying the inference pipeline. However, existing methods typically perform dense computation over all images and spatial tokens, despite the fact that normal samples dominate real-world scenarios and anomalies usually occupy only small regions. Token pruning offers a promising solution, but introduces an asymmetric pruning risk in anomaly detection: retaining normal tokens mainly incurs redundant computation, whereas removing anomalous tokens may eliminate the only evidence for detection and localization. This risk is particularly severe in early layers, where pruning provides the greatest computational benefit but anomaly semantics remain unreliable. We propose KeepAD, a defect-preserving token pruning framework that formulates token selection as high-recall, anomaly-aware routing. In shallow layers, KeepAD combines coverage-preserving selection over local $2\times2$ patch neighborhoods with deterministic anomaly rescue to reduce the risk of discarding subtle defects. In deeper layers, frozen normal and abnormal prototypes guide pruning under an image-adaptive token budget, aggressively removing low-risk normal tokens while preserving local anomaly evidence. Dense-to-sparse self-distillation further supervises early token routing without introducing additional inference overhead. Experiments on six industrial and seven medical zero-shot anomaly detection benchmarks show that KeepAD reduces the token retention ratio to below $20\%$, while limiting the average degradation in image-level and pixel-level AUROC to within $2.7$ percentage points. At the most aggressive operating point, KeepAD achieves a $7.9\times$ speedup over the strongest CLIP-based baseline.

</details>


### [95] [MultiCompose: Multi-Concept Personalized Composition with Per-Subject Attribute Binding](https://arxiv.org/abs/2608.03708)
*Ruirui Zhang,Zhengkai Zhao,Pan Gao*

Main category: cs.CV

TL;DR: MultiCompose is a composition framework for text-to-image diffusion models that enables personalized multi-subject generation with attribute binding, paired with MSP-Bench, a benchmark jointly evaluating identity fidelity, attribute binding accuracy, and attribute misalignment.


<details>
  <summary>Details</summary>
Motivation: Existing text-to-image diffusion models struggle to generate a single image containing multiple personalized subjects with user-specified attributes (clothing, accessories, held objects). Concurrent concept checkpoints cause overlapping cross-attention responses leading to identity degradation and attribute misalignment, and no established benchmark evaluates both failure modes jointly.

Method: MultiCompose decouples per-concept personalization from multi-subject inference through (1) a semantic preservation regularization to maintain attribute binding capacity during fine-tuning, and (2) a two-phase inference procedure that automatically establishes subject layout and composes per-concept predictions via spatially exclusive masks. Also introduces MSP-Bench with a dual-pathway protocol evaluating ID fidelity, BIND accuracy, and MIS.

Result: MultiCompose outperforms existing methods on both conventional metrics and the proposed MSP-Bench, demonstrating the benchmark's ability to reveal failure modes that conventional metrics miss.

Conclusion: The proposed framework and benchmark together address the gap in personalized multi-subject image generation with attribute binding, providing both an effective solution and a rigorous evaluation protocol.

Abstract: Text-to-image diffusion models enable personalization of specific visual concepts from a small number of reference images. However, generating a single image that contains multiple personalized subjects, each bound to user-specified attributes such as clothing, accessories, and held objects, remains largely unaddressed. Without explicit spatial constraints, concurrently activated concept checkpoints produce overlapping cross-attention responses, causing per-subject identity degradation and attribute misalignment. Moreover, no established benchmark jointly evaluates these two failure modes in the personalized multi-subject setting. We present MultiCompose, a composition framework that decouples per-concept personalization from multi-subject inference. A semantic preservation regularization maintains attribute binding capacity during fine-tuning, while a two-phase inference procedure automatically establishes subject layout and composes per-concept predictions through spatially exclusive masks. We further introduce MSP-Bench, a benchmark that jointly evaluates identity fidelity (ID), attribute binding accuracy (BIND), and attribute misalignment (MIS) through a dual-pathway protocol. Experiments show that MultiCompose outperforms existing methods on both conventional metrics and MSP-Bench, confirming the benchmark's ability to reveal failure modes that conventional metrics overlook. Code is available at https://github.com/I2-Multimedia-Lab/MultiCompose

</details>


### [96] [Attention is Case-Sensitive](https://arxiv.org/abs/2608.03711)
*Maximilian Dillitzer,Tin Stribor Sohn,Jason J. Corso,Michael Auerbach*

Main category: cs.CV

TL;DR: This paper presents a systematic empirical study showing that letter casing (uppercase/alternating vs. lowercase) modulates internal attention allocation in LLMs and VLMs, analogous to how uppercase lettering captures attention in human visual perception. Across 13 models, the "casing effect" concentrates attention on target text spans in all non-reasoning models, though increased attention concentration does not improve task accuracy and can degrade it in high-entropy contexts. The effect is mitigated in reasoning models' deliberative thinking phases, and transfers partially to VLMs by reorganizing cross-modal attention from images toward text.


<details>
  <summary>Details</summary>
Motivation: To characterize whether LLMs exhibit an attention-allocation property analogous to human visual salience from uppercase lettering, and to understand how pretraining internalizes typographic emphasis as a latent property.

Method: Systematic empirical analysis across 13 models (9 LLMs and 4 VLMs) with diverse tokenization schemes, examining attention allocation when target information is formatted in alternating or uppercase case against lowercase context. Extends analysis to vision-language models to examine cross-modal attention reorganization.

Result: The casing effect is universal across all evaluated non-reasoning models, robustly shifting attention but not improving downstream accuracy—and degrading it in high-entropy contexts like alternating case. Reasoning models' thinking phases act as semantic buffers mitigating typographic sensitivity. VLMs show partial transfer, predominantly disengaging attention from images toward text, with secondary concentration of residual visual attention on target regions.

Conclusion: Letter casing serves as a zero-shot mechanism for attention steering that requires no model access or fine-tuning, revealing a previously under-explored latent property of pretrained transformers and providing new foundational understanding of how pretraining internalizes typographic emphasis.

Abstract: In human visual perception, uppercase lettering serves as a natural salience cue that captures attention within lowercase text. In this paper, we present a systematic empirical characterization study revealing that Large Language Models (LLMs) exhibit an analogous property: letter casing modulates internal attention allocation. Through analysis across 13 models, nine LLMs and four Vision-Language Models (VLMs), with diverse tokenization schemes, we show that formatting target information in alternating or uppercase against a lowercase context concentrates attention on those textual spans. In text this effect is universal, holding across every evaluated non-reasoning model. We frame it as a previously under-explored latent property of pretrained transformers rather than a prescriptive method. Our investigation reveals a central attention-performance divergence: while this "casing effect" robustly shifts attention, its impact on downstream accuracy is non-trivial, increased concentration does not inherently improve task accuracy and, in high-entropy contexts like alternating case, can degrade it. We further identify a boundary condition: the deliberative "thinking" phase in reasoning models acts as a semantic buffer that mitigates typographic sensitivity in text. Extending the study to VLMs, we find the effect transfers partially: the same prompt-side casing reorganizes cross-modal attention along two coupled axes, predominantly a macroscopic disengagement from the image toward the text prompt, and secondarily a concentration of the residual visual attention on the target region. By isolating casing as a zero-shot mechanism for attention steering that requires no model access or fine-tuning, we provide a new foundational understanding of how pretraining internalizes typographic emphasis.

</details>


### [97] [Towards Reliable and Reproducible Fetal Brain Biometry: A Deep Learning Approach Using MRI](https://arxiv.org/abs/2608.03724)
*Francesca Maccarone,Marina Di Stefano,Giorgio Longari,Giulia Frigerio,Gloria Rizzato,Rocco Prudentino,Nivedita Agarwal,Tommaso Ciceri,Denis Peruzzo,Simone Melzi*

Main category: cs.CV

TL;DR: A fully automated deep learning pipeline for fetal brain biometry from 3D super-resolution MRI, jointly estimating linear measurements and anatomical landmarks via a 3D CNN and geometric optimization.


<details>
  <summary>Details</summary>
Motivation: Manual fetal brain measurements are time-consuming and variable; existing automated methods lack reproducibility and anatomical interpretability of landmark localization.

Method: A four-step pipeline: (1) 3D CNN regresses landmark coordinates from brain segmentation label maps; (2) measurement-specific geometric optimization refines landmark positions and computes measurements; evaluated on two public fetal MRI datasets (150 volumes, 20-37 weeks) across five biometric measurements.

Result: Achieves comparable or improved accuracy compared to the only available automated pipeline for most measurements, with comprehensive evaluation of both measurement accuracy and landmark localization.

Conclusion: The pipeline is efficient, interpretable, and scalable, supporting integration into clinical workflows for reliable fetal brain biometry.

Abstract: Fetal brain biometry is essential for quantitative assessment of brain development, supporting gestational age estimation, developmental monitoring, and detection of abnormalities. In clinical practice, measurements are manually performed, making them time-consuming and prone to variability. While automated approaches have been proposed, reproducible methods remain limited, particularly those providing anatomically interpretable landmark localization. We present a fully automated deep learning-based framework for reliable and reproducible brain biometry from 3D super-resolution-reconstructed fetal brain MRI. The proposed four-step pipeline derives biometric parameters by jointly estimating linear measurements and their corresponding anatomical landmarks. A 3D convolutional neural network is trained to regress landmark coordinates from brain segmentation label maps, followed by measurement-specific geometric optimization to refine landmark positions and compute measurements. The pipeline is evaluated on two publicly available fetal MRI datasets comprising 150 volumes (gestational age range: 20-37 weeks) acquired across different scanners and protocols, assessing five key biometric measurements across varying acquisition settings and providing a comprehensive evaluation of both measurement accuracy and landmark localization using quantitative metrics and visual assessment. Compared with the only available automated pipeline, the proposed method achieves comparable or improved accuracy for most measurements. In conclusion, we introduce a straightforward pipeline for reliable biometry estimations, with efficiency, interpretability and scalability that support integration into clinical workflows.

</details>


### [98] [TDVR: Joint Text Disambiguation and Viewpoint Reasoning for Zero-Shot 3D Visual Grounding](https://arxiv.org/abs/2608.03763)
*Qingxi Du,Junbo Wang,Yuke Li,Yining Zhu*

Main category: cs.CV

TL;DR: TDVR is a training-free reasoning framework for zero-shot 3D visual grounding that uses LLM-based disambiguation of ambiguous text queries and viewpoint reasoning to overcome deficient viewpoints, achieving significant improvements over SOTA methods on the ScanRefer dataset.


<details>
  <summary>Details</summary>
Motivation: Existing zero-shot 3D visual grounding methods struggle with two key challenges: ambiguous query text that is unclear or imprecise, and deficient viewpoints that fail to capture objects from adequate angles, limiting grounding accuracy.

Method: TDVR constructs a semantic 3D scene graph from detected instances, then uses an LLM to fuse the original query with appearance and spatial relationship descriptions to disambiguate the text via chain-of-thought reasoning. It performs viewpoint reasoning on the scene graph and structured query to find optimal views, discriminates distracting objects, and integrates viewpoint, confusion, category, and appearance scores to identify the target object.

Result: On the ScanRefer dataset, TDVR outperforms state-of-the-art methods by 15.25% and 14.46% in Acc@0.25 and Acc@0.5 respectively, while being completely training-free.

Conclusion: TDVR demonstrates that a training-free framework combining scene graph construction, LLM-based query disambiguation, and viewpoint reasoning can significantly enhance zero-shot 3D visual grounding, particularly for ambiguous queries and deficient viewpoints.

Abstract: Zero-shot 3D visual grounding aims to localize specific objects based on textual descriptions and 3D visual input. However, the effectiveness of existing methods is significantly hindered by the ambiguous query text and deficient viewpoints. To address these issues, we propose TDVR, a training-free reasoning framework that disambiguates the input text and infers accurate viewpoints for zero-shot 3D visual grounding. First, we construct semantic 3D scene graph from the detected instances in the 3D point cloud. Subsequently, we put the original query, appearance and spatial relationship descriptions into the LLM for fusion, thereby disambiguating the initial input. We leverage chain-of-thought reasoning to generate the structured representation of disambiguated query. Then taking the scene graph and structured query as input, we get the optimal view via viewpoint reasoning to solve the problem of missing viewpoints during grounding. Based on the obtained optimal viewpoint, we further discriminate the distracting objects, enabling the model with the ability to distinguish similar instances. After that, we match the category text and appearance images with the query by computing the similarity of feature vectors. Finally, the target object was identified by integrating the viewpoint score, confusion score, category score, and appearance score. Compared with previous methods, our TDVR has stronger capabilities in viewpoint reasoning, similar object discrimination, and ambiguous query understanding. Experimental results on the public ScanRefer dataset show that our method outperforms the existing state-of-the-art methods by 15.25% and 14.46% in Acc@0.25 and Acc@0.5 respectively, demonstrating the effectiveness of our TDVR in addressing ambiguous query text and deficient viewpoints.

</details>


### [99] [AgenticVAU: Multi-Agent Explore-Verify Reasoning for Video Anomaly Understanding](https://arxiv.org/abs/2608.03779)
*Yuxiang Duan,Huining Li,Ao Li,Shuai Feng,Lanju Kong,Ning Liu,Jian Zhang,Xingdong Sheng,Yuntao Du*

Main category: cs.CV

TL;DR: AgenticVAU is a training-free multi-agent framework for video anomaly understanding that casts the task as an explore-verify process, using four specialized agents (visual-rule construction, search planning, video observation, decision) communicating through an anchor registry to outperform baselines on VAU-Bench benchmarks.


<details>
  <summary>Details</summary>
Motivation: Existing VAU methods rely on specialized training or limited observations, which restricts generalization and evidence coverage. Single-agent approaches integrate exploration, observation, and decision-making in a unified process, lacking role specialization and structured evidence coordination.

Method: AgenticVAU is a training-free multi-agent framework that casts VAU as an explore-verify process. It uses four specialized agents for visual-rule construction, search planning, video observation, and final decision. These agents communicate through an anchor registry (a shared evidence memory), and the system interleaves broad temporal exploration, dense local verification, and cross-interval comparison until sufficient evidence is collected.

Result: Experiments on ECVA, UCF-Crime, and MSAD subsets of VAU-Bench show AgenticVAU outperforms zero-shot inference and reinforcement learning-based baselines.

Conclusion: Multi-agent collaboration with role specialization and structured evidence coordination provides significant value for video anomaly understanding, as demonstrated by AgenticVAU's superior performance.

Abstract: Video anomaly understanding (VAU) focuses on comprehensively interpreting abnormal events in videos, requiring models to identify anomalous occurrences, discover their supporting evidence, and explain the underlying causes beyond simple anomaly detection. Existing VAU methods often rely on specialized training or limited observations, restricting generalization or evidence coverage. Although single-agent alternatives support adaptive video observation, they still integrate exploration, observation, and decision-making within a unified reasoning process, offering limited role specialization and structured evidence coordination. To address these limitations, we present AgenticVAU, a training-free multi-agent framework that casts VAU as an explore--verify process, where the system first discovers potential anomalies and then verifies them through targeted observations. To achieve this, four specialized agents are introduced to handle visual-rule construction, search planning, video observation, and final decision, respectively. These agents communicate through an anchor registry, a shared evidence memory that binds each observation. Guided by this agent framework, AgenticVAU interleaves broad temporal exploration, dense local verification, and cross-interval comparison until sufficient evidence is collected. We conduct extensive experiments on the ECVA, UCF-Crime, and MSAD subsets of VAU-Bench, the results show that AgenticVAU outperforms zero-shot inference and reinforcement learning-based baselines, demonstrating the value of multi-agent collaboration for video anomaly understanding.

</details>


### [100] [OmniPack: Unified Token Compression for Efficient Omni-modal Large Language Models](https://arxiv.org/abs/2608.03812)
*Wanshun Su,Yang Shi,Feihu Liu,Ziwen Yu,Yan Min,Zhuoran Zhang,Qixun Wang,Haotian Wang,Shixuan Liu,Yuanxing Zhang,Peng Wu,Chengfu Huo,Liang Ding*

Main category: cs.CV

TL;DR: OmniPack is a training-free token compression framework for Omni-LLMs that coordinates structural compression before the LLM with task-relevant semantic refinement within the LLM, achieving 98.0% of original performance at 16.7% FLOPs on Qwen2.5-Omni-7B.


<details>
  <summary>Details</summary>
Motivation: Processing long and highly redundant visual and audio token sequences in Omni-LLMs incurs substantial computational overhead, and existing compression methods degrade at low token budgets—pre-LLM compression may discard structurally important and globally distributed evidence, while inner-LLM compression underexploits query-conditioned audio-visual collaboration.

Method: OmniPack is a training-free framework with two complementary stages: (1) before the LLM, structural redundancy is removed through modality-specific importance, global coverage, and similarity-aware merging; (2) within the LLM, after sufficient multimodal interaction, diverse task-relevant representations are consolidated through textual guidance and audio-visual collaboration.

Result: On five benchmarks with three Omni-LLM backbones, OmniPack consistently achieves the best performance-efficiency trade-off across diverse retention ratios. On Qwen2.5-Omni-7B, it preserves 98.0% of original performance at 16.7% FLOPs, and still retains 92.9% of performance with only 6.8% of original FLOPs, outperforming all existing methods.

Conclusion: Coordinating pre-LLM structural compression with in-LLM task-relevant semantic refinement effectively addresses the limitations of existing methods, enabling aggressive token compression without significant performance loss for Omni-LLMs.

Abstract: Omni-modal large language models (Omni-LLMs) have achieved remarkable performance on audio-visual understanding tasks, but processing long and highly redundant visual and audio token sequences incurs substantial computational overhead, demanding aggressive token compression for efficient deployment. Existing methods often degrade at low token budgets: pre-LLM compression may discard structurally important and globally distributed evidence, whereas inner-LLM compression often underexploits query-conditioned audio-visual collaboration. To address these limitations, we propose OmniPack, a training-free framework that coordinates structural compression before the LLM with task-relevant semantic refinement within the LLM. Before the LLM, OmniPack removes structural redundancy through modality-specific importance, global coverage, and similarity-aware merging. After sufficient multimodal interaction, it further consolidates diverse, task-relevant representations through textual guidance and audio-visual collaboration. Extensive experiments on five benchmarks with three Omni-LLM backbones demonstrate that OmniPack consistently achieves the best performance-efficiency trade-off across diverse retention ratios, outperforming all existing methods. Notably, on Qwen2.5-Omni-7B, OmniPack preserves 98.0% of the original performance while reducing FLOPs to 16.7%, and still retains 92.9% of the original performance with only 6.8% of the original FLOPs.

</details>


### [101] [UHP Detection: LVLMs have their Unique Hallucination Pattern in the Consistency Space](https://arxiv.org/abs/2608.03817)
*Amir Mohammad Ezzati,Kiyan Rezaee,Bardiya Kariminia,Mohamad Amin Yousefi,Asal Mohammadjafari Mamaqani,Behrad Samimi,Mohammad Hossein Rohban*

Main category: cs.CV

TL;DR: This paper proposes UHP (Unique Hallucination Pattern) Detection, a fully black-box framework that models hallucination in LVLMs as a structured uncertainty pattern defined by two axes—perturbation modality (image vs. text) and logical polarity (statement vs. negation)—producing four complementary consistency groups to detect hallucinations more accurately than single-metric baselines.


<details>
  <summary>Details</summary>
Motivation: Existing black-box hallucination detection methods rely on a single consistency metric, which inadequately captures the diverse manifestations of uncertainty that hallucinations exhibit across different behavioral probes.

Method: UHP Detection characterizes hallucination via two axes: perturbation modality (image vs. text) and logical polarity (statement vs. negation), creating four complementary consistency groups. From these, within-group and between-group features are extracted to train a lightweight classifier.

Result: UHP Detection outperforms prior black-box and white-box baselines on AMBER and PhD benchmarks across three LVLMs, achieving up to +18.72% AUC-ROC and +20.07% AUC-PR improvements over the strongest black-box methods. Ablations confirm each consistency group contributes complementary information, and cross-dataset evaluation shows the learned pattern generalizes across benchmarks.

Conclusion: Hallucination behavior reflects a model-specific consistency pattern, and structuring uncertainty across multiple axes yields more robust black-box hallucination detection. Code is publicly released.

Abstract: Large vision--language models (LVLMs) demonstrate strong multimodal reasoning capabilities but remain prone to hallucination, where model predictions are not grounded in visual evidence. Existing black-box hallucination detection methods estimate uncertainty through a single consistency metric, implicitly assuming that model uncertainty can be adequately characterized by a single measure. However, hallucinations exhibit diverse manifestations of uncertainty across different behavioral probes, making a single measure insufficient to characterize their underlying behavior. We propose \emph{Unique Hallucination Pattern (UHP) Detection}, a fully black-box framework that models hallucination as a structured uncertainty pattern defined by two axes: perturbation modality (image vs.\ text) and logical polarity (a statement vs.\ its negation). Their intersection produces four complementary consistency groups that capture distinct manifestations of model uncertainty, from which both within-group and between-group features are extracted to train a lightweight classifier. Through comprehensive experiments on AMBER and PhD across three LVLMs, UHP Detection consistently outperforms prior black-box and white-box baselines, with improvements of up to $+18.72\%$ AUC-ROC and $+20.07\%$ AUC-PR over the strongest black-box methods. Extensive ablation studies demonstrate that each consistency group contributes complementary information and that their combination forms a structured hallucination pattern. Furthermore, cross-dataset evaluation shows that this learned pattern generalizes across benchmarks, indicating that hallucination behavior reflects a model-specific consistency pattern. \textbf{Code is publicly available at} https://github.com/amirezzati/uhpdet.

</details>


### [102] [FlowForm: Synergizing Fluid Physics with Topological Consistency for Satellite Flood Synthesis](https://arxiv.org/abs/2608.03822)
*Zhang Weihui,Wang Ruizhi,Xu Hongye,Wang Huiqiong,Sun Li,Song Mingli*

Main category: cs.CV

TL;DR: FlowForm is a satellite flood synthesis framework combining SWE-inspired latent regularization (Flood Descriptor Module) with structure-aware conditioning (Terrain Anchor Adapter), plus a new FloodScape paired dataset, achieving better visual fidelity, paired-image similarity, and flood region consistency.


<details>
  <summary>Details</summary>
Motivation: High-quality paired satellite flood imagery is scarce, and existing generative augmentation methods produce implausible spatial flood layouts and distort scene structures.

Method: FlowForm integrates two components: (1) Flood Descriptor Module (FDM) imposes differentiable penalties on steady-state Shallow Water Equation residuals in auxiliary latent fields at the diffusion bottleneck; (2) Terrain Anchor Adapter (TAA) injects depth, semantic, and edge features at four encoder scales of the U-Net. The authors also curate FloodScape, a large-scale high-resolution paired pre/post-disaster dataset.

Result: FlowForm outperforms baselines across visual fidelity, paired-image similarity, and flooded-region consistency, including zero-shot generalization on a geographically held-out flood event and component ablation sensitivity.

Conclusion: Integrating physics-informed latent regularization with multi-modal structure conditioning improves the plausibility and consistency of synthetic satellite flood imagery, validated on a new large-scale paired dataset.

Abstract: Developing robust flood assessment models requires high-quality paired satellite imagery, yet such data remain scarce for flood-specific image generation. Although generative models provide a promising means of data augmentation, existing methods often yield implausible spatial layouts of flooded regions and distort scene structures. We propose FlowForm, a framework for satellite flood synthesis that integrates SWE-inspired latent regularization with structure-aware conditioning. The Flood Descriptor Module (FDM) imposes differentiable penalties on residuals of the steady-state Shallow Water Equation in auxiliary latent fields at the diffusion bottleneck. The Terrain Anchor Adapter (TAA) injects depth, semantic, and edge features at four encoder scales of the U-Net. We further curate FloodScape, a large-scale, high-resolution dataset comprising paired satellite images acquired before and after disasters. In addition to standard image-generation metrics, we evaluate the consistency of flooded regions, zero-shot generalization to a geographically held-out flood event, and sensitivity to individual components. Across all reported comparisons, FlowForm achieves higher visual fidelity, greater similarity between paired images, and stronger consistency of flooded regions.

</details>


### [103] [Geo-Embed: Towards Unified Multimodal Embeddings for Urban Understanding](https://arxiv.org/abs/2608.03826)
*Jiapeng Li,Yong Li,Junjie Zhou,Fan Zhang,Yu Liu*

Main category: cs.CV

TL;DR: This paper introduces GeoMEB, a large-scale multimodal embedding benchmark with 45 urban evaluation tasks across retrieval, VQA, change detection, classification, and visual grounding (1.32M training examples, 286K evaluation queries), and Geo-Embed, a unified embedding model adapting a shared vision-language backbone to instruction-conditioned query-target matching over heterogeneous geospatial inputs. Geo-Embed achieves a 15.3% relative improvement over the strongest baseline on GeoMEB.


<details>
  <summary>Details</summary>
Motivation: Existing multimodal embedding models and benchmarks are designed for general-purpose image-text matching, leaving unclear whether a unified embedding space can support heterogeneous geospatial tasks involving spatial relationships, fine-grained semantics, and temporal changes in urban applications.

Method: Two key contributions: (1) GeoMEB standardizes 45 urban evaluation tasks across five task categories with 1.32M training examples and 286K evaluation queries; (2) Geo-Embed adapts a shared vision-language backbone to instruction-conditioned query-target matching over heterogeneous geospatial inputs including single/multiple images, text, regions, and masks.

Result: Geo-Embed achieves the strongest overall performance among representative multimodal embedders on GeoMEB, with a 15.3% relative improvement over the strongest baseline.

Conclusion: The results motivate future geospatial embedders to organize training and evaluation around explicit query-target relations, including semantic, cross-view, region-level, and temporal correspondence.

Abstract: Geospatial and urban applications increasingly require models to compare heterogeneous evidence across street-view imagery, remote-sensing observations, text descriptions, region proposals, and temporal change cues. However, existing multimodal embedding models and benchmarks are still largely designed and evaluated around general-purpose image-text matching, leaving unclear whether unified embedding space can support heterogeneous geospatial tasks involving spatial relationships, fine-grained semantics, and temporal changes. To address this gap, we make three key contributions. First, we introduce GeoMEB, a large-scale multimodal embedding benchmark that standardizes 45 urban evaluation tasks across retrieval, visual question answering, change detection, classification, and visual grounding, together with training collections comprising 1.32M examples and 286K evaluation queries. Second, we present Geo-Embed, a unified embedding model that adapts a shared vision-language backbone to instruction-conditioned query-target matching over heterogeneous geospatial inputs, including single images, multiple images, text, regions, and masks. On GeoMEB, Geo-Embed achieves the strongest overall performance among representative multimodal embedders, with a 15.3% relative improvement over the strongest baseline. These results motivate future geospatial embedders that organize training and evaluation around explicit query-target relations, including semantic, cross-view, region-level, and temporal correspondence.

</details>


### [104] [LiteMVS: Efficient Multi-View Stereo with Foundation Distillation and Expert Aggregation](https://arxiv.org/abs/2608.03851)
*Tianbao Zhang,Zeyu Liu,Shuyu Wu,Fanxing Li,Zhaoxin Fan,Wenjun Wu,Danping Zou*

Main category: cs.CV

TL;DR: LiteMVS is a lightweight multi-view depth estimation model that integrates plane-sweep geometric reasoning with monocular semantic and structural priors, enhancing 3D reconstruction quality and providing a reliable geometric foundation for 4D representation learning.


<details>
  <summary>Details</summary>
Motivation: Existing MVS methods fail in textureless or repetitive regions due to reliance on geometric correspondences, while monocular depth models lack multi-view geometric constraints. Additionally, high-quality 3D geometry is needed not just for static reconstruction but as a foundation for temporally consistent 4D representations in robotics and embodied AI.

Method: LiteMVS integrates plane-sweep geometric reasoning with monocular priors by: (1) enriching the cost volume with semantic descriptors from lightweight segmentation models and vision foundation models, (2) employing a Mixture-of-Experts (MoE) formulation for adaptive geometric aggregation across depth hypotheses, and (3) distilling geometric priors from vision foundation models to strengthen monocular guidance without increasing inference cost.

Result: Experiments on ScanNetv2 and 7-Scenes demonstrate that LiteMVS achieves high-quality depth prediction and 3D reconstruction while maintaining competitive efficiency.

Conclusion: LiteMVS not only improves depth estimation and 3D reconstruction in static scenes but also provides a more reliable geometric foundation for subsequent temporal modeling and 4D representation learning, making it suitable for robotics and embodied intelligence applications.

Abstract: Real-time 3D perception is crucial for robotics, augmented reality, and embodied intelligence applications. Existing multi-view stereo (MVS) methods primarily rely on geometric correspondences, which often fail in textureless or repetitive regions, while monocular depth models leverage strong image-level priors but lack robust multi-view geometric constraints. More importantly, in robotics and embodied manipulation scenarios, high-quality 3D geometry is not only essential for static reconstruction, but also serves as a critical foundation for learning temporally consistent 4D representations. To obtain visual representations with stronger structural awareness and greater potential for spatiotemporal extension, we present LiteMVS, a lightweight multi-view depth estimation model that integrates plane-sweep geometric reasoning with strong monocular semantic and structural priors. The central idea of LiteMVS is to efficiently inject high-level monocular knowledge, obtained from lightweight segmentation models and large-scale vision foundation models, into a multi-view stereo framework. In particular, LiteMVS enriches the cost volume with semantic descriptors and employs a Mixture-of-Experts (MoE) formulation to enable adaptive geometric aggregation across depth hypotheses. Moreover, geometric priors distilled from vision foundation models further strengthen monocular guidance without increasing inference cost. Through this design, LiteMVS not only improves depth estimation and 3D reconstruction quality in static scenes, but also provides a more reliable geometric foundation for subsequent temporal modeling and 4D representation learning. Experiments on ScanNetv2 and 7-Scenes demonstrate that LiteMVS achieves high-quality depth prediction and 3D reconstruction while maintaining competitive efficiency.

</details>


### [105] [CPrefix: A Combinatorial Tensor Framework for Structured Discrete Color Mappings](https://arxiv.org/abs/2608.03863)
*Yvan Richard*

Main category: cs.CV

TL;DR: CPrefix is a combinatorial tensor-based framework for representing, reconstructing, and analyzing discrete multi-channel mappings using multinomial counting observables. It creates a latent discrete Pascal simplex representation that separates combinatorial structure from measured values, validated on ICC color profiles through accurate reconstruction and perceptual gamut transport.


<details>
  <summary>Details</summary>
Motivation: Sampled representations of discrete multi-channel mappings provide accurate evaluations but fail to reveal the underlying structural organization of the mapping. A representation is needed that exposes the combinatorial/observable structure while still enabling faithful reconstruction.

Method: The framework constructs a counting tensor from multinomial counting observables whose support forms a discrete Pascal simplex (a latent combinatorial representation, not a constraint). It enables representation, reconstruction, and structural analysis within a unified tensor framework, separating combinatorial organization from measured values.

Result: Validated on ICC display and printer profiles via latent reconstruction and perceptual gamut transport. Accurate reconstruction demonstrates color mappings admit faithful observable representations, and reconstruction residuals reveal compatibility between mappings and the representation.

Conclusion: The framework is domain-independent and applicable to any structured multi-channel mapping beyond color, including spectral measurements and other discrete systems.

Abstract: Discrete multi-channel mappings are typically represented through sampled values, providing accurate evaluations but limited insight into their underlying structure. We introduce CPrefix, a combinatorial observable representation for discrete mappings, realized within a unified tensor framework that enables representation, reconstruction, and structural analysis.
  The framework is based on a counting tensor induced by multinomial counting observables. Its support forms a discrete Pascal simplex, not as a constraint on the observable space, but as a latent combinatorial representation from which mappings are reconstructed. This formulation separates the combinatorial organization of a mapping from its measured values, exposing the observable structure underlying the mapping.
  The framework is validated on ICC display and printer profiles through latent reconstruction and perceptual gamut transport. Accurate reconstruction demonstrates that color mappings admit faithful observable representations, while reconstruction residuals provide insight into the compatibility of the underlying mapping with the proposed representation.
  Although demonstrated on color transformations, the framework is independent of the physical interpretation of the observables, making it applicable to structured multi-channel mappings arising from color imaging, spectral measurements and other discrete systems.

</details>


### [106] [BanglaWild: An In-the-Wild Bengali Scene Text Recognition Benchmark for OCR and Vision-Language Models](https://arxiv.org/abs/2608.03884)
*Sadab Shiper,Tawsif Tashwar Dipto,Mir Md Inzamam,Eshat Tanzeem*

Main category: cs.CV

TL;DR: Introduces BANGLAWILD, a benchmark of 2,535 in-the-wild Bengali scene text images with gold transcriptions, diagnostic attributes, and orthographic standard forms. Evaluates 15 VLMs and 3 conventional OCR systems, revealing that larger models don't outperform smaller ones, visual mis-recognition accounts for ~60% of errors while conjunct errors are under 2% (challenging a long-standing assumption), and LoRA reduces catastrophic failures in weak models without improving strong ones.


<details>
  <summary>Details</summary>
Motivation: In-the-wild Bengali scene text recognition is largely unmeasured—existing resources focus on handwritten documents or constrained sign-board parsing, report only aggregate edit-distance metrics, and evaluate either conventional OCR or VLMs but never both on the same in-the-wild data. This gap prevents a proper understanding of model capabilities and error patterns in real-world Bengali text recognition.

Method: Constructs the BANGLAWILD benchmark with 2,535 images, each annotated with verbatim gold transcription, two categorical axes, four diagnostic attributes, and an orthographically standard form. Evaluates 15 VLMs and 3 conventional OCR systems under three prompting strategies, fine-tunes 6 open-source models with LoRA, and supplements edit-distance metrics with an LLM-as-a-Judge evaluation. A fifteen-class error taxonomy is used to categorize errors.

Result: Larger models within the same family do not consistently outperform smaller ones. Visual mis-recognition accounts for ~60% of errors in the strongest systems, while conjunct-related errors contribute under 2%, challenging long-standing assumptions in Bengali OCR research. This visual-dominant error profile holds across architectures, including the conventional baseline. Prompt language mainly affects cross-script drift, and LoRA reduces catastrophic failures in weak models without lifting the performance ceiling on already competent ones.

Conclusion: The paper establishes a comprehensive benchmark for in-the-wild Bengali scene text recognition, provides new insights into error distributions that contradict prior assumptions about conjunct errors being dominant, and offers practical findings about model scaling, prompt language effects, and fine-tuning strategies. Code and data will be publicly released.

Abstract: In-the-wild Bengali scene text recognition is largely unmeasured: existing resources target handwritten documents or constrained sign-board parsing, report only aggregate edit-distance metrics, and evaluate either conventional OCR or VLMs, never both on the same in-the-wild data. To address this gap, we introduce BANGLAWILD, a benchmark of 2,535 Bengali scene text images, each paired with a verbatim gold transcription, two categorical axes, four diagnostic attributes, and an orthographically standard form where the in-image text deviates from canonical spelling. We evaluate fifteen VLMs and three conventional OCR systems under three prompting strategies, fine-tune 6 open-source models with LoRA, and complement edit-distance metrics with an LLM-as-a-Judge evaluation. Our results reveal a persistent gap in which larger models within the same family do not outperform smaller ones. Our fifteen-class error taxonomy shows that visual mis-recognition accounts for ~60% of errors in the strongest systems, while conjunct-related errors contribute under 2%, challenging a long-standing assumption in Bengali OCR research; the same visual dominant profile also holds across architectures, including the one conventional baseline that reads Bengali reliably. Prompt language mainly affects cross-script drift and LoRA reduces catastrophic failures in weak models without lifting the ceiling on already competent ones. Code and data will be publicly released.

</details>


### [107] [MuRA: Multi-Rank Adaptation for Efficient and Effective Test-Time Vision-Language Generalization](https://arxiv.org/abs/2608.03885)
*Gengyuan Liu,Nanzhou Wang,Chang Liu,Qinwen Wu,Zhenhao Wang,Jiacong Wang,Bokui Chen,Xiangyang Ji*

Main category: cs.CV

TL;DR: MuRA (Multi-Rank Adaptation) is a test-time adaptation framework that dynamically selects and fuses low-rank adaptation modules of varying capacities based on token-level visual complexity, addressing the bottleneck of static rank configurations in vision-language models under distribution shifts.


<details>
  <summary>Details</summary>
Motivation: Vision-language models degrade significantly under distribution shifts. While TTA via LoRA is parameter-efficient, static rank configurations create a fundamental bottleneck: since visual inputs have varying information densities, a fixed rank forces an optimization compromise—underfitting on complex scenes and overfitting on simple ones.

Method: MuRA employs (1) Multi-Rank Orthogonal Decomposition for knowledge-preserving initialization, (2) Unified Component Fusion with Continuous Router Updating to learn semantic-to-rank mappings, and (3) is applied at the deepest visual layer for the shortest gradient backpropagation path. Theoretical justifications prove necessity and gradient stability of the adaptive mechanism.

Result: MuRA achieves state-of-the-art accuracy across extensive domain generalization and cross-dataset benchmarks while significantly reducing both computational and memory overhead.

Conclusion: Dynamic rank adaptation based on visual complexity is superior to static rank configurations for test-time adaptation, and MuRA's design successfully bridges the optimization compromise inherent in fixed-rank methods.

Abstract: Vision-language models exhibit remarkable zero-shot capabilities but suffer significant performance degradation under distribution shifts. While test-time adaptation (TTA) via Low-Rank Adaptation offers a parameter-efficient solution, we identify a fundamental bottleneck in current methods: the reliance on static rank configurations. Because visual inputs inherently possess varying information densities, a fixed rank forces an inevitable optimization compromise, leading to underfitting on complex scenes and overfitting on simple ones. To bridge this gap, we propose Multi-Rank Adaptation (MuRA), a novel framework that dynamically selects and fuses adaptation modules of varying capacities based on token-level visual complexity. MuRA synergizes Multi-Rank Orthogonal Decomposition to provide a superior, knowledge-preserving initialization, and Unified Component Fusion with Continuous Router Updating to sustainably learn semantic-to-rank mappings. Furthermore, we provide rigorous theoretical justifications mathematically proving the necessity and gradient stability of this adaptive mechanism. Crucially, MuRA's dynamic design uniquely thrives at the deepest visual layer, capitalizing on the shortest gradient backpropagation path. Extensive experiments demonstrate that MuRA achieves state-of-the-art accuracy across extensive domain generalization and cross-dataset benchmarks while significantly reducing both computational and memory overhead.

</details>


### [108] [CARE-X: Towards Clinically Useful Radiology VLMs with Auxiliary Supervision, Reward-Aligned Learning, and Tool-Augmented Measurement](https://arxiv.org/abs/2608.03890)
*Mercy Prasanna Ranjit,Anirban Porya,Sathvik Joel,Niharika Vadlamudi,Nikhilesh Chowdary Eathamukkala,Prasanth V,Abhyuday Kumara Swamy,Pranay Narhari Umredkar,Pradeep Narayan,Vivek Rajagopal,Tanuja Ganu*

Main category: cs.CV

TL;DR: CARE-X is a chest X-ray VLM that unifies auxiliary discriminative supervision (focal-loss classification and composite-loss grounding heads) with reward-aligned generation via DAPO optimization, achieving state-of-the-art performance on report generation, 94.0% VQA accuracy on ReXVQA, near-parity spatial grounding with dedicated detection heads, and +43.6 pp F1 improvement in measurement-dependent diagnoses through hybrid inference with native tool-calling.


<details>
  <summary>Details</summary>
Motivation: Current chest X-ray VLMs focus on fluent report generation but fail to address the full needs of radiologists, who require tunable classification thresholds, spatial localization, and anatomical measurements. There is a disconnect between what radiologists need and what generative models provide.

Method: CARE-X augments a generative VLM backbone with focal-loss classification and composite-loss grounding heads, co-trained with the language-modeling objective. It then applies Decoupled Clip and Dynamic Sampling Policy Optimization (DAPO) using task-specific reward signals for report generation, VQA, and spatial grounding. For measurement-dependent tasks, it couples Qwen3-VL-4B-Instruct with native tool-calling to invoke deterministic measurement tools while maintaining visual access.

Result: State-of-the-art performance on most metrics across four report-generation benchmarks, 94.0% VQA accuracy on ReXVQA (+6.0 pp over the next-best baseline), generative spatial decoding near parity with dedicated detection heads, and +43.6 pp average F1 over perception-only baselines across five measurement-dependent conditions.

Conclusion: Structured prediction and generation reinforce one another, demonstrating that unifying discriminative supervision with reward-aligned generation addresses the gap between what radiologists need and what generative models provide. Hybrid inference with tool-calling effectively handles measurement-dependent diagnoses.

Abstract: A clinically useful chest X-ray system must go beyond fluent report generation: it should classify findings with tunable decision thresholds, localize them spatially, and derive the anatomical measurements upon which many diagnoses depend. Today's Vision-Language Models (VLMs) treat these as separate problems, if they address them at all, leaving a gap between what radiologists need and what generative models provide. We introduce CARE-X, a chest X-ray VLM that narrows this gap by unifying auxiliary discriminative supervision with reward-aligned generation. CARE-X augments its generative backbone with focal-loss classification and composite-loss grounding heads, co-trained alongside the language-modeling objective. This auxiliary supervision produces discriminative diagnostic predictions with tunable decision thresholds and precise spatial localization while also improving report quality, providing evidence that structured prediction and generation reinforce one another. Building on this foundation, Decoupled Clip and Dynamic Sampling Policy Optimization (DAPO) leverages task-specific reward signals for report generation, visual question answering (VQA), and spatial grounding, directly optimizing the clinical quality metrics that matter in practice. The result is state-of-the-art performance on the majority of metrics across four report-generation benchmarks, 94.0% VQA accuracy on ReXVQA (+6.0 pp over the next-best baseline), and generative spatial decoding that reaches near parity with dedicated detection heads. Separately, to address measurement-dependent diagnoses, we couple Qwen3-VL-4B-Instruct with native tool-calling capabilities for invoking deterministic measurement tools, while retaining full visual access to the image. This hybrid inference yields +43.6 pp average F1 over perception-only baselines across five measurement-dependent conditions.

</details>


### [109] [NCGR: Noise-Conditional Gated Rectification for Camera Extrinsic Perturbations in BEV 3D Object Detection](https://arxiv.org/abs/2608.03895)
*Wenbin Pan,Wanhao Liu,Liwei Luo,Panshuo Li,Yong Xu,Renquan Lu*

Main category: cs.CV

TL;DR: NCGR is a rectification module for camera-based BEV 3D detection that compensates for extrinsic perturbation errors by predicting a 2D offset per query-camera pair, modulated by a camera-level gate, without explicitly estimating full 6-DoF extrinsics. It uses scheduled interpolation and a clean-teacher/perturbed-student framework with BEV-consistency supervision to enable blind inference.


<details>
  <summary>Details</summary>
Motivation: Camera-based BEV 3D detection assumes accurate fixed camera extrinsics, but extrinsic perturbations cause projection errors in spatial cross-attention (SCA), making queries sample features from incorrect regions and degrading performance. Existing approaches do not address this failure mode efficiently without full extrinsic estimation.

Method: NCGR predicts a 2D rectification offset for each query-camera pair, gated by a camera-level gate to correct the base projection before deformable sampling. During training, perturbation-derived condition/gate quantities are gradually replaced by counterparts from a predicted auxiliary scalar via scheduled interpolation. A weight-shared clean-teacher/perturbed-student pair is trained with a BEV-consistency objective for supervision.

Result: On nuScenes with simulated dynamic and static extrinsic perturbations, NCGR achieves 39.69% NDS in a five-camera dynamic stress test, versus 28.00% for BEVFormer and 33.23% for CAPE. Under clean extrinsics, NCGR maintains comparable performance to BEVFormer.

Conclusion: NCGR effectively mitigates extrinsic perturbation-induced degradation in BEV 3D detectors while remaining robust under clean conditions, enabling blind inference without needing perturbation metadata at test time.

Abstract: Camera-based bird's-eye-view (BEV) 3D detection typically assumes accurate and fixed camera extrinsics. In detectors using spatial cross-attention (SCA), extrinsic perturbations displace the image-plane projections of BEV reference points, causing queries to sample features from incorrect regions and degrading detection performance. To address this failure mode, Noise-Conditional Gated Rectification (NCGR) is proposed to compensate for projection errors without explicitly estimating a full six-degree-of-freedom extrinsic correction. For each query-camera pair, a 2D rectification offset is predicted and modulated by a camera-level gate to rectify the base projection before native deformable sampling. During training, the perturbation-derived quantities used to construct the condition and gate are gradually replaced through scheduled interpolation by counterparts generated from an auxiliary scalar predicted from camera features. This transition enables blind inference without perturbation metadata. During training, a weight-shared clean-teacher/perturbed-student pair is used, and the rectification module is supervised by a BEV-consistency objective between the two branches. NCGR is evaluated on nuScenes with simulated dynamic and static extrinsic perturbations. In a five-camera dynamic stress test, NCGR achieves 39.69% NDS, compared with 28.00% for BEVFormer and 33.23% for CAPE. Under clean extrinsics, NCGR maintains performance comparable to that of BEVFormer.

</details>


### [110] [UniEvo-RS: Omni-Prompt Unified Remote Sensing Segmentation with Representative Exemplar-Driven Prototype Evolution](https://arxiv.org/abs/2608.03911)
*Kunquan Zhang,Peilang Li,Xikun Hu,Yunkai Yang,Yushan Zou,Zhiwei Zhang,Runmin Dong*

Main category: cs.CV

TL;DR: UniEvo-RS is an omni-prompt unified remote sensing segmentation framework with representative exemplar-driven prototype evolution, unifying text-driven and visual-driven prompts, and enabling training-free progressive accuracy improvement on unseen categories during batch annotation.


<details>
  <summary>Details</summary>
Motivation: Static vision-language models degrade on novel scenes, unseen categories, and confusing backgrounds in remote sensing annotation. Existing unified paradigms rely on intra-image specific prompts and lack flexible task routing for multi-intent operational workflows.

Method: (1) Constructs a multi-instruction prompt dataset unifying text-driven and visual-driven prompts within a single architecture with dynamic task routing. (2) Introduces a representative feedback-driven, training-free prototype evolution mechanism that contrasts manual annotations with initial predictions on exemplars, distilling errors into positive and negative prototypes to enhance LLM query recall and suppress spatial background noise under fixed-budget clustering memory.

Result: Achieves state-of-the-art performance across most settings, unifying diverse prompting tasks. With minimal interaction on a few exemplars, it enables training-free, progressive accuracy enhancement on unseen categories during batch annotation.

Conclusion: UniEvo-RS effectively addresses static model degradation and lack of task routing in RS segmentation, achieving SOTA performance and enabling efficient, scalable batch annotation through exemplar-driven prototype evolution.

Abstract: Prompt-driven vision-language models (VLMs) hold immense promise for accelerating dense remote sensing (RS) annotation, but static models suffer from severe performance degradation when deployed on novel scenes, unseen categories, or visually confusing backgrounds. Moreover, existing unified paradigms primarily rely on intra-image specific prompts, lacking flexible task routing to adapt to multi-intent operational workflows. In practical batch mapping, annotators typically refine a small set of representative samples before processing large datasets. Motivated by this practice, we propose UniEvo-RS, an omni-prompt unified RS segmentation framework equipped with representative exemplar-driven prototype evolution. First, we construct a multi-instruction prompt dataset that unifies text-driven and visual-driven prompts within a single architecture, establishing a dynamic task-routing mechanism for highly diverse RS annotation scenarios. Second, we introduce a representative feedback-driven, training-free prototype evolution mechanism. By contrasting manual annotations with initial predictions on exemplars, UniEvo-RS distills prediction errors into positive and negative prototypes. These prototypes enhance LLM query recall and suppress spatial background noise under a fixed-budget clustering memory. Extensive experiments show that UniEvo-RS unifies diverse prompting tasks, achieving state-of-the-art performance across most settings. Crucially, with minimal interaction on a few exemplars, it enables training-free, progressive accuracy enhancement on unseen categories during batch annotation.

</details>


### [111] [StreamDAM: Presence-Aware Memory for Real-Time Streaming Video Object Segmentation](https://arxiv.org/abs/2608.03912)
*Xiang Chen*

Main category: cs.CV

TL;DR: The paper addresses the failure of high-accuracy VOS trackers under real-time streaming constraints, introducing \method{} which rebuilds the memory pipeline for frame-rate operation via in-model optimization and a learned presence signal, outperforming baselines across four benchmarks.


<details>
  <summary>Details</summary>
Motivation: State-of-the-art VOS trackers like DAM4SAM achieve top accuracy offline but collapse under honest streaming at 30 fps because their rich memory pipelines are too slow and blind to object presence. The failures trace to the memory pipeline, motivating a redesign for streaming.

Method: \method{} makes the memory machinery itself run at frame rate through in-model optimization rather than bolted-on fallbacks, and governs it with a single learned presence signal that controls memory entry, reading depth, output withholding, and re-detection.

Result: Across four benchmarks and five modern baselines, \method{} is the strongest streaming tracker, recovers nearly all offline accuracy under the clock, and exceeds the offline model on the hardest content.

Conclusion: A fixed policy cannot win because presence-control decisions must be made per frame—helpful control for true object disappearance hurts when the object is merely hard to see, so adaptive per-frame control is necessary.

Abstract: Quality-tier video object segmentation (VOS) trackers such as DAM4SAM top accuracy leaderboards, but they are measured offline, one frame at a time with no clock. Under an honest streaming protocol at 30 frames per second, where a frame that misses its budget is served the last mask already computed, the winner collapses: the rich memory that makes it accurate is too slow to keep up, and what it emits is blind to whether the object is even present. We trace both failures to one place, the tracker's memory pipeline, and rebuild it for streaming. \method{} makes the memory machinery itself run at frame rate through in-model optimization rather than a bolted-on fallback, and governs it with a single learned presence signal that decides what enters memory, how far back the tracker reads, when to withhold output, and when to re-detect. A mechanism analysis shows why a fixed policy cannot win: the control that helps when an object truly disappears is the one that hurts when it is merely hard to see, so the choice must be made per frame. Across four benchmarks and five modern baselines, \method{} is the strongest streaming tracker, recovers nearly all of the offline model's accuracy under the clock, and on the hardest content exceeds the offline model it is built from.

</details>


### [112] [When and Where to Look: Adaptive Visual Evidence Scheduling for Efficient Long Video Understanding](https://arxiv.org/abs/2608.03918)
*Ke Li,Jiayu Chen,Maoliang Li,Zihao Zheng,Hailong Zou,Hengyi Zhang,Xuanzhe Liu,Xiang Chen*

Main category: cs.CV

TL;DR: EcoFrame is a training-free framework for adaptive visual evidence scheduling in long-video understanding, using entropy-gated budget expansion and attention-guided candidate proposal to achieve better accuracy-efficiency trade-offs than existing methods.


<details>
  <summary>Details</summary>
Motivation: Existing methods for long-video understanding either rely on static one-shot frame selection with fixed budgets (lacking adaptability) or agent-based schedulers that achieve adaptivity through costly multi-round reasoning and interactive search (inefficient).

Method: EcoFrame uses two key mechanisms: (1) entropy-gated budget scheduling that leverages the VLM's output uncertainty to stop early when evidence is sufficient or progressively expand the frame budget, and (2) attention-guided candidate proposal that converts frame-level attention into a temporal prior, enabling dense local search in informative regions while preserving global coverage when attention is diffuse.

Result: On Video-MME, LongVideoBench, and MLVU, EcoFrame achieves better accuracy-efficiency trade-offs across multiple VLM backbones. On Qwen2.5-VL, it achieves 64.4 average accuracy (surpassing BOLT at 63.5), with 1.85x speedup over AKS and BOLT, and up to 13.5x inference speedup over agent-based A.I.R. while maintaining comparable accuracy.

Conclusion: EcoFrame demonstrates that adaptive, query-specific frame scheduling can be achieved without training and without costly agent-based reasoning, offering a training-free and low-overhead alternative for efficient long-video understanding.

Abstract: Efficient long-video understanding requires vision--language models (VLMs) to reason over a small number of frames selected as sparse visual evidence. Existing relevance-based methods rely on static one-shot selection with fixed frame budgets and candidate pools, while agent-based schedulers achieve adaptivity through costly multi-round reasoning and interactive search. We propose EcoFrame, a training-free framework for low-overhead query-adaptive visual evidence scheduling. EcoFrame leverages the VLM's inference feedback to determine when to increase the frame budget and where to search for additional candidate evidence. Specifically, entropy-gated budget scheduling uses output uncertainty to stop early when the current evidence is sufficient or progressively expand the frame budget otherwise. Meanwhile, attention-guided candidate proposal converts frame-level attention into a temporal prior, enabling dense local search in informative regions while preserving global coverage when attention is diffuse. Experiments on Video-MME, LongVideoBench, and MLVU demonstrate that EcoFrame achieves a better accuracy--efficiency trade-off across multiple VLM backbones. On Qwen2.5-VL, EcoFrame achieves an average accuracy of 64.4, surpassing BOLT at 63.5, while providing a $1.85\times$ speedup over AKS and BOLT. Compared with the agent-based A.I.R., EcoFrame maintains comparable accuracy with up to a $13.5\times$ inference speedup. Code will be available at https://github.com/AK-DREAM/EcoFrame.

</details>


### [113] [Low-Dimensional High-Leverage Subspace Optimization: Beyond Full-Parameter Coupled Training for Neural Network Quantization](https://arxiv.org/abs/2608.03919)
*Peng Xia,Junbiao Pang,Zheng Huang*

Main category: cs.CV

TL;DR: This paper identifies normalization affine parameters as a low-dimensional high-leverage subspace dominating quantization robustness, and proposes Normalization Affine Preconditioning (NAP) for targeted subspace optimization in both PTQ and QAT, achieving significant accuracy recovery for low-bit quantized compact networks with negligible tuning cost.


<details>
  <summary>Details</summary>
Motivation: Low-bit quantization suffers severe accuracy degradation on compact networks because the dominant full-parameter coupled training paradigm ignores parameter subspace heterogeneity. Compact networks have limited feature redundancy that leaves little room to absorb quantization errors, and conventional methods (PTQ/QAT) fail to address this due to monolithic optimization approaches that suffer from gradient coupling or lack of inherent quantization friendliness.

Method: The paper proposes Normalization Affine Preconditioning (NAP), which identifies normalization affine parameters as a low-dimensional high-leverage subspace. For PTQ, NAP freezes backbone weights and fine-tunes only affine parameters under the target fake-quantization graph. For QAT, it introduces an alternating QAT-NAP schema that decouples feature learning from numerical calibration. Theoretical analysis shows BN affine parameters can fully cancel channel-wise affine components of quantization distortion, and distillation-guided NAP acts as directional flatness optimization.

Result: Experiments on ImageNet and CIFAR-100 show NAP recovers severely collapsed low-bit quantization, consistently boosts reconstruction-based PTQ, and outperforms saturated full-parameter QAT with negligible tuning cost.

Conclusion: This work reveals the principle of targeted low-dimensional subspace optimization, offering a new perspective beyond full-parameter coupled training for efficient deep learning, and demonstrates that normalization affine parameters constitute a critical subspace that dominates quantization robustness.

Abstract: Low-bit quantization suffers severe accuracy degradation on compact networks, rooted in the dominant full-parameter coupled training paradigm that ignores parameter subspace heterogeneity. Their limited feature redundancy leaves little room to absorb quantization errors. Conventional pipelines adopt monolithic optimization: PTQ reconstructs fixed pretrained models without improving inherent quantization friendliness; QAT updates all parameters jointly, suffering from gradient coupling between backbone weights and calibration parameters. In this paper, we identify normalization affine parameters as a low-dimensional high-leverage subspace dominating quantization robustness, and propose Normalization Affine Preconditioning (NAP) for targeted subspace optimization. For PTQ, NAP freezes backbone weights and fine-tunes only affine parameters under the target fake-quantization graph on full-precision models, proactively boosting quantization friendliness before downstream reconstruction. For QAT, we introduce an alternating QAT-NAP schema that decouples feature learning and numerical calibration, breaking the performance ceiling of saturated joint training. Theoretical analysis confirms BN affine parameters fully cancel the channel-wise affine component of quantization distortion, while nonlinear rounding and clipping residuals form the irreducible error boundary; distillation-guided NAP acts as directional flatness optimization, projecting teacher-student logit mismatch onto the restricted subspace. Experiments on ImageNet and CIFAR-100 show NAP recovers severely collapsed low-bit quantization, consistently boosts reconstruction-based PTQ, and outperforms saturated full-parameter QAT with negligible tuning cost. This work reveals the principle of targeted low-dimensional subspace optimization, offering a new perspective beyond full-parameter coupled training for efficient deep learning.

</details>


### [114] [GeoMAR: Unleashing Geometrically Aligned Features for Masked Autoregressive Blind Face Restoration](https://arxiv.org/abs/2608.03923)
*Lu Gan,Hanyu Yan,Chaofeng Chen,Junqi Hu,Dan Zeng*

Main category: cs.CV

TL;DR: GeoMAR is a blind face restoration framework that uses geometrically aligned features with masked autoregressive refinement to address ambiguous conditioning and fragile prediction under severe degradation, achieving competitive perceptual quality on synthetic and real-world benchmarks.


<details>
  <summary>Details</summary>
Motivation: Existing codebook-based blind face restoration (BFR) methods suffer from two key limitations: (1) ambiguous conditioning features that do not provide reliable spatial/geometric guidance, and (2) a fragile one-step prediction mechanism that struggles under severe degradation conditions.

Method: GeoMAR introduces: (1) a dual-input extraction pipeline that produces component-based geometric descriptions with explicit spatial anchors as textual priors; (2) an Aligned Geometric Priors Injector using a KV-Q exchange strategy to fuse these priors with low-quality features for geometrically aligned feature generation; and (3) a reformulated prediction mechanism that converts one-step mapping into a multi-step masked autoregressive (MAR) process for coarse-to-fine refinement of complex facial regions.

Result: Experiments on one synthetic and three real-world benchmarks show GeoMAR achieves highly competitive perceptual quality and coherent visual structures compared with existing methods.

Conclusion: The proposed framework effectively addresses the ambiguity and fragility issues in codebook-based BFR by combining geometrically aligned feature conditioning with autoregressive refinement, demonstrating strong performance across benchmarks.

Abstract: Codebook-based blind face restoration (BFR) often suffers from ambiguous conditioning features and a fragile prediction mechanism under severe degradation. To address these challenges, we propose GeoMAR, a framework designed to unleash geometrically aligned features with masked autoregressive (MAR) refinement for robust face restoration. For feature conditioning, we introduce a dual-input extraction pipeline to extract component-based geometric descriptions with explicit, spatially faithful anchors. These textual priors are integrated with low-quality (LQ) features via an Aligned Geometric Priors Injector, which employs a KV-Q exchange strategy to generate geometrically aligned features. For prediction mechanism, we reformulate the one-step mapping into a multi-step MAR process. This coarse-to-fine generation progressively refines complex facial regions based on increasingly reliable context. Experiments on one synthetic and three real-world benchmarks demonstrate that GeoMAR achieves highly competitive perceptual quality and coherent visual structures compared with existing methods. The code is available at https://github.com/BRL-SYSU/GeoMAR.git.

</details>


### [115] [Progressive Learning of a Diffusion-based Inpainting Model for Separating Overlapped Fingerprints](https://arxiv.org/abs/2608.03937)
*Noor Hussein,Anil K. Jain,Karthik Nandakumar*

Main category: cs.CV

TL;DR: A diffusion-based pipeline for separating overlapped fingerprints, formulated as an inpainting task, using a progressively trained model built on Stable Diffusion with fingerprint priors and overlap-aware multi-channel conditioning.


<details>
  <summary>Details</summary>
Motivation: Overlapped friction ridge patterns are a recurring problem in latent fingerprints from crime scenes and live-scan scenarios where residual prints corrupt subsequent acquisitions. Existing approaches either rely on rule-based orientation field completion requiring strong domain knowledge, or use end-to-end deep neural networks that ignore domain-specific considerations.

Method: The separation problem is formulated as an inpainting task. The authors progressively learn a diffusion model in multiple stages: starting from a pre-trained Stable Diffusion model, they incorporate a fingerprint prior, add the ability to complete partial fingerprints, and finally propose overlap-aware inpainting that reconstructs each component print using a diffusion inpainting model with multi-channel conditioning.

Result: Experiments on two public datasets demonstrate that component fingerprints reconstructed using the proposed diffusion-based inpainting method can match with their mated counterparts with very high probability.

Conclusion: The proposed diffusion-based inpainting approach successfully separates overlapped fingerprints, outperforming prior methods by leveraging domain knowledge through a progressive training strategy and multi-channel conditioning within a generative diffusion framework.

Abstract: Overlapped friction ridge patterns are a recurring problem in latent fingerprints recovered from crime scenes and in live-scan scenarios where residual fingerprints on the sensor may corrupt subsequent acquisitions. Existing approaches for separating overlapped fingerprints either rely on rule-based orientation field completion that requires strong domain knowledge or train end-to-end deep neural networks that do not account for domain-specific considerations. This work introduces a diffusion-based pipeline for separating component fingerprints from an image containing overlapping friction ridge patterns. We formulate the separation problem as an inpainting task and progressively learn a diffusion model for this task in multiple stages. Starting from a pre-trained Stable Diffusion model, we progressively incorporate a fingerprint prior, add the ability to complete partial fingerprints, and finally propose \textbf{overlap-aware inpainting} that reconstructs each component print using a diffusion inpainting model based on multi-channel conditioning. Experiments on two public datasets demonstrate that component fingerprints reconstructed using the proposed diffusion-based inpainting method can match with their mated counterparts with very high probability.

</details>


### [116] [UniWorld-Design: From Pixel Generation to Layer-Native Design](https://arxiv.org/abs/2608.03971)
*Zongjian Li,Zhiyuan Yan,Chenxu Bai,Chen Chen,Haoxiang Sun,Shaodong Wang,Feize Wu,Shenghai Yuan,Bin Lin,Zheyuan Liu,Yuwei Niu,Li Yuan*

Main category: cs.CV

TL;DR: UniWorld-Design is a framework that redefines image generation from flat pixel synthesis to structured visual composition using semantic RGBA layers as the atomic units, comprising a Text-to-RGBA (T2RGBA) model for generating standalone layered assets and an Image-to-Layer (I2L) model for decomposing images into ordered, complete semantic layers with instruction-driven decomposition capabilities.


<details>
  <summary>Details</summary>
Motivation: The insight that pixels define how an image is rendered, whereas layers define how an image is created, understood, and edited. Just as human designers work with layers rather than raw pixels, the authors aim to equip multimodal generative models with a layer-native design space for more structured and editable image generation.

Method: The framework consists of two models: (1) T2RGBA generates standalone RGBA assets directly from text, and (2) I2L conditions on a finished image, a global instruction, and per-layer prompts to jointly produce ordered, complete semantic RGBA layers. The instruction interface supports top-level decomposition, recursive decomposition, and targeted extraction, making layering an instruction-addressable operation for agentic editing.

Result: On the Crello benchmark, I2L reduces per-layer RGB L1 error by 37% and achieves a 34% relative improvement in Alpha Soft IoU over Qwen-Image-Layered. T2RGBA achieves the highest CLIP Score, outperforming LayerDiffuse and OmniAlpha.

Conclusion: Because I2L learns complete semantic objects rather than visible-pixel partitions, its layers remain usable when moved or removed, demonstrating a more robust and editable representation for image generation and editing.

Abstract: We introduce UniWorld-Design, a framework that redefines image generation from flat pixel synthesis to structured visual composition, with semantic RGBA layers as the atomic units of generation, understanding, and editing. Our key insight is that pixels define how an image is rendered, whereas layers define how an image is created, understood, and edited. Just as human designers create and manipulate visual content through layers rather than raw pixels, UniWorld-Design equips multimodal generative models with a layer-native design space. UniWorld-Design comprises two models. The Text-to-RGBA (T2RGBA) model generates standalone RGBA assets directly from text. The Image-to-Layer (I2L) model conditions on a finished image, a global instruction and per-layer prompts, and jointly produces ordered, complete semantic RGBA layers. Its instruction interface supports top-level decomposition, recursive decomposition and targeted extraction, making layering an instruction-addressable operation for agentic editing. Because I2L learns complete semantic objects rather than visible-pixel partitions, its layers stay usable when moved or removed. On the Crello benchmark, I2L reduces per-layer RGB L1 error by 37% and achieves a 34% relative improvement in Alpha Soft IoU over Qwen-Image-Layered. Separately, T2RGBA achieves the highest CLIP Score, outperforming LayerDiffuse and OmniAlpha.

</details>


### [117] [JoyAI-Video-Edit: Real-Time Open-Ended Video Editing with Autoregressive Diffusion](https://arxiv.org/abs/2608.03974)
*Yicheng Xiao,Wenxun Dai,Xinran Qin,Lin Song,Maoquan Zhang,Hang Xu,Yukang Chen,Yitong Li,Guohui Zhang,Yuan Zhang,Xuying Zhang,Tommy Zhang,Jianlong Yuan,Peihao Li,Shuai Lu,Siming Fu,Chuyang Zhao,Xin Han,Jie Huang,Wenbo Li,Guoqing Ma,Wei Huang,Xiaojuan Qi,Haoyang Huang,Nan Duan*

Main category: cs.CV

TL;DR: JoyAI-Video-Edit is a 16B-parameter autoregressive diffusion framework for real-time, open-ended video editing that operates causally without future frames or predefined duration. It achieves ~30 FPS 720p editing on a single Nvidia B200 GPU via chunk-wise autoregressive adaptation, Source-Anchored Distribution Matching Distillation, and Long-Horizon Autoregressive Distillation.


<details>
  <summary>Details</summary>
Motivation: Real-time video editing demands low-latency causal generation with bounded compute while maintaining source fidelity and long-term temporal consistency—a challenge existing methods struggle with.

Method: The framework combines three key techniques: (1) chunk-wise autoregressive adaptation for causal generation, (2) Source-Anchored Distribution Matching Distillation (SA-DMD) to preserve source fidelity during two-step generation, and (3) Long-Horizon Autoregressive Distillation to mitigate accumulated temporal drift from train-inference mismatch.

Result: The system substantially outperforms existing streaming editors and remains competitive with strong offline systems on both short and long videos, achieving end-to-end 720p video editing at approximately 30 FPS on a single Nvidia B200 GPU.

Conclusion: JoyAI-Video-Edit demonstrates that real-time, open-ended video editing is achievable at production quality, providing a practical solution for streaming video editing without future-frame access or predefined duration constraints.

Abstract: Real-time video editing requires low-latency causal generation with bounded computational resources while preserving source fidelity and long-term temporal consistency. We present JoyAI-Video-Edit, a 16B-parameter autoregressive diffusion framework for real-time, open-ended video editing without access to future frames or a predefined video duration. Our method combines chunk-wise autoregressive adaptation, Source-Anchored Distribution Matching Distillation (SA-DMD), and Long-Horizon Autoregressive Distillation to reduce train--inference mismatch, preserve source fidelity during two-step generation, and mitigate accumulated temporal drift. Extensive automatic and human evaluations show that JoyAI-Video-Edit substantially outperforms existing streaming editors and remains competitive with strong offline systems on both short and long videos. The complete system achieves end-to-end 720p video editing at approximately 30 FPS on a single Nvidia B200 GPU. Code is available at https://github.com/jd-opensource/JoyAI-Video-Edit.

</details>


### [118] [Video-DeepResearch: Towards the Next-Generation Multimodal Deepresearch Agent](https://arxiv.org/abs/2608.03979)
*Zhen Fang,Yu Zeng,Wenxuan Huang,Yiming Zhao,Shiting Huang,Tianfei Ren,Qi Lu,Qingnan Ren,Qisheng Su,Lionel Z. Wang,Qingyu Yin,Shuang Chen,Zehui Chen,Lin Chen,Zhenfei Yin,Yao Hu,Shaohui Lin,Wanli Ouyang,Shaosheng Cao,Feng Zhao*

Main category: cs.CV

TL;DR: Introduces Video-DeepResearch (Video-DR), a framework extending multimodal agents to continuous video streams with dense spatiotemporal grounding and open-web exploration. It addresses modality bias and parametric knowledge leakage through a decoupled perception-exploration pipeline with stage-wise tool unlocking and a two-stage training recipe (SFT + GRPO). Reports state-of-the-art performance on the new Video-DR-Bench benchmark.


<details>
  <summary>Details</summary>
Motivation: Current multimodal agents perform poorly on continuous video streams, exhibiting two critical bottlenecks: (1) modality bias, where agents bypass visual tools in favor of textual search, and (2) parametric knowledge leakage, where models rely on internal memory instead of genuine tool-augmented execution. These issues highlight the need for a framework that forces exhaustive cross-frame visual grounding before web retrieval.

Method: Video-DR features a decoupled perception-exploration pipeline with stage-wise tool unlocking that compels exhaustive cross-frame visual grounding prior to web retrieval. It uses a two-stage training recipe: supervised fine-tuning followed by Group Relative Policy Optimization (GRPO), enabling autonomous exploration that breaks the imitation-learning ceiling. The authors also curated Video-DR-Bench, a human-AI collaborative benchmark with 200 complex, multi-hop VQA instances.

Result: Video-DeepResearch-35B-A3B achieves 64.0% average accuracy on Video-DR-Bench, surpassing Claude-4.5-Sonnet (59.0%) by 5.0 points and outperforming GPT-5 (52.5%) and Gemini 2.5 Pro (57.5%). The 30B-A3B variant achieves 59.3%, competitive with Claude-4.5-Sonnet, demonstrating effectiveness even at compact scale.

Conclusion: The proposed training paradigm effectively addresses modality bias and knowledge leakage in video-based multimodal agents, establishing a new state-of-the-art while showing scalability to smaller model sizes.

Abstract: We introduce Video-DeepResearch (Video-DR), extending multimodal agents from static images to continuous video streams, a setting that demands dense spatiotemporal grounding coupled with open-web exploration. Preliminary evaluations reveal two critical bottlenecks in current models: (1) modality bias, where agents bypass visual tools in favor of textual search, and (2) parametric knowledge leakage, where models rely on internal memory rather than genuine tool-augmented execution. To address these challenges, we propose Video-DR, featuring a decoupled perception-exploration pipeline with stage-wise tool unlocking that compels exhaustive cross-frame visual grounding prior to web retrieval. Our framework adopts a two-stage training recipe: supervised fine-tuning followed by Group Relative Policy Optimization (GRPO), enabling autonomous exploration that breaks the imitation-learning ceiling. Furthermore, we curate Video-DR-Bench, a human-AI collaborative benchmark comprising 200 complex, multi-hop VQA instances. Empirical results demonstrate that our Video-DeepResearch-35B-A3B establishes a new state-of-the-art of 64.0% average accuracy, surpassing proprietary Claude-4.5-Sonnet (59.0%) by 5.0 points and significantly outperforming GPT-5 (52.5%) and Gemini 2.5 Pro (57.5%). The 30B-A3B variant achieves 59.3%, competitive with Claude-4.5-Sonnet and demonstrating the effectiveness of our training paradigm even at compact scale. Code: https://github.com/Osilly/Vision-DeepResearch.

</details>


### [119] [Perceptual Anchoring: Prototype-Guided Text Calibration for Training-free Open-Vocabulary Semantic Segmentation](https://arxiv.org/abs/2608.03991)
*Wanli Ma,Jiangwen Lu,Qinmu Peng,Xinge You*

Main category: cs.CV

TL;DR: PTC (Prototype-Guided Text Calibration) improves training-free open-vocabulary semantic segmentation by calibrating text embeddings using visual prototypes derived from reliable visual evidence, reducing the semantic gap between generic text concepts and instance-specific visual appearances without additional training or external models.


<details>
  <summary>Details</summary>
Motivation: Existing training-free OVSS methods treat text embeddings as fixed classification references with only generic category concepts, creating a semantic gap with visual representations that capture specific instance appearances. This gap leads to incomplete masks and erroneous predictions in non-target regions.

Method: PTC operates in two stages: (1) Perceiving stage - selects reliable visual evidence based on initial matching scores to build category-specific visual prototypes; (2) Anchoring stage - calibrates text embeddings using these prototypes, with calibration strength adaptively adjusted based on the amount of visual evidence. It is a plug-and-play module requiring no training or external models.

Result: Extensive experiments across eight benchmarks show PTC significantly improves six representative methods, yielding more complete and accurate segmentation results while preserving generic category semantics and open-vocabulary generalization.

Conclusion: PTC validates that calibrating text embeddings with perceptual evidence is a simple and effective approach to improving visual-text alignment for training-free OVSS, without sacrificing generalization.

Abstract: Training-free open-vocabulary semantic segmentation (OVSS) partitions an image into semantically distinct regions based on arbitrary text descriptions, without learning any additional parameters. However, existing methods typically focus on improving visual representations while treating text embeddings that encode only generic category concepts as fixed classification references. The resulting semantic gap between these generic concepts and the visual representations that capture the specific appearances of target instances often causes incomplete masks and erroneous predictions in non-target regions. Inspired by the symbol-percept correspondence underlying perceptual anchoring, we propose Prototype-Guided Text Calibration (PTC) for training-free OVSS. In the Perceiving stage, PTC selects reliable visual evidence based on initial matching scores to construct category-specific visual prototypes. In the Anchoring stage, PTC uses these prototypes to calibrate their corresponding text embeddings, with the calibration strength adaptively adjusted based on the amount of visual evidence. Consequently, the calibrated text embeddings align more accurately with instance-specific visual representations while preserving generic category semantics and open-vocabulary generalization. Moreover, PTC requires neither additional training nor external models and can serve as a plug-and-play module for existing methods. Extensive experiments across eight benchmarks show that PTC significantly enhances the performance of six representative methods and yields more complete and accurate segmentation results. These results validate PTC as a simple and effective approach to improving visual-text alignment.

</details>


### [120] [ParVL: Parallel Scaling and Expandable Compute Allocation for Multimodal LLMs](https://arxiv.org/abs/2608.04010)
*Yang Yang,Qinyu Zhao,Mouxiang Chen,Xiaohui Li,Lixin Gu,Wenhai Wang,Hongjie Zhang,Wenwei Zhang*

Main category: cs.CV

TL;DR: ParVL is a parallel vision-language scaling framework for MLLMs that reuses existing ViT and LLM backbone parameters across multiple branches to scale computation without increasing parameters or sequential latency, systematically studying the vision-language computation allocation trade-off.


<details>
  <summary>Details</summary>
Motivation: Existing MLLM scaling strategies (increasing model parameters or sequential inference computation) incur substantial memory or latency overhead and fail to alter the rigid, fixed computation allocation between ViT and LLM components, limiting task-specific optimization.

Method: ParVL scales parallel computation by reusing existing ViT and LLM backbone parameters across multiple vision and language branches, instantiating each parallel stream with branch-specific prefix parameters over a shared backbone. The model is trained end-to-end via full-parameter supervised fine-tuning on roughly 13B tokens, systematically studying the computation-allocation trade-off between ViT encoder and LLM decoder.

Result: ParVL improves overall multimodal performance over same-recipe single-branch baselines, and the best evaluated vision-language allocation varies across tasks.

Conclusion: Parallel computation allocation between vision and language modalities is a key design dimension for MLLMs, and ParVL demonstrates that shared-backbone parallel branches can outperform single-branch baselines while also revealing that optimal vision-language allocation is task-dependent.

Abstract: Existing scaling strategies for Multimodal Large Language Models (MLLMs) typically expand either model parameters or sequential inference computation, incurring substantial memory or latency overhead. More importantly, most existing methods fail to alter the rigid, fixed computation allocation between the Vision Transformer and the Large Language Model components, limiting task-specific optimization. To address this, we introduce the Parallel Vision-Language (ParVL) scaling framework for MLLMs, which scales parallel computation by reusing the existing ViT and LLM backbone parameters across multiple vision and language branches. This framework raises a central question: given a fixed backbone parameter budget, how should additional shared-backbone computation be allocated between the vision and language modalities? We instantiate each parallel computational stream with branch-specific prefix parameters over a shared backbone, and train the entire model end-to-end via full-parameter supervised fine-tuning on roughly 13B tokens. We systematically study the computation-allocation trade-off between the ViT encoder and LLM decoder. ParVL improves overall multimodal performance over same-recipe single-branch baselines, and the best evaluated vision--language allocation varies across tasks. Code is available at https://github.com/YangYangGirl/ParVL.

</details>
