<div id=toc></div>

# Table of Contents

- [cs.CV](#cs.CV) [Total: 67]


<div id='cs.CV'></div>

# cs.CV [[Back]](#toc)

### [1] [What to Preserve, Where to Adapt: A Depth-Wise Analysis of Forgetting in Continual Gynecological Image Segmentation](https://arxiv.org/abs/2608.13660)
*Amal Saqib,Tausifa Jan Saleem,Numan Saeed,Mohammad Yaqub*

Main category: cs.CV

TL;DR: This paper studies catastrophic forgetting in continual learning for gynecological medical image segmentation, showing through block-wise ablation and controlled adaptation experiments that forgetting is strongly influenced by where parameter updates occur across network depth—with updates to shallow encoders and decoders causing the most severe forgetting.


<details>
  <summary>Details</summary>
Motivation: Medical image segmentation models are typically trained on static datasets, but clinical practice requires sequential learning across heterogeneous data (varying imaging modalities, anatomical structures, annotation protocols). Existing continual learning methods fail under these large distribution shifts due to catastrophic forgetting, and there is limited understanding of how network architecture regions contribute to this forgetting.

Method: The paper uses block-wise ablation analysis to identify which encoder-decoder regions most affect segmentation performance, and controlled adaptation experiments to investigate how restricting updates to specific network regions (e.g., bottleneck-adjacent areas vs. shallow encoders/decoders) influences forgetting during continual gynecological image segmentation.

Result: Ablating early encoder and late decoder regions causes the largest performance degradation, indicating uneven dependence across the network hierarchy. Forgetting remains limited when updates are restricted to bottleneck-adjacent regions, but increases sharply when shallower encoders and decoders become trainable—even with only a small subset of parameters updated.

Conclusion: Forgetting in encoder-decoder architectures is strongly influenced by where updates occur across network depth during continual learning, providing insights that could inform more effective continual learning strategies for medical image segmentation.

Abstract: Medical image segmentation models are typically trained under the assumption that all data are available simultaneously. However, in clinical practice, datasets often arrive sequentially, requiring models to adapt continuously to evolving data distributions. We study this problem in gynecological image segmentation, where substantial heterogeneity across imaging modalities, anatomical structures, and annotation protocols creates a particularly challenging continual learning setting. Under these large distribution shifts, existing continual learning methods struggle to preserve previously learned knowledge, leading to catastrophic forgetting. To better understand forgetting in this setting, we investigate how different encoder--decoder regions influence segmentation performance and forgetting during continual gynecological segmentation. Through block-wise ablation analysis, we observe that ablating early encoder and late decoder regions results in the largest performance degradation, indicating that segmentation performance depends unevenly across the network hierarchy. Using controlled adaptation experiments, we further show that forgetting remains limited when updates are restricted to bottleneck-adjacent regions, but increases sharply once shallower encoders and decoders become trainable, even when only a small subset of parameters is updated. These findings suggest that forgetting in the encoder-decoder architecture is strongly influenced by where updates occur across network depth during continual learning. Full code and analysis pipelines will be made publicly available upon acceptance.

</details>


### [2] [Multiphase-Diff: Diffusion-Based Generative Modeling for High-Contrast Multiphase Physical Systems with Sharp Interfaces](https://arxiv.org/abs/2608.13669)
*Yining Huang,Zhenyu Liang*

Main category: cs.CV

TL;DR: Multiphase-Diff is a physics-constrained diffusion framework for high-contrast, sharp-interface multiphase fields that addresses three coupled challenges: singular gradient terms at coefficient jumps, low-magnitude phase erasure under extreme contrast, and unbalanced global likelihood scaling. It introduces a conservative flux residual, an analytic bijective signal representation with exponential decoding, and Jacobi-preconditioned likelihood, outperforming seven baselines across three benchmarks.


<details>
  <summary>Details</summary>
Motivation: Physics-constrained diffusion for high-contrast multiphase fields faces three coupled difficulties: (i) pointwise strong-form PDE residuals contain singular gradient terms at coefficient jumps that penalize physical interfaces; (ii) under extreme contrast, low-magnitude phases fall below the noise floor and are erased or misscaled; and (iii) a single global likelihood scale allows high-magnitude phases to dominate supervision.

Method: Multiphase-Diff makes three contributions: (i) a conservative flux residual that avoids differentiating discontinuous coefficients and enforces discrete conservation; (ii) an analytic bijective representation mapping low-amplitude signals to order-one latent scales with exponential decoding guaranteeing coefficient positivity; and (iii) a Jacobi-preconditioned likelihood that normalizes local residual scales for balanced supervision.

Result: Experiments on three multiphase benchmarks demonstrate superiority over seven baselines in both physical and distributional fidelity, with robustness across phase contrasts and compositions.

Conclusion: Multiphase-Diff establishes effectiveness for scientific sample generation in the challenging high-contrast, sharp-interface multiphase regime.

Abstract: Physics-constrained diffusion for high-contrast, sharp-interface multiphase fields faces three coupled difficulties. At coefficient jumps, expanded pointwise strong-form PDE residuals contain singular gradient terms that can penalize physical interfaces. Under extreme contrast, low-magnitude phases may fall below the diffusion noise floor and be erased, misscaled, or generated with negative coefficients, while a global likelihood scale allows high-magnitude phases to dominate supervision. We therefore propose Multiphase-Diff, which makes three corresponding contributions: (i) a conservative flux residual that avoids differentiating discontinuous coefficients and enforces discrete conservation; (ii) an analytic bijective representation that maps low-amplitude signals to order-one latent scales and guarantees coefficient positivity through exponential decoding; and (iii) a Jacobi-preconditioned likelihood that normalizes local residual scales for balanced supervision. Experiments on three complementary multiphase benchmarks demonstrate the superiority of Multiphase-Diff over seven baselines in both physical and distributional fidelity and its robustness across phase contrasts and compositions, establishing its effectiveness for scientific sample generation in this challenging regime.

</details>


### [3] [PROVE: Training-Free Prompt Recovery using Verifiable Evidence](https://arxiv.org/abs/2608.13671)
*Rupayan Mallick,Mahsa Khoshnoodi,Sarah Adel Bargal*

Main category: cs.CV

TL;DR: PROVE is a training-free, black-box prompt inversion attack that reconstructs prompts by composing verifiable scene descriptions grounded in explicit image evidence, outperforming optimization, captioning, and RL-based baselines on MS-COCO, Flickr30K, and Lexica without any training or generator access.


<details>
  <summary>Details</summary>
Motivation: As text-to-image models and prompt marketplaces proliferate, recovered prompts can enable unauthorized reproduction and redistribution of copyrighted creative works. Existing prompt inversion methods (optimization-based, captioning, RL-based) suffer from unreadable prompts, hallucinated details, and overfitting with evaluation circularity, motivating a more reliable and auditable approach.

Method: PROVE reconstructs prompts by composing verifiable scene descriptions rather than optimizing token sequences. It is training-free and black-box, formalized through a precision-constrained recall maximization objective, ensuring every recovered claim is grounded in explicit image evidence.

Result: Across MS-COCO, Flickr30K, and Lexica with state-of-the-art text-to-image generators, PROVE consistently outperforms optimization, captioning, and RL-based baselines on image similarity (DINO, LPIPS) and text-image alignment (CLIP), without training, generator access, or fine-tuning.

Conclusion: PROVE demonstrates a stronger and more practical prompt inversion attack, offering fully auditable prompts with verified evidence, addressing critical copyright and content ownership concerns in AI-generated content.

Abstract: Modern text-to-image models can generate highly realistic images from natural-language prompts, while recent advances in prompt inversion have made it increasingly feasible to recover those prompts from generated outputs, raising new concerns for copyright protection and content ownership. As prompt marketplaces emerge, recovered prompts can enable both the unauthorized reproduction and redistribution of copyrighted creative works, and the exposure of the prompts that encode an artist's creative recipe in AI-generated content. Existing prompt inversion methods rely on gradient-based optimization, autoregressive captioning, or reinforcement learning. However, optimization-based methods often produce unreadable prompts, captioning methods hallucinate unverified details, and RL-based approaches frequently overfit to specific generators while introducing evaluation circularity. We introduce PROVE (Prompt Recovery with Verified Evidence), a training-free, black-box prompt inversion attack that reconstructs prompts by composing verifiable scene descriptions rather than optimizing token sequences, targeting both original copyrighted works and AI-generated content. The resulting prompts are fully auditable, with every recovered claim grounded in explicit image evidence, and are formalized through a precision-constrained recall maximization objective. Across MS-COCO, Flickr30K, and Lexica, using state-of-the-art text-to-image generators, PROVE consistently outperforms optimization, captioning, and RL-based baselines on image similarity (DINO, LPIPS) and text-image alignment (CLIP), without any training, generator access, or fine-tuning, demonstrating a stronger and more practical prompt inversion attack.

</details>


### [4] [MedPlex: Deep Vision-Language Co-Adaptation for Clinically Grounded Medical Segmentation](https://arxiv.org/abs/2608.13690)
*Rafi Ibn Sultan,Hui Zhu,Chengyin Li,Dongxiao Zhu*

Main category: cs.CV

TL;DR: MedPlex is an end-to-end Vision-Language Model framework that integrates text guidance continuously throughout the segmentation encoding hierarchy via Bidirectional Fusion, along with class-level and region-level concept alignment, achieving state-of-the-art results on CT and MR multi-organ, cardiac, and tumor segmentation benchmarks.


<details>
  <summary>Details</summary>
Motivation: Medical image segmentation is treated as a vision-only problem despite clinical interpretation relying on textual knowledge (anatomy, location, appearance, context). Existing VLM-based text-guided methods only use language as a late conditioning signal, which limits its influence on visual representation learning.

Method: MedPlex introduces a bidirectional fusion mechanism that allows visual and textual representations to evolve jointly across the encoding hierarchy. It employs class-level concept alignment to anchor anatomical targets to clinical concept profiles, and region-level alignment to preserve individual concepts (shape, location, appearance, texture) through class-specific visual evidence, making text guidance a continuous component throughout the encoder.

Result: MedPlex achieves state-of-the-art performance on CT and MR benchmarks for multi-organ, cardiac substructure, and tumor segmentation, including settings with real free-text clinical supervision.

Conclusion: Language can provide structured supervision throughout the encoder rather than serving only as a late-stage cue, significantly improving medical image segmentation performance.

Abstract: Medical image segmentation is still largely treated as a vision-only problem, although clinical interpretation often relies on textual knowledge of anatomy, location, appearance, and surrounding context. Existing text-guided segmentation methods within the Vision-Language Model (VLM) paradigm often use language only as a late conditioning signal, limiting its influence on visual representation learning. We introduce MedPlex (Medical Plexus of Vision and Language), an end-to-end VLM framework that makes text guidance a continuous, clinically grounded component of segmentation learning. Through Bi-Fusion (Bidirectional Fusion), visual and textual representations evolve jointly across the encoding hierarchy. MedPlex further introduces class-level and region-level concept alignment to organize the shared representation at complementary granularities. Class-level alignment anchors each anatomical target to an aggregated clinical concept profile, while region-level alignment preserves individual concepts, such as shape, location, appearance, and texture, through class-specific visual evidence. In this way, language provides structured supervision throughout the encoder rather than serving only as a late-stage cue. MedPlex achieves state-of-the-art performance across CT and MR benchmarks for multi-organ, cardiac substructure, and tumor segmentation, including settings with real free-text clinical supervision. Code: https://github.com/rafiibnsultan/MedPlex.

</details>


### [5] [Limitations of Synthetic Data Generation in Specialized Data-Scarce Domains](https://arxiv.org/abs/2608.13729)
*Edward Zhang,Marcel Hussing,Tanay Tandon,Shenbagaraj Kannapiran,Jason Hughes,Youkang Wang,Joshua Caswell,Agelos Kratimenos,Yi Fan Li,Milan Manoj,Ethan Sanchez,Sumukh Shrote,Camillo Jose Taylor,Daniel A. Hashimoto,Eric Eaton*

Main category: cs.CV

TL;DR: This paper evaluates the effectiveness of generative models (distribution modeling and sample perturbation) for synthetic data augmentation in sparse, high-variance medical imaging domains (5 trauma classification tasks). Results show no generative approach consistently beats strong non-generative baselines, with feature-space analysis revealing failure modes including memorization/collapse, distributional drift, and generation of simplified canonical instances.


<details>
  <summary>Details</summary>
Motivation: While synthetic image generation has shown promise for data scarcity in natural image benchmarks like ImageNet, its effectiveness in sparse, high-variance real-world domains (where images differ substantially from common datasets and data is expensive to obtain) remains unclear.

Method: Evaluates two schools of generative sparse data extension—distribution modeling and sample perturbation—against non-generative data augmentation baselines across five trauma classification tasks using subject-wise train-validation splits, with downstream classifier performance and feature-space analysis.

Result: No generative approach consistently outperforms a strong non-generative baseline. Feature-space analysis identifies three recurring failure modes: memorization or collapse, distributional drift, and generation of visually plausible but simplified canonical instances that are easier to classify than real data.

Conclusion: Generative data augmentation does not provide reliable advantages over non-generative baselines in sparse, high-variance medical imaging domains, and produces simplified or degraded samples despite visual plausibility.

Abstract: Advances in diffusion-based generative models have motivated the use of synthetic image generation to alleviate data scarcity in vision tasks. While this strategy has shown promise in natural image benchmarks such as ImageNet, its effectiveness in sparse, high-variance real-world domains remains unclear. In this work, we focus on domains where images differ substantially from common image datasets and additional data are expensive to obtain. Against non-generative data augmentation baselines, we evaluate the downstream classifier performance improvements yielded by two schools of generative sparse data extension: distribution modeling and sample perturbation. Across five trauma classification tasks using subject-wise train--validation splits, no generative approach consistently outperforms a strong non-generative baseline. Feature-space analysis reveals recurring failure modes: memorization or collapse, distributional drift, and generation of visually plausible but simplified canonical instances that are easier to classify than real data.

</details>


### [6] [CAST: Closed-form Analytic Semantic Transfer for Zero-Shot Classifier Extension](https://arxiv.org/abs/2608.13751)
*William Heyden,Habib Ullah,Muhammad Salman Siddiqui,Fadi Al Machot*

Main category: cs.CV

TL;DR: CAST is a training-free, image-free framework for zero-shot learning that extends pre-trained classifiers to unseen classes via closed-form weight injection, with a theoretical error decomposition featuring the computable semantic extrapolation residual ρ_u for dataset curation.


<details>
  <summary>Details</summary>
Motivation: Adapting large pre-trained models to novel categories typically requires examples from the target distribution, which are often unavailable in many domains. Zero-shot learning addresses this via auxiliary semantic information, but existing approaches may require iterative optimization or target-distribution examples.

Method: CAST (Closed-form Analytic Semantic Transfer) extends a pre-trained classifier to unseen classes through weight injection in a closed-form manner, requiring no training, no images, and no iterative optimization. It also derives a finite-sample error decomposition identifying the semantic extrapolation residual ρ_u, a computable and model-agnostic measure.

Result: CAST matches or exceeds existing image-free approaches on standard zero-shot learning benchmarks and approaches the performance of few-shot adaptation methods, while requiring neither iterative optimization nor target-distribution examples.

Conclusion: CAST provides a principled, theoretically grounded, training-free alternative for zero-shot learning, and the semantic extrapolation residual offers a practical criterion for dataset curation and benchmark design.

Abstract: Large pre-trained models have become foundational components of modern machine learning systems. Yet adapting these models to novel categories typically requires examples from the target distribution. In many domains, however, such data are unavailable. Zero-shot learning (ZSL) permits recognition under these limitations through relying on auxiliary semantic information such as textual descriptions. We introduce CAST (Closed-form Analytic Semantic Transfer), a training-free, image-free framework for extending a pre-trained classifier to previously unseen classes through weight injection. We provide a theoretical foundation for CAST and derive a finite-sample error decomposition that identifies the \emph{semantic extrapolation residual} $ρ_u$. The residual is a computable, model-agnostic measure and provides a principled criterion for dataset curation and benchmark design. Experiments on standard zero-shot learning benchmarks demonstrate that CAST matches or exceeds existing image-free approaches and approaches the performance of few-shot adaptation methods, while requiring neither iterative optimization nor examples from the target distribution.

</details>


### [7] [ChartProbe: A Diagnostic Study on Visual Reasoning through Perception, Grounding, and Simple Reasoning](https://arxiv.org/abs/2608.13766)
*Mahsa Khoshnoodi,Sarah Adel Bargal*

Main category: cs.CV

TL;DR: ChartProbe is a diagnostic framework that generates probes directly from chart-rendering code, decomposing complex chart reasoning into three skills (perception, grounding, simple reasoning). Fine-tuning models on simple skills alone yields large gains on held-out complex-reasoning questions without any complex-reasoning supervision, across three out-of-distribution settings.


<details>
  <summary>Details</summary>
Motivation: VLMs remain unreliable on chart questions requiring reasoning over visual quantities, and this weakness is typically attributed to a reasoning deficit and addressed with more reasoning supervision. The authors question whether the difficulty lies in reasoning itself or in simpler foundational skills (perception, grounding, simple reasoning) that complex reasoning operates on.

Method: ChartProbe generates diagnostic probes directly from the code that renders each chart, ensuring exact gold answers without human annotation and attributing each failure to a single skill. The intervention withholds complex questions and reasoning traces entirely, fine-tunes on one simple skill at a time, and measures transfer to held-out complex-reasoning questions across three open-weight VLMs.

Result: Supervising simpler skills alone produces large gains on complex-reasoning questions the model never trained on. The gains hold across three out-of-distribution settings: unseen pie charts, human-written ChartQA benchmark, and non-chart CLEVR domain. Where skills are weak and the model can be taught to read the image, training them recovers much of complex reasoning at no reasoning-data cost.

Conclusion: Complex visual reasoning can improve without complex-reasoning supervision, suggesting that the bottleneck in chart reasoning may be foundational skills rather than reasoning itself.

Abstract: Vision-language models (VLMs) remain unreliable on chart questions that require reasoning over visual quantities, and this weakness is usually attributed to a reasoning deficit and addressed with more reasoning supervision. We ask whether the difficulty lies in reasoning itself, or in the simpler skills that reasoning operates on: reading the plotted elements (\emph{perception}), locating them and binding them to their labels (\emph{grounding}), and performing single-step computations such as ranking, totals, and differences (\emph{simple reasoning}). We introduce \textbf{ChartProbe}, a diagnostic framework whose probes are generated directly from the code that renders each chart, so every gold answer is exact by construction, needs no human annotation, and attributes each failure to a single skill. ChartProbe enables an intervention prior work does not attempt: instead of synthesizing complex-reasoning data, we withhold complex questions and reasoning traces entirely, fine-tune on one simple skill at a time, and measure transfer to held-out complex-reasoning questions. Across three open-weight VLMs, supervising the simpler skills alone produces large gains on complex-reasoning questions the model never trained on: where these skills are weak and the model can be taught to read the image, training them recovers much of complex reasoning at no reasoning-data cost. The gains hold across three out-of-distribution settings: an unseen chart type (pie charts), a human-written benchmark disjoint from our images and templates (ChartQA), and a non-chart visual domain (CLEVR). Complex visual reasoning can therefore improve without complex-reasoning supervision.

</details>


### [8] [Kolmogorov-Arnold Networks for Spatially Independent Multispectral Land Classification](https://arxiv.org/abs/2608.13769)
*Katherine L. Bauer,Teemu Harkonen,Simo Sarkka,Arturo Sanchez-Azofeifa*

Main category: cs.CV

TL;DR: This study evaluates the Kolmogorov-Arnold network (KAN) for land classification using Landsat 8 imagery, comparing it against random forest and multilayer perceptron models. KAN matched random forest accuracy and outperformed the multilayer perceptron on an independent Calgary dataset while requiring substantially fewer trainable parameters and offering greater interpretability.


<details>
  <summary>Details</summary>
Motivation: Land classification from satellite imagery is critical for land management, environmental monitoring, and urban planning, and there is ongoing interest in finding architectures that are both accurate and compact.

Method: The Kolmogorov-Arnold network was trained and tested on Landsat 8 imagery from Edmonton, Alberta, and evaluated on an independent dataset from Calgary, Alberta across five land classes (agriculture, urban, water, forest, and bare ground). It was compared with random forest and multilayer perceptron baselines.

Result: On the Calgary dataset, the Kolmogorov-Arnold network matched random forest accuracy and outperformed the multilayer perceptron, while using substantially fewer trainable parameters and providing greater interpretability.

Conclusion: The Kolmogorov-Arnold network is a viable compact and interpretable alternative to traditional machine learning methods for land classification from multispectral satellite imagery.

Abstract: Land classification from satellite imagery is important for land management, environmental monitoring, and urban planning. Machine learning methods such as random forests and multilayer perceptrons have shown strong performance on multispectral data, while the Kolmogorov-Arnold network has emerged as an alternative architecture with compact model structures. This study evaluates the Kolmogorov-Arnold network for land classification using Landsat 8 imagery and compares it with random forest and multilayer perceptron models. The models were trained and tested on data from Edmonton, Alberta and evaluated on an independent dataset from Calgary, Alberta across five land classes: agriculture, urban, water, forest, and bare ground. For the Calgary dataset, the Kolmogorov-Arnold network matched the accuracy of the random forest and outperformed the multilayer perceptron, while requiring substantially fewer trainable parameters and providing greater interpretability.

</details>


### [9] [Doomed to Re-Annotate, Forever: The ImageNet Story](https://arxiv.org/abs/2608.13783)
*Illia Volkov,Nikita Kisel,Tetiana Mishkina,Klara Janouskova,Jiri Matas*

Main category: cs.CV

TL;DR: The paper presents ReImageNet, a comprehensive reannotation of the ImageNet-1k validation set that goes beyond prior correction attempts by including multilabel correction, object localization, revised class definitions, and semantic attributes. The reannotation reveals ~12% of original labels are incorrect, 33.3% of images are multilabel, and 3.8% contain no relevant object. New labels boost top-1 accuracy by up to 1.2% for supervised models and 5-6% for MLLMs. The work introduces an iterative annotation pipeline with human-LLM collaboration and releases all annotations publicly.


<details>
  <summary>Details</summary>
Motivation: ImageNet-1k remains the most commonly reported benchmark in visual recognition, yet its 2012 noisy labels are still predominantly used despite repeated reports of quality issues. Prior correction attempts have been insufficient, motivating a comprehensive reannotation effort to obtain accurate and complete validation set annotations.

Method: The authors build a repeated refinement pipeline with error checking, arguing that annotation at ImageNet scale cannot be done in one pass. They employ human and LLM collaboration with appropriate tooling, which they identify as the current quality ceiling. The reannotation includes multilabel correction, object localization, revised class definitions, and semantic attributes (text-recognition, rendition, reflection, crowd, dominant).

Result: The reannotation reveals approximately 12% of original ImageNet-1k labels are incorrect, 33.3% of images are multilabel, and 3.8% contain no object from an ImageNet-1k class. With new labels, top-1 accuracy increases by up to 1.2% for supervised models and 5-6% for MLLMs. The issues propagate into derivative test sets, indicating the problem is structural.

Conclusion: ImageNet-1k labeling issues are structural and propagate into derivative benchmarks. Comprehensive reannotation with iterative refinement and human-LLM collaboration yields significantly better labels, and all annotations, class definitions, guidelines, and code are publicly released for the community.

Abstract: Top-1 accuracy on ImageNet-1k remains the most commonly reported metric in visual recognition. Quality issues with the dataset have been repeatedly reported, yet the original 2012 noisy labels are still predominantly used. The paper presents a comprehensive effort, which goes well beyond prior correction attempts, towards obtaining accurate and complete ImageNet-1k validation set annotations. The result, ReImageNet, includes multilabel correction, object localization, revised class definitions, and semantic attributes (text-recognition, rendition, reflection, crowd, dominant). The reannotation reveals that approximately 12% of the original ImageNet-1k labels are incorrect, 33.3% of images are multilabel and 3.8% contain no object from an ImageNet-1k class. With the new labels, top-1 accuracy increases by up to 1.2% for supervised models and by 5-6% for MLLMs. We argue that annotation at ImageNet scale cannot realistically be completed in one pass, as errors and definitional issues are discovered only through annotating, and we build our pipeline around repeated refinement and error checking. We observed that human and LLM collaboration with appropriate tooling represents the current quality ceiling for annotation at this scale. ImageNet-1k issues propagate into its derivative test sets, indicating that the problem is structural rather than specific to any single benchmark. All annotations, class definitions, guidelines, and analysis code have been publicly released. Project page: https://vrg.fel.cvut.cz/reimagenet Annotations: https://huggingface.co/datasets/vrg-prague/ReImageNet Code: https://github.com/klarajanouskova/ImageNet

</details>


### [10] [Face Re-morphing: Differential Morphing Attack Detection via Feature-Space Similarity Changes](https://arxiv.org/abs/2608.13858)
*Jie Jin,Masakatsu Nishigaki,Tetsushi Ohki*

Main category: cs.CV

TL;DR: This paper proposes Face Re-morphing, a D-MAD method that detects face morphing attacks by applying an additional morphing operation and measuring the change in cosine similarity between document-live and live-re-morphed image pairs in feature space. The method shows effectiveness across various morphing conditions and performs favorably on FRLL-Morphs and FEI Morph datasets.


<details>
  <summary>Details</summary>
Motivation: Existing D-MAD methods rely on static feature differences, constituent-face reconstruction, or multi-cue fusion, which have limitations. The paper aims to explore a novel, complementary detection cue based on the response to an additional morphing operation in feature space.

Method: The method takes a document image and a trusted live image, generates a re-morphed image by applying an additional morphing operation, and uses the change between document-live and live-re-morphed cosine similarities as the detection score.

Result: Experiments on FRLL-Morphs and FEI Morph show the proposed cue is effective across different morphing conditions, re-morphing methods, and face recognition models. Favorable results are shown on AMSL, and the method performs well under the Criminal condition on FEI Morph Version 1, particularly with MorDIFF.

Conclusion: Re-morphing-induced similarity change provides a complementary and effective cue for differential morphing attack detection.

Abstract: Face morphing attacks pose a serious threat to face recognition systems because a single morphed document image can be matched to multiple contributors. Differential morphing attack detection (D-MAD) addresses this threat by comparing a document image with a trusted live image, but existing methods often rely on static feature differences, constituent-face reconstruction, or multi-cue fusion. This paper proposes Face Re-morphing, a D-MAD method that uses the feature-space response to an additional morphing operation as a detection cue. Given a document image and a trusted live image, the proposed method generates a re-morphed image and uses the change between the document--live and live--re-morphed cosine similarities as the detection score. Experiments on FRLL-Morphs and FEI Morph show that the proposed cue is effective across different morphing conditions, re-morphing methods, and face recognition models. Comparisons with existing methods show favorable results on AMSL and indicate that the proposed method performs well under the Criminal condition on FEI Morph Version~1, particularly when using MorDIFF. These results indicate that re-morphing-induced similarity change provides a complementary cue for D-MAD.

</details>


### [11] [XSA-MAD: Cross-modal Semantic Alignment for Morphing Attack Detection](https://arxiv.org/abs/2608.13861)
*Jie Jin,Mahiro Tokumasu,Yu Makino,Masakatsu Nishigaki,Tetsushi Ohki*

Main category: cs.CV

TL;DR: XSA-MAD is a CLIP-based multimodal framework for morphing attack detection that explicitly models semantic inconsistencies between bona-fide and morphed faces using four interpretable attributes (identity, facial geometry, texture, consistency), achieving strong cross-dataset generalization with an EER of 2.92% on GAN-based morphs.


<details>
  <summary>Details</summary>
Motivation: Existing image-based morphing attack detection (MAD) methods rely solely on visual cues and generalize poorly to unseen generation techniques, limiting their practical applicability against diverse and evolving morphing attacks.

Method: The proposed XSA-MAD is a CLIP-based multimodal framework that decomposes morphing concepts into four interpretable attributes (identity, facial geometry, texture, and consistency), encodes them as structured and attribute-aware textual representations, and progressively aligns the image encoder with this discriminative textual space to produce a unified semantic representation.

Result: Experiments on MAD22 and MorDIFF following training on SMDD demonstrate strong generalization across diverse morphing principles. XSA-MAD achieves an equal error rate of 2.92% on GAN-based morphs and consistently outperforms existing methods under high-fidelity generative attacks.

Conclusion: By capturing generation-invariant, concept-level semantic discrepancies rather than relying on superficial visual cues, multimodal semantic modeling significantly improves generalization to unseen morphing attack techniques.

Abstract: Morphing attacks pose a serious threat to face recognition systems. However, existing image-based morphing attack detection (MAD) methods often generalize poorly to unseen generation techniques because they rely solely on visual cues. We propose XSA-MAD, a CLIP-based multimodal framework that explicitly models semantic inconsistencies between bona-fide and morphed faces. Morphing concepts are decomposed into four interpretable attributes, including identity, facial geometry, texture, and consistency, and are encoded as structured and attribute-aware textual representations. The image encoder is progressively aligned with this discriminative textual space, resulting in a unified semantic representation that captures generation-invariant and concept-level discrepancies between bona-fide and morph images. Experiments on MAD22 and MorDIFF, following training on SMDD, demonstrate strong generalization across diverse morphing principles. In particular, XSA-MAD achieves an equal error rate of 2.92% on GAN-based morphs and consistently outperforms existing methods under high-fidelity generative attacks.

</details>


### [12] [Attention Capture Is Not Detection: A Two-Stage Account of How Humans Miss Localized AI Image Edits](https://arxiv.org/abs/2608.13865)
*Chiao-Chieh Deng*

Main category: cs.CV

TL;DR: This paper argues that AI-edit detectability is not a single, undifferentiated property. Using an eye-tracking study (N=59) and mixed-effects analysis, the authors show that whether an edit is noticed (attention capture) and whether it is correctly judged as fake are dissociable stages: edit area drives attention capture while semantic plausibility drives judgment accuracy and look-but-fail-to-see (LBFS) errors. They further show a Transformer-based generative eye-movement model can operationalize the attention-capture stage with strong discriminative power.


<details>
  <summary>Details</summary>
Motivation: Platforms that curb AI-generated disinformation currently treat detectability as binary—an edit either receives a warning or not. The authors argue this is an oversimplified model and that a more nuanced, two-stage account (attention capture vs. judgment) is needed to understand and ultimately combat AI-edit disinformation.

Method: The study employs a controlled eye-tracking experiment with 59 participants using a Latin-square design, crossing two factors (edit area and semantic plausibility). Mixed-effects modeling analyzes the dissociation between attention capture and judgment accuracy. A Transformer-based generative model of eye movements is then trained to predict per-image attention and LBFS incidence, compared against a two-parameter linear baseline, with ablations and honest reporting of limitations.

Result: Edit area drives attention capture (p<0.001) while semantic plausibility drives judgment accuracy and LBFS error rates (p<0.001), with the dissociation surviving multiple-comparison correction (a secondary interaction does not). The Transformer eye-movement model tracks per-image attention strongly (Pearson r=0.77–0.82) and modestly outperforms a linear baseline on LBFS prediction (r=0.52 vs. 0.48) even without plausibility labels.

Conclusion: Detectability of AI edits should be modeled as two dissociable stages rather than a single property. A generative eye-movement model can serve as a computational proxy for the attention-capture stage, though its predictive power on LBFS is modest. The authors advocate for responsibly reporting machine learning capabilities and limitations in the context of curbing AI-driven disinformation.

Abstract: As AI-generated image edits proliferate, the platforms meant to curb the resulting disinformation treat detectability as a single, undifferentiated property: an edit either gets a warning or it does not. We show this is the wrong model. Across a controlled eye-tracking study ($N=59$, Latin-square design, four conditions crossing edit area and semantic plausibility), a mixed-effects analysis reveals that whether an edit is noticed and whether it is correctly judged as fake are dissociable stages, governed by different factors: edit area drives attention capture ($p<0.001$) while semantic plausibility drives judgment accuracy and look-but-fail-to-see (LBFS) error rates ($p<0.001$). This dissociation survives correction for multiple comparisons; a secondary interaction between the two factors does not. This two-stage account extends a long-standing distinction in visual attention research (between pre-attentive capture and effortful recognition) into the new domain of AI-edit detectability. We then test whether a generative eye-movement model can computationally operationalize the attention-capture stage: a Transformer trained to generate scanpaths tracks per-image attention with strong discriminative power (Pearson $r=0.77$--$0.82$ across held-out stimuli) and, on the harder task of predicting LBFS incidence, modestly outperforms a two-parameter linear baseline even without access to the plausibility label ($r=0.52$ vs. $r=0.48$). We report this comparison, our ablations, and our method's limitations (a single fixed train/validation split, not leave-one-subject-out) without inflation, consistent with responsibly communicating what a machine learning system can and cannot do to help curb AI-driven disinformation.

</details>


### [13] [Consensus-gated Multi-Agent Neural Architecture Search for Seismic Fault Segmentation](https://arxiv.org/abs/2608.13889)
*Shehram Baig,Ahmad Mustafa*

Main category: cs.CV

TL;DR: The paper presents an agentic neural architecture search (NAS) system using a panel of three LLMs (Claude, GPT-5.1, Gemini 2.5 Pro) that debate candidate architectures to consensus, write PyTorch code, and validate through an automated training loop with a 450K parameter budget. It discovered a 425K-parameter encoder-decoder that achieves the highest F1 (0.578) and IoU on seismic fault segmentation from the Thebe dataset, outperforming much larger published baselines (U-Net 31M, DeepLabV3 39.6M, Attention U-Net 1.83M), all in ~8 candidates, ~101 LLM calls, and roughly one GPU-day.


<details>
  <summary>Details</summary>
Motivation: Seismic fault segmentation networks are typically borrowed from computer vision/medical imaging domains that rely on large labeled datasets, but geophysical applications face tight labeled data budgets. Manually designing data-optimal architectures is time-consuming, and classical NAS is restricted to hand-crafted search spaces and large compute budgets—motivating a more practical, low-cost architecture search approach.

Method: The method is an agentic NAS system where a panel of three LLMs (Claude, GPT-5.1, Gemini 2.5 Pro) debates each candidate architecture to unanimous consensus, authors complete PyTorch implementations, cross-reviews code, and submits designs to an automated validate-train-score loop. Key constraints include a hard 450K parameter budget, keep-or-revert lineage, memory of failed mechanisms, and operation directly on source code rather than a predefined operation menu.

Result: The search discovered a 425K-parameter encoder-decoder with strip-pooling bottleneck, squeeze-and-excitation gating, asymmetric one-conv decoder, and feature-pyramid fusion neck. It achieved the highest F1 (0.578) and IoU among all models tested while being the smallest, outperforming U-Net (31M, F1 0.484), DeepLabV3-ResNet50 (39.6M, 0.516), and Attention U-Net (1.83M, 0.502). The search cost only ~101 LLM calls (~1.15M input / 0.39M output tokens) and roughly one GPU-day.

Conclusion: Consensus-gated LLM panels operating on source code provide a practical, low-cost route to domain-specific architecture discovery, achieving superior performance to much larger baselines under tight data and compute budgets using only eight training candidates and a single consumer GPU.

Abstract: Neural networks for seismic fault segmentation are often borrowed from computer vision and medical imaging domains where they train under relatively much larger labeled data resources. Optimizing their architecture under tight labeled data budgets as are common in geophysical applications is not a trivial problem. Manually designing data-optimal architectures is time-consuming while classical neural architecture search (NAS) is restricted to hand-crafted search spaces and large compute budgets. We present an agentic NAS system in which a panel of three large language models (Claude, GPT-5.1, and Gemini~2.5~Pro) debates each candidate architecture to unanimous consensus, authors the complete PyTorch implementation, cross-reviews it, and submits it to an automated validate-train-score loop with a hard 450K parameter budget, keep-or-revert lineage, and a memory of failed mechanisms. Operating on source code rather than a predefined operation menu, the search ran on a single consumer GPU and trained only eight candidates. It discovered \ours{}: a 425K-parameter encoder-decoder with a strip-pooling bottleneck, squeeze-and-excitation gating, an asymmetric one-conv decoder, and a feature-pyramid fusion neck. Trained under a protocol identical to all baselines on sections derived from the Thebe fault dataset, it attains the highest F1 (0.578) and IoU of all models tested while being the smallest, outperforming a published-capacity U-Net (31M parameters, F1 0.484), DeepLabV3-ResNet50 (39.6M, 0.516), an Attention U-Net(1.83M, 0.502). The search cost 101 LLM calls ($\sim$1.15M input / 0.39M output tokens) and roughly one GPU-day, making consensus-gated LLM panels a practical, low-cost route to domain-specific architecture discovery.

</details>


### [14] [Beyond Control Points: Arcsecond Relative-Motion Estimation of Vision Measurement Platforms With Incomplete or Absent Control Fields](https://arxiv.org/abs/2608.13918)
*Meng Lian,Jian Wang,Shuixin Pan,Haibo Liu,Yueqiang Zhang,Yulan Guo*

Main category: cs.CV

TL;DR: A control-adaptive differential framework for long-range vision-based deformation monitoring that estimates inter-frame platform motion directly from image displacements and known 3D points, achieving high accuracy, calibration robustness, and computational efficiency without nonlinear optimization or initial pose estimates.


<details>
  <summary>Details</summary>
Motivation: Long-range vision-based deformation monitoring is highly sensitive to camera platform motion. Absolute-pose differencing relies on dedicated control data and propagates two independent pose errors into the relative-motion estimate, which is problematic.

Method: Develops a control-adaptive differential framework that estimates inter-frame platform motion directly from image displacements and known 3D points. Rotation is recovered without dedicated control points, while one surveyed control point enables prior-constrained translation recovery, and two nonparallel control rays recover full 3D translation. Requires no nonlinear optimization or initial pose estimate. Derives rotation observability condition, leakage bounds, and a single-point axial-prior bias law.

Result: Under 0.5-pixel image noise, attitude changes up to 30 arcmin, and 3D perturbations up to 2 mm, achieves rotation RMSE of 2.97 arcsec with 0.46 ms average runtime. With one control point, translation RMSE is 1.19 mm. Bridge experiment shows 0.85 mm median coordinate-wise displacement RMSE. Maintains zero divergence on public RGB-D and stereo sequences.

Conclusion: The framework establishes state-of-the-art accuracy, calibration robustness, and computational efficiency among evaluated methods for long-range deformation monitoring.

Abstract: Long-range vision-based deformation monitoring is highly sensitive to motion of the camera platform. Absolute-pose differencing typically relies on dedicated control data and propagates two independent pose errors into the relative-motion estimate. We develop a control-adaptive differential framework that estimates inter-frame platform motion directly from image displacements and known 3D points. With no dedicated control point, the framework recovers platform rotation from measurement-point observations. One surveyed control point enables prior-constrained translation recovery, while two nonparallel control rays recover full 3D translation. The framework requires neither nonlinear optimization nor an initial pose estimate. Excluding control data from the rotation stage makes the rotation estimate exactly immune to contamination confined to the control field. The inherited differential formulation also cancels translational extrinsic errors exactly. We derive the rotation observability condition, a leakage bound for unmodeled translation and nonrigid point motion, and the single-point axial-prior bias law. Under 0.5-pixel image noise, attitude changes of up to 30~arcmin, and 3D point perturbations of up to 2~mm, the multi-camera estimator achieves a rotation RMSE of 2.97~arcsec and an average runtime of 0.46~ms. With one surveyed control point, its prior-constrained translation RMSE is 1.19~mm. In a bridge experiment without a stable control field, the median coordinate-wise displacement RMSE relative to total-station measurements is 0.85~mm. The estimator also maintains zero divergence under the tested 3D coordinate perturbations on public RGB-D and stereo sequences. These results establish state-of-the-art accuracy, calibration robustness, and computational efficiency among the evaluated methods.

</details>


### [15] [OpenBelief-Nav: Evidence-Preserving Object Memory for Open-Vocabulary Language-Guided Navigation](https://arxiv.org/abs/2608.13923)
*Dinh Tuan Nguyen,Anh Dao,Phuong Nam Dang,Quan-Dung Pham,Tuyen P. Le,Truong Nguyen,Quan Nguyen*

Main category: cs.CV

TL;DR: OpenBelief-Nav presents an evidence-preserving object memory for open-vocabulary 3D scene graphs that retains observation-level phrases, reliability cues, and frame-mask provenance instead of committing to a single fused feature or semantic label. It demonstrates improved mIoU on ScanNet200 and Replica scenes, competitive navigation success rates on HM3D-YCB trials, and enhanced correction success on Unitree G1 robot runs.


<details>
  <summary>Details</summary>
Motivation: Existing open-vocabulary 3D scene graphs typically expose mapped objects through a single fused feature or committed semantic label, which can discard minority yet task-relevant hypotheses before the task-time interface is even reached.

Method: OpenBelief-Nav keeps observation-level evidence (phrases, reliability cues, frame-mask provenance) while maintaining separate aggregate geometric and visual representations. Semantically related phrases are consolidated into a vocabulary-independent object belief, from which task-specific readouts perform either fixed-vocabulary projection or free-form retrieval. A correction policy allows up to two verified candidate attempts.

Result: On five ScanNet200 scenes, full-belief projection achieves mIoU 0.2742 vs 0.2393 for early-commit; on eight Replica scenes, 0.2912 vs 0.2701. On 78 HM3D-YCB navigation trials, consensus and early-commit retrieval achieve 60/78 successes vs 58/78 belief-weighted and 55/78 DualMap. On 20 Unitree G1 runs (10 matched cases), the correction policy improves target-confirmation success from 6/10 to 8/10.

Conclusion: Preserving evidence across the mapping pipeline—rather than committing early—improves both semantic quality and downstream task performance, including navigation and embodied correction behavior.

Abstract: Open-vocabulary 3D scene graphs provide compact semantic memory for language-guided navigation, but mapped objects are often exposed through a single fused feature or committed semantic label. Such commitment can remove minority yet task-relevant hypotheses from the task-time interface. We present OpenBelief-Nav, an evidence-preserving object memory that retains observation-level phrases, reliability cues, and frame-mask provenance while maintaining separate aggregate geometric and visual representations. Semantically related phrases are consolidated into a vocabulary-independent object belief from which task-specific readouts perform fixed-vocabulary projection or free-form retrieval. On five ScanNet200 and eight Replica scenes, full-belief projection achieves mIoU scores of 0.2742 and 0.2912, compared with 0.2393 and 0.2701 for a matched early-commit readout. Across 78 HM3D-YCB navigation trials, consensus and early-commit retrieval each achieve 60/78 successes, compared with 58/78 for belief-weighted retrieval and 55/78 for DualMap. Across 20 Unitree G1 runs organized as 10 matched evaluation cases, a correction policy permitting at most two verified candidate attempts improves target-confirmation success from 6/10 to 8/10 relative to top-1-only execution. Code will be released upon acceptance at https://openbelief-nav.github.io/.

</details>


### [16] [RGBX-Next: Towards Realistic Generative Rendering from G-Buffers](https://arxiv.org/abs/2608.13929)
*Zheng Zeng,Marco Salvi,Lifan Wu,Jan Novák,Daqi Lin,Saeed Hadadan,Yichen Sheng,Robert Pottorff,Shiqiu Liu,Ravi Ramamoorthi,Ling-Qi Yan,Miloš Hašan*

Main category: cs.CV

TL;DR: RGBX-Next is a unified generative framework based on finetuned diffusion transformers (DiT) that supports both forward rendering (G-buffers to realistic images/videos/streams) and inverse rendering (images/videos/streams to G-buffers), achieving high-quality generative rendering and intrinsic decomposition with a general finetuning recipe.


<details>
  <summary>Details</summary>
Motivation: Diffusion models excel at generating image, video, and streaming content but lack precise control compared to traditional 3D rendering pipelines. The authors propose bridging this gap by using generative models as learned renderers conditioned on G-buffers, enabling controllable generation.

Method: The paper introduces a general recipe for finetuning diffusion transformer (DiT) models to serve as both forward renderers (G-buffers to images/videos/streams) and inverse renderers (images/videos/streams to G-buffers), forming a unified framework called RGBX-Next.

Result: The resulting models achieve high quality in both realistic generative rendering and intrinsic decomposition, demonstrating effectiveness across both forward and inverse rendering tasks.

Conclusion: The design principles of finetuning DiT models into generative renderers are expected to benefit future research on controllable generative forward and inverse rendering, and all models will be released publicly.

Abstract: Diffusion models have achieved impressive results in image, video, and streaming generation. However, compared to traditional 3D rendering, they still lack precise control over the generated output. We believe a viable path forward is to use generative models as learned renderers conditioned on traditionally rendered G-buffers. We introduce RGBX-Next, a unified generative framework for forward and inverse rendering, which allows estimating G-buffers from images, videos, and streams, and rendering realistic images, videos, and streams from G-buffers. Our key contribution is a general recipe for finetuning diffusion transformer (DiT) models into generative forward and inverse renderers. We show that the resulting models achieve high quality in both realistic generative rendering and intrinsic decomposition. We will make all our models publicly available. We believe that the design principles presented in this paper will benefit future research on controllable generative forward and inverse rendering.

</details>


### [17] [CoANeRV: Coordinate-Aware Token-Space Neural Video Representation](https://arxiv.org/abs/2608.13938)
*Jialong Guo,Ke Liu,Mengxuan Li,Jiajun Bu,Haishuai Wang*

Main category: cs.CV

TL;DR: CoANeRV is a coordinate-aware token-space framework that adapts token-conditioned neural fields to amortized video representation, forming compact video tokens in one feed-forward pass and using a shared coordinate-conditioned decoder for reconstruction—avoiding per-video optimization while improving quality and reducing memory.


<details>
  <summary>Details</summary>
Motivation: Existing neural video representation (NeRV) methods require either costly per-video optimization or video-specific weight generation, making it difficult to scale to efficient amortized video representation across many videos.

Method: CoANeRV forms compact video tokens in a single feed-forward pass and uses a shared coordinate-conditioned decoder. It introduces a coordinate-aware decoding architecture with axis-adaptive positional encoding and temperature-modulated cross-attention to align spatio-temporal queries with video tokens, plus block-wise coordinate querying to reduce peak attention memory.

Result: Experiments show CoANeRV consistently improves reconstruction quality over prior feed-forward NeRV and INR baselines across diverse video datasets, reduces peak memory compared to attention-based coordinate decoders, and provides efficient amortized encoding without per-video optimization.

Conclusion: The results support the proposed combination of feed-forward token formation, spatio-temporal coordinate retrieval, and memory-bounded dense querying as an effective and scalable approach for amortized video representation.

Abstract: Neural representations for videos (NeRV) have shown strong reconstruction fidelity by storing video-specific information in network weights. However, existing formulations typically require either costly per-video optimization or video-specific weight generation, making it difficult to scale to efficient amortized video representation. We propose CoANeRV, a coordinate-aware token-space framework that adapts the broader token-conditioned neural-field paradigm to amortized video representation. CoANeRV forms compact video tokens in one feed-forward pass and uses a shared coordinate-conditioned decoder to reconstruct continuous spatio-temporal queries, avoiding per-video decoder optimization or generation while retaining coordinate-level reconstruction flexibility. To make token-space reconstruction effective, CoANeRV introduces a coordinate-aware decoding architecture that aligns spatio-temporal queries with video tokens through axis-adaptive positional encoding and temperature-modulated cross-attention. Block-wise coordinate querying further reduces peak attention memory, making high-resolution reconstruction practical. Experiments on diverse video datasets show that CoANeRV consistently improves reconstruction quality over prior feed-forward NeRV and INR baselines, reduces peak memory compared with attention-based coordinate decoders, and provides efficient amortized encoding without per-video optimization. These results support the proposed video-specific combination of feed-forward token formation, spatio-temporal coordinate retrieval, and memory-bounded dense querying. The code is available at https://github.com/jialong2023/CoANeRV.

</details>


### [18] [CMCNet: Aligning Ultrasound Image Embeddings with Textual TI-RADS Representations for Fine-Grained Thyroid Classification](https://arxiv.org/abs/2608.13939)
*Bingxin Yu,Xueli Wang,Jerry Zhou,Wenyan Wang,Li Wen,Lan Huang,Xin Feng,Fengfeng Zhou,Kewei Li*

Main category: cs.CV

TL;DR: The paper introduces STN, a dataset of 600 thyroid nodules with ultrasound images and complete ACR TI-RADS feature annotations, and proposes CMCNet, a method that aligns image embeddings to fixed textual embeddings using a Center-Margin Contrastive Loss for multi-class TI-RADS risk level prediction. The approach outperforms multitask learning, InfoNCE, center loss, and VQA-style baselines, particularly in imbalanced settings, requiring only images at inference.


<details>
  <summary>Details</summary>
Motivation: Most deep learning approaches for thyroid nodule ultrasound diagnosis focus on binary malignancy classification, while multi-class TI-RADS risk prediction and explicit use of feature-level supervision remain underexplored due to limited annotated data. The authors aim to follow the clinical decision process and investigate how structured feature information can guide representation learning.

Method: The authors construct the STN dataset with 600 thyroid nodules containing paired transverse/longitudinal ultrasound images, bounding boxes, and labels for all five TI-RADS feature categories. They propose CMCNet, which aligns image embeddings to fixed textual embeddings (derived from standardized feature descriptions serving as stable surrogate representations) via a Center-Margin Contrastive Loss that promotes both intra-class compactness and inter-class separation.

Result: The embedding alignment strategy in CMCNet is more data-efficient and robust than direct multitask learning, and consistently outperforms InfoNCE, center loss, a strong multitask baseline, and a VQA-style multimodal model, particularly in imbalanced settings.

Conclusion: Structured feature information can effectively guide representation learning for TI-RADS risk level prediction, and image-text embedding alignment via Center-Margin Contrastive Loss provides a superior training paradigm over existing baselines. The dataset and code are publicly released to facilitate further research.

Abstract: Ultrasound is the primary imaging modality for assessing thyroid nodules, and the ACR TI-RADS framework standardizes diagnosis through five ultrasound feature categories that are aggregated into five risk levels (TR1-TR5). Although widely adopted in clinical practice, most deep learning approaches focus on binary malignancy classification, while multi-class prediction and explicit utilization of feature-level supervision remain underexplored, largely due to limited annotated data. In this study, we introduce the STN dataset of 600 thyroid nodules with paired transverse and longitudinal ultrasound images, bounding box annotations, and complete labels for all five TI-RADS feature categories. Following the clinical decision process, we investigate how structured feature information can guide representation learning during training while requiring only images at inference. We demonstrate that text embeddings derived from standardized feature descriptions form a stable surrogate representation for TI-RADS risk levels. Based on this observation, we propose CMCNet, which aligns image embeddings to fixed textual embeddings via a Center-Margin Contrastive Loss that simultaneously promotes intra-class compactness and inter-class separation. Experimental results show that this embedding alignment strategy is more data-efficient and robust than direct multitask learning, and consistently outperforms InfoNCE, center loss, a strong multitask baseline, and a VQA-style multimodal model, particularly in imbalanced settings. The dataset is freely available at doi: 10.5281/zenodo.19125693 and the source code is available at: https://www.healthinformaticslab.org/supp/.

</details>


### [19] [Fast Implicit Neural Light Field Representation via Geometric Decomposition and Multi-Resolution Low-Rank Features](https://arxiv.org/abs/2608.13949)
*Yao Guo,Ligen Shi,Shuchen Sun,Jun Qiu,Chang Liu*

Main category: cs.CV

TL;DR: The paper proposes a fast implicit neural representation for light fields that decomposes the 4D light field into horizontal disparity, spatial texture, and vertical disparity planes, each encoded using multi-resolution low-rank features (2D grids combined with 1D line features), decoded by a lightweight MLP. It achieves competitive quality with better trade-offs in parameters, training time, and inference efficiency.


<details>
  <summary>Details</summary>
Motivation: Directly fitting 4D ray coordinates with neural networks for light field reconstruction is slow due to the high dimensionality, strong spatial-angular redundancy, and structured disparity variations, requiring considerable optimization time to achieve both view appearance and cross-view consistency.

Method: The 4D light field is decomposed into three planes: a horizontal disparity plane, a spatial texture plane, and a vertical disparity plane. Each plane uses a low-rank representation that combines a low-resolution 2D grid with element-wise products of two high-resolution 1D line features at multiple resolution levels. Fused features are decoded by a lightweight MLP to output RGB values.

Result: Experiments on public light field datasets demonstrate competitive reconstruction quality while providing a better trade-off among model parameters, training time, and inference efficiency.

Conclusion: Geometric decomposition combined with multi-resolution low-rank features enables efficient and compact implicit light field representation without sacrificing reconstruction quality.

Abstract: Implicit neural representations provide a compact and continuous way to reconstruct dense light fields from sampled ray coordinates. However, fast light field reconstruction remains challenging because a light field is a high-dimensional signal with strong spatial-angular redundancy and structured disparity variations. Directly fitting 4D ray coordinates with a neural network often requires considerable optimization time to recover both view appearance and cross-view consistency. To address this issue, this paper proposes a fast implicit light field representation based on geometric decomposition and multi-resolution low-rank features. The proposed method decomposes a 4D light field into a horizontal disparity plane, a spatial texture plane, and a vertical disparity plane. Each plane is represented by a low-rank structure that combines a low-resolution 2D grid with the element-wise product of two high-resolution 1D line features at multiple resolution levels. The fused features are decoded by a lightweight multilayer perceptron to predict RGB values. Experiments on public light field datasets show that the proposed method achieves competitive reconstruction quality while providing a better trade-off among model parameters, training time, and inference efficiency.

</details>


### [20] [SAFE: Scene-Aware Feature Modulation for Color Constancy with Learned Color Space in Pure-Color Scenes](https://arxiv.org/abs/2608.13967)
*Yuan-Kang Lee,Kuan-Lin Chen,Chih-Heng Chang,Jian-Jiun Ding*

Main category: cs.CV

TL;DR: The paper proposes SAFE, a scene-aware feature modulation network combined with a Learned Color Space (LCS) for addressing color constancy in pure-color scenes where standard chromaticity-based cues collapse. SAFE reduces mean angular error by 10%, best-25% error by 20%, and worst-25% error by 5.8% over baselines.


<details>
  <summary>Details</summary>
Motivation: Pure-color scenes pose a fundamental challenge to color constancy because when most pixels share a narrow band of hues, chromaticity-based cues collapse to a single point, making standard estimators ambiguous and unreliable.

Method: The framework couples two innovations: (i) SAFE, a Scene-Aware FeaturE modulation network that organizes illumination cues into a structured four-token representation, selectively reweighted based on scene complexity features; (ii) the Learned Color Space (LCS), a scene-dependent chromaticity normalization that directly addresses the chromaticity collapse problem for pure-color scenes.

Result: SAFE consistently improves performance in pure-color scenes. Compared to the best-performing baseline per metric, it reduces the mean angular error by 10%, the best-25% error by 20%, and the worst-25% error by 5.8%.

Conclusion: The proposed SAFE framework with LCS effectively addresses the chromaticity collapse problem in pure-color scenes, delivering consistent improvements across all error metrics over existing baselines.

Abstract: Color constancy on pure-color scenes is challenging: when most pixels share a narrow band of hues, every chromaticity-based cue collapses to a single point and standard estimators become ambiguous. We propose a compact framework that couples two innovations: (i) SAFE, a Scene-Aware FeaturE modulation network that organizes illumination cues into a structured four-token representation, which is then selectively reweighted based on scene complexity features; (ii) the Learned Color Space (LCS), a scene-dependent chromaticity normalization that directly addresses the chromaticity collapse problem for pure-color scenes. Experiment results show that SAFE consistently improves performance in pure-color scenes. Compared to the best-performing baseline in each metric, it reduces the mean angular error by 10%, the best-25% error by 20%, and the worst-25% error by 5.8%.

</details>


### [21] [PPOM: Marginalizing Patch-Grid Phase for CLIP-Based Generalizable Vision-Language Prompt Tuning](https://arxiv.org/abs/2608.13969)
*Liang Wang,Haoyang Li,Chao Wang,Guodong Long,Jing Jiang,Yan Peng*

Main category: cs.CV

TL;DR: PPOM is a training-free inference operator that marginalizes over patch-grid phase shifts to reduce prediction sensitivity in CLIP-based prompt tuning, improving host performance without retraining.


<details>
  <summary>Details</summary>
Motivation: Prompt tuning on CLIP models is sensitive to patch-grid alignment (phase) imposed by frozen vision transformers, causing unpredictable variations in predictions based on how the image aligns with the patch lattice.

Method: Treating phase shift as a nuisance variable, PPOM evaluates the identity view and reflection-padded translations, pairs opposite shifts into horizontal, vertical, and diagonal antithetic families, and assigns equal mass to these families and the identity prediction to avoid view-count bias during phase integration.

Result: Across multiple prompt-learning hosts, PPOM improves host performance without re-training.

Conclusion: PPOM provides a deterministic interface between prompt adaptation and patch-grid sensitivity, effectively marginalizing the nuisance phase parameter in a training-free manner.

Abstract: Prompt tuning adapts CLIP-based vision-language models with few trainable parameters, yet its predictions remain sensitive to the spatial sampling imposed by a frozen vision transformer. In particular, non-overlapping patch tokenization makes predictions depend on the alignment (phase) between image and the patch lattice. To reduce prediction sensitivity to patch-grid alignment, we introduce Patch-Phase Orbit Marginalization (PPOM), a training-free inference operator that treats phase shift as a nuisance variable. Given a patch stride, PPOM evaluates the identity view and reflection-padded translations, pairs opposite shifts into horizontal, vertical, and diagonal antithetic families, and assigns equal mass to these families and the identity prediction to avoid view-count bias during phase integration. In summary, PPOM provides a deterministic interface between prompt adaptation and patch-grid sensitivity. Across multiple prompt-learning hosts, PPOM improves host performance without re-training.

</details>


### [22] [Rethinking Auxiliary Modalities in Multimodal Zero-shot Anomaly Detection: From Semantic Fusion to Conditional Modulation](https://arxiv.org/abs/2608.13973)
*Peng Wu,Xin Ge,Yujia Sun,Guansong Pang*

Main category: cs.CV

TL;DR: A plug-and-play auxiliary-conditioned enhancement framework that preserves RGB-based zero-shot anomaly detection semantics while using auxiliary modalities as conditional signals for feature refinement, achieving SOTA on multimodal ZSAD.


<details>
  <summary>Details</summary>
Motivation: RGB-only zero-shot anomaly detection struggles with anomalies involving geometric deformation, depth variation, or subtle surface changes. While auxiliary modalities could help, existing multimodal methods fuse them directly into shared semantic space, which disturbs the text-aligned anomaly semantics from RGB foundation models and requires modality-specific architectures.

Method: The framework keeps the original RGB image-text anomaly matching pathway intact and uses auxiliary observations as conditional signals. A lightweight meta-learning module generates sample-adaptive low-rank residual updates for RGB feature refinement (global conditioning), while uncertainty-aware spatial modulation based on initial RGB anomaly response and auxiliary reliability determines where local residual updates are applied (local conditioning).

Result: Extensive experiments on MVTec 3D-AD and Eyecandies demonstrate consistent improvements across multiple popular RGB-based zero-shot anomaly detectors, achieving state-of-the-art performance for multimodal zero-shot anomaly detection.

Conclusion: Global-to-local conditional modulation enables selective multimodal enhancement while preserving original RGB anomaly semantics, offering a plug-and-play solution that works with existing RGB-based ZSAD detectors without requiring joint multimodal semantic space reconstruction or modality-specific architectures.

Abstract: Recent foundation model-based methods have endowed RGB images with strong zero-shot anomaly detection (ZSAD) through vision-language pretraining. However, RGB observations alone remain limited in perceiving anomalies dominated by geometric deformation, depth variation, or subtle surface changes. Auxiliary modalities can provide complementary structural information, but existing multimodal methods typically fuse them directly into a shared semantic space, which may disturb the text-aligned anomaly semantics established by RGB foundation models and often requires modality-specific architectures. To address this issue, we propose a plug-and-play auxiliary-conditioned enhancement framework for zero-shot anomaly detection. Instead of reconstructing a joint multimodal anomaly semantic space, our framework preserves the original RGB image-text anomaly matching pathway and uses auxiliary observations as conditional signals for RGB feature refinement, allowing auxiliary modalities to seamlessly enhance existing RGB-based zero-shot anomaly detectors. Specifically, a lightweight meta-learning module takes global RGB and auxiliary representations as input and generates sample-adaptive low-rank residual updates to determine how RGB features should be refined. We further construct uncertainty-aware spatial modulation from the initial RGB anomaly response and auxiliary reliability, which determines where local residual updates are strengthened or suppressed. This global-to-local conditional modulation enables selective multimodal enhancement while preserving the original RGB anomaly semantics. Extensive experiments on MVTec 3D-AD and Eyecandies demonstrate that our framework consistently improves multiple popular RGB-based zero-shot anomaly detectors, achieving state-of-the-art performance for multimodal zero-shot anomaly detection.

</details>


### [23] [ProFocus: Interpreting Affective Experience in Artistic Images with Progressive Visual Focusing](https://arxiv.org/abs/2608.13974)
*Zhiyan Zhang,Zicheng Yan,Jianqi Chen,Peipei Song,Shanshan Wang,Xun Yang*

Main category: cs.CV

TL;DR: ProFocus is a novel framework for affective interpretation of artistic images that models visual representation learning based on hierarchical cognitive theory of human aesthetic appreciation. It consists of a Hierarchical Art Critic (HAC) that generates structured linguistic priors across three cognitive levels (atmospheric style, narrative subjects, concrete details) and a Progressive Hint Fusion (PHF) module that sequentially injects these hierarchical hints into visual features, outperforming state-of-the-art methods on ArtEmis v1.0 and v2.0.


<details>
  <summary>Details</summary>
Motivation: Existing methods rely on general-purpose visual embeddings like CLIP, which fail to capture the nuanced cues underlying artistic emotion. Visual art is intentionally created to elicit emotional responses through abstract concepts and visual metaphors, making affective interpretation particularly challenging compared to natural images.

Method: ProFocus contains two components: (1) Hierarchical Art Critic (HAC) uses multimodal large language models to generate structured linguistic priors at three cognitive levels—atmospheric style, narrative subjects, and concrete details; (2) Progressive Hint Fusion (PHF) sequentially injects these hierarchical hints into visual features, enabling progressive focusing that mirrors human perception rather than conventional cross-modal fusion.

Result: Extensive experiments on ArtEmis v1.0 and v2.0 datasets demonstrate that ProFocus consistently outperforms state-of-the-art methods in both emotion recognition and affective explanation tasks.

Conclusion: Modeling visual representation learning based on hierarchical cognitive theory of human aesthetic appreciation, combined with progressive sequential hint injection, enables more faithful affective interpretation of artistic images and captures subtle emotional cues that general-purpose embeddings miss.

Abstract: Interpreting the emotional responses triggered by images is central to achieving emotional intelligence. Compared with natural images, visual art is intentionally created to elicit emotional responses from its viewers through abstract concepts and visual metaphors, making affective interpretation particularly challenging. However, most existing methods rely on general-purpose visual embeddings (e.g., CLIP), failing to capture the nuanced cues underlying artistic emotion. To address this gap, we propose \textbf{ProFocus}, a novel framework that models affective experience in artistic images via progressive visual focusing. The key idea is to model visual representation learning inspired by a hierarchical cognitive theory of human aesthetic appreciation. Technically, ProFocus contains two core components: a Hierarchical Art Critic (HAC) and a Progressive Hint Fusion (PHF) module. HAC leverages multimodal large language models to generate structured linguistic priors at three cognitive levels--atmospheric style, narrative subjects, and concrete details--thereby translating artistic perception into coherent semantic guidance. Building upon these priors, PHF departs from conventional cross-modal fusion by sequentially injecting the hierarchical hints into visual features, enabling a progressive focusing process that mirrors human perception. This design allows the model to capture subtle affective cues and produce more faithful explanations. Extensive experiments on the ArtEmis v1.0 and v2.0 datasets demonstrate that ProFocus consistently outperforms state-of-the-art methods in both emotion recognition and affective explanation. Project page: https://github.com/Zhang-Zhiyan/ProFocus.

</details>


### [24] [FIRM: Fine-Grained Intra-Token Representation of Masks for Remote Sensing Reasoning Segmentation](https://arxiv.org/abs/2608.13980)
*Weidong Tang,Kaiyu Li,Yikai Wang,Yanan Wu,Haotian Gan,Shihong Wang,Xiangyong Cao*

Main category: cs.CV

TL;DR: FIRM introduces a fine-grained intra-token representation for MLLM-based reasoning segmentation in remote sensing, where each visual token predicts an r×r sub-cell binary mask pattern instead of a single label, enabling precise pixel-level masks even when small targets, thin structures, or adjacent instances share a token. It achieves leading results across five benchmarks, including 70.5/80.5 gIoU/cIoU on LaSeRS and a 3.0-point average gain on EarthReason.


<details>
  <summary>Details</summary>
Motivation: MLLMs assign a single binary mask label per visual token, but in remote sensing images, small targets, thin structures, and adjacent instances can occupy different sub-regions of the same token, causing boundary coarseness and merging of nearby targets. This representational gap limits fine-grained segmentation quality.

Method: FIRM predicts a mask code per visual token that specifies an r×r binary sub-cell pattern rather than a single foreground/background label. The complete grid of mask codes is predicted in one mask pass; fixed lookup converts codes into discrete sub-cell masks, and marginalizing the code distribution yields a soft structural field. A lightweight continuous renderer refines boundaries within each sub-cell using pre-merge visual features and image details.

Result: FIRM achieves leading results across five reasoning and referring segmentation benchmarks on satellite and UAV imagery, including 70.5/80.5 gIoU/cIoU on LaSeRS and a 3.0-point average improvement on EarthReason.

Conclusion: Explicitly representing intra-token mask patterns is valuable for fine-grained MLLM segmentation in remote sensing, significantly improving segmentation precision over single-label token assignments.

Abstract: Reasoning segmentation requires multimodal large language models (MLLMs) to translate implicit instructions into precise pixel-level masks. MLLMs encode an image as visual tokens, each of which merges a group of image patches. In remote sensing images, small targets, thin structures, and adjacent instances can occupy different parts of the same visual token. Assigning a single binary mask label to such a token loses its internal spatial structure, causing nearby targets to merge and object boundaries to become coarse. To bridge this representational gap, we introduce FIRM, a Fine-grained Intra-token Representation of Masks. For each visual token, FIRM predicts a mask code that specifies an $r\times r$ binary sub-cell pattern rather than a single foreground/background label. Given a target identified by the MLLM, the complete grid of mask codes is predicted in one mask pass. Fixed lookup converts the predicted codes into a discrete sub-cell mask, while marginalizing the code distribution yields a soft structural field. To further recover fine-grained boundaries within each sub-cell, we introduce a lightweight continuous renderer that refines this field using pre-merge visual features and image details. Across five reasoning and referring segmentation benchmarks on satellite and UAV images, FIRM achieves leading results, including $70.5/80.5$ gIoU/cIoU on LaSeRS and a $3.0$-point average gain on EarthReason. These results demonstrate the value of explicitly representing intra-token mask patterns for fine-grained MLLM segmentation.

</details>


### [25] [MedClaw: Heuristic Agent Harness for Long-Horizon Surgical Video Reasoning](https://arxiv.org/abs/2608.14015)
*Yingying Fan,Penghui Du,Leyan Zhu,Runze He,Zimeng Wu,Yuxuan Zhang,Liang Chen,Jiahao Xie,Jiangtang Wang,Shuai Shao,Anchao Yang,Yutong Bai,Yan Wang*

Main category: cs.CV

TL;DR: The paper presents a novel agent harness for long-horizon temporal reasoning in surgical videos that separates reasoning from perception, using a text-only orchestrator to plan evidence gathering via tool calls executed by frozen vision-language sub-agents. It introduces a gradient-free Heuristic Skill Distillation loop that mines low-scoring traces to grow an external skill library (e.g., directed re-look) from only ~100 labeled examples. The authors also release MedClawBench, a de-leaked 1,123-question benchmark, showing consistent gains over one-shot VLMs and general video-agent frameworks, especially on long out-of-domain neurosurgery videos.


<details>
  <summary>Details</summary>
Motivation: Surgical videos spanning tens of minutes require long-horizon temporal reasoning (what happens before/after/across stages), which existing approaches handle poorly. One-shot VLMs compress the whole video and lose detail, while data-hungry video agents that train the model where to look transfer poorly to out-of-domain surgery. There is a need for an approach that improves via evolving context rather than optimizing weights.

Method: Builds an agent harness decoupling reasoning from perception: a text-only orchestrator plans evidence collection and issues auditable tool calls, while frozen vision-language sub-agents execute each call (viewing, cropping, inspecting frames, retrieving external knowledge). Proposes a gradient-free, reward-gated Heuristic Skill Distillation loop that mines the agent's own low-scoring traces and retains candidate skills only if they raise validation reward, yielding reusable skills like directed re-look. Introduces MedClawBench benchmark (1,123 questions over neurosurgery recordings plus a public lecture-video split) for evaluation.

Result: The agent consistently outperforms one-shot VLMs and general video-agent frameworks across both datasets and all four evaluation dimensions, with the largest gains on long, out-of-domain neurosurgery videos. The skill distillation loop adapts from only about 100 labeled examples, far fewer than supervised or reinforcement fine-tuning requires.

Conclusion: Decoupling reasoning from perception via an evolving external skill library provides a scalable, auditable, and sample-efficient alternative to fine-tuning for long-horizon surgical video understanding, and the released benchmark enables further progress in this domain.

Abstract: Understanding tens-of-minutes surgical videos requires long-horizon temporal reasoning, answering what happens before, after, or across stages of a procedure by grounding the question in visual evidence spread across time. Existing approaches handle this poorly: a one-shot vision-language model (VLM) compresses the whole procedure to fit its context window and loses the detail a "before" or "after" question depends on, while video agents that train the model where to look are data-hungry and transfer poorly to out-of-domain surgery. We build an agent harness that separates reasoning from perception and improves by evolving context rather than optimizing weights. A text-only orchestrator plans which evidence to gather and issues an auditable sequence of tool calls, while frozen vision-language sub-agents execute each call over the pixels, viewing, cropping, inspecting frames, and retrieving external knowledge. We further propose a gradient-free, reward-gated Heuristic Skill Distillation loop that mines the agent's own low-scoring traces and keeps a candidate skill only when it raises a validation reward, yielding reusable retrieval skills, notably directed re-look. Growing an external skill library rather than tuning weights, the loop adapts from only about 100 labeled examples, far fewer than supervised or reinforcement fine-tuning requires. To evaluate this agent, we introduce MedClawBench, a de-leaked, doctor-grounded benchmark of 1,123 questions over self-built long neurosurgery recordings and a held-out public lecture-video test split. Across both datasets and all four evaluation dimensions, our agent consistently outperforms one-shot VLMs and general video-agent frameworks, with the largest gains on the long, out-of-domain neurosurgery videos. Project page: https://fyycs.github.io/medclaw/.

</details>


### [26] [Content Based Video Narration of Gameplay with Vision Language Models](https://arxiv.org/abs/2608.14016)
*Mathew Varghese*

Main category: cs.CV

TL;DR: The paper presents a content-based video narration system that generates spoken, esports-style commentary for arbitrary gameplay recordings using a general-purpose vision-language model and text-to-speech, requiring no game-specific instrumentation or task-specific training. The system uses three key mechanisms—temporal mosaic packing (combining nine frames into one image), context-conditioned prompting (replaying recent narrations to prevent repetition), and duration-conditioned generation with elastic alignment (ensuring narration fills time slots exactly)—and supports local on-device TTS.


<details>
  <summary>Details</summary>
Motivation: Live game commentary is extremely scarce outside professional esports broadcasts. The authors aim to democratize game narration by creating a system that can generate commentary for arbitrary gameplay without requiring game-specific telemetry, instrumentation, or task-specific training—making it broadly applicable across different games and settings.

Method: The system combines a general-purpose vision-language model (VLM) with a text-to-speech back end. Three mechanisms enable it: (1) Temporal mosaic packing arranges nine uniformly sampled frames into a single 3x3 image allowing the image-native VLM to reason about motion with one image payload per segment; (2) Context-conditioned prompting replays the K most recent narrations as assistant-role history to suppress repetition; (3) Duration-conditioned generation and elastic alignment constrain narration length in the prompt and time-scale or symmetrically pad synthesized audio to fill each segment slot exactly for frame-accurate muxing. The implementation supports cloud TTS or a 6-bit quantized 4B-parameter on-device TTS model on Apple silicon.

Result: The paper reports a qualitative case study on real-time strategy footage, a cost model showing the mosaic reduces per-minute image payloads by 9x, and a candid account of observed failure modes including hallucinated game state, resolution loss from mosaicking, and prosody artifacts from time-scaling. The system is released as a reproducible baseline.

Conclusion: The system provides a viable approach for automatic game commentary generation without game-specific hooks, demonstrating the feasibility of content-based narration using general-purpose VLMs. An evaluation protocol for quantitative study is planned for a full version of the paper.

Abstract: Live game commentary is scarce: it exists for professional esports broadcasts and almost nowhere else. We present a content-based video narration system that produces spoken, esports-style commentary for arbitrary gameplay recordings using a general-purpose vision-language model (VLM) and a text-to-speech back end, with no game-specific instrumentation, no engine telemetry, and no task-specific training. Three mechanisms carry the system. Temporal mosaic packing arranges nine uniformly sampled frames into a single 3x3 image, letting an image-native VLM reason about motion while consuming one image payload per segment instead of nine. Context-conditioned prompting replays the K most recent narrations as assistant-role history, suppressing the repetition that dominates per-segment captioning of static scenes. Duration-conditioned generation and elastic alignment constrain narration length in the prompt, then time-scale or symmetrically pad the synthesized audio so each utterance fills its segment slot exactly, giving frame-accurate muxing without a forced aligner. The implementation supports either cloud TTS or a 6-bit quantized 4B-parameter on-device TTS model on Apple silicon, making the speech stage fully local. We report a qualitative case study on real-time strategy footage, a cost model showing the mosaic reduces per-minute image payloads by 9x, and a candid account of observed failure modes - hallucinated game state, resolution loss from mosaicking, and prosody artifacts from time-scaling. We release the system as a reproducible baseline, with an evaluation protocol for the quantitative study a full version will report.

</details>


### [27] [ForgeWM: Progressive Causal Training for Few-Step Action-Conditioned Video World Models](https://arxiv.org/abs/2608.14022)
*Xinye Li,Lingshuai Lin,Lei Wang,Liuzhou Zhang,Jialin Cui,Qingshan Li,Guanchu Wang,Qingbin Liu,Xi Chen,Jiang Bian,Wai Lam*

Main category: cs.CV

TL;DR: ForgeWM is a progressive framework that transforms a bidirectional action-conditioned video generator into efficient few-step interactive world models via a four-stage recipe (domain adaptation, teacher-forced causal training, causal consistency distillation, and on-policy distribution matching). It produces budget-specialized students operating at 1, 2, and 4 denoising steps, supports a dual-path deployment protocol with latency-critical interaction and replay-time refinement, and demonstrates state-of-the-art performance on Minecraft and gamepad-controlled FPS gameplay.


<details>
  <summary>Details</summary>
Motivation: Action-conditioned video world models require low-latency causal generation and reliable responses to game-native controls. Extending causal distillation to interactive settings is challenging because discrete keyboard states and continuous mouse motion must remain aligned with temporally compressed latent chunks during causal training and autoregressive rollout.

Method: ForgeWM progressively transforms a bidirectional action-conditioned video generator into efficient few-step world models through four stages: (1) domain adaptation, (2) teacher-forced causal training, (3) causal consistency distillation, and (4) on-policy distribution matching with a bidirectional teacher. It creates budget-specialized students for 1, 2, and 4 denoising steps, and supports a dual-path deployment protocol combining latency-critical interaction with optional replay-time refinement (one-step student re-noises and refines its saved draft).

Result: On paired Minecraft trajectories, ForgeWM leads evaluated systems in Imaging Quality, reference-aligned motion-profile agreement, action-sign accuracy, and mouse-control accuracy, while achieving the lowest reference LPIPS. The four-stage recipe transfers to gamepad-controlled FPS gameplay. Replay-time refinement matches four-step reference quality while remaining roughly three times closer to the experienced trajectory than regeneration from noise.

Conclusion: ForgeWM demonstrates effectiveness for controllable few-step video generation in interactive world models, achieving state-of-the-art quality and latency trade-offs across different game environments and control modalities.

Abstract: Action-conditioned video world models require low-latency causal generation and reliable responses to game-native controls. Although causal distillation enables one- or few-step video synthesis, extending it to interactive world models remains challenging, as discrete keyboard states and continuous mouse motion must remain aligned with temporally compressed latent chunks during causal training and autoregressive rollout. We introduce ForgeWM, a progressive framework that transforms a bidirectional action-conditioned video generator into efficient few-step world models through domain adaptation, teacher-forced causal training, causal consistency distillation, and on-policy distribution matching with a bidirectional teacher. The resulting budget-specialized students operate at steady-state denoising budgets of 1, 2, and 4 steps. ForgeWM further supports a dual-path deployment protocol combining latency-critical interaction with optional replay-time refinement, where the one-step student re-noises and refines its saved draft. On paired Minecraft trajectories, ForgeWM leads the evaluated systems in Imaging Quality, reference-aligned motion-profile agreement, action-sign accuracy, and mouse-control accuracy, while achieving the lowest reference LPIPS; the same four-stage recipe transfers to gamepad-controlled FPS gameplay. Replay-time refinement matches four-step reference quality while remaining roughly three times closer to the experienced trajectory than regeneration from noise. These results demonstrate ForgeWM's effectiveness for controllable few-step video generation.

</details>


### [28] [SSP: An Event-Matched Syn2Sim2Phy Cross-Domain Evaluation Framework for Autonomous Driving VLA Models](https://arxiv.org/abs/2608.14024)
*Haojie Feng,Peizhi Zhang,Xinrui Zhang,Zhuoren Li,Junpeng Huang,Xiurong Wang,Dongxiao Yin,Yuxiang Zhang,Junfan Zhu,Lu Xiong*

Main category: cs.CV

TL;DR: This paper proposes SSP (Synthetic-Simulation-Physical), an event-matched cross-domain evaluation framework for vision-language-action (VLA) models in autonomous driving. SSP anchors comparisons across synthetic, simulated, and physical domains to the same safety-critical interaction, using validated event specifications and transfer audits. Evaluation of three VLA models (OpenEMMA, LLaViDA, Alpamayo-R1) shows domain performance varies by scenario, with no single domain universally superior.


<details>
  <summary>Details</summary>
Motivation: Existing VLA model evaluations use independently selected data from synthetic, simulated, and physical domains, making measured performance gaps confounded by scenario content differences rather than genuine domain sensitivity. A principled framework is needed to compare VLA behavior across domains on matched events.

Method: SSP builds a validated event specification from a synthetic long-tail video, preserving road topology, participant roles, relative motion, conflict evolution, passing order, response constraints, and event phases. Platform-specific realizations are constructed in CARLA and on a closed proving ground, with transfer audits confirming preservation of mandatory event properties. Heterogeneous VLA outputs (from OpenEMMA, LLaViDA, Alpamayo-R1) are mapped to common semantic slots and a 1-second trajectory window to assess output validity, semantic accuracy, critical-interaction recognition, trajectory quality, and risk response.

Result: Across Cut-in and vulnerable-road-user crossing cases, macro-averaged Integrated VLA Capability Scores are 0.259, 0.291, and 0.325 for Synthetic, Simulation, and Physical domains respectively, with the best domain varying by scenario. Alpamayo-R1, OpenEMMA, and LLaViDA achieve scores of 0.405, 0.338, and 0.131 respectively.

Conclusion: SSP provides a reproducible scene-transfer chain and evidence-qualified evaluation of VLA behavior. The framework does not assume the Physical domain is universally superior; instead it reveals scenario-dependent domain sensitivity, enabling more honest and rigorous cross-domain assessment of VLA models.

Abstract: Vision-language-action (VLA) models for autonomous driving jointly produce scene interpretation, language-based reasoning, and driving trajectories. Existing evaluations often use independently selected synthetic, simulated, and physical data, so measured performance gaps can be confounded by changes in scenario content rather than genuine domain sensitivity. We propose SSP (Synthetic-Simulation-Physical), an event-matched Syn2Sim2Phy evaluation framework that anchors cross-domain comparison to the same safety-critical interaction. Starting from a synthetic long-tail video, SSP builds a validated event specification that preserves road topology, participant roles, relative motion, conflict evolution, passing order, response constraints, and event phases. Platform-specific realizations are then constructed in CARLA and on a closed proving ground and are evaluated only after transfer audits confirm preservation of mandatory event properties. SSP maps heterogeneous outputs from OpenEMMA, LLaViDA, and Alpamayo-R1 into common semantic slots and a 1 s trajectory window to assess output validity, semantic accuracy, critical-interaction recognition, trajectory quality, and risk response. Across Cut-in and vulnerable-road-user crossing cases, the macro-averaged Integrated VLA Capability Scores are 0.259, 0.291, and 0.325 in the Synthetic, Simulation, and Physical domains, respectively, while the best domain varies by scenario. Alpamayo-R1, OpenEMMA, and LLaViDA obtain scores of 0.405, 0.338, and 0.131. SSP provides a reproducible scene-transfer chain and an evidence-qualified evaluation of VLA behavior without assuming that the Physical domain is universally superior.

</details>


### [29] [E-S2Feat:Semantic-Guided Spiking Local Feature Detection and Description for Event Cameras](https://arxiv.org/abs/2608.14027)
*Yang Yi,Juntao Hua,Jinpu Zhang,Liangwei Fan,Hui Shen,Dewen Hu*

Main category: cs.CV

TL;DR: E-S2Feat is a spiking neural network framework for event-based local feature detection and description, featuring a module-specific spiking activation mechanism and a semantic-guided feature modulation mechanism. It outperforms baselines like SuperEvent in pose estimation, matches ANN counterparts in accuracy, and achieves ~4.8x better theoretical computational energy efficiency, with validation in full SLAM systems on TUM-VIE.


<details>
  <summary>Details</summary>
Motivation: Event-based local feature methods suffer from event sparsity, noise, and limited texture, hindering robust feature learning. Additionally, deploying such methods on resource-constrained platforms (e.g., UAVs) requires a balance between accuracy and energy efficiency, motivating the need for a spiking neural network-based framework.

Method: The framework jointly optimizes local feature learning via two mechanisms: (1) a module-specific spiking activation mechanism that preserves fine-grained structural cues and discriminative information under low-bit, energy-efficient inference, and (2) a semantic-guided feature modulation mechanism that uses semantic priors to refine keypoint response distributions and enhance local descriptor discriminability for greater geometric stability and discriminative capability.

Result: On ECD and EDS datasets, E-S2Feat significantly outperforms baselines like SuperEvent in pose estimation accuracy. It achieves accuracy comparable to its ANN counterpart while delivering approximately 4.8x improvement in theoretical computational energy efficiency. Visual-inertial odometry experiments on TUM-VIE validate its effectiveness in complete SLAM systems.

Conclusion: E-S2Feat demonstrates that spiking neural networks can effectively handle event-based local feature learning, achieving strong accuracy while being significantly more energy-efficient—making it suitable for resource-constrained platforms and practical SLAM applications.

Abstract: Benefiting from high temporal resolution and dynamic range, event-based local feature methods have attracted increasing attention. However, event sparsity, noise, and limited texture still hinder robust local feature learning. Deploying such methods on resource-constrained platforms such as unmanned aerial vehicles also requires balancing accuracy and energy efficiency. To address these challenges, this paper proposes \textbf{E-S2Feat}, a spiking neural network framework for event-based local feature detection and description. The framework jointly optimizes local feature learning from the perspectives of feature representation and selection. First, a module-specific spiking activation mechanism preserves fine-grained structural cues and discriminative information under low-bit, energy-efficient inference, thereby improving overall representation fidelity. Furthermore, a semantic-guided feature modulation mechanism leverages semantic priors to refine keypoint response distributions and enhance local descriptor discriminability, thereby guiding the model to extract local features with greater geometric stability and stronger discriminative capability. Experiments on the ECD and EDS datasets show that the proposed method significantly outperforms baseline methods such as SuperEvent in pose estimation accuracy. It also achieves accuracy comparable to its artificial neural network counterpart while delivering an approximately 4.8-fold improvement in theoretical computational energy efficiency. Visual-inertial odometry experiments on the TUM-VIE dataset further verify the effectiveness and practical application potential of the proposed method in complete SLAM systems.

</details>


### [30] [Beyond Text Conditioning: A Systematic Study of MLLM-DiT Fusion for Video Generation](https://arxiv.org/abs/2608.14043)
*Yanbo Ding,Yijia Fan,Caihua Shan,Yifan Yang,Yifei Shen,Weijie Wang,Xirui Hu,Dongsheng Li,Lili Qiu,Yuqing Yang,Yali Wang*

Main category: cs.CV

TL;DR: This paper proposes BiVidGen, a hybrid framework for video generation where an MLLM first generates discrete semantic visual tokens (via an EMA-based tokenizer and autoregressive causal modeling) which are then fed to a Diffusion Transformer (DiT) as explicit conditioning through multi-layer cross-attention, improving semantic alignment and temporal coherence over text-only DiT baselines.


<details>
  <summary>Details</summary>
Motivation: Diffusion Transformers (DiTs) dominate video generation but lack high-level semantic planning capabilities. While hybrid MLLM+diffusion architectures have proven beneficial in image synthesis, they remain underexplored in video generation, where MLLMs are often used only as frozen feature encoders rather than semantic generators. The paper systematically investigates how to properly integrate an MLLM with a DiT for video generation.

Method: The paper conducts a systematic study answering three questions: (1) what intermediate representation should bridge MLLM and DiT (finding: discrete semantic visual tokens from an EMA-based tokenizer), (2) how the MLLM should generate it (finding: autoregressive causal modeling), and (3) how the DiT should incorporate it (finding: explicit visual-token conditioning via multi-layer cross-attention is superior to prompt refinement or latent bridging). Based on these findings, BiVidGen is proposed—a hybrid framework where an MLLM generates semantic visual tokens autoregressively and a DiT renders video conditioned on both text and these tokens.

Result: Extensive experiments show BiVidGen improves semantic alignment and temporal coherence over a fine-tuned DiT baseline, achieving stronger performance on VBench-Long benchmark.

Conclusion: Explicit MLLM-based visual planning provides an effective intermediate interface for text-to-video generation, demonstrating that semantic visual token conditioning can go beyond text-only conditioning in DiT-based video generation.

Abstract: Diffusion Transformers (DiTs) have become the dominant paradigm for high-fidelity video generation, yet their ability to perform high-level semantic planning remains limited. While hybrid architectures integrating MLLMs with diffusion backbones have shown strong advantages in image synthesis, such designs remain underexplored in video generation, where existing approaches often treat MLLMs primarily as frozen feature encoders rather than semantic generators. To fill this gap, we systematically study how an MLLM should be integrated with a DiT for video generation by answering three questions: what intermediate representation should bridge the MLLM and DiT, how the MLLM should generate it, and how the DiT should incorporate it during diffusion rendering. Our analysis reveals three key findings: (1) discrete semantic visual tokens produced by an EMA-based tokenizer provide a stable and expressive interface, (2) autoregressive causal modeling is effective for generating these tokens, and (3) explicit visual-token conditioning is more effective than prompt refinement or latent bridging. Based on these findings, we propose BiVidGen, a hybrid framework where an MLLM first generates semantic visual tokens and a DiT renders videos conditioned on both text and these tokens via multi-layer cross-attention. Extensive experiments show that BiVidGen improves semantic alignment and temporal coherence over a fine-tuned DiT baseline, achieving stronger performance on VBench-Long. These results demonstrate that explicit MLLM-based visual planning provides an effective intermediate interface for text-to-video generation beyond text-only conditioning.

</details>


### [31] [Source-Agnostic Image Translation Based on Latent Aware Adaptive Masking](https://arxiv.org/abs/2608.14046)
*Tomislav Dobrički,Byung-Woo Hong*

Main category: cs.CV

TL;DR: A source-agnostic framework that dynamically refines binary masks during reverse diffusion using time-dependent statistical thresholding of prediction discrepancies, enabling automated domain localization and seamless image-to-image translation without specialized training.


<details>
  <summary>Details</summary>
Motivation: Existing unsupervised image-to-image translation methods rely on fixed thresholds or source-specific training, limiting their adaptability across diverse source distributions and failing to capture the diffusion model's varying predictive confidence across noise levels.

Method: The framework computes prediction discrepancies of a pretrained diffusion model at each latent time step and applies a time-dependent statistical thresholding scheme based on empirical mean and standard deviation of discrepancies, dynamically refining the binary mask throughout the reverse diffusion process.

Result: On AFHQ and CelebA-HQ datasets, the approach outperforms state-of-the-art unsupervised Image-to-Image methods in both realism (FID, KID) and faithfulness (SSIM, LPIPS), requiring only a pretrained target-domain model.

Conclusion: The proposed source-agnostic approach enables precise, automated localization and seamless translation across diverse source distributions without specialized training or source-specific conditioning, achieving superior performance over existing methods.

Abstract: In this work, we propose a source-agnostic framework that dynamically refines a binary mask throughout the reverse diffusion process by computing the discrepancies of a pretrained diffusion model's prediction for each latent time step. Rather than relying on a fixed threshold, our method introduces a time-dependent statistical thresholding scheme derived from the empirical mean and standard deviation of prediction discrepancies across the latent noisy images from the target distribution. This allows the mask to adapt to the model's varying predictive confidence at different noise levels, effectively isolating domain-specific regions while preserving global structural coherence. Experimental results on the AFHQ and Celeba-HQ datasets demonstrate that our approach outperforms state-of-the-art unsupervised Image-to-Image methods in both realism (FID, KID) and faithfulness (SSIM, LPIPS). By requiring only a pretrained model of the target domain, our approach enables precise, automated localization and seamless translation across diverse source distributions without any specialized training. The project source code is available at: https://github.com/dtoma95/PM-Edit

</details>


### [32] [Discovery and Spatial Characterisation of Multiple Shortcut Groups for Auditing Vision Model Bias](https://arxiv.org/abs/2608.14051)
*Akshit Achara,Vishnunarayan Manickam,Thomas Day,Esther Puyol Anton,Alexander Hammers,Andrew P. King*

Main category: cs.CV

TL;DR: This paper addresses the limitation of existing attribution-map-based analyses of spurious correlations, which aggregate per-image maps across datasets and can mask recurring shortcut patterns in image subsets. The authors propose grouping per-image shortcut and task contribution maps into recurring spatial patterns via K-means and non-negative matrix factorisation, and visualize these groups across multiple datasets and model architectures.


<details>
  <summary>Details</summary>
Motivation: Out-of-distribution testing highlights subgroup performance disparities from shortcut learning but does not localize spatial regions associated with it. Existing methods using attribution maps (e.g., conditional alignment) compare task/sensitive/reference models to produce shortcut and task contribution maps, but they aggregate these across the dataset, potentially obscuring recurring spatial shortcut patterns present only in subsets of images.

Method: Per-image shortcut and task contribution maps are generated from a task model, a sensitive attribute model, and a bias-reduced reference model via conditional alignment. These maps are then grouped into recurring spatial patterns using K-means clustering and non-negative matrix factorisation, and visualized through contribution maps and representative examples. Additionally, input occlusion and test-time feature interventions are used to validate task contribution regions, and a combined shortcut suppression and task amplification feature intervention is proposed.

Result: Across CelebA, CheXpert, Waterbirds, Camelyon17, and ISIC2019, with ResNet and ViT models, the discovered shortcut groups reveal both shared and distinct spatial patterns of shortcut and task contribution, with varying subgroup composition and error rates. Occlusion and intervention experiments confirm that masking or suppressing task contribution regions degrades classification performance, and the proposed combined shortcut suppression and task amplification intervention generally reduces performance disparities.

Conclusion: The proposed grouping approach enables targeted inspection of image subsets with higher error rates by revealing recurring spatial shortcut patterns that existing aggregate-based analyses would miss, and the feature intervention approach provides a practical method for reducing subgroup performance disparities.

Abstract: Deep learning models trained on datasets with spurious correlations can achieve high average accuracy whilst relying on shortcut features that do not generalise out of distribution. Whilst out-of-distribution testing highlights subgroup performance disparities arising from shortcut learning, it does not localise the regions within images that are associated with it. Existing research mostly uses attribution maps from interpretability methods to understand the spatial nature of spurious correlations. For example, conditional alignment methods separate task-relevant evidence from evidence tied to spurious correlations by comparing attribution maps from a task model, a sensitive attribute model, and a bias-reduced reference model. This yields shortcut-aligned and task-aligned contribution maps for each image. However, existing methods aggregate these maps across the dataset, potentially masking recurring spatial shortcut patterns that occur only in subsets of images. We address this limitation by grouping per-image shortcut and task contribution maps into recurring spatial patterns using K-means and non-negative matrix factorisation, and visualising the resulting shortcut groups through contribution maps and representative examples. Across CelebA, CheXpert, Waterbirds, Camelyon17, and ISIC2019, and across ResNet and ViT models, the discovered shortcut groups reveal both shared and distinct spatial patterns of shortcut and task contribution, with varying subgroup composition and error rates, enabling targeted inspection of image subsets with higher error rates. We perform input occlusion and internal test-time interventions to show that masking or suppressing task contribution regions substantially degrades the model classification performance and propose a combined shortcut suppression and task amplification feature intervention approach which generally reduces performance disparities.

</details>


### [33] [Voxel-based 3D Facies Segmentation from Seismic Data: A Comparative Study](https://arxiv.org/abs/2608.14058)
*Duc-Thanh Pham,Minh-Tan Pham,Anh Nguyen,Van Nguyen*

Main category: cs.CV

TL;DR: This paper presents a reproducible benchmark for voxel-based 3D seismic facies segmentation, comparing modern 3D segmentation architectures on public datasets (F3 and Parihaka) with standardized splits and metrics, highlighting the limitations of 2D slice-based approaches.


<details>
  <summary>Details</summary>
Motivation: Existing seismic facies segmentation methods typically reduce 3D seismic volumes to 2D slices (inline/crossline), which introduces discontinuities across slices and fails to preserve spatial and structural continuity in 3D data, limiting coherent geological pattern learning. A standardized benchmark for true 3D segmentation is needed.

Method: The authors build a comparative and reproducible benchmark for voxel-based 3D seismic facies segmentation using public datasets (Netherlands F3 and Parihaka) with standardized data splits and evaluation metrics, and evaluate three representative families of modern 3D segmentation architectures.

Result: Strong baseline results are established across the three families of 3D segmentation architectures, demonstrating both the potential and remaining challenges of voxel-based 3D seismic facies segmentation.

Conclusion: True 3D voxel-based segmentation is essential for preserving spatial continuity in seismic facies analysis, and this benchmark provides a standardized foundation to foster future research and reproducible comparisons in this domain.

Abstract: Seismic facies segmentation has emerged as a significant challenge in geophysics, requiring robust methods and systems to effectively identify geologically analogous facies with limited labeled data. Although existing studies have shown promising results in 2D facies segmentation, they often preprocess the original 3D seismic volumes into sets of 2D slices, typically the inline and crossline directions, and treat this problem as a purely 2D segmentation task. This simplification introduces discontinuities across slices and fails to preserve the spatial and structural continuity in 3D seismic data, thus limiting the model's ability to learn coherent geological patterns. In this work, we present a comparative and reproducible benchmark for voxel-based 3D seismic facies segmentation, built upon publicly available seismic volumes including the Netherlands F3 and the Parihaka datasets, with standardized data splits and evaluation metrics. By evaluating the three representative families of modern 3D segmentation architectures, we establish strong baseline results that highlight the potential and remaining challenges for future research in this domain.

</details>


### [34] [InstructVVT: Instruction-Driven Video Virtual Try-On without Auxiliary Spatial Priors](https://arxiv.org/abs/2608.14070)
*Dingbao Shao,Song Wu,Xinyu Chen,Qian Wang,Jiahang Li,Kuai Jiang,Jiang Lin,Yuhang Liu,Ziyu Chen,Duo Li,Jiaxin Hu,Shengrong Gu,Ziheng Tang,Rongrong Liu,Yanlun Peng,Liang Li,Junlan Feng,Lujia Jin,Ting Zhang,Jian Yang,Zili Yi*

Main category: cs.CV

TL;DR: InstructVVT is an instruction-driven, reference-guided video virtual try-on framework based on a Diffusion Transformer that eliminates the need for inference-time spatial priors. It uses a dual-level reference conditioning scheme (MLLM-derived semantic edit tokens plus fine-grained visual garment injection) and a try-on-specific reward with DiffusionNFT alignment to outperform state-of-the-art methods in garment fidelity, structural preservation, and temporal consistency.


<details>
  <summary>Details</summary>
Motivation: Existing video virtual try-on methods rely on handcrafted spatial priors (masks, poses) that fail in unconstrained real-world videos and compress rich visual context into incomplete structural signals. Additionally, standard reconstruction objectives do not fully capture try-on-specific human preferences.

Method: The framework operates on a Diffusion Transformer with a dual-level reference conditioning scheme: (1) an MLLM generates semantic edit tokens for target disambiguation and structural preservation, and (2) a lightweight conditioning pathway injects fine-grained visual garment details. A try-on-specific reward with the DiffusionNFT algorithm aligns the model with human preferences. No spatial priors are required at inference time.

Result: Extensive experiments on ViViD-S and TripVVT-Bench show InstructVVT outperforms state-of-the-art open-source methods in garment fidelity, structural preservation, and temporal consistency, despite requiring fewer inference-time controls.

Conclusion: InstructVVT demonstrates that recovering fine-grained control directly from the input triplet (source video, reference garment, instruction) via dual-level conditioning is sufficient for high-quality video virtual try-on, eliminating dependence on fragile spatial priors while achieving superior performance.

Abstract: Video virtual try-on is a highly constrained editing task requiring the precise replacement of a target person's clothing while strictly preserving the original video's spatial structure and temporal dynamics. Existing methods heavily rely on auxiliary handcrafted spatial priors (e.g., masks, poses) for editing control. However, these priors are prone to failure in unconstrained real-world videos and often compress rich visual context into incomplete structural signals. Furthermore, standard reconstruction objectives fail to fully capture try-on-specific human preferences. To address these challenges, we propose InstructVVT, an instruction-driven and reference-guided video virtual try-on framework based on a Diffusion Transformer (DiT) that operates without inference-time spatial priors. Our core insight is to recover fine-grained control directly from the input triplet (source video, reference garment, and instruction) via a dual-level reference conditioning scheme. Specifically, an MLLM infers semantic edit tokens for target disambiguation and structural preservation, while a lightweight conditioning pathway explicitly injects fine-grained visual garment details. Finally, we design a try-on-specific reward and utilize the DiffusionNFT algorithm to align the model with human preferences. Extensive experiments on ViViD-S and TripVVT-Bench demonstrate that InstructVVT outperforms state-of-the-art open-source methods in garment fidelity, structural preservation, and temporal consistency, despite requiring fewer inference-time controls.

</details>


### [35] [Owner3D: Ownership-Guided Style Writing for Training-Free Localized 3D Stylization](https://arxiv.org/abs/2608.14078)
*Suchang Tao,Kaifeng Shi,Zhiyan Liu,Zhuoyuan Jiang,Yuqi Ouyang*

Main category: cs.CV

TL;DR: Owner3D is a training-free framework for localized 3D stylization that integrates ownership-guided style writing, boundary dual slots, and surface-first texture readout into large reconstruction models (LRMs) to prevent style leakage and preserve non-target region appearance, reducing appearance leakage by 86.4% and 89.9% over StyleSplat and LAENeRF.


<details>
  <summary>Details</summary>
Motivation: Localized 3D stylization is challenging in LRMs because style is injected into shared triplane features before rendering, causing style leakage and boundary ambiguity between target and non-target surfaces.

Method: The method introduces (1) ownership-guided style writing to restrict style injection to target regions in a single localized triplane without extra training; (2) boundary dual slots with separate local feature sources for target and non-target regions to resolve boundary ambiguity; and (3) surface-first texture readout that hierarchically combines surface, 3D, and triplane ownership evidence to robustly recover appearance under incomplete visibility.

Result: On a benchmark from Google Scanned Objects and PartNet, Owner3D consistently outperforms existing 3D stylization methods in target-region style fidelity and non-target appearance preservation, reducing appearance leakage by 86.4% and 89.9% compared with StyleSplat and LAENeRF, respectively.

Conclusion: Owner3D demonstrates that training-free ownership-guided control can effectively achieve localized 3D stylization in LRMs, significantly reducing style leakage while preserving non-target regions.

Abstract: Localized 3D stylization aims to modify the appearance of a specified object part while preserving the remaining surfaces. In large reconstruction models (LRMs), this task is challenging because style is injected into intermediate appearance representations before rendering, while compact triplane features are shared across target and non-target surfaces, causing style leakage and boundary ambiguity. We propose Owner3D, a training-free framework for localized 3D stylization that integrates localized appearance control directly into the LRM reconstruction process. Specifically, Owner3D introduces ownership-guided style writing to restrict reference-style injection to target regions, producing a single localized stylized triplane without additional training while avoiding separate global style and appearance representations. To resolve appearance ambiguity near semantic boundaries, we further introduce boundary dual slots that maintain separate local feature sources for target and non-target regions. Finally, a surface-first texture readout hierarchically combines surface, 3D, and triplane ownership evidence to robustly recover appearance under incomplete visibility. Experiments on a benchmark constructed from Google Scanned Objects and PartNet demonstrate that Owner3D consistently outperforms existing 3D stylization methods in target-region style fidelity and non-target appearance preservation, reducing appearance leakage by 86.4% and 89.9% compared with StyleSplat and LAENeRF, respectively.

</details>


### [36] [CoDS: Robust Collaborative Perception via Expert-driven Detection and BEV Segmentation](https://arxiv.org/abs/2608.14085)
*Jinlong Wang,Yuang Jia,Junhong Lin,Nannan Li,Wei Gao*

Main category: cs.CV

TL;DR: CoDS is a collaborative perception framework that jointly trains detection and BEV segmentation to improve robustness against multi-source noise (pose errors, communication delays). It introduces a Collaborative Reliability Map (CoRM) to quantify feature quality, a Semantic Mixture-of-Experts (S-MoE) for differentiated feature extraction, and a Bidirectional Task Complementary Interaction (BTCI) for task-aware feature refinement. It outperforms baselines on OPV2V and V2V4Real datasets.


<details>
  <summary>Details</summary>
Motivation: Multi-source noise (pose errors, communication delays) degrades fusion feature quality in collaborative perception, constraining perception performance. Joint training of detection and BEV segmentation offers a natural remedy but requires handling spatial inconsistency in fusion quality and feature noise degradation.

Method: 1) Collaborative Reliability Map (CoRM) explicitly quantifies feature quality distribution to address spatial inconsistency. 2) Semantic Mixture-of-Experts (S-MoE) module extracts differentiated features for inconsistent feature demands. 3) Bidirectional Task Complementary Interaction (BTCI) refines task-aware features through bidirectional injection to mitigate feature noise degradation.

Result: Extensive experiments on OPV2V and V2V4Real datasets show CoDS surpasses existing baselines on both detection and BEV segmentation tasks, and maintains stable robustness under multi-source noise.

Conclusion: Joint training of detection and BEV segmentation with explicit reliability quantification and expert-driven feature extraction provides a robust solution to noise degradation in collaborative perception.

Abstract: Collaborative perception breaks through single-view limitations via multi-agent information exchange. However, multi-source noise such as pose errors and communication delays degrades fusion feature quality, constraining perception performance. Joint training of detection and BEV segmentation provides a natural remedy, where segmented road regions help constrain target distributions and detection bounding boxes help recover ambiguous segmentation boundaries. To this end, we propose a robust Collaborative perception framework with expert-driven Detection and bev Segmentation (CoDS). To address spatial inconsistency in fusion quality, we first introduce the Collaborative Reliability Map (CoRM) to explicitly quantify feature quality distribution. Based on CoRM, we design the Semantic Mixture-of-Experts (S-MoE) module to extract differentiated features for inconsistent feature demands. Finally, to further mitigate feature noise degradation, the Bidirectional Task Complementary Interaction (BTCI) refines task-aware features through bidirectional injection. Extensive experiments on OPV2V and V2V4Real datasets show that our CoDS surpasses existing baselines on both tasks and maintains stable robustness under multi-source noise. Code: https://github.com/JinlongW128/CoDS and https://openi.pcl.ac.cn/OpenAIDriving/CoDS.

</details>


### [37] [Fixed-Budget Gaussian Volume Encoding with Structure-Aware Allocation](https://arxiv.org/abs/2608.14112)
*Michael R. Martin,Joseph Insley,Victor A. Mateevitsi,Silvio Rizzi,Kwan-Liu Ma*

Main category: cs.CV

TL;DR: This work encodes scientific simulation scalar volumes as anisotropic Gaussian primitives under a fixed budget, using one-shot analytical allocation from local field structure followed by direct refinement. It achieves up to 51x encoding speedup with truncation-aware evaluation, encodes a billion-voxel volume with 1.4M Gaussians in under four minutes on one desktop GPU, and delivers 15.0-38.7 dB PSNR at compression ratios from 2.2x to over 40,000x across five datasets.


<details>
  <summary>Details</summary>
Motivation: Scientific simulations produce scalar volumes faster than they can be stored, transferred, and loaded, while in situ reduction must operate under limited simulation resources—requiring an efficient compression approach that balances fidelity, encoding speed, and post-hoc flexibility.

Method: Encode scalar fields as anisotropic Gaussian primitives under a fixed budget. The complete primitive set is allocated analytically from local field structure (position, orientation, shape), then refined directly against the scalar field without densification, pruning, or count changes. Truncation-aware field evaluation accelerates encoding, and the budget/iteration schedule controls refinement-time cost. Primitives retain scalar attributes rather than baked appearance.

Result: Truncation-aware field evaluation reduces encoding time by up to 51x; 1.4M Gaussians encode a billion-voxel volume in at most four minutes on one desktop GPU (under one minute with reduced-iteration refinement). Across five datasets (2.1M to 1.1B voxels), compression-useful configurations achieve 15.0-38.7 dB PSNR at compression ratios from 2.2x to over 40,000x.

Conclusion: Gaussian primitive encoding provides a compact, controllable, and flexible representation for scientific scalar volumes. One-shot allocation plus direct refinement offers a favorable accuracy/speed trade-off, and retaining scalar attributes enables post-hoc transfer-function, colormap, lighting, and viewpoint changes without re-encoding.

Abstract: Scientific simulations often produce scalar volumes faster than they can be stored, transferred, and loaded, while in situ reduction must use only a limited share of simulation resources. This work encodes scalar fields as anisotropic Gaussian primitives under a fixed budget. The complete primitive set is allocated analytically from local field structure, including position, orientation, and shape, then refined directly against the scalar field without densification, pruning, or count changes. The selected budget determines encoded storage before refinement and, together with the iteration schedule, provides a controllable refinement-time budget. In a controlled benchmark, truncation-aware field evaluation reduces encoding time by up to 51x; 1.4 million Gaussians encode a billion-voxel volume in at most four minutes on one desktop GPU, with reduced-iteration refinement completing in under one minute. Across five datasets spanning 2.1 million to 1.1 billion evaluated voxels, compression-useful configurations achieve 15.0-38.7 dB PSNR at compression ratios from 2.2x to over 40,000x. Pre-encoding structure statistics characterize fields for which one-shot allocation yields limited gains from additional capacity. Because primitives retain scalar attributes rather than baked appearance, a single compact model serves every subsequent visualization state - supporting post-hoc transfer-function, colormap, lighting, and viewpoint changes without re-encoding.

</details>


### [38] [HiCo-GS: Hierarchical Context Aggregation and Geometric Consistency for Octree Gaussian Splatting](https://arxiv.org/abs/2608.14136)
*Wei Zhang,Shengkai Yu,Shiqiang Gong,Qi Zhang,Qiang Li,Qi Wang*

Main category: cs.CV

TL;DR: HiCo-GS presents a high-fidelity 3D Gaussian Splatting framework for city-scale novel view synthesis that addresses cross-level feature isolation in octree-based anchor representations. It introduces two modules: Cross-Level Context Aggregation (CLCA) for bidirectional hierarchical prior injection between coarse and fine anchor levels, and Depth-Normal Geometric Consistency (DNGC) regularization for cleaner geometry. The paper also contributes the China-Pagoda dataset with 8 ancient pagodas (1,200+ images each) featuring complex ornamental structures, and demonstrates state-of-the-art rendering quality on Mill19, UrbanScene3D, MatrixCity, and China-Pagoda benchmarks.


<details>
  <summary>Details</summary>
Motivation: Existing octree-based anchor Gaussian Splatting methods suffer from cross-level feature isolation, where each level's anchor features are optimized independently with no inter-level communication. This causes color drift on building facades and over-smoothing in textured regions, limiting rendering quality in city-scale scenes.

Method: HiCo-GS consists of two complementary modules: (1) Cross-Level Context Aggregation (CLCA) leverages the octree's spatial containment structure to aggregate per-level context vectors into parent-self-child triplets, fused via a lightweight MLP with residual connections, enabling bidirectional hierarchical prior injection. (2) Depth-Normal Geometric Consistency (DNGC) regularization enforces agreement between rendered normals and depth-derived normals through an alpha-weighted consistency loss, complemented by edge-aware smoothness losses with progressive warmup that exploit urban planar priors.

Result: HiCo-GS achieves state-of-the-art rendering quality and substantially cleaner geometry across real-world and synthetic urban benchmarks including Mill19, UrbanScene3D, MatrixCity, and the newly introduced China-Pagoda dataset, at negligible computational overhead.

Conclusion: The proposed HiCo-GS framework effectively resolves cross-level feature isolation in octree-based anchor Gaussian Splatting through bidirectional hierarchical context aggregation and geometric consistency regularization, setting a new state-of-the-art for city-scale novel view synthesis while also contributing a benchmark dataset of ancient Chinese pagodas.

Abstract: Octree-based anchor Gaussian Splatting has emerged as a scalable representation for city-scale novel view synthesis, where multi-level anchors adaptively capture scene content from coarse building structures to fine architectural details. However, we identify a fundamental limitation in existing methods: cross-level feature isolation, where each level's anchor features are optimized independently with no inter-level communication, causing color drift on building facades and over-smoothing in textured regions. We present HiCo-GS, a high-fidelity reconstruction framework with two complementary modules. Cross-Level Context Aggregation (CLCA) enables bidirectional hierarchical prior injection by leveraging the octree's spatial containment structure to aggregate per-level context vectors into parent-self-child triplets, fused via a lightweight MLP with residual connection. Coarse-level structural priors flow down to inform fine-level anchors, while fine-level detail statistics feed back to prevent over-smoothing, at negligible computational overhead. Depth-Normal Geometric Consistency (DNGC) regularization enforces agreement between rendered normals and depth-derived normals through an alpha-weighted consistency loss, complemented by edge-aware smoothness losses with progressive warmup that exploit the strong planar priors ubiquitous in urban geometry to suppress floating artifacts. We further introduce the China-Pagoda dataset comprising 8 ancient Chinese pagodas with over 1,200 images each, featuring dense ornamental carvings, curved multi-layer eaves, and repetitive fine-grained textures. Extensive experiments on Mill19, UrbanScene3D, MatrixCity, and China-Pagoda demonstrate that HiCo-GS achieves state-of-the-art rendering quality and substantially cleaner geometry across real-world and synthetic urban benchmarks.Code: https://github.com/WZ-CS/HiCo-GS.

</details>


### [39] [SPARGen: Unifying Spatial Perception and Reasoning through Native Multimodal Generation](https://arxiv.org/abs/2608.14138)
*Jinsheng Quan,Jianhua Li,Siyi Xie,Xuanke Shi,Kewang Deng,Zukai Chen,Feifei Shao,Lei Yang,Quan Wang,Yawei Luo*

Main category: cs.CV

TL;DR: SPARGen is a unified multimodal framework that reformulates 3D reconstruction, dense correspondence, and spatial reasoning as instruction-conditioned generation tasks within a single native multimodal generative model, achieving competitive performance across these heterogeneous spatial tasks.


<details>
  <summary>Details</summary>
Motivation: Existing approaches address spatial perception tasks (3D reconstruction, correspondence, spatial reasoning) separately using task-specific architectures or external geometric modules, which limits knowledge transfer among complementary representations of the same physical scene.

Method: SPARGen casts all three spatial tasks as instruction-conditioned generation tasks. It serializes compact structured and linguistic outputs as token sequences while generating dense geometric fields in image-aligned forms, allowing spatial supervision to jointly shape shared representations within a native multimodal generative model.

Result: Experiments across benchmarks for 3D reconstruction, correspondence, and spatial reasoning show that SPARGen achieves competitive performance across these heterogeneous spatial tasks within a single native multimodal generative framework.

Conclusion: A single generative multimodal framework can jointly handle multiple spatial perception tasks without task-specific architectures or external geometric modules, demonstrating effective knowledge sharing across complementary spatial representations.

Abstract: Spatial perception and reasoning from visual observations require recovering geometric structure, establishing correspondences, and understanding spatial relations. Existing approaches typically address these capabilities separately using task-specific architectures or external geometric modules, limiting knowledge transfer among complementary representations of the same physical scene. We introduce SPARGen, a unified multimodal framework that casts 3D reconstruction, dense correspondence, and spatial reasoning as instruction-conditioned generation tasks. SPARGen serializes compact structured and linguistic outputs as token sequences while generating dense geometric fields in image-aligned forms, enabling spatial supervision to jointly shape shared representations within a native multimodal generative model. Experiments across benchmarks for 3D reconstruction, correspondence, and spatial reasoning show that SPARGen achieves competitive performance across heterogeneous spatial tasks within a single native multimodal generative framework.

</details>


### [40] [PISA: A Pseudo-Individual Source-Domain Feature Adaptation Framework for Test-Time Open-Vocabulary Object Detection](https://arxiv.org/abs/2608.14142)
*Ziyan He,Xiongtai Yang,Tao Wang*

Main category: cs.CV

TL;DR: PISA is a source-free open-vocabulary object detection test-time adaptation method that uses a Corruption-Invariant Feature Extractor, a Feature Alignment Module, and a multi-scale alignment framework to transform corruption-invariant features into pseudo source-domain features for supervision, outperforming existing methods by 3.92% AP@50% on COCO-C.


<details>
  <summary>Details</summary>
Motivation: Existing OVOD-TTA methods degrade significantly when initial predictions are poor (either re-scoring with test-time info or pseudo-label self-training), and conventional source-domain estimation methods fail to capture dense, concrete features needed for detection (only recovering abstract, sparse classification-oriented representations).

Method: PISA comprises three core components: (1) Corruption-Invariant Feature Extractor (CIFE) exploits the invariance of CLIP's visual features across corrupted images for robustness; (2) Feature Alignment Module (FAM) and (3) multi-scale alignment framework (BAA) transform corruption-invariant features into pseudo-individual source-domain features close to the original source-domain features, providing dense supervision instead of unreliable pseudo-labels.

Result: Experiments on corrupted VOC-C, COCO-C, and LVIS-C benchmarks across three base models show substantial improvements in both localization precision and category recognition accuracy. PISA achieves state-of-the-art performance, surpassing existing methods by 3.92% in AP@50% on COCO-C without requiring source-domain data.

Conclusion: PISA demonstrates that using dense, concrete pseudo-individual source-domain features for supervision (rather than unreliable pseudo-labels) is an effective strategy for source-free OVOD-TTA, and the method can be seamlessly integrated into open-vocabulary visual backbones.

Abstract: Open-vocabulary object detection test-time adaptation (OVOD-TTA) aims to address the performance degradation that pre-trained base models suffer when encountering image-domain shifts. Existing source-free OVOD-TTA methods rely either on refined test-time information for re-scoring or on pseudo-labels for self-training, leading to significant accuracy degradation when initial predictions are poor. Meanwhile, most conventional source-domain estimation methods recover abstract, sparse representations suitable for the classification task, but fail to capture the dense, concrete features required for detection. To address these issues, we propose PISA, a novel source-free OVOD-TTA method that can be seamlessly integrated into open-vocabulary visual backbones. The core components of our method are the Corruption-Invariant Feature Extractor (CIFE), the Feature Alignment Module (FAM), and a multi-scale alignment framework (BAA). To capture detection-suitable features, we develop CIFE to exploit the invariance of CLIP's visual features across corrupted images, ensuring robustness against various corruptions. We further develop FAM and BAA for the pre-training and adaptation to transform the corruption-invariant features into pseudo-individual source-domain features that are close to the original source-domain features. In this way, dense and concrete pseudo-individual source-domain features are used for supervision instead of unreliable pseudo-label signals. Experiments on the corrupted VOC-C, COCO-C, and LVIS-C benchmarks across three base models demonstrate that PISA substantially improves both the localization precision and the category recognition accuracy of the original models. Notably, PISA achieves state-of-the-art performance without requiring access to source-domain data, surpassing existing methods by 3.92% in AP@50% on COCO-C.

</details>


### [41] [Self-Supervised Visual On-Policy Distillation](https://arxiv.org/abs/2608.14144)
*Yijiang Li,Yijun Liang,Yunjie Tian,Bingyang Wang,Ke Zhang,Zhenfei Yin,Di Fu,Philip Torr,Nuno Vasconcelos*

Main category: cs.CV

TL;DR: The paper introduces S2VOPD, a self-supervised visual on-policy distillation method that inverts the traditional teacher-student asymmetry: instead of adding privileged information to the teacher, it subtracts information from the student via strong augmentations. This creates effective learning signals without ground-truth annotations, rewards, or a stronger teacher. The method improves Qwen3.5-4B from 70.7% to 77.4% across six fine-grained perception benchmarks, surpassing larger models including Qwen3-VL (235B) and GPT-5.4, while recovering 96% of the improvement of privileged-information methods.


<details>
  <summary>Details</summary>
Motivation: Traditional visual on-policy distillation requires informative asymmetry from a stronger teacher or privileged supervision, raising the question of where asymmetry can come from when nothing privileged is available. The authors identify this gap and seek to create effective learning signals without any privileged information.

Method: S2VOPD inverts the source of asymmetry: instead of enriching the teacher, it subtracts information from the student by conditioning it on a strongly augmented view of the same image, while the teacher distribution conditions on the original image. The method systematically explores a broad design space of augmentations, finding that asymmetry matters, strength matters (peaking at moderate levels), and the augmentation gap must remain task-consistent.

Result: Across six fine-grained perception benchmarks, S2VOPD improves Qwen3.5-4B from 70.7% to 77.4%, outperforming all compared open-source models up to Qwen3-VL (235B) and surpassing GPT-5.4. It recovers 96% of the improvement achieved by methods with privileged information while keeping training data identical.

Conclusion: Informative asymmetry for distillation can be created for free by subtracting information from the student rather than adding it to the teacher. Three key findings emerge: asymmetry is essential (symmetric self-distillation degrades performance), moderate augmentation strength is optimal, and task-consistent augmentations are crucial to avoid uninformative discrepancies.

Abstract: Visual on-policy distillation relies heavily on an informative teacher-student asymmetry, through either a larger, stronger teacher or privileged supervision, such as reference answers or ground-truth regions of interest. This raises a fundamental question: where can informative asymmetry come from when nothing privileged is available? We answer this by inverting where the asymmetry comes from. Rather than adding privileged information to the teacher, we subtract information from the student. This asymmetry creates the same effective learning signal for free as a teacher with access to information unavailable to the student, without ground-truth annotations, rewards, or a separate stronger teacher model. Building on this principle, we introduce Self-Supervised Visual On-Policy Distillation (S$^2$VOPD), a simple yet effective method that constructs on-policy learning signals from asymmetric augmented views. S$^2$VOPD distills the teacher's distribution conditioned on the original image on-policy into the student distribution conditioned on a strongly augmented view of the same image. We systematically explore a broad design space of visual augmentations and uncover that (1) asymmetry matters: all four augmentation families improve performance, while symmetric self-distillation degrades it; (2) strength matters: performance peaks at a moderate strength; and (3) the gap must remain task-consistent: augmentations that completely remove the question-relevant evidence can induce large but uninformative discrepancies. Across six fine-grained perception benchmarks, S$^2$VOPD improves Qwen3.5-4B from 70.7% to 77.4%, above all open-source models compared, up to Qwen3-VL at 235B, and surpasses GPT-5.4. While holding training data the same, it recovers 96% of the improvement achieved by methods with privileged information. Website is at https://williamium3000.github.io/s2vopd

</details>


### [42] [CSG-Mamba: A Convolutional Scoring Gating Vision State Space Network for Endoscopic Polyp Segmentation](https://arxiv.org/abs/2608.14146)
*Yuliang Wang,Jiaqi Wu,Jiaye Song,Shuxia Ren*

Main category: cs.CV

TL;DR: CSG-Mamba is a Vision State Space network for endoscopic polyp segmentation that inserts a Convolutional Scoring Gating (CSG) module at the bottleneck of a VM-UNet-style encoder-decoder to address low-contrast boundaries and irregular contours, achieving strong Dice and boundary accuracy on Kvasir-SEG and CVC-ColonDB.


<details>
  <summary>Details</summary>
Motivation: Existing Vision Mamba segmentation models convert 2D features into 1D scanning sequences, which can weaken local geometric continuity and over-smooth irregular polyp contours. Additionally, endoscopic images present challenges such as low-contrast boundaries, texture interference, specular highlights, and device-dependent appearance shifts that require improved segmentation.

Method: CSG-Mamba builds on a VM-UNet-style asymmetric U-shaped encoder-decoder and inserts a Convolutional Scoring Gating (CSG) module at the semantically rich bottleneck. The CSG module generates a local spatial score map using pointwise and large-kernel depthwise convolutions, then recalibrates state-space features via multiplicative gating.

Result: Experiments with three random seeds show CSG-Mamba achieves 0.9220 Dice and 15.87 HD95 on Kvasir-SEG, and 0.7418 Dice and 0.6570 mIoU on CVC-ColonDB, outperforming baselines on most overlap and recall metrics while maintaining competitive boundary accuracy.

Conclusion: CSG-Mamba effectively combines local convolution-based scoring with global state-space modeling, improving polyp segmentation performance on overlap and recall metrics while preserving boundary accuracy.

Abstract: Accurate polyp segmentation is critical for computer-aided colonoscopy, yet endoscopic images often contain low-contrast boundaries, mucosal texture interference, specular highlights, and device-dependent appearance shifts. Vision State Space Models (SSMs) provide efficient long-range modeling with linear complexity, but existing Vision Mamba segmentation models typically convert 2D features into 1D scanning sequences, which may weaken local geometric continuity and over-smooth irregular contours. We propose CSG-Mamba, a convolutional scoring gating Vision State Space network for endoscopic polyp segmentation. Built on a VM-UNet-style asymmetric U-shaped encoder-decoder, CSG-Mamba inserts a Convolutional Scoring Gating (CSG) module at the semantically rich bottleneck. CSG generates a local spatial score map through pointwise and large-kernel depthwise convolutions and recalibrates state-space features by multiplicative gating. Experiments with three random seeds show that CSG-Mamba achieves 0.9220 Dice and 15.87 HD95 on Kvasir-SEG, and 0.7418 Dice and 0.6570 mIoU on CVC-ColonDB, outperforming the baselines on most overlap and recall metrics while maintaining competitive boundary accuracy.

</details>


### [43] [SCVIB: Editable State-Conditioned Visual Instance Binding forMulti-Turn Personalized Localization](https://arxiv.org/abs/2608.14148)
*Xiongtai Yang,Ziyan He,Tao Wang*

Main category: cs.CV

TL;DR: This paper introduces editable state-conditioned visual instance binding, a multi-turn localization setting where support-defined instances are introduced across turns and protocol-defined state events determine the final target. The authors create SCVIB (1,050 support-query pairs, 1,500 episodes across five visual domains) and propose TT-VG, combining a Target-State Transition Tree (TSTT) with Visual Evidence Grounding Adaptation (VEGA), improving Joint@0.5 from 60.13% to 70.27%.


<details>
  <summary>Details</summary>
Motivation: Direct Seq-free inference achieves only 60.13% Joint@0.5 on SCVIB, showing that resolving the final reference does not ensure effective use of corresponding visual evidence for query-side localization. This reveals a gap between reference resolution and visual evidence utilization in multi-turn personalized localization.

Method: TT-VG combines two components: (1) TSTT (Target-State Transition Tree) compiles visible interaction into protocol-defined events, executes them over versioned target states, and resolves the final-query reference to corresponding support evidence; (2) VEGA performs support-conditioned grounding of the resolved instance using a Visual Evidence Package, adapted on trajectory-derived same-instance pairs.

Result: TT-VG reaches 70.27% Joint@0.5, improving over the 60.13% baseline. Under matched target resolution, VEGA exceeds the strongest comparison method by 16.20 points. Largest gains are on Counter-Recency and Rollback scenarios requiring routing to non-latest or restored support evidence.

Conclusion: SCVIB serves as a controlled testbed for editable state-conditioned visual instance binding, and the results highlight effective use of resolved support evidence for query-side same-instance localization as a central challenge in multi-turn personalized localization.

Abstract: We introduce editable state-conditioned visual instance binding, a multi-turn localization setting in which several support-defined instances are introduced across turns and protocol-defined state events determine the final target. We instantiate this setting as SCVIB, comprising 1,050 manually verified support--query base pairs and 1,500 episodes spanning five visual domains, three difficulty levels, and four target-state dependency groups. Direct Seq-free inference reaches only 60.13\% Joint@0.5, indicating that resolving the final reference does not ensure effective use of the corresponding visual evidence for query-side localization. We address this gap with TT-VG (Transition-Tree Visual Grounding), which combines a Target-State Transition Tree (TSTT) with Visual Evidence Grounding Adaptation (VEGA). TSTT compiles the visible interaction into protocol-defined events, executes them over versioned target states, and resolves the final-query reference to the corresponding support evidence. Adapted on trajectory-derived same-instance pairs, VEGA performs support-conditioned grounding of the resolved instance using a Visual Evidence Package. TT-VG reaches 70.27\% Joint@0.5; under matched target resolution, VEGA exceeds the strongest comparison method by 16.20 points. Gains over direct inference are largest on Counter-Recency and Rollback, which require routing to non-latest or restored support evidence. Together, these results establish SCVIB as a controlled testbed and highlight the effective use of resolved support evidence for query-side same-instance localization as a central challenge in multi-turn personalized localization.

</details>


### [44] [Concept Guidance: Precise, Training-Free Latent Control for Text-to-Image Generation](https://arxiv.org/abs/2608.14172)
*Nikolai Röhrich,Isabell Hans,Felix Krause,Björn Ommer*

Main category: cs.CV

TL;DR: The paper introduces Concept Guidance (CoG), a training-free method for text-to-image diffusion models that provides precise concept-specific guidance by identifying and reinforcing concept-relevant layers in the network.


<details>
  <summary>Details</summary>
Motivation: Text-to-image diffusion models lack continuous concept-specific guidance mechanisms and demonstrate poor reliability for tasks requiring high local coherence (e.g., text and hands), limiting their practical utility.

Method: CoG introduces the notion of concept-wise mutual information to quantify each layer's concept-specific impact, then guides denoising using a weighted combination of predictions generated with concept-relevant layers skipped. It requires no training, external models, gradients, or prompt engineering.

Result: CoG demonstrates performance improvements across various targets and popular models including PixArt-alpha, SD3, SD3.5, and FLUX.1-dev.

Conclusion: The localization of concept-specific structure generation to distinct network layers can be leveraged for precise, target-specific guidance in diffusion models, working out-of-the-box without additional training.

Abstract: Text-to-image diffusion models have two major drawbacks that severely limit their practical utility: (1) standard models lack an intrinsic mechanism for continuous, concept-specific guidance (e.g., for precisely controlling how aesthetically pleasing an image looks), and (2) they lack reliability for tasks requiring high local coherence (e.g., generating text or human hands). To tackle these issues, we introduce a novel notion of concept-wise mutual information and find large, concept-dependent differences between individual layers, demonstrating that the generation of specific structures is localized in distinct parts of the network. We exploit this insight by reinforcing the impact of concept-relevant layers in Concept Guidance (CoG), a precise, target-specific guidance method that works for models out-of-the-box without additional training, external models, gradients, or prompt engineering. CoG first quantifies each layer's concept-specific impact and then guides denoising using a weighted combination of predictions generated with concept-relevant layers skipped. We demonstrate performance increases across various targets and popular models like PixArt-alpha, SD3, SD3.5, and FLUX.1-dev. Code is available at https://github.com/CompVis/concept_guidance

</details>


### [45] [LightTeaNet: A Weakly Supervised Lightweight CNN for Multi-Label Tea Leaf Disease Detection and Localization](https://arxiv.org/abs/2608.14178)
*Naif Haider Chowdhury,Md Rahim,Syed Farhan Hasan,Murad Hasan,Prithwiraj Bhattacharjee*

Main category: cs.CV

TL;DR: This paper introduces LightTeaNet, a lightweight CNN for weakly supervised multi-label classification and disease localization in tea leaves using Class Activation Mapping (CAM), achieving high classification performance (F1-score of 0.9179) without requiring bounding box annotations.


<details>
  <summary>Details</summary>
Motivation: Tea production in South and Southeast Asia suffers from multiple diseases that reduce yield and quality. Traditional manual inspection is inconsistent, labor-intensive, and requires extensive monitoring, motivating the need for an automated, efficient solution.

Method: LightTeaNet is a lightweight CNN that performs weakly supervised multi-label classification and disease localization. It learns from image-level labels only, uses Class Activation Mapping (CAM) for automatic disease region localization, incorporates Depthwise Separable Convolutions for parameter efficiency, and integrates Channel Attention for enhanced feature discrimination.

Result: LightTeaNet achieved a Precision of 0.9615, Recall of 0.8772, and F1-score of 0.9179. It also demonstrated a mAP@0.50 of 0.1810 without any manual annotations, delivering competitive localization performance.

Conclusion: The results validate LightTeaNet as an interpretable and resource-efficient framework for intelligent disease monitoring in agriculture, offering a practical alternative to annotation-heavy object detection models like YOLO.

Abstract: Tea is known as an important crop in many parts of South and Southeast Asia, yet the production of tea is still hampered by the multiple diseases that decrease the quantity and quality. Traditional methods of inspection, which are manual, are not consistent, labor-intensive, and depend on extensive monitoring. This paper introduces a lightweight convolutional neural network (CNN) designed for weakly supervised multi-label classification and disease localization in tea leaves called LightTeaNet. LightTeaNet learns directly from image-level labels and employs Class Activation Mapping (CAM) to localize disease-affected regions automatically, unlike conventional object detection models such as YOLO, which require extensive bounding box annotations. For Parameter efficiency, the network integrates Depthwise Separable Convolutions, and for enhanced feature discrimination, it integrates Channel Attention. LightTeaNet has achieved a Precision of 0.9615, a Recall of 0.8772, and an F1-score of 0.9179, while it shows mAP@0.50=0.1810 without any manual annotations, which delivers a competitive localization performance in the experimental results. These results validate the model as an interpretable as well as a resource-efficient framework for intelligent disease monitoring in agriculture.

</details>


### [46] [RankT2I: A Submodular Framework for Discovering Interpretable and Diverse Semantics in Text-to-Image Models](https://arxiv.org/abs/2608.14226)
*Ritika Allada,Pinar Yanardag*

Main category: cs.CV

TL;DR: RankT2I is a training-free, model-agnostic framework that automatically discovers editable semantics in text-to-image models by using a multimodal vision-language model to gather candidate semantics and a submodular set selection objective to identify relevant, editable, and diverse semantics across domains.


<details>
  <summary>Details</summary>
Motivation: Existing text-to-image editing approaches require users to manually specify which semantics to modify in an image, which is time-consuming and involves trial and error. The paper aims to automate the discovery of editable semantics to make the editing process more efficient.

Method: RankT2I uses a multimodal vision-language model to first collect a broad set of candidate semantics for a given visual domain, then frames semantic discovery as a set selection problem solved with a submodular objective that balances relevance, editability, and diversity.

Result: The method efficiently identifies a wide range of semantics for text-to-image editing models across several domains and outperforms existing methods.

Conclusion: RankT2I successfully automates the discovery of editable semantics in diffusion and FLUX-based models without requiring training, and demonstrates superior performance over existing approaches.

Abstract: Recent advances in text-to-image (T2I) models have revolutionized the field of image generation and editing. However, identifying semantics that a T2I model can successfully edit in an image continues to be a challenging task. Most existing approaches require users to manually specify semantics to modify a particular image, a time-consuming process that often involves extensive trial and error. In this paper, we present RankT2I, a novel, training-free, and model-agnostic framework that automates the discovery of editable semantics in diffusion and FLUX-based models. Given a visual domain, we first utilize a multimodal vision-language model to gather a broad set of candidate semantics. We then frame semantic discovery as a set selection problem and use a submodular objective to identify semantics that are relevant, editable, and diverse. Our method helps users efficiently identify a wide range of semantics for text-to-image editing models across several domains while outperforming existing methods.

</details>


### [47] [AppleScab-LT: A Longitudinal Real-Field Apple Scab Dataset for Temporal Disease Progression Analysis](https://arxiv.org/abs/2608.14235)
*Aamir Hilal,Shabir Ahmad Sofi,Neeraj Goel*

Main category: cs.CV

TL;DR: This paper introduces AppleScab-LT, a longitudinal real-field dataset tracking apple scab progression through repeated observations of individually tracked infected leaves. It contains 21 longitudinal leaf sequences, 2,101 high-resolution images, and 264 progressive temporal samples, with quantitative disease descriptors for temporal disease analysis.


<details>
  <summary>Details</summary>
Motivation: Existing plant disease datasets consist mostly of static images captured at a single time point, which limits analysis of temporal disease evolution and severity progression. Reliable longitudinal datasets capturing disease progression under natural field conditions are lacking.

Method: The dataset was constructed through systematic orchard monitoring under natural conditions, incorporating longitudinal leaf tracking, expert-guided disease verification, polygon-based annotation, leaf isolation, disease severity quantification, and temporal sequence construction. A comprehensive quality assurance framework was applied including standardized annotation protocols, expert validation, automated integrity checks, sequence-level verification, and temporal consistency analysis.

Result: AppleScab-LT contains 21 longitudinal leaf sequences, 2,101 high-resolution images, and 264 progressive temporal samples from repeated monitoring of the same infected leaves. It captures variability in severity accumulation, progression rates, monitoring duration, and inter-leaf progression, with quantitative disease descriptors based on pixel severity, color-intensity severity, and normalized relative severity.

Conclusion: AppleScab-LT provides a reliable resource for temporal disease intelligence, disease progression modelling, precision agriculture, and future crop health monitoring.

Abstract: The development of reliable plant disease monitoring systems is constrained by limited longitudinal datasets capturing disease progression under natural field conditions. Although existing plant disease datasets have advanced image-based recognition, most consist of static images acquired at a single time point, limiting analysis of temporal disease evolution and severity progression. To address this gap, this study presents AppleScab-LT, a longitudinal real-field dataset developed to monitor apple scab progression through repeated observations of individually tracked infected leaves. Guided by a research-question-driven framework, the dataset was systematically developed, validated, and characterized for reliable longitudinal disease analysis. AppleScab-LT was constructed through systematic orchard monitoring under natural environmental conditions, incorporating longitudinal leaf tracking, expert-guided disease verification, polygon-based annotation, leaf isolation, disease severity quantification, and temporal sequence construction. A comprehensive quality assurance framework, including standardized annotation protocols, expert validation, automated integrity checks, sequence-level verification, and temporal consistency analysis, was applied throughout curation. The dataset contains 21 longitudinal leaf sequences, 2,101 high-resolution images, and 264 progressive temporal samples from repeated monitoring of same infected leaves. It captures variability in severity accumulation, progression rates, monitoring duration, and inter-leaf progression. Quantitative disease descriptors based on pixel severity, color-intensity severity, and normalized relative severity provide standardized measurements for temporal disease analysis. AppleScab-LT provides a reliable resource for temporal disease intelligence, disease progression modelling, precision agriculture, and future crop health monitoring

</details>


### [48] [Zero-Shot Skeleton-Based Action Anticipation](https://arxiv.org/abs/2608.14243)
*Hongsong Wang,Pengbo Yan,Yang Zhang,Qiuxia Lai*

Main category: cs.CV

TL;DR: This paper introduces Zero-Shot Skeleton-Based Action Anticipation (ZS-SkAA), a new task combining partial observation action anticipation with zero-shot generalization to unseen action classes. The authors propose a baseline model with a spatio-temporal feature extractor and a mutual information estimation/maximization module that aligns partial visual features with semantic class embeddings, plus a benchmark protocol on NTU RGB+D. Experiments show the model achieves high zero-shot accuracy, establishing ZS-SkAA as a foundational research direction.


<details>
  <summary>Details</summary>
Motivation: Existing skeleton-based action anticipation (AA) methods assume all action classes are seen during training, limiting deployment in real-world scenarios where novel actions inevitably occur. This work addresses this gap by studying zero-shot generalization for skeleton-based action anticipation.

Method: The authors propose (1) a baseline model consisting of a spatio-temporal feature extractor and a mutual information estimation and maximization module that aligns partial visual features with semantic class embeddings across modalities, and (2) a benchmark protocol adapted from the NTU RGB+D dataset for rigorous ZS-SkAA evaluation.

Result: The proposed baseline model achieves high zero-shot accuracy on NTU RGB+D, demonstrating its effectiveness as a strong baseline for the ZS-SkAA task.

Conclusion: This work establishes ZS-SkAA as a vital research direction for real-world systems requiring generalization to novel actions, providing both a baseline model and a benchmark protocol for future research.

Abstract: Action anticipation (AA) aims to recognize ongoing human or humanoids actions from partial observations, enabling robots to predict intentions before the actions are completed. Although skeleton-based AA offers efficiency advantages, existing approaches assume that all action classes are seen during training, which limits their deployment in real-world scenarios where novel actions inevitably arise. To address this gap, we study the new task of Zero-Shot Skeleton-Based Action Anticipation (ZS-SkAA). This task requires recognizing unseen action classes using only limited early-stage skeleton sequences, combining the challenges of partial observations, temporal dynamics, and zero-shot generalization. To establish foundational research for ZS-SkAA, we introduce:(1) A baseline model comprising a spatio-temporal feature extractor and a mutual information estimation and maximization module. This baseline model explicitly aligns partial visual features with semantic class embeddings across modalities by estimating and maximizing their mutual information, enhancing generalization to unseen classes.(2) A benchmark protocol using the NTU RGB+D dataset, which is adapted for rigorous ZS-SkAA evaluation. Experiments demonstrate the effectiveness of our model as a strong baseline for ZS-SkAA, achieving high zero-shot accuracy on NTU RGB+D. This work establishes ZS-SkAA as a vital research direction for real-world systems requiring generalization to novel actions.

</details>


### [49] [On the Robustness of Temporal Vision-Language Models for Surgical Endoscopy Videos](https://arxiv.org/abs/2608.14262)
*Darakshan Rashid,Raza Imam,Ufaq Khan,Muhammad Bilal,Shazad Ashraf,Dwarikanath Mahapatra,Mohammad Yaqub,Muhammad Haris Khan,Imran Razzak,Brejesh Lall,Lena Maier-Hein,Yutong Xie*

Main category: cs.CV

TL;DR: This paper studies the robustness of temporal vision-language models (TVLMs) for surgical video understanding under clinically realistic endoscopic corruptions (defocus, haze, motion blur, noise, cautery smoke, packet loss). The authors introduce Endo-C6, a corruption benchmark with six endoscopy-specific perturbations, evaluate three TVLM baselines across 294 dataset-level evaluations, and propose RobustEndoCLIP via few-shot parameter-efficient tuning with VeRA that outperforms existing baselines in corrupted settings.


<details>
  <summary>Details</summary>
Motivation: Off-the-shelf temporal vision-language models used for surgical video understanding may not be robust to clinically realistic acquisition artifacts in endoscopy, yet this robustness remains insufficiently characterized. Structured distribution shifts from degradations like defocus, haze, motion blur, noise, cautery smoke, and packet loss could compromise video-text alignment and ultimately clinical reliability.

Method: The authors introduce Endo-C6, a compact benchmark of six endoscopy-realistic corruptions at fixed high severity, applied to public GI endoscopy and laparoscopic cholecystectomy videos. They benchmark three surgical TVLM baselines under a standardized prompt protocol with mean and worst-case robustness analysis across 294 dataset-level evaluations, and propose RobustEndoCLIP via few-shot parameter-efficient tuning with VeRA.

Result: Off-the-shelf TVLMs can exhibit severe worst-case collapse under endoscopy-specific corruptions. RobustEndoCLIP, obtained through lightweight few-shot adaptation with VeRA, outperforms existing TVLM baselines and substantially improves corrupted performance and robustness without altering the prompt-based interface.

Conclusion: Lightweight few-shot parameter-efficient adaptation can substantially improve TVLM robustness under endoscopic corruptions while preserving the reusable prompt-based interface. Endo-C6 is expected to support standardized robustness reporting and promote more reliable clinical vision-language systems.

Abstract: Temporal vision-language models (TVLMs) offer a reusable, prompt-based interface for surgical video understanding, yet, their robustness under clinically realistic acquisition artifacts in endoscopy remains insufficiently characterized. In practice, degradations such as defocus, haze, motion blur, noise, cautery smoke, and packet loss introduce structured distribution shifts which may compromise video-text alignment. We study the robustness of temporal VLMs under such shifts caused by corruptions in clip frames. We introduce Endo-C6, a compact corruption benchmark of six endoscopy-realistic perturbations evaluated at a fixed high severity, and apply it to public Gastrointestinal (GI) endoscopy and laparoscopic cholecystectomy videos. Under a standardized prompt protocol, we benchmark 3 recent surgical TVLM baselines and analyze robustness in both mean and worst-case settings, spanning 294 dataset-level evaluations. Finally, we present RobustEndoCLIP, obtained by few-shot parameter-efficient tuning with VeRA, outperforming existing TVLM baselines. Our findings show that off-the-shelf TVLMs can exhibit severe worst-case collapse under endoscopy-specific corruptions, whereas lightweight few-shot adaptation can substantially improve corrupted performance and robustness without changing the prompt-based interface. We expect Endo-C6 to support standardized robustness reporting and promote more reliable clinical vision-language systems.

</details>


### [50] [Learning to Forecast Crop Growth from Earth Observation Data](https://arxiv.org/abs/2608.14281)
*Dominik Senti,Mehmet Ozgur Turkoglu,Michele Volpi,Helge Aasen*

Main category: cs.CV

TL;DR: The paper investigates whether Earth observation time series and meteorological drivers can predict future canopy development (leaf area index) for winter wheat at country scale in Switzerland, using deep learning Seq2Seq models with a novel unimodal shape regulariser.


<details>
  <summary>Details</summary>
Motivation: Forecasting crop growth across agricultural landscapes is important for improving productivity, resilience, and operational management of farming systems, yet cloud cover and revisit gaps make LAI supervision sparse and lead to implausible oscillation in model predictions.

Method: The authors formulate crop growth prediction as forecasting future LAI trajectories beyond the last available Sentinel-2 observation. They introduce a lightweight unimodal shape regulariser to improve trajectory plausibility and compare deep learning Seq2Seq models with classic ML baselines on a multi-year dataset spanning all of Switzerland (20M+ pixel-level LAI time series with meteorological variables).

Result: Seq2Seq models generalise well across years, achieving R^2 above 0.8 and consistently outperforming conventional approaches, while the unimodal shape regulariser improves trajectory plausibility with negligible loss in accuracy.

Conclusion: Remote sensing and weather-driven sequence modelling can learn crop growth dynamics at landscape scale, demonstrating the feasibility of country-scale crop canopy forecasting.

Abstract: Forecasting crop growth across agricultural landscapes is important for improving the productivity, resilience, and operational management of farming systems. In this work, we investigate whether Earth observation time series and meteorological drivers can be used to predict future canopy development at country scale. We focus on winter wheat and formulate crop growth prediction as forecasting future leaf area index (LAI) trajectories beyond the last available Sentinel-2 observation. We evaluate this task on a multi-year dataset which spans the entire country of Switzerland, containing over 20 million pixel-level Sentinel-2-derived LAI time series paired with meteorological variables. Because cloud cover and revisit gaps leave LAI supervision sparse, models fit the few valid (cloud-free) LAI observations yet oscillate implausibly between them, producing trajectories no real canopy could follow. We introduce a lightweight unimodal shape regulariser which improves trajectory plausibility with negligible loss in accuracy. We compare deep learning sequence-to-sequence (Seq2Seq) models with classic machine learning baselines and show that Seq2Seq models generalise well across years, achieving $\mathrm{R}^2$ above 0.8 and consistently outperforming conventional approaches. Together, these results demonstrate that remote sensing and weather-driven sequence modelling can learn crop growth dynamics at landscape scale. S

</details>


### [51] [MAGneT-3D: Monocular and Domain-Generalizable Temporal 3D Detection](https://arxiv.org/abs/2608.14282)
*Mohamed Kotb,Johannes Meier,Christoph Reich,Oussema Dhaouadi,Luis Denninger,Daniel Cremers*

Main category: cs.CV

TL;DR: MAGneT-3D is the first domain-generalized monocular temporal 3D object detection method. It uses a Domain-Robust Anchor Generator (DRAG) to adaptively derive 3D proposals during inference and a Temporal Refinement and Identity Merging (TRIM) strategy to reduce dependence on static learnable queries. On a cross-dataset benchmark (nuScenes, Waymo, Lyft, ONCE), it improves NDS from 12.1% to 18.6% under zero-shot domain shifts.


<details>
  <summary>Details</summary>
Motivation: Query-based 3D detectors rely on learnable queries that fit the spatial distribution of training data (e.g., field-of-view), which severely hinders generalization to unseen datasets and environments when applied to monocular video.

Method: Proposes DRAG, which adaptively derives 3D proposals during inference instead of using static learnable queries, and TRIM, a temporal refinement and identity merging strategy to further enable domain generalization and reduce dependence on specific 3D proposals.

Result: On a new cross-dataset benchmark spanning nuScenes, Waymo, Lyft, and ONCE, MAGneT-3D outperforms all baselines under zero-shot domain shifts, improving NDS from 12.1% to 18.6% while also increasing in-domain accuracy.

Conclusion: Domain-robust anchor generation and temporal refinement effectively address the domain generalization limitation of query-based monocular 3D detectors, improving both cross-domain and in-domain performance.

Abstract: Monocular temporal 3D detection aims to detect objects in 3D, given a monocular video. Query-based 3D detectors unify detection and cross-view association, but their learnable queries fit the spatial distribution of the training data (e.g., field-of-view). We show that this issue is especially severe when these models are applied to monocular video, hindering generalization to unseen datasets and environments. To address this limitation, we introduce MAGneT-3D, the first method for domain-generalized monocular temporal 3D object detection. Instead of relying on static learnable queries, we propose a Domain-Robust Anchor Generator (DRAG) approach that adaptively derives 3D proposals during inference. To further enable domain generalization, we propose a Temporal Refinement and Identity Merging (TRIM) strategy, reducing dependence on specific 3D proposals. To enable comprehensive domain-generalization evaluation, we establish a cross-dataset benchmark spanning nuScenes, Waymo, Lyft, and ONCE. Under zero-shot domain shifts, MAGneT-3D outperforms all baselines, improving NDS from 12.1% to 18.6% while also increasing in-domain accuracy.

</details>


### [52] [Seeing Red, Thinking Bad: Color Bias in Vision Language Models](https://arxiv.org/abs/2608.14286)
*Kohsuke Ide,Ryousuke Yamada,Yoshihiro Fukuhara,Hirokatsu Kataoka,Yutaka Satoh*

Main category: cs.CV

TL;DR: This paper investigates how visual styling biases in rendered text affect Vision Language Models (VLMs), introducing "Stealth Visual Prompts" that subtly change text styling (color, contrast) while preserving semantics. Experiments show that coloring positive words green shifts sentiment predictions positively, causing VLMs to miss negative words, and reducing text-background contrast leads to more incorrect VQA outputs, revealing that visual styling can diverge VLMs' interpretation from human semantic understanding.


<details>
  <summary>Details</summary>
Motivation: VLMs are increasingly used in industrial decision-making systems (recruitment, recommendation), motivating a careful analysis of how they process visual and textual information. Specifically, the authors aim to understand how VLMs interpret text rendered as images and whether visual styling biases influence their analysis in ways that diverge from human understanding.

Method: The authors introduce Stealth Visual Prompts, which subtly alter visual styling (color and contrast) of rendered text while preserving semantic content. These prompts systematically control visual styling of words and measure their impact on VLM analysis. They further analyze how these perturbations affect latent representations of the vision encoder, using sentiment prediction and Visual Question Answering (VQA) tasks.

Result: Coloring positive words green consistently shifts sentiment predictions in a positive direction, causing VLMs to fail to properly account for negative words. This behavior correlates with changes in latent representations of the vision encoder induced by color variations. Additionally, reducing text-background contrast increases reliance on visually salient cues and leads to more incorrect VQA outputs.

Conclusion: The visual styling of rendered text can guide VLMs' interpretation in ways that diverge from human semantic understanding, highlighting a significant bias in how VLMs process visually-rendered text and raising concerns for their use in real-world decision-making systems.

Abstract: Vision language models (VLMs) are increasingly used in industrial decision-making systems, such as recruitment support and recommendation. This motivates careful analysis of how VLMs process visual and textual information. In this work, we study how VLMs interpret text rendered as an image, and investigate the influence of visual styling biases. To this end, we introduce Stealth Visual Prompts, which subtly change visual styling of text, such as color and contrast, while preserving semantic content. Using these prompts, we systematically control the visual styling of words in text and measure their impact on the analysis performed by VLMs. We further analyze how such visual perturbations affect the latent representations of the vision encoder. From our experiments, we observed that coloring positive words in green consistently shifts sentiment predictions toward a positive direction. As a result, VLMs often fail to properly account for negative words present in the text. Our analysis suggests that this behavior is correlated with changes in the latent representations of the vision encoder induced by color variations. In addition, we show that reducing text--background contrast increases reliance on visually salient cues and leads to more incorrect Visual Question Answering (VQA) outputs. These results suggest that the visual styling of rendered text can guide VLMs' interpretation in ways that diverge from human semantic understanding.
  Project page: https://github.com/KohsukeIde/color-bias-vlm

</details>


### [53] [Conditional Neural Optimal Transport for Predicting Cellular Phenotypes from Molecular Structure](https://arxiv.org/abs/2608.14293)
*Gauthier Avité,Maxime Sanchez-Renauld,Nicolas Bourriez,Auguste Genovesio*

Main category: cs.CV

TL;DR: The paper introduces a molecule-conditioned Neural Optimal Transport (NOT) model with Monge-Gap regularization to predict cellular phenotypes from molecular structure, outperforming baselines and generalizing to unseen molecules.


<details>
  <summary>Details</summary>
Motivation: High-content microscopy enables cellular response profiling but the vast chemical space makes exhaustive phenotypic characterization infeasible, motivating computational models that predict image-derived phenotypes without requiring actual treated cells.

Method: The problem is formulated as an inductive conditional transport problem in image representation space. A molecule-conditioned Neural Optimal Transport (NOT) model with Monge-Gap regularization is used to transport negative-control unperturbed phenotypes toward perturbed phenotypes, conditioned on molecular structure.

Result: Classical optimal transport baselines with static couplings fail on large-scale datasets. NOT recovers molecule-specific effects, reduces technical variation, and outperforms baselines on unseen active molecules. Transport in a compressed representation space improves performance and scalability, with the molecular encoder identified as the primary limitation to generalization.

Conclusion: NOT is a promising framework for predicting cellular phenotypes from molecular structure and negative-control phenotypes, with improved molecular representations identified as a key direction for enhancing out-of-distribution performance.

Abstract: High-content microscopy enables systematic profiling of cellular responses to chemical perturbations, but the scale of the chemical space makes exhaustive phenotypic characterization experimentally infeasible. This motivates computational models that can predict image-derived phenotypes without acquiring the corresponding treated cells. We formulate molecule-induced phenotype prediction as an inductive conditional transport problem in image representation space. Given a negative-control phenotype and the structure of a molecule, we aim to predict the phenotype induced by the corresponding molecule. We first evaluate classical optimal transport baselines and show that static couplings do not yield useful predictions on large-scale phenotypic image datasets. We then introduce a molecule-conditioned Neural Optimal Transport (NOT) model with a Monge-Gap regularization training objective that learns to transport negative-control unperturbed phenotypes toward perturbed phenotypes using molecular structure as conditioning information. NOT recovers molecule-specific phenotypic effects while reducing microscopy-associated technical variation, thereby facilitating comparisons across experimental batches. On unseen active molecules, the model outperforms baseline approaches, demonstrating that chemically conditioned transport can generalize beyond the molecules observed during training. We identified the molecular encoder as the main limitation to this generalization, while transport in a compressed representation space improves performance and scalability. These results establish NOT as a promising framework for predicting cellular phenotypes from molecular structure and negative-control phenotypes, while highlighting the development of more informative molecular representations as a key direction for improving out-of-distribution performance.

</details>


### [54] [Spatial Message Passing in Language Space for Pathology Image Interpretation](https://arxiv.org/abs/2608.14309)
*Jing-Cheng Yang,Hao-Jung Wang,Jinhao Du,Yang Hu,Ming-shan Tsai,Jens Rittscher,Bin Li*

Main category: cs.CV

TL;DR: SLMP performs spatial reasoning entirely in language space for gigapixel WSI analysis, representing tissue as a spatial text graph where LLMs refine descriptions via message passing between adjacent tiles, improving tumor description accuracy by +3.3 to +19.6 percentage points without fine-tuning.


<details>
  <summary>Details</summary>
Motivation: MLLMs can generate pathological descriptions from images, but gigapixel WSIs exceed their context limits. Standard tiling severs tissue neighborhoods that define tumor-stroma interfaces and morphology, losing critical spatial context.

Method: SLMP represents a WSI region as a spatial text graph where tiles are nodes initialized with MLLM descriptions and edges encode spatial adjacency. An LLM refines each tile's description by integrating language messages from adjacent tiles under a shared aggregation policy that acts as an adaptive local kernel on text. The policy is an inspectable prompt optimized via textual gradients from observed tissue phenotypes, requiring no weight updates.

Result: SLMP improves tile-level tumor description accuracy by +3.3 to +19.6 percentage points across general-purpose and pathology-specialized backbones on HER2 and CAMELYON16 regions. Random-neighbor ablations confirm gains stem from spatial context, and optimized policies reveal interpretable tissue-specific rules.

Conclusion: SLMP substantially improves general-purpose MLLMs and narrows their gap to pathology-specialized counterparts without any weight updates or fine-tuning, offering a transparent and flexible mechanism for incorporating spatial reasoning into MLLM-based pathology analysis.

Abstract: Multimodal Large Language Models (MLLMs) can generate pathological descriptions from histological images, but gigapixel Whole Slide Images (WSIs) exceed their visual context limits. The standard tiling workaround makes WSIs tractable yet severs the tissue neighborhoods that define tumor-stroma interfaces and morphology. We introduce Spatial Language Message Passing (SLMP), a framework that performs spatial reasoning entirely in language space, human-readable by construction. SLMP represents a WSI region as a spatial text graph: tiles are nodes initialized with MLLM descriptions, and edges encode spatial adjacency. For each tile, an LLM refines its description by integrating language messages from adjacent tiles under a shared aggregation policy that, on the tile grid, acts as an adaptive local kernel operating on text rather than learned embeddings. This policy is an inspectable prompt that can be refined from model-observed tissue phenotypes via textual gradients, enabling automatic semantic optimization from local cellular context to broader tissue morphology without fine-tuning MLLM weights. On representative HER2 and CAMELYON16 regions, SLMP improves tile-level tumor description accuracy in settings spanning general-purpose and pathology-specialized backbones, with gains of +3.3 to +19.6 percentage points. Random-neighbor ablations confirm that these gains stem from spatial context rather than additional text alone, and inspecting the optimized policies reveals interpretable, tissue-specific decision rules. Besides, without any weight updates or fine-tuning the backbone MLLM, SLMP substantially improves general-purpose MLLMs and narrows its gap to pathology-specialized counterparts, offering a transparent and flexible mechanism for incorporating spatial reasoning into MLLM-based pathology analysis.

</details>


### [55] [Intelligent Detection of Mechanical, Electrical, and Plumbing (MEP) Metrics Based on 2D Floor Plans](https://arxiv.org/abs/2608.14317)
*Tarandeep Singh Mandhiratta,ANK Zaman,Abdul-Rahman Mawlood-Yunis*

Main category: cs.CV

TL;DR: This paper presents a Mask RCNN-based neural network model that automatically detects and extracts lighting symbols, light types, and associated text from 2D floor plans, achieving bbox_mAP of 0.7596 and segm_mAP of 0.7111, aimed at improving design efficiency and estimating power requirements in building design.


<details>
  <summary>Details</summary>
Motivation: To enable efficient floor designing by automating the detection of lighting elements from floor plans, allowing designers to determine the number and type of lights needed per floor and estimate power requirements, while improving workflow efficiency in architecture and construction industries.

Method: The model uses Mask RCNN as the base architecture. Floor plan images are annotated and converted into the COCO data format for model training, enabling detection of lighting symbols, classification of light types, and extraction of associated text.

Result: The model achieved bbox_mAP and segm_mAP of 0.7596 and 0.7111 respectively, with strong performance at different IoU thresholds: bbox_mAP 50 of 0.9850 and segm_mAP 75 of 0.9219.

Conclusion: The developed model successfully automates the detection of MEP objects (specifically lighting) from floor plans, representing a first step toward tools that will support energy-efficient building design and improve workflows in architecture and construction industries.

Abstract: This research developed a neural network-based model to extract various information from 2D floor plans. We detect lighting symbols, identify the appropriate type of light, and extract the associated texts with lights. The study aims to enable efficient floor designing and determining the number and type of lights needed per floor, i.e., allow efficient design and estimate the power requirement of the floor plan. The model was developed using Mask RCNN as the base. The images were annotated and converted into a Coco data format for training the model. The model achieved bbox\_mAP and segm\_mAP values of 0.7596 and 0.7111, respectively. It also performed well at different IoU thresholds, i.e., with bbox\_mAP 50 and segm\_mAP 75 values of 0.9850 and 0.9219, respectively. The developed model will help various industries, such as architecture and construction, to improve design time and create efficient workflows by automatically detecting Mechanical, Electrical, and Plumbing (MEP) objects from floor plans, and it is the first step towards building tools that will help energy-efficient building design.

</details>


### [56] [TRIAGE: Risk-Controlled Pseudo-Label Admission for Annotation-Efficient Semi-Supervised Retinal OCT Classification](https://arxiv.org/abs/2608.14321)
*Md Ashraful Hossen Akash,Shyla Afroge,Abdullah Al Mamun,Md. Kishor Morol,Tze Hui Liew*

Main category: cs.CV

TL;DR: TRIAGE is a risk-controlled semi-supervised framework for OCT scan classification that uses a patient-level conformal risk controller with an asymmetric cost matrix to handle the unequal consequences of different classification errors. It integrates a hierarchical classifier for partial subtype supervision, a primal-dual coverage control mechanism, and a context-aware Transformer teacher, achieving state-of-the-art performance with very limited labeled data.


<details>
  <summary>Details</summary>
Motivation: OCT imaging suffers from a lack of automation due to the high cost of specialist annotations. Semi-supervised learning can leverage unlabeled B-scans, but existing pseudo-labeling techniques rely solely on prediction confidence and ignore the asymmetry between different types of diagnostic errors (e.g., under-grading vs. over-grading disease severity).

Method: TRIAGE consists of three key modules: (1) a hierarchical classifier supporting partially abnormal supervision of disease subtypes, (2) a patient-grouped conformal risk controller with primal-dual coverage control using an asymmetric cost matrix, and (3) a context-aware Transformer teacher for cross-slice verification.

Result: On the Noor Eye Hospital dataset (16,822 B-scans), TRIAGE achieves 89.66% scan-level accuracy, 0.8805 macro-F1, 0.9641 macro-AUC, and 8.34% under-grading rate with only 20% labeled data; 76.88% accuracy with 5% labeled data. It outperforms six state-of-the-art semi-supervised methods and reduces under-grading rate by 42.7% compared to fixed threshold methods. On OCT-C8, it achieves 98.00% accuracy for 3-class with 1% labels and 95.94% for 8-class with 10% labels.

Conclusion: TRIAGE demonstrates that incorporating asymmetric error costs and patient-level conformal risk control into semi-supervised learning significantly improves OCT classification performance and reduces dangerous under-grading errors even with minimal labeled data, making automated OCT diagnosis more feasible and clinically safe.

Abstract: The advanced retinal disease diagnosing imaging modality, optical coherence tomography (OCT), encounters a lack of automation because of the high expenses for annotations performed by specialists. The use of SSL solves the problem of insufficient annotations using unlabeled B-scans; however, most of the current techniques for generating pseudo-labels are based on prediction confidence without considering the asymmetry between different types of errors. This paper proposes TRIAGE, a risk-controlled semi-supervised framework for OCT scans classification, which uses the concept of a patient-level conformal risk controller with an asymmetric cost matrix. TRIAGE unites three crucial modules: a hierarchical classifier that is capable of working with partially abnormal supervision of the disease subtypes, a patient-grouped conformal risk controller with primal-dual coverage control, and a context-aware Transformer teacher for cross-slice verification. On the dataset from Noor Eye Hospital (16,822 B-scans, 161 patients, and 554 volumes) with a test set of unseen patients, TRIAGE demonstrates 89.66% scan-level accuracy, 0.8805 macro-F1, 0.9641 macro-AUC, and an 8.34% under-grading rate when using only 20% of the labeled data. With only 5% of the labeled data, TRIAGE keeps 76.88% accuracy and a 0.1656 under-grading rate. Compared with the other six state-of-the-art semi-supervised methods, TRIAGE significantly outperforms them with ablation study demonstrating the contribution of each module in the overall framework performance (by 42.7% in terms of under-grading rate comparing to fixed threshold methods). TRIAGE demonstrates 98.00% accuracy for 3-class classification with 1% labeled data and 95.94% accuracy for 8-class classification with 10% labeled data on the OCT-C8 dataset.

</details>


### [57] [Weakly Supervised Polar Low Segmentation in Sentinel-1 SAR Imagery](https://arxiv.org/abs/2608.14366)
*Andrea Federici,Jakob Grahn,Giacomo Boracchi,Filippo Maria Bianchi*

Main category: cs.CV

TL;DR: CREST is a weakly supervised semantic segmentation framework that generates pixel-level masks for polar lows in SAR imagery using only image-level labels, improving on Adversarial Erasing with spatial-connectedness constraints and reliability-weighted training.


<details>
  <summary>Details</summary>
Motivation: Pixel-level segmentation of polar lows from SAR imagery is challenging because no pixel-level training masks exist, and the targets have inherently subjective, diffuse boundaries. Existing Adversarial Erasing approaches accumulate irrelevant background features that degrade pseudo-label quality.

Method: CREST builds on Adversarial Erasing (AER) with two key innovations: (i) a Constrained Ordinal Region Expansion (CORE) module that encodes the spatial-connectedness prior of polar lows, constraining region expansion from a high-confidence seed, and (ii) a Dynamic Bootstrapping (DB) loss that treats mining order as a proxy for label reliability, down-weighting noisier, later-mined regions. It produces multi-class masks where classes indicate region reliability.

Result: On Sentinel-1 SAR data, CREST follows cyclone structure more closely than standard AER and returns multi-class instead of binary masks. On BUS-UCLM breast ultrasound and PASCAL VOC person data (which have dense masks), CREST outperforms the equivalent AER pipeline under identical settings.

Conclusion: CREST demonstrates that WSSS can effectively segment polar lows from image-level labels alone by enforcing spatial-connectedness priors and reliability-aware supervision, generalizing beyond SAR data to other connected-domain segmentation tasks.

Abstract: Polar lows are intense maritime cyclones that form rapidly at high latitudes. Deep learning can detect them in Synthetic Aperture Radar (SAR) imagery, but pixel-level segmentation remains an open challenge. No pixel-level masks are available for training, and a polar low's extent is inherently subjective, with diffuse boundaries that even experts delineate inconsistently. We propose Constrained Region Erasing with Soft Targets (CREST), a Weakly Supervised Semantic Segmentation (WSSS) framework that generates masks solely from image-level labels. Our approach builds on Adversarial Erasing (AER), which iteratively mines discriminative regions, erases them, and retrains a classifier to reveal complementary cues that become pseudo-labels for segmentation. However, standard AER also collects irrelevant background features, degrading pseudo-label quality. CREST addresses this with (i) a Constrained Ordinal Region Expansion (CORE) module that encodes the spatial-connectedness prior of polar lows, constraining region expansion from a high-confidence seed, and (ii) a Dynamic Bootstrapping (DB) loss that treats the mining order as a proxy for label reliability, attenuating supervision from noisier, later-mined regions. On Sentinel-1 SAR data, CREST follows the cyclone structure more closely than standard AER, and returns a multi-class rather than binary mask whose classes indicate the reliability assigned to each region. We further evaluate on BUS-UCLM breast ultrasound and PASCAL VOC person data, whose targets satisfy the same connectedness prior but come with the dense masks the SAR data lacks. On both datasets, CREST performs better than the equivalent AER pipeline under identical settings.

</details>


### [58] [GBU-Palm: A Multimodal Video Dataset and Benchmark for Palm Presentation Attack Detection](https://arxiv.org/abs/2608.14389)
*Yingjie Ma,Zitong Yu,Wei Jia,Ajay Kumar,Linlin Shen*

Main category: cs.CV

TL;DR: This paper introduces GBU-Palm, a large-scale multimodal video dataset and benchmark for palm presentation attack detection (PAD), containing 21,326 videos from 105 subjects across six environments, with RGB-NIR synchronized data and Print/Replay attacks. It benchmarks four video architectures under environment-matched and held-out settings, revealing architecture-dependent degradation under environmental shift and showing RGB-NIR fusion does not consistently outperform RGB-only. Detailed failure analysis via TA/TR/FA/FR decomposition, spectral masking, temporal-order intervention, and frozen-backbone NIR probing reveals distinct failure patterns across architectures.


<details>
  <summary>Details</summary>
Motivation: Existing palm PAD datasets are limited by static imagery, restricted acquisition conditions, or insufficient multimodal video data, which hinders systematic evaluation across environments, modalities, and attack types. A large-scale, multimodal video benchmark with controlled protocols is needed for robust cross-environment evaluation.

Method: The authors construct GBU-Palm with 21,326 videos from 210 palms across six acquisition environments for bona fide, Print, and Replay attacks, including 6,310 synchronized RGB-NIR samples. They design leakage-controlled protocols separating palm identity and attack lineage, and benchmark four video architectures under environment-matched and held-out-environment settings. They also analyze model behavior using TA/TR/FA/FR decomposition, spectral masking, temporal-order intervention, and frozen-backbone NIR probing.

Result: Results show substantial architecture-dependent degradation under environmental shift, and RGB-NIR fusion does not consistently outperform RGB-only input. The analysis reveals distinct failure patterns and evidence utilization across different architectures.

Conclusion: GBU-Palm provides a unified and challenging benchmark for developing and evaluating robust multimodal palm PAD methods under cross-environment conditions, filling a critical gap in existing datasets.

Abstract: Existing palm presentation attack detection (PAD) datasets are often limited by static imagery, restricted acquisition conditions, or insufficient multimodal video data, hindering systematic evaluation across environments, modalities, and attack types. We present GBU-Palm, a large-scale multimodal video dataset and benchmark containing 21,326 videos from 105 subjects and 210 palms across six acquisition environments, including bona fide, Print, and Replay presentations, with 6,310 synchronized RGB-NIR samples. We construct leakage-controlled protocols that separate palm identity and attack lineage and benchmark four representative video architectures under environment-matched and held-out-environment settings. Results reveal substantial architecture-dependent degradation under environmental shift and show that RGB-NIR fusion does not consistently outperform RGB-only input. We further analyze model behavior through true accept (TA), true reject (TR), false accept (FA), and false reject (FR) decomposition, spectral masking, temporal-order intervention, and frozen-backbone NIR probing, revealing distinct failure patterns and evidence utilization across architectures. GBU-Palm provides a unified and challenging benchmark for developing and evaluating robust multimodal palm PAD methods under cross-environment conditions.

</details>


### [59] [Can We Defend Against AI-Generated Video Attacks on Real-World Crisis Events? A Systematic Evaluation of Detectors, Generators and Social Dissemination](https://arxiv.org/abs/2608.14391)
*Shuo Liang,Yixing Ma,Pengfei Zhou,Xingyan Chen,Zihan Mei,Manting Li,Feihan Chen,Zhiwen Wang,Bin Xu,Haotian Zhang,Jiajun Song,Shiya Su,Run Liu,Zhenghang Ni,Yifa Yu,Jintao Hong,Bolong Feng,Yifei Liu,Zirui Zhang,Jingxuan Zhang,Songlin Zhao,Yifan Bai,Kang Tan,Yizhe Liu,Junhao Du,Yongtao Ge,Zhaopan Xv,Xinyuan Zhang,Mengru Ma,Chunhua Shen,Wei Wang,Yang You,Zheng Zhu,Kaipeng Zhang,Wangbo Zhao*

Main category: cs.CV

TL;DR: RA-Bench is a benchmark for AI-generated video detection, containing 17,886 videos (1,830 real anchors across 10 social-risk categories and 16,056 generated clips from nine generators). Evaluation reveals that no detector family generalizes consistently, generation properties affect detector families differently, videos that mislead humans are also hard for detectors, and social dissemination makes detection harder.


<details>
  <summary>Details</summary>
Motivation: Recent video generators can fabricate realistic depictions of real-world crises, creating substantial misinformation risks, yet existing benchmarks provide limited evidence on detector and generator behavior in such settings—including how detectability varies with generation conditions, how people perceive generated videos, and whether detectors remain reliable during social dissemination.

Method: The paper introduces RA-Bench, a benchmark using Real videos as Anchors, containing 17,886 videos: 1,830 real-video anchors across 10 social-risk categories and 16,056 generated clips from four open-source and five closed-source generators. Evaluation is organized along three dimensions: detector generalization across seven traditional detectors, ten zero-shot multimodal models under three review settings, and two fine-tuned MLLMs; detectability variation with generation quality, conditioning information, and sampling seeds; and human authenticity judgments plus detector reliability during social dissemination.

Result: None of the three detector families generalizes consistently across RA-Bench instances. Generation properties affect detector families differently, while source-level detection patterns remain stable across seeds. Videos that mislead people are also difficult for current detectors, and social dissemination makes detection harder.

Conclusion: Current methods struggle to detect realistic AI-generated videos, highlighting the need for detectors robust to evolving video generators.

Abstract: Recent video generators can fabricate realistic depictions of wars, disasters, public emergencies, and other real-world crises, creating substantial risks of misinformation. Existing benchmarks, however, provide limited evidence on detector and generator behavior in such settings, including how detectability varies with generation conditions, how people perceive generated videos, and whether detectors remain reliable during social dissemination. To address this gap, we introduce RA-Bench, a benchmark for AI-generated video detection that uses Real videos as Anchors. RA-Bench contains 17,886 videos, comprising 1,830 real-video anchors across 10 social-risk categories and 16,056 generated clips from four open-source and five closed-source generators. Based on RA-Bench, we organize our evaluation along three dimensions. We first assess detector generalization across seven traditional detectors, ten zero-shot multimodal models under three review settings, and two MLLMs specifically fine-tuned on AI-generated video detection. Across these methods, none of the three detector families generalizes consistently across RA-Bench instances. We then examine how detectability varies with generation quality, conditioning information, and sampling seeds. These analyses show that generation properties affect detector families differently, while source-level detection patterns remain stable across seeds. Finally, we study human authenticity judgments and detector reliability during social dissemination. We find that videos that mislead people are also difficult for current detectors, and that social dissemination makes detection harder. Together, these findings show that current methods struggle to detect realistic AI-generated videos, highlighting the need for detectors robust to evolving video generators.

</details>


### [60] [IRGNN: Efficient Invariant Radar Graph Neural Network for Radar Point Cloud Object Detection](https://arxiv.org/abs/2608.14394)
*Xiao Guo,Wanke Xia,Lili Yang,Caicong Wu*

Main category: cs.CV

TL;DR: IRGNN is an Invariant Radar Graph Neural Network for radar point cloud object detection in autonomous driving. It reconstructs sparse radar points into translation- and rotation-invariant graph representations, uses an improved MPNN with residual connections and virtual node layers, and achieves state-of-the-art results on RadarScenes with reduced computational cost and memory usage.


<details>
  <summary>Details</summary>
Motivation: LiDAR-based object detection methods degrade under adverse weather conditions. While radar point clouds offer robustness to bad weather and low illumination, they are sparse, unordered, and less informative than LiDAR, making it difficult to directly apply existing LiDAR-based perception methods.

Method: IRGNN first reconstructs radar point clouds into graph representations using translation- and rotation-invariant feature designs. It then employs an improved message passing neural network (MPNN) with residual connections and a virtual node layer to enhance local feature propagation and global context modeling. Finally, task-specific heads are applied to the learned graph representations for object classification and bounding box prediction.

Result: On the RadarScenes dataset, IRGNN outperforms existing radar-based object detection methods and achieves competitive performance. It also significantly reduces computational cost and memory usage during inference.

Conclusion: IRGNN demonstrates effectiveness and practical potential for efficient radar-based perception in autonomous driving, offering a robust and computationally efficient alternative to LiDAR-based methods under adverse weather conditions.

Abstract: Perception is a fundamental component of autonomous driving systems. While LiDAR-based methods have achieved remarkable progress in object detection, their reliability can degrade under adverse weather conditions. Radar point clouds provide a robust alternative due to their resilience to bad weather and low-illumination scenarios. However, radar point clouds are typically sparse, unordered, and less informative than LiDAR data, making it challenging to directly apply existing LiDAR-based perception methods. To address these challenges, we propose IRGNN, an Invariant Radar Graph Neural Network for radar point cloud object detection. IRGNN first reconstructs radar point clouds into graph representations using translation- and rotation-invariant feature designs, enabling robust modeling of sparse radar measurements. It then employs an improved message passing neural network (MPNN) with residual connections and a virtual node layer to enhance local feature propagation and global context modeling. Finally, task-specific heads are applied to the learned graph representations for object classification and bounding box prediction. Experimental results on the RadarScenes dataset show that IRGNN outperforms existing radar-based object detection methods and achieves competitive performance. In addition, IRGNN significantly reduces computational cost and memory usage during inference, demonstrating its effectiveness and practical potential for efficient radar-based perception in autonomous driving.

</details>


### [61] [CRAFT: Constrained Reward via Attention Fine-Tuning for Subject Personalization without Composed Targets](https://arxiv.org/abs/2608.14403)
*Jihun Park,Kyoungmin Lee,Jongmin Gim,Hyeonseo Jo,Jaeyeul Kim,Han Zou,Zhenpeng Zhan,Yan Zhang,Sunghoon Im*

Main category: cs.CV

TL;DR: CRAFT (Constrained Reward via Attention Fine-Tuning) is a single-step ReFL framework for subject-driven image personalization that fine-tunes a pre-trained reference-aware MMDiT via LoRA adapters using only 10K reference-only samples with subject masks, requiring no composed-target supervision. It uses attention-level rewards (aligning noise- and phrase-token attention with reference subjects) and attention-gated pixel-level identity rewards to achieve state-of-the-art performance on XVerseBench with FLUX.2-klein-9B.


<details>
  <summary>Details</summary>
Motivation: Existing generalized subject-driven personalization methods rely on massive paired (reference, composed-target) datasets (150K to 2M+ examples), which require costly multi-stage curation pipelines (LLM prompt generation, T2I synthesis, VLM filtering, correspondence labeling) and tightly couple methods to specific target synthesizers. CRAFT aims to eliminate this dependency by using a compact reference-only data construction with no composed-target supervision.

Method: CRAFT is a single-step ReFL framework that fine-tunes a reference-aware MMDiT via LoRA adapters on just 10K reference images with subject masks. It implements a 'Where to look' principle: attention-level rewards align noise- and phrase-token attention with the correct reference subject, producing per-subject attention masks that gate a pixel-level identity reward, keeping image-space supervision consistent with learned attention routing.

Result: Applied to FLUX.2-klein-9B, CRAFT achieves state-of-the-art performance on XVerseBench using only 10K reference-only samples (no composed-target supervision), outperforming prior generalized methods that require 150K to over 2M composed-target pairs. The recipe also transfers to other reference-aware backbones with consistent improvements.

Conclusion: CRAFT demonstrates that subject-driven personalization can be achieved without expensive composed-target supervision, using only a small reference-only dataset with attention-based rewards. This significantly reduces data curation costs and decouples the method from specific target synthesizers, while achieving superior performance and transferability across backbones.

Abstract: Subject-driven image personalization---generating new images that preserve the identity of one or several reference subjects in novel scenes---is a foundational capability for modern visual content creation. It is currently dominated by generalized methods that fine-tune a pretrained multimodal diffusion transformer (MMDiT) on hundreds of thousands to millions of paired \emph{(reference, composed-target)} examples, where each composed target is a synthesized image of the subject in a novel scene. Producing such targets demands a costly multi-stage curation pipeline---LLM-based prompt generation, T2I-based composed-target synthesis, reference-subject extraction, VLM-based quality filtering, and correspondence labeling---and tightly couples each method to a particular target synthesizer and curation choice. We introduce \emph{CRAFT} (Constrained Reward via Attention Fine-Tuning), a single-step ReFL framework that fine-tunes a pre-trained \emph{reference-aware} MMDiT via LoRA adapters using a compact reference-only data construction---$10$K reference images and subject masks, with no composed-target supervision. CRAFT realizes a \emph{Where to look} principle: attention-level rewards align noise- and phrase-token attention with the correct reference subject, and the resulting per-subject attention masks gate a pixel-level identity reward to keep image-space supervision consistent with the learned attention routing. Applied to FLUX.2-klein-9B, CRAFT achieves state-of-the-art performance on XVerseBench \rev{while using no composed-target supervision---only $10$K reference-only samples, whereas prior generalized methods require $150$K to over $2$M composed-target pairs}. The same recipe transfers to other reference-aware backbones, consistently improving performance. Project page: https://jihun999.github.io/projects/CRAFT/.

</details>


### [62] [GhostPoint: Self-Supervised Representation Learning by Hallucinating Occluded LiDAR Structure](https://arxiv.org/abs/2608.14428)
*Mohamed Abdelsamad,Bin Yang,Michael Ulrich,Miao Zhang,Yakov Miron,Alexandru Paul Condurache,Abhinav Valada*

Main category: cs.CV

TL;DR: GhostPoint is a self-supervised learning framework for 3D object detection from LiDAR point clouds that addresses the visible-surface bias in existing SSL methods by hallucinating latent features in local neighborhoods around discovered instances via instance voxel dilation, achieving state-of-the-art downstream detection performance on nuScenes and Waymo.


<details>
  <summary>Details</summary>
Motivation: Existing SSL methods for LiDAR define objectives only on measured returns from visible surfaces, leaving occluded and unobserved regions unconstrained. While this visible-surface bias works for point-wise tasks like segmentation, 3D detection requires robustness to missing structure, so current SSL pretraining transfers weakly to detection.

Method: GhostPoint hallucinates latent features in local neighborhoods around discovered instances using a novel instance voxel dilation technique. An encoder processes observed returns while an additional predictor infers neighborhood representations. Supervision occurs at two levels: encoder-level supervision where observed (visible/masked) voxels match teacher-encoder targets, and predictor-level supervision where unobserved voxels match teacher-predictor hallucinations.

Result: Extensive evaluations on nuScenes and Waymo show GhostPoint achieves state-of-the-art performance with consistently improved downstream 3D detection, particularly under sparse scans and limited label settings.

Conclusion: Explicitly modeling structure beyond observed returns through hallucinated neighborhood features is key to enabling SSL that transfers effectively to 3D object detection, overcoming the limitations of visible-surface-biased pretraining objectives.

Abstract: 3D object detection from LiDAR point clouds is a core problem in autonomous driving. Recent advances in self-supervised learning (SSL) enable scalable pretraining and transfers well to per-point tasks such as semantic and panoptic segmentation, but transfer to 3D detection remains weaker. We analyze recent SSL methods and find that most objectives are defined only on measured LiDAR returns from visible surfaces, leaving occluded and unobserved regions unconstrained. This visible-surface bias can be sufficient for point-wise prediction, but 3D detection requires robustness to missing structure. To address this gap, we propose GhostPoint, an SSL framework that hallucinates latent features in local neighborhoods around discovered instances, generated via a novel instance voxel dilation. In GhostPoint, an encoder processes observed returns, and an additional predictor infers neighborhood representations from observed context. In addition to standard encoder-level supervision, we introduce a predictor-level supervision scheme on sampled voxels from generated neighborhoods. Specifically, observed (visible/masked) voxels match teacher-encoder targets, while unobserved voxels match teacher-predictor hallucinations. This design encourages the learned representation to explicitly model structure beyond observed returns. Extensive evaluations on nuScenes and Waymo demonstrate that our method achieves state-of-the-art performance, consistently improving downstream 3D detection, especially under sparse scans and limited labels.

</details>


### [63] [Style or Signature? Artist-Disjoint Evaluation of Style Classification in Frozen Vision Embeddings](https://arxiv.org/abs/2608.14435)
*Rory Ashton*

Main category: cs.CV

TL;DR: The paper shows that high style-classification accuracy from frozen CLIP embeddings partly reflects artist recognition rather than true style understanding. Under an artist-disjoint protocol (no work of an artist used to classify another of their works), accuracy drops from 0.87 to 0.77 across four 20th-century movements, with Surrealism dropping 20 points while Impressionism and Cubism barely change. This pattern persists across four image encoders, including a vision-only self-supervised model, and the authors argue artist-disjoint evaluation is necessary to measure stylistic understanding.


<details>
  <summary>Details</summary>
Motivation: Standard evaluation of style classification uses random splits where works by the same artist appear in both training and test sets, so classifiers can succeed by recognizing individual artists rather than understanding art-historical style. This overestimates stylistic understanding in frozen embeddings.

Method: The authors introduce an artist-disjoint protocol where every artist is held out in turn so no work is ever classified using other works by its own painter. They use a balanced dataset of 320 paintings across four 20th-century movements (Impressionism, Cubism, Surrealism, and presumably another) and evaluate 5-NN style classification under both standard and artist-disjoint protocols, across four image encoders including a vision-only self-supervised model.

Result: 5-NN style accuracy falls from 0.87 to 0.77 under the artist-disjoint protocol. The drop is uneven: Impressionism and Cubism barely change, while Surrealism drops by 20 points. The pattern holds across all four image encoders, including the vision-only model, indicating the effect is rooted in visual structure rather than language.

Conclusion: Artist-disjoint evaluation is necessary to genuinely measure stylistic understanding in frozen embeddings. Some movements (Impressionism, Cubism) have robust shared visual form making artists barely recognizable yet style robust, while Surrealism shows the opposite pattern, where apparent style accuracy depended on artist recognition.

Abstract: Frozen image embeddings from models such as CLIP are increasingly used to classify paintings by art-historical style, with high reported accuracy. We ask whether this accuracy reflects an understanding of style or the recognition of individual artists. Standard evaluation uses random splits in which works by the same artist appear on both sides, so a classifier can succeed by recognising the painter rather than the movement. We re-evaluate style classification under an artist-disjoint protocol, holding out every artist in turn so that no work is ever classified using other works by its own painter. On a balanced dataset of 320 paintings across four twentieth-century movements, 5-NN style accuracy falls from 0.87 to 0.77 under this protocol, and the drop is sharply uneven. Impressionism and Cubism barely move, while Surrealism falls twenty points. The pattern holds across four image encoders, including a vision-only self-supervised model, which places the effect in visual structure rather than language. Where an encoder captures genuine shared form, individual artists are barely recognisable yet style is robust, while Surrealism shows the opposite. We argue that artist-disjoint evaluation is necessary to measure stylistic understanding in frozen embeddings.

</details>


### [64] [Marionette: Predicting World States, Rendering Geometry, Painting Appearance](https://arxiv.org/abs/2608.14530)
*Zian Meng,Zhen Li,Chuanhao Li,Qiang Li,Kaipeng Zhang*

Main category: cs.CV

TL;DR: Marionette is a world model for interactive games that explicitly predicts a 276-dimensional 3D world state (articulated skeletons, root trajectories, rotations) using a two-stage autoregressive dynamics model, converts it to pose-control videos via a zero-parameter graphics renderer, and synthesizes photorealistic RGB frames with a control-conditioned video diffusion model. The explicit state enables direct controllability and long-horizon consistency repair without retraining the observation model, at no detectable loss in appearance fidelity.


<details>
  <summary>Details</summary>
Motivation: Autoregressive game world models that predict directly in pixel or latent space force structured properties (pose, geometry, occlusion) to be implicitly maintained by the same generative sequence, causing error accumulation over long horizons and making consistency and controllability fragile.

Method: (1) A two-stage autoregressive dynamics model predicts an explicit 276-dimensional 3D world state with multi-entity articulated skeletons, metric root trajectories, and rotations. (2) A zero-parameter graphics bridge converts the predicted state into pose-control videos, computing world-space geometry and occlusion in closed form. (3) A control-conditioned video-diffusion observation model synthesizes photorealistic RGB observations from the structured controls.

Result: (1) Predicted world state is directly controllable: forcing mismatched actions changes root-aligned joint error by 31% across 48 held-out segments. (2) Long-horizon behavior is determinable and repairable in state: free-running characters drift to 21.2m apart (recorded ~5m) with a third of frames showing ground penetration; adding a terrain collider and separation cap to the explicit state cuts penetration by 66% and keeps characters engaged with no observation-model changes. (3) Appearance fidelity is preserved: FVD of 831 vs 799 for recorded pose.

Conclusion: By routing appearance through an explicit predicted world state, Marionette demonstrates that structured game world modeling can be made directly controllable and repairable over long horizons without sacrificing appearance fidelity, suggesting a promising direction for interpretable and steerable interactive world models.

Abstract: Interactive game world models typically autoregress visual observations directly in pixel or latent space, forcing structured properties such as pose, geometry, and occlusion to be implicitly maintained by the same generative sequence. Over long horizons, errors in these latent world properties accumulate, making consistency and controllability fragile. We explicitly model the evolving world state, delegate exact geometric computation to a fixed, zero-parameter renderer, and leave the neural model to synthesize appearance. We instantiate this idea as Marionette, a world model for interactive games with articulated characters. First, a two-stage autoregressive dynamics model predicts an explicit and interpretable 276-dimensional 3D world state comprising multi-entity articulated skeletons, metric root trajectories, and rotations. Second, a zero-parameter graphics bridge converts the predicted state into pose-control videos, computing world-space geometry and occlusion in closed form. Third, a control-conditioned video-diffusion observation model synthesizes photorealistic RGB observations from the resulting structured controls. Our experiments establish two properties of Marionette. First, the predicted world state is directly controllable. Forcing a mismatched action stream changes root-aligned joint error by 31% across 48 held-out segments. Second, long-horizon behaviour is determined in the state, and can be repaired there. Left free, the two generated characters drift to 21.2 m apart (recorded sessions stay near 5 m) and a third of frames show ground penetration. Two rules imposed on the explicit state, a terrain collider and a separation cap, cut penetration by 66% and keep the pair engaged, with no change to the observation model. Routing appearance through the predicted state costs no fidelity we can detect, at an FVD of 831 against 799 for recorded pose.

</details>


### [65] [Decoding the Past: An Uncertainty-Aware Deep Learning Framework for Sex Attribution in Prehistoric Hand Stencils](https://arxiv.org/abs/2608.14539)
*Karel Becerra,Boris Mederos,Dean Snow,Ramón A. Mollineda*

Main category: cs.CV

TL;DR: This paper presents an uncertainty-aware deep learning framework for determining the biological sex of individuals who created Upper Paleolithic hand stencils, using dual image processing, contour extraction, silhouette augmentation, and ensemble-based decision aggregation with two neural network architectures, validated through triangulated schemes including latent-space mapping and explainable AI.


<details>
  <summary>Details</summary>
Motivation: Determining the biological sex of prehistoric hand stencil creators remains challenging due to absence of ground truth, population differences between contemporary and prehistoric groups, and image degradation. Traditional morphometric methods suffer from high structural overlap across sexes, poor cross-population generalizability, and subjective feature engineering.

Method: The framework combines dual image processing, dual contour extraction, structured silhouette augmentation, model architectural diversity (EfficientNet-B3 and MobileViT-S), and ensemble-based decision aggregation. It generates twelve plausible silhouette realizations per stencil, processed by two ensembles of ten deep neural networks each, trained on 14,036 contemporary hand samples. A triangulated validation scheme integrates ensemble predictions with unsupervised 2D latent-space manifold mapping (UMAP + k-NN) and explainable AI spatial attributions (LayerCAM).

Result: On contemporary data, ensemble models achieve classification accuracies exceeding 88% in older age groups. On prehistoric stencils, the framework produces sex predictions with confidence measures of internal agreement, distinguishing morphologically stable from ambiguous cases. Convergence is observed across ensemble predictions, latent-space structure, and interpretability analyses.

Conclusion: The study demonstrates that uncertainty can become a measurable component of archaeological inference, enabling robust and reproducible decoding of ancient rock art through explicit modeling, propagation, and aggregation of uncertainty throughout the analytical pipeline.

Abstract: Determining the biological sex of the individuals who created Upper Paleolithic hand stencils remains a challenging problem due to the absence of ground truth, population differences between contemporary and prehistoric groups, and the uncertainty introduced by image degradation. Traditional morphometric methods suffer from high structural overlap across sexes, poor cross-population generalizability, and subjective feature engineering. This study presents an uncertainty-aware deep learning framework for sex attribution in prehistoric hand stencils that explicitly models, propagates, and aggregates uncertainty throughout the analytical pipeline. The methodology combines dual image processing, dual contour extraction, structured silhouette augmentation, model architectural diversity, and ensemble-based decision aggregation. The pipeline generates twelve plausible silhouette realizations per stencil to capture boundary uncertainties, which are processed by two ensembles of ten deep neural networks each (EfficientNet-B3 and MobileViT-S) trained on 14,036 contemporary hand samples. Furthermore, a triangulated validation scheme integrates ensemble predictions with unsupervised 2D latent-space manifold mapping (UMAP + k-NN) and explainable AI spatial attributions (LayerCAM) to ensure anatomical consistency. On contemporary data, ensemble models achieve strong classification performance, with accuracies exceeding 88% in older age groups. When applied to prehistoric stencils, the framework produces both sex predictions and confidence measures of internal agreement, enabling the distinction between morphologically stable and ambiguous cases. Convergence across ensemble predictions, latent-space structure, and interpretability analyses shows that uncertainty can become a measurable component of archaeological inference, enabling robust and reproducible decoding of ancient rock art.

</details>


### [66] [MagnifiQ: Patch-aware Text Guided Progressive Upscaling for High-Resolution Image Restoration](https://arxiv.org/abs/2608.14543)
*Mahesh Reddy,Yashesh Savani,Antoine Mercier,Hong Cai,Fatih Porikli,Guillaume Berger*

Main category: cs.CV

TL;DR: MagnifiQ is a high-resolution image restoration framework that progressively upscales images from 1024x1024 to 4096x4096 using a pre-trained text-to-image diffusion model (SDXL) adapted by replacing self-attention with linear-cost convolutional operations, along with patch-specific text prompts for localized semantic guidance, achieving superior perceptual quality and human preference over prior diffusion-based methods.


<details>
  <summary>Details</summary>
Motivation: High-resolution (4K) image restoration from degraded inputs is challenging because it must preserve global structural consistency while recovering fine-grained local details. Direct diffusion-based restoration at 4K resolution is computationally expensive and prone to repeated or inconsistent textures.

Method: MagnifiQ progressively upscales and restores images across resolution stages (e.g., 1024x1024 to 4096x4096). It adapts a pre-trained text-to-image diffusion model (SDXL) by replacing self-attention layers with convolutional operations (linear cost scaling with resolution). It uses a progressive upscaling strategy that iteratively refines intermediate outputs, and patch-specific text prompts for spatially localized semantic guidance.

Result: Extensive experiments on synthetic and real-world degraded images show MagnifiQ outperforms prior diffusion-based restoration methods in perceptual quality and human preference, producing sharper textures and more coherent 4K results with practical speed-quality trade-offs.

Conclusion: MagnifiQ demonstrates that combining a scalable backbone (convolution-based attention replacement) with progressive upscaling and patch-specific prompts enables efficient, high-quality 4K image restoration that maintains global coherence while recovering fine details.

Abstract: High-resolution image restoration from degraded inputs is challenging because it must preserve global structural consistency while recovering fine-grained local details, especially at 4K resolution where direct diffusion-based restoration is computationally expensive and prone to repeated or inconsistent textures. In this work, we introduce MagnifiQ, an image restoration framework that progressively upscales and restores images across resolutions, e.g., from 1024x1024 to 4096x4096. Our approach leverages a pre-trained text-to-image diffusion model such as SDXL and adapts it for more scalable high-resolution inference by replacing its original self-attention layers with convolutional operations whose computational cost grows linearly with image resolution. We further propose a progressive upscaling strategy that iteratively restores images over multiple resolution stages, refining each intermediate output rather than directly hallucinating the final 4K image, thereby improving global coherence and reducing high-resolution artifacts. To enhance local details while controlling content drift, MagnifiQ uses patch-specific text prompts that provide spatially localized semantic guidance during restoration. Extensive experiments on synthetic and real-world degraded images show that MagnifiQ outperforms prior diffusion-based restoration methods in perceptual quality and human preference, producing sharper textures and more coherent 4K results while offering practical speed--quality trade-offs through its scalable backbone and progressive design.

</details>


### [67] [CPI-Bench: A Comprehensive,Practical and Intelligent Benchmark for Real-World Image Editing](https://arxiv.org/abs/2608.14546)
*Qinye Zhou,Jun Zheng,Yongchao Du,Yuan Wang,Zhengrui Chen,Zuan Gao,Taihang Hu,Chao Lin,Yefeng Shen,Xingjian Wang,Zhao Wang,Zhengtao Wu,Xiaoli Xu,Zhengze Xu,Hao Yan,Denghui Yang,Yuhang Yu,Huayu Zhang,Mingzhou Zhang,Mengting Chen*

Main category: cs.CV

TL;DR: This paper proposes CPI-Bench, a comprehensive, practical, and intelligent benchmark for real-world image editing, comprising three subsets (General, Practical, and Intelligent) that cover multi-image editing, real-user scenarios, and reasoning-based editing. It outperforms existing benchmarks in differentiating model performance and achieves the highest alignment with human perceptual judgments on the Arena Image Edit Leaderboard.


<details>
  <summary>Details</summary>
Motivation: Existing image editing benchmarks are confined to single-image tasks with limited coverage, failing to differentiate model performance in complex multi-image editing, reasoning-heavy instructions, and practical deployment settings.

Method: Proposes CPI-Bench with three core subsets: (1) CPI-General-Bench covering diverse editing tasks including multi-image editing, (2) CPI-Practical-Bench focused on high-frequency real-user scenarios, and (3) CPI-Intelligent-Bench for highly demanding reasoning-based editing.

Result: Evaluation of mainstream image editing models shows CPI-Bench enhances performance differentiation among models. Ranking analysis reveals CPI-Bench achieves the highest alignment with the Arena Image Edit Leaderboard, indicating faithful capture of human evaluator preferences and perceptions.

Conclusion: CPI-Bench provides a comprehensive and reliable quantification of gaps in general editing capabilities, practical deployment efficacy, and advanced reasoning-based editing, serving as a robust proxy for real-world user experience and offering guidance for future model optimization.

Abstract: With the rapid advancement of image editing models and their widespread application across various domains, there is an increasingly urgent need to deploy these model capabilities directly into real-world scenarios. However, existing benchmarks remain confined to simple single-image tasks, suffering from limited coverage dimensions and an inability to effectively differentiate performance among diverse models. Consequently, they fail to reliably evaluate model performance in complex multi-image editing, highly demanding reasoning instructions, and practical deployment settings. To address these limitations, we propose CPI-Bench, a Comprehensive, Practical andIntelligent benchmark for real-world image editing. CPI-Bench comprises three core subsets: CPI-General-Bench, which comprehensively covers diverse editing tasks and pioneers the inclusion of multi-image editing evaluation; CPI-Practical-Bench, which focuses on high-frequency real-user application scenarios; and CPI-Intelligent-Bench, which is dedicated to evaluating capabilities in highly demanding reasoning-based editing. Evaluation results of mainstream image editing models based on CPI-Bench demonstrate that CPI-Bench enhances performance differentiation among models. It provides a comprehensive and reliable quantification of gaps in general editing capabilities, practical deployment efficacy, and advanced reasoning-based editing, offering invaluable guidance for the future optimization of image editing models. Crucially, our ranking analysis reveals that CPI-Bench achieves the highest alignment with the Arena Image Edit Leaderboard, indicating it faithfully captures the preferences and perceptual judgments of human evaluators, serving as a robust proxy for real-world user experience.

</details>
